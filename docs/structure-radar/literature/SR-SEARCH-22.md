# StructureRadar literature ledger — search batch 22

SEARCH_TASK=SR-SEARCH-01
BATCH_ID=SR-BATCH-LITERATURE_SEARCH-22-R01
SEARCH_DATE=2026-08-19
STRUCTURES=SR-STR-209,SR-STR-210,SR-STR-211,SR-STR-212,SR-STR-213,SR-STR-214,SR-STR-215,SR-STR-217,SR-STR-218,SR-STR-219
SEARCH_BATCH_SIZE=10
NOVELTY_BY_SEARCH_ABSENCE=false

## Primary-source literature checked
- D. R. Heath-Brown and J.-L. Colliot-Thélène, *The density of rational points in curves and surfaces*, Annals of Mathematics 155 (2002), 553–598; arXiv:math/0405392. Uniform fixed-degree bounded-height point counts are adjacent to SR-STR-210/217, but they do not provide the repo heavy-ray/determinant collision theorem or uniform summation over a growing physical stratum family.
- D. R. Heath-Brown, *Small solutions of quadratic congruences, and character sums with binary quadratic forms*, Mathematika 62 (2016), 551–571; arXiv:1411.4816. This gives small-solution and character-sum technology adjacent to SR-STR-209/210, not the repo centered common-core discrepancy, projective-collision decomposition, or heavy primitive-ray multiplicity bound.
- Stephan Baier and Arpit Bansal, *Large sieve with sparse sets of moduli for Z[i]*, arXiv:1811.07300. This supplies Gaussian-integer large-sieve technology adjacent to SR-STR-211, but its averaging framework does not identify or preserve the exact correlated moving common core C and physical weights.
- Kevin Ford, *The distribution of integers with a divisor in a given interval*, arXiv:math/0401223. Uniform divisor-in-interval estimates are the natural external context for SR-STR-213, but the repo moving physical window and conditional reverse-completion Boolean still require a measure adapter.
- R. de la Bretèche and T. D. Browning, *Sums of arithmetic functions over values of binary forms*, arXiv:math/0604119. Their uniform upper-bound framework for arithmetic functions on binary-form values is adjacent to the first-moment species in SR-STR-215, but it does not retain the nested K-free divisor allocation, both exact CRT congruences, and primitive physical rectangle simultaneously.
- Zev Klagsbrun, *Selmer Ranks of Quadratic Twists of Elliptic Curves with Partial Rational Two-Torsion*, arXiv:1201.5408. Twist-family Selmer-rank results are adjacent to the j=1728/rational-2-torsion moving-family receiver in SR-STR-219, but they do not count the repo active physical directions or provide the required first-nonboundary-point sparsity theorem.

## Search outcome
- SR-STR-209: exact principal-plus-centered common-core decomposition and projective collision gate remain repo-specific. Generic quadratic-character technology does not by itself turn centered excess into a same-measure physical deficit.
- SR-STR-210: determinant-method and small-solution technology are adjacent to genuine mover collisions, but the heavy primitive-ray branch and separated-q branch still require receiver-specific multiplicity/control.
- SR-STR-211: Gaussian large-sieve literature is adjacent only; an average over unrelated Gaussian moduli cannot replace the exact correlated common core C with the original physical mask.
- SR-STR-212: the radial squareclass capacity/overlap mechanism is retained as repo exact localization. It forces polynomially many radial values in a heavy survivor but does not close that support branch.
- SR-STR-213: Ford gives the correct divisor-in-moving-interval theorem species, but the physical measure and conditional reverse-completion adapter remain missing.
- SR-STR-214: rectangular product capacity is exhausted at full exponent; the reciprocal-solvability and post-mask deficits must be measured on the same charged pair population and cannot gain an extra multiplication saving.
- SR-STR-215: de la Bretèche–Browning is adjacent first-moment/binary-form technology only. No checked source retains the nested K-free divisor allocation, both CRT congruences and the full primitive rectangle uniformly, so the exact external gate remains open.
- SR-STR-217: fixed-degree rational-point bounds support only the per-fixed-stratum capacity heuristic/input; they do not justify summing over a growing family of strata with uniform constants.
- SR-STR-218: the fixed-tuple projective-lattice estimate and cross-tuple quantifier firewall remain repo exact; local L2/Fourier normalization cannot erase the number of moving modulus tuples.
- SR-STR-219: twist/Selmer literature is adjacent, but fixed-direction B^o(1) fiber compression does not imply power sparsity of active directions; a family-level specialization/first-nonboundary-point theorem is still required.

## SR-STR-209 — Common-core centered root discrepancy and variable-modulus character/projective-collision dichotomy
Stratifying the exact common core C0=B^(kappa+o(1)) separates a low-core allocation branch from a polynomial-core branch. The root indicator has an exact principal-plus-centered decomposition; polynomial-core saturation forces positive centered excess. That centered root discrepancy has an exact multiplicative-character expansion with an L2 coefficient identity, and large fixed-C discrepancy forces projective collision energy. The obstruction then splits into concentrated projective collisions or a diffuse exact-modulus norm-divisor graph. Stage14-t67,t69,t70 supplies a common-support precursor of the centered/projective-collision gate. Canonical root moduli collapse losslessly, generic private-prime spacing does not improve the natural large-sieve scale, and a small common-support J cannot be ruled out from squareclass algebra alone; any fixed-power gain must use the physical reconstruction/selector measure rather than assume nontrivial overlap.
Potential weapon types: CHARACTER_SUM, ENERGY, DISPERSION.
Applicability gaps: The principal root mass is not itself a saving on the accepted physical subset; saturation can move into centered excess.; Collision energy must distinguish proportional/diagonal pairs from genuine movers before determinant spacing is charged.; Batch14 Stage14-s7 historical whole-family exponents are superseded by the current square-root exponent 1/2; only the reusable exact mechanism/provenance is normalized here, with no strict sub-square-root claim.; Batch15 preserves CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2 and STRICT_SUBSQRT_POWER_SAVING_PROVED=false; theorem-window and receiver refinements are provenance, not a new whole-family exponent.; Batch19 preserves CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2 and STRICT_SUBSQRT_POWER_SAVING_PROVED=false. Historical t-route milestones such as 5/8, 19/34 and 23/44 are provenance only, not current whole-family theorems; no perfect-cuboid nonexistence claim is imported.; The t79 active-support and t71-t78 common-support refinements are receiver localizations; small common support or near-full character support can still saturate without a new correlation theorem.
Transfer verdict: `ADJACENT_CHARACTER_DISPERSION_WITH_PHYSICAL_COLLISION_GATE` — centered common-core excess still needs a same-measure physical collision theorem.
Arsenal decision: `ACTIVE`.

## SR-STR-210 — Heavy primitive-ray versus genuine determinant-mover dichotomy with quotient-scale stratification
After the projective-collision reduction, diagonal collisions cannot support concentrated saturation. The surviving concentrated branch is either a heavy primitive ray or a genuine nonzero determinant mover, while the diffuse exact-modulus branch remains separate. For genuine movers write the nonzero determinant as C*q; super-determinant moduli are empty, near-maximal mover moduli have only a B^o(1) q-dictionary, and separated moduli retain a polynomial q-range.
Potential weapon types: DETERMINANT_METHOD, INCIDENCE, DISPERSION.
Applicability gaps: A global primitive-ray multiplicity bound is not proved.; The separated-modulus polynomial q range still needs a genuine mover-collision theorem; the heavy-ray branch is unchanged.; Batch14 Stage14-s7 historical whole-family exponents are superseded by the current square-root exponent 1/2; only the reusable exact mechanism/provenance is normalized here, with no strict sub-square-root claim.
Transfer verdict: `ADJACENT_DETERMINANT_METHOD_WITH_HEAVY_RAY_GATE` — genuine movers are compatible with determinant technology, but heavy-ray multiplicity and separated-q control remain open.
Arsenal decision: `ACTIVE`.

## SR-STR-211 — Diffuse common-core Gaussian norm-factor correlation and bilinear theorem gate
On the diffuse polynomial common-core branch, accepted primitive Gaussian norms satisfy N=X^2+Y^2=C*m with only B^o(1) divisor, primitive-representation and reverse-physical fibers. Splitting the complementary quotient m closes the subpolynomial branch directly; the polynomial branch factors in Z[i] and leaves a genuinely weighted Gaussian-factor bilinear correlation at the exact correlated core C. No independent second-modulus saving is available, so a strict deficit requires a mask-preserving bilinear/dispersion theorem adapter.
Potential weapon types: CORRELATION, DISPERSION, LARGE_SIEVE.
Applicability gaps: The subpolynomial complementary quotient is closed, but the polynomial Gaussian-factor branch still needs a uniform physical-weight adapter.; A modulus average unrelated to the exact correlated C cannot be imported.
Transfer verdict: `ADJACENT_GAUSSIAN_LARGE_SIEVE_WITH_CORRELATED_CORE_GATE` — large-sieve averaging cannot replace the exact moving common-core correlation.
Arsenal decision: `ACTIVE`.

## SR-STR-212 — Heavy primitive-ray radial squareclass capacity and agreement-overlap compression
For a concentrated heavy primitive ray, fixed radial data have B^o(1) reverse physical fiber. The radial scale injects into a fixed-squarefree-kernel square-value problem; its squareclass reconstructs the xi-agreement product and forces a large agreement/kernel overlap G. Freezing G collapses the primitive agreement-pair freedom and bounds atomic radial multiplicity by B^(1/4-phi+o(1)), forcing any heavy survivor to occupy polynomially many normalized radial values rather than hide in endpoint or inner reconstruction entropy.
Potential weapon types: INCIDENCE, SQUARECLASS, CAPACITY_BOUND.
Applicability gaps: Generic density of squares cannot be charged because the moving value is a structured physical product.; The capacity bound localizes heavy mass but does not by itself close the surviving radial-support branch.; Batch14 Stage14-s7 historical whole-family exponents are superseded by the current square-root exponent 1/2; only the reusable exact mechanism/provenance is normalized here, with no strict sub-square-root claim.
Transfer verdict: `REPO_HEAVY_RAY_CAPACITY_LOCALIZATION` — heavy mass is localized to polynomially many radial values without a global fixed-power saving.
Arsenal decision: `ACTIVE`.

## SR-STR-213 — Complementary-dilation bare unitary shadow versus conditional physical-completion deficit
After primitive-ratio and complementary-dilation stratification, the weighted physical incidence can be outerized to a bare unitary-divisor shadow plus a conditional canonical reverse-completion Boolean. Fixed E splits into an endpoint one-dimensional completion branch or a two-sided unitary shadow; the unitary restriction is pointwise dominated by the corresponding ordinary divisor shadow at zero upper-bound cost. Thus survival is governed by a support-versus-completion-deficit budget, not by treating the inner multiplicity as fresh density.
Potential weapon types: DIVISOR_INTERVAL, INCIDENCE, CONDITIONAL_COMPLETION.
Applicability gaps: Removing the unitary restriction is legal for an upper bound but supplies no fixed-power saving.; Ford-type divisor interval estimates still require the exact physical measure and moving-window adapter.; Batch15 preserves CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2 and STRICT_SUBSQRT_POWER_SAVING_PROVED=false; theorem-window and receiver refinements are provenance, not a new whole-family exponent.
Transfer verdict: `ADJACENT_DIVISOR_INTERVAL_WITH_PHYSICAL_COMPLETION_GATE` — Ford-type interval counts still need the moving-window/conditional-completion measure adapter.
Arsenal decision: `ACTIVE`.

## SR-STR-214 — Rectangular product capacity exhaustion and reciprocal CRT completion pullback
The fixed-E moving ordinary-divisor window straightens to a rectangular product set. Pair capacity is controlled by the rectangle size, multiplication has only divisor-many fibers, and the coprime/unitary restriction causes no fixed-power loss, so bare arithmetic capacity is exhausted. Pulling completion back to the primitive pair shows that the remaining Boolean is exactly reciprocal divisor/CRT solvability followed by the residual post-column mask; the total completion deficit splits into reciprocal-solvability and post-mask deficits without an extra product-capacity saving.
Potential weapon types: MULTIPLICATIVE_ENERGY, CRT, COMPLETION_DEFICIT.
Applicability gaps: The principal rectangular support can remain full exponent, so multiplication/coprimality alone cannot yield the required deficit.; Reciprocal solvability and post-mask acceptance must be measured on the same charged physical pair population.
Transfer verdict: `REPO_RECTANGULAR_CAPACITY_COMPLETION_PULLBACK` — multiplication capacity is exhausted; only reciprocal-solvability and post-mask deficits remain.
Arsenal decision: `ACTIVE`.

## SR-STR-215 — Seeded/seedless K-free reciprocal divisor-allocation CRT first-moment gate
Explicit homogeneous reciprocal seeds show that reciprocal solvability can have full exponent. Away from that diagonal seed, witnesses normalize to a K-free moving divisor-allocation system retaining two exact CRT congruences. Bounded witness multiplicity makes support at B-power scale equivalent to a single first moment, exposing the theorem species UniformPrimitiveRectangleNestedKFreeQuadraticDivisorRootFirstMoment. The frozen H audit finds no direct off-the-shelf theorem or complete mask-preserving transfer, so this remains an external gate rather than a proved saving.
Potential weapon types: DIVISOR_SUM, CRT, FIRST_MOMENT, EXTERNAL_GATE.
Applicability gaps: The seeded packet is full exponent and must be separated rather than treated as generic sparsity.; No direct theorem currently retains the nested K-free divisor allocation, both CRT congruences and the full primitive physical rectangle uniformly.
Transfer verdict: `EXTERNAL_GATE_NESTED_KFREE_CRT_FIRST_MOMENT` — no checked theorem preserves all nested K-free, dual-CRT and primitive-rectangle quantifiers.
Arsenal decision: `ACTIVE`.

## SR-STR-217 — Collective fixed-degree stratum proliferation reverse-count budget
If every fixed physical rational stratum of M-degree d contributes at most B^(2/d+o(1)) while a target active population V(B) has square-root scale B^(1/2), then any explanation by a growing degree-d family necessarily requires N_d(B) >= B^(1/2-2/d-o(1)). In particular d=8 requires B^(1/4-o(1)), d=12 requires B^(1/3-o(1)), and d=16 requires B^(3/8-o(1)). This is a necessary capacity budget, not an existence theorem for such strata or a proof of the square-root law.
Potential weapon types: HEIGHT_COUNTING, MOVING_FAMILY_GATE, CAPACITY_BOUND.
Applicability gaps: The B^(2/d+o(1)) bound is per fixed stratum and does not provide uniform constants for a moving family.; No higher-degree strata are constructed and no B^(1/2) asymptotic is proved.; Finite fingerprint dispersion is supporting evidence only, not a theorem of geometric proliferation.
Transfer verdict: `ADJACENT_FIXED_DEGREE_POINT_COUNT_WITH_MOVING_STRATUM_GATE` — per-fixed-stratum point counts do not provide moving-family uniformity.
Arsenal decision: `ACTIVE`.

## SR-STR-218 — Fixed-tuple projective all-short lattice refinement and cross-tuple energy quantifier firewall
After projective root data are fixed, a complete all-short local state tuple is a rank-two projective lattice whose index is the product state modulus, so its centered boundary discrepancy is O(M B^epsilon) on a regular Euclid box and the generic positive q-loss disappears. The Hilbert/Fourier l2 bounds are conditional on a fixed physical support cell and therefore cannot be summed as one globally normalized coefficient vector over all moving modulus tuples; cross-tuple cardinality must remain explicit.
Potential weapon types: LATTICE_COUNT, NO_DOUBLE_COUNT, QUANTIFIER_FIREWALL.
Applicability gaps: The fixed-tuple lattice estimate does not remove the number of distinct moving modulus tuples.; Historical 41/42-type optimization values are superseded; the reusable content is the fixed-tuple geometry and the cross-tuple quantifier boundary.
Transfer verdict: `REPO_FIXED_TUPLE_LATTICE_QUANTIFIER_FIREWALL` — fixed-tuple geometry is valid while cross-tuple cardinality remains explicit.
Arsenal decision: `ACTIVE`.

## SR-STR-219 — Fixed-direction j=1728 fiber compression versus active-direction sparsity barrier
The s6 half-angle cross-square quartic is the fixed-direction j=1728 squareclass quartic up to the fixed class -1, so each primitive direction has only B^o(1) physical partners in the polynomial-height window. Consequently physical edge count and active-direction count have the same power exponent. This fixed-fiber compression, local solvability, and the universal boundary rational anchor do not force a power-sparse set of active directions; a family-level first-nonboundary-point or specialization theorem is required. Stage14-t21--t23 gives a parallel fixed-direction compression: after the split-kernel direction is fixed, one elliptic quotient with rational 2-torsion controls all scales with B^o(1) multiplicity. Thus edge count and active-direction count have the same power exponent; a torsion first-moment saving does not by itself control the partition second moment, and the positive-rank active-direction family remains the moving support problem.
Potential weapon types: FIBER_COMPRESSION, OBSTRUCTION, SPECIALIZATION_GATE.
Applicability gaps: Subpolynomial degree per active direction alone gives no power saving for the number of active directions.; Global solubility of the direction curve is not a useful filter when the universal boundary point is always present.
Transfer verdict: `ADJACENT_TWIST_SELMER_WITH_ACTIVE_DIRECTION_GATE` — fixed-direction compression and twist arithmetic do not bound the number of active directions.
Arsenal decision: `ACTIVE`.

## Firewalls
- Search absence is not a novelty claim.
- Principal/centered root mass, heavy-ray and genuine-mover branches remain distinct until a same-measure theorem closes them.
- Gaussian large-sieve averages cannot be substituted for the exact correlated common core C.
- Ford/divisor and binary-form first-moment estimates require the exact moving physical window, primitive rectangle and CRT masks before transfer.
- Per-fixed-stratum rational-point bounds are not summed over a growing family without uniform constants and a stratum-count budget.
- Fixed-direction elliptic fiber compression does not imply sparsity of active directions.
- CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT remains 1/2; STRICT_SUBSQRT_POWER_SAVING_PROVED=false.
- No perfect-cuboid existence or nonexistence claim is made.
