# Stage14-Work-cfX44 receiver matrix

| Route / realization | Charged measure | Inner arithmetic receiver | Status | Cross-promotion |
|---|---|---|---|---|
| main aligned fixed-E two-sided | primitive fixed-E pair packet | `UniformPrimitiveRectangleNestedKFreeQuadraticDivisorRootFirstMoment` | parked external gate | none |
| s aligned fixed-E two-sided | same aligned packet as main | same main external gate | parked external gate | restricted main↔s identity only |
| s nonaligned scalar branches | scalar outer variable + retained filtered-τ3 witness | q17-form reciprocal divisor/CRT first moment conditioned on witness | active | no q17 fixed-E measure transfer |
| s polynomial `(E,m)` branch | charged outer pair `(E,m)` + retained filtered-τ3 witness | q17-form reciprocal divisor/CRT first moment conditioned on witness | active | pair measure not scalarized |
| fixed-U | fixed-U Gaussian residue packet | `SuperKaiIndividualGaussianResidueLongIntervalPrimeOccupancyLowerRatio` | parked external gate | none |

## Exact inner-kernel cancellation

For `f|W1(lambda)`, write `F_-=f`, `F_+=W1/f`. Then

```text
W1+f^2 == 0 (mod 2Uf)  <=>  F_+ + F_- == 0 (mod 2U),
W1-f^2 == 0 (mod 2Vf)  <=>  F_+ - F_- == 0 (mod 2V).
```

Thus the s7-134 simultaneous quadratic divisor-root predicate is exactly q17's reciprocal-CRT inner form after cancellation of the divisor variable.

```text
SECOND_REVERSE_SELF_COUPLED_MODULUS_CANCELLATION_PROVED=true
S_NONALIGNED_SECOND_REVERSE_INNER_KERNEL_IDENTIFIED_WITH_Q17_RECIPROCAL_CRT_FORM=true
```

## Measure boundary

The arithmetic kernel identity does not imply theorem-measure identity.

```text
Q17_TO_S_CONDITIONED_MEASURE_ADAPTER_PROVED=false
COMMON_INNER_KERNEL_DOES_NOT_IDENTIFY_CHARGED_MEASURE=true
S_THEOREM_SPECIES_MEASURE_VARIANT_COUNT=2
PAIR_TO_SCALAR_HOST_ADAPTER_PROVED=false
```

The s scalar and `(E,m)` variants therefore remain separate theorem contracts even though their innermost reciprocal-CRT equations agree.

## Deficit / recharge ledger

```text
FIRST_LAYER_FILTERED_TAU3_SUPPORT_ADAPTER_ALREADY_CONSUMED=true
SECOND_REVERSE_SUPPORT_TO_MOMENT_ADAPTER_ALREADY_CONSUMED=true
RESOLVED_SUPPORT_TO_MOMENT_ADAPTER_RECHARGE_FORBIDDEN=true
POST_MASK_REMAINS_SEPARATELY_CHARGED=true
RECIPROCAL_CRT_SAVING_CROSS_PROMOTABLE_TO_POST_MASK=false
```

## q ledger

```text
Q_COMPONENT=NOT_TRIGGERED
Q_LEDGER_BASELINE=Stage14-q17+Stage14-q20
Q17_INNER_KERNEL_ALREADY_SEARCHED=true
Q20_CONDITIONED_CORRELATION_ALREADY_SEARCHED=true
Q21_NEEDED=false
```

A new q pass becomes legitimate only after an exact q17→conditioned-s measure adapter, a materially new post-mask theorem species, a new external result, or another receiver change.

## Global locks

```text
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
COMMON_ADAPTER_PROVED=false
SAVING_CROSS_PROMOTABLE=false
WHOLE_STAGE14_BLOCKED_BY_EXTERNAL_GATES=false
NEW_INTEGRATED_WHOLE_FAMILY_POWER_SAVING_PROVED=false
```
