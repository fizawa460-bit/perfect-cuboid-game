# Receiver-ready import certificate

A downstream Stage14 line may import a receiver only after every row is verified.

| Field | S certificate | fixed-U certificate |
|---|---|---|
| current receiver | `LargeSwitchPrimitivePythagoreanTwoLegIncidence` | `SharedUInvisibleCenteredProjectiveSelectorDispersion` |
| supersedes | fixed-split/projective slope dispersion | broad `SharedUPhysicalBipartiteDispersion` |
| coefficient type | positive physical state pairs | centered physical selector multiplicity |
| required labels | `H0,L0,W0,K_switch,K_agree,xi0` | fixed `U`, divisor fan, `delta`, branch and reconstruction masks |
| proved input | primitive Pythagorean identity and leg divisibilities | complete projective trace and constant-density mean |
| open output | power-saving physical lift count | centered selector mean square `<=R_U P^2 B^o(1)` |
| separate slice | n/a | one mixed branch |
| forbidden promotion | Gaussian bridge or character cancellation | complete box to sparse selector |

## Stale-contract rules

- `PrimePairProjectiveSlopeDispersion` is not the live s receiver.
- `SharedUPhysicalBipartiteDispersion` is a historical broad receiver, not the minimal t55 target.
- Complete-family cancellation cannot replace centered physical-selector dispersion.
- A signed Gaussian estimate cannot replace a positive pair-level incidence.
- Missing labels, masks, centering, scale, or supersession metadata force `STALE`.
- Receiver-to-receiver promotion requires a separately proved exact bridge.
