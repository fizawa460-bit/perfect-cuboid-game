# Stage14-Work-ciX47 receiver / supersession matrix

| Route / layer | Consumed facts | Active unresolved receiver | Superseded / forbidden shortcut |
|---|---|---|---|
| main aligned fixed-E | completed clean-room H audit | `UniformPrimitiveRectangleNestedKFreeQuadraticDivisorRootFirstMoment` | do not treat the completed-negative H audit as a whole-Stage14 blocker |
| s nonaligned scalar | filtered-tau3 support adapter; exact reciprocal-CRT kernel; pushforward upper fiber; hit-packet↔hit-witness exponent equivalence | `UniformScalarFilteredTau3Q17GoodPacketPushforwardIntersectionLowerCoverage` then post-mask | upper fiber does not imply lower coverage; no q17 primitive-pair measure substitution |
| s nonaligned polynomial pair | same inner adapters, but charged `(E,m)` outer measure | `UniformPolynomialOuterPairFilteredTau3Q17GoodPacketPushforwardIntersectionLowerCoverage` then post-mask | no `Em` scalarization; no scalar theorem substitution |
| fixed-U | completed tH33 audit | `SuperKaiIndividualGaussianResidueLongIntervalPrimeOccupancyLowerRatio` | no modulus/residue averaging cross-promotion |
| q ledger | q17 reciprocal-CRT kernel; q20 conditioned correlation | q21 good-packet / pushforward-image intersection coverage | do not rerun q17 inner-kernel search as q21 |

## Charged-once locks

```text
Q17_INNER_KERNEL_DEFICIT_RECHARGED=false
FILTERED_TAU3_SUPPORT_ADAPTER_RECHARGED=false
SECOND_REVERSE_MULTIPLICITY_RECHARGED=false
PUSHFORWARD_UPPER_FIBER_RECHARGED=false
GOOD_PACKET_HIT_FIBER_RECHARGE_FORBIDDEN=true
POST_MASK_REMAINS_SEPARATELY_CHARGED=true
PAIR_TO_SCALAR_HOST_ADAPTER_PROVED=false
```

## q21 exact target

For each frozen principal cell, with

```text
pi : Lambda -> Theta,
P=pi(Lambda),
G subset Theta = q17-good packet support,
H=G intersect P,
```

prove a fixed-power lower-coverage statement preserving the charged measure, e.g.

```text
#H >= B^(-o(1)) #P
```

or the exact branch-equivalent lower ratio needed to force

```text
delta_hit=0.
```

Two noninterchangeable variants remain:

```text
scalar Lambda,
polynomial outer-pair Lambda(E,m).
```

No q21 conclusion may be promoted to the residual root/canonical/post-column post-mask without a new exact adapter.
