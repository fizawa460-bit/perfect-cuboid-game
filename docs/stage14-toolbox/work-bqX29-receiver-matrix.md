# Stage14-Work-bqX29 receiver / supersession matrix

| route | merged input | charged outer coordinate | new conclusion | live receiver | cross-promotion |
|---|---|---|---|---|---|
| global heavy | 4fb..4fd | exact radial scale `h` | `M_C=B^(mu+o(1))` survivor forces `|H_*|>=B^(mu-o(1))`, with `mu<=1/4-phi<=1/24` | polynomial radial occupancy | none |
| s heavy | s7-84..86 + merged 4fd | same exact radial scale `h` | `(J,a,b)` is `B^o(1)` over fixed `h`; older short-radial mass-capacity gap is superseded by 4fd | same polynomial radial occupancy as global heavy | same global packet only |
| fixed-U | t124 | nonboundary cofactor / selected projective class | finite D4 atomic branch discharged by physical chamber + baseline split | selected projective-class near-total prime depletion | none |

## Supersession locks

```text
BPX28_GENERIC_ATOMIC_CAPACITY_FINAL_RECEIVER=false
MAINLINE_HEAVY_ATOMIC_CAPACITY_GAP_SUPERSEDED=true
S_HEAVY_CAPACITY_GAP_SUPERSEDED_BY_MERGED_4FD=true
FIXED_U_FINITE_BOUNDARY_ATOMIC_BRANCH_DISCHARGED=true
```

## Proven common interface

The global mainline and s-route now use the identical fixed-heavy-ray exact radial coordinate `h`:

```text
Xr*Yr=r0*h^2,
d0*J*a*b=c0*h,
#(J,a,b | h)=B^o(1).
```

Together with merged 4fd,

```text
B^(mu-o(1)) <= #H_* <= B^(1/4-phi+o(1)).
```

Therefore:

```text
GLOBAL_S_COMMON_RADIAL_OUTER_COORDINATE_PROVED=true
GLOBAL_S_RADIAL_FIBER_MULTIPLICITY=Bo1
```

This does **not** identify the fixed-U cofactor norm, its selected projective class, its prime measure, or quantifier order with `h`.

```text
COMMON_ARITHMETIC_OUTER_COORDINATE_ADAPTER_PROVED=false
COMMON_ADAPTER_PROVED=false
SAVING_CROSS_PROMOTABLE=false
```

## H matrix

| route | H needed now? | reason |
|---|---:|---|
| main heavy radial | no | exact radial predicate still has internal arithmetic opening |
| main non-heavy branches | yes | existing three-divisor / mover / diffuse correlation audits remain outstanding |
| s | no | same heavy radial coordinate; internal continuation first |
| fixed-U | no | t125 must freeze exact cofactor-to-class map and prime interval before tH29 |

```text
MAINLINE_H_NEEDED=true
S_ROUTE_H_NEEDED=false
FIXED_U_H_NEEDED=false
TH29_NEEDED=false
WHOLE_MAINLINE_BLOCKED_BY_H=false
```

## Next

```text
NEXT_INTEGRATED_TARGET=RadialOccupancyVersusSelectedProjectiveClassDepletionAdapterOrNoGo
NEXT_REVISIT_CONDITION=4fg+s7-89+t127
```
