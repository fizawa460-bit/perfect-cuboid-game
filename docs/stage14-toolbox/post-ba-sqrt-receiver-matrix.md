# Toolbox bb receiver matrix

| route | audited merged chain | current conclusion | live receiver |
|---|---|---|---|
| global | `s7-41 / 4cz -> X13 -> s7-42--44 / 4da--dc -> sH44 / 4dH` | `1/2` upper bound; no strict sub-square-root saving | `SquareRootThetaQuarterGaussianNormDivisorSplitPhysicalAdmissibilityZeroFrequencyDensity` |
| fixed `U` | `tH22 / t80 -> t81 -> t82 -> tH23 -> t83` | determinant-quotient switch; no fixed-`U` packet saving proved | `SharedUBalancedFixedUSelectorDivisorShortDeterminantQuotientCanonicalPrimeCoverBinaryNormEnergy` |

The bound improves from `23/44` to `1/2`; these are successive complete
whole-family estimates and are not multiplied.  The two live receivers have
different quantifier order and coefficient space and must not be identified.

`sH44` audits the frozen `s7-44` dual-root-line target.  `4dH` audits the later
`4dc` Gaussian-product physical-completion target.  Both return certified
delta zero, but one cannot be reused as the other's snapshot certificate.
Likewise `tH22` and `tH23` are consumed historical audits; `t83` requests no
`tH24`.
