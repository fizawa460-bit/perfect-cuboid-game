# Stage14-Work-bkX23 receiver / supersession matrix

| Route | Frozen labels already discharged | Live state space | Live Boolean selector | Principal density | Centered identity | Current saving |
|---|---|---|---|---|---|---|
| global / s | `ell_*`, Gaussian root, atomic chart, collision localization, `B^o(1)` completion witnesses | primitive rational slopes `(a:b)` on fixed root line and slope windows | `A_phys(a,b)` | `mu_G=B^(-o(1))` on a saturating sequence | `E|A_phys-mu_G|^2=mu_G(1-mu_G)` | none |
| fixed-U | common skeleton, exact prime action, prime-average localization | canonical-LPF background Gaussian cofactor fibers | fixed full boundary `b_*(x)` | `rho_*=B^(-o(1))` on survivor | `E|b_*-rho_*|^2=rho_*(1-rho_*)` | none |

## Common template

```text
COMMON_FIXED_BOOLEAN_PRINCIPAL_DENSITY_TEMPLATE_PROVED=true
COMMON_CENTERED_BERNOULLI_L2_IDENTITY_PROVED=true
COMMON_PRINCIPAL_DENSITY_EXPONENT_ZERO_SURVIVOR_PROVED=true
```

Both routes have reached

```text
f = mu + f^circ,
E f^circ = 0,
E|f^circ|^2 = mu(1-mu),
mu = B^(-o(1))
```

for one fixed Boolean physical selector on one frozen background family.

This is probability algebra, not an arithmetic identification.

## Non-identifications

```text
GLOBAL_FIXED_U_BACKGROUND_SPACES_IDENTIFIED=false
GLOBAL_FIXED_U_BACKGROUND_MEASURES_IDENTIFIED=false
GLOBAL_FIXED_U_BOOLEAN_SELECTORS_IDENTIFIED=false
COMMON_ARITHMETIC_ACCEPTANCE_ADAPTER_PROVED=false
SAVING_CROSS_PROMOTABLE=false
```

The global slope family and fixed-U Gaussian cofactor family have different variables, physical measures, coefficient maps and quantifier order. No fixed-U density bound may be cross-promoted to the global primitive-slope family without an explicit bridge.

## Supersession / charged-once matrix

| Earlier object | Current legal role | Re-charge allowed? |
|---|---|---|
| global candidate-prime image / collision energy | localization to a heavy fixed prime and primitive direction family | no |
| global root orientation / atomic chart | finite-label localization | no |
| global `B^o(1)` allocation/completion witnesses | finite fibers inside `A_phys` | no density saving from multiplicity alone |
| fixed-U generic-prime average / mover energy | localization to common skeleton/action | no |
| fixed-U exact action image | localization to one fixed full boundary | no |
| q12 Gaussian collision literature | radar for an earlier collision receiver | no current saving |

```text
COLLISION_ENERGY_RECHARGE_ALLOWED=false
PRIME_AVERAGE_RECHARGE_ALLOWED=false
FINITE_WITNESS_MULTIPLICITY_IMPLIES_DENSITY_SAVING=false
Q12_COLLISION_SAVING_CROSS_PROMOTED=false
```

## Current receivers

```text
CURRENT_GLOBAL_RECEIVER=FullConductorInteriorDensePrimitiveQuarterPythagoreanFixedSubpolynomialGaussianPrimeFixedRootFixedAtomicChartPrimitiveRationalSlopeBalancedReciprocalExistencePrincipalDensity
CURRENT_FIXED_U_RECEIVER=SharedUCanonicalLPFFixedFullElementaryBoundaryExponentZeroIntermediatePrincipalDensityUnderBackgroundGaussianCofactorMeasure
COMMON_RECEIVER=FrozenBackgroundFamilyFixedBooleanPhysicalAcceptancePrincipalDensity
```

## Next bridge test

Global:

```text
PrimitiveSlopePhysicalAcceptanceWitnessDecomposition
```

Fixed-U:

```text
FixedFullBoundaryBackgroundGaussianCofactorDensityDecomposition
```

Cross-route:

```text
BackgroundFiberPrincipalDensityAdapterOrNoGo
```
