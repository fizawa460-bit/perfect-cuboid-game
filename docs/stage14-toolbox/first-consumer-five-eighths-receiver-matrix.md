# Stage14-toolbox-ax current consumer / receiver matrix

This matrix is the canonical toolbox snapshot after the first merged consumers of `4cq`, `s7-30`, and `t69`.

| Source | First legal merged consumer(s) | Proved consequence | Current disposition |
|---|---|---|---|
| `14-4cq` | `14-X8`, `14-4cr` | exact dual-Cayley minimax / mainline promotion to `2/3`; Cayley `C_-/C_+` Gaussian orientation split | structurally active, but `2/3` is superseded as the global exponent by merged `s7-31` |
| `14-s7-30` (merged PR #517) | `14-s7-31` | fixed-outer common-gcd square-divisor lock removes the `sqrt(M)` term and proves `5/8` | strongest merged whole-family certificate |
| `14-t69` | `14-t70` | full common Cayley support CRT-compresses to one primitive root line; large-`J` branch near-linear | fixed-`U` live problem is only the small-common-support physical square-scale energy |

## Canonical current certificates

```text
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=5/8
CURRENT_GLOBAL_S_RECEIVER=FiveEighthsTwoBoundaryCommonCoreReciprocalIncidence
CURRENT_FIXED_U_RECEIVER=SharedUPrivateLargestPrimeSmallCommonSupportPhysicalSquareScaleEnergy
```

The two receivers are not equivalent and are not to be cross-promoted.

## Historical / superseded barriers

```text
3/4   toolbox-aw / 4cp / s7-29
11/16 merged s7-30 PR #517
2/3   X8 / 4cr
5/8   merged s7-31 (current)
```

The order above is chronological, not monotone by denominator.  The strongest bound is the smallest exponent, `5/8`.

## Duplicate-stage guard

Two PRs used the label `Stage14-s7-30`:

- PR #517: merged; proves `11/16`; canonical source on main.
- PR #521: closed without merge; not a theorem source and must never replace the merged result.

Any toolbox consumer that names the PR #521 short-cofactor receiver as a merged prerequisite is stale.

## Safe promotion rules

- Comparing two proved bounds on the same charged-once physical universe by `min` is legal.
- Multiplying independent-looking savings from common-core, dual-Cayley, and fixed-`U` analyses is not legal without an explicit joint packet theorem.
- The `4cq -> X8/4cr` `2/3` result remains reusable structure even though its exponent is globally dominated by `5/8`.
- `t70` is a fixed-`U` coefficient-space refinement and does not improve or weaken the global `5/8` theorem.
- `tH18` is consumed; `tH19` is not yet needed at the t70 boundary.
- No toolbox-H line is currently required.

## Next audit frontier

```text
4cr  -> first merged consumer
s7-31 -> first merged consumer
t70  -> first merged consumer
```

The next toolbox stage should refresh only after those consumers exist/merge, and should preserve the current `5/8` certificate unless a strictly stronger merged whole-family theorem appears.
