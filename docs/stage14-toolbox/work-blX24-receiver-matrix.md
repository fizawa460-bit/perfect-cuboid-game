# Stage14-Work-blX24 receiver matrix

| Route | Polynomial principal scale after localization | Subpolynomial inner objects | Current live density/support receiver | New fixed-power saving proved? |
|---|---|---|---|---|
| Global main/s | primitive rational slopes, ambient exponent `1/2` | heavy prime/root/chart labels and allocation/completion witness fibers | canonical balanced integer/Gaussian allocation density × conditional reciprocal/post-column completion density | no |
| Fixed-U t | outer canonical-LPF `Q` family | fixed-Q Gaussian background fiber `Omega_Q`, `|Omega_Q|=B^o(1)` | boundary-bearing `Q` support weight `omega_B(Q)` | no |

The abstract Bernoulli principal/centered template from Work-bkX23 remains valid, but the background scales do not match at the inner-fiber level.

```text
GLOBAL_BACKGROUND_POLYNOMIAL_EXPONENT=1/2
FIXED_U_FIXED_Q_BACKGROUND_FIBER_EXPONENT=0
DIRECT_LOCAL_BACKGROUND_FIBER_SCALE_MATCH=false
DIRECT_GLOBAL_TO_FIXED_Q_FIBER_DENSITY_ADAPTER_NOGO=true
FUTURE_OUTER_SUPPORT_ADAPTER_NOT_RULED_OUT=true
COMMON_PRINCIPAL_SCALE_RELOCATION_PRINCIPLE_PROVED=true
COMMON_ARITHMETIC_ACCEPTANCE_ADAPTER_PROVED=false
COMMON_ADAPTER_PROVED=false
SAVING_CROSS_PROMOTABLE=false
```

## Global charged-once chain

Merged 4dz and s7-68 give

```text
mu_G = mu_can * mu_recip
```

on nested Boolean slope families. The factors are conditional/cardinality ratios, not independent local densities. On square-root saturation each is exponent-zero; any fixed-power deficit in either closes the branch.

```text
GLOBAL_CANONICAL_ALLOCATION_RECIPROCAL_CHAIN_IMPORTED=true
GLOBAL_DENSITY_FACTORS_INDEPENDENT=false
GLOBAL_FINITE_WITNESS_MULTIPLICITY_RECHARGE_ALLOWED=false
```

## Fixed-U charged-once tower

Merged t105 gives

```text
rho_Q=N_Q/M_Q,
M_Q=B^o(1),
0<=omega_B(Q)=N_Q<=B^o(1).
```

Nonempty fixed-Q acceptance already forces `rho_Q=B^(-o(1))`; the local fiber density is therefore not an independent polynomial analytic length. The positive principal scale is the outer support of `omega_B(Q)`.

```text
FIXED_U_LOCAL_FIBER_DENSITY_FIXED_POWER_SAVING_AVAILABLE=false
FIXED_U_OUTER_Q_OCCUPANCY_IS_REMAINING_PRINCIPAL_SCALE=true
T104_TWO_SIDED_MINIMAL_PRINCIPAL_SURVIVOR_LOCK_SUPERSEDED=true
```

## H matrix

| Route | H needed now? | Reason |
|---|---:|---|
| mainline | false | exact polynomial receiver still needs one internal arithmetic substitution |
| s | false | canonical allocation/reciprocal factors were just exposed |
| fixed-U | false | outer `Q` support weight must first be arithmetically decomposed |

```text
MAINLINE_H_NEEDED=false
S_ROUTE_H_NEEDED=false
FIXED_U_H_NEEDED=false
TH28_NEEDED=false
```

Next integrated target:

```text
PolynomialPrincipalSupportArithmeticDecomposition
```
