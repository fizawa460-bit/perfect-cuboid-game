# Stage14-Work-ckX49 receiver / supersession matrix

| Route / layer | Consumed facts | Active unresolved receiver | Superseded / forbidden shortcut |
|---|---|---|---|
| main aligned fixed-E | completed clean-room H audit | `UniformPrimitiveRectangleNestedKFreeQuadraticDivisorRootFirstMoment` | do not promote completed-negative H audit to a whole-Stage14 blocker |
| s nonaligned scalar | filtered-tau3 support, q17 witness expansion, exact joint incidence | `UniformScalarFilteredTau3ConditionedQ17ReciprocalCRTJointIncidenceFirstMomentLowerBound`, then post-mask | do not recharge indicator/support/second-moment steps |
| s nonaligned polynomial pair | same inner reductions on charged `(E,m)` measure | `UniformPolynomialOuterPairFilteredTau3ConditionedQ17ReciprocalCRTJointIncidenceFirstMomentLowerBound`, then post-mask | no `(E,m)->Em` scalarization |
| fixed-U | completed tH33 audit | `SuperKaiIndividualGaussianResidueLongIntervalPrimeOccupancyLowerRatio` | no modulus/residue averaging cross-promotion |
| q ledger | q22 indicator first-moment radar plus successful s7-147/148 handoffs | q23 exact joint filtered-tau3 / reciprocal-CRT first moment | do not rerun q22 Boolean-indicator search |

## Charged-once locks

```text
GOOD_INDICATOR_TO_Q17_WITNESS_EQUIVALENCE_CONSUMED=true
JOINT_FILTERED_TAU3_Q17_CRT_INCIDENCE_NORMAL_FORM_CONSUMED=true
GOOD_PACKET_SECOND_MOMENT_RECHARGE_FORBIDDEN=true
Q17_WITNESS_MULTIPLICITY_RECHARGED_AS_SAVING=false
RESOLVED_SUPPORT_TO_MOMENT_ADAPTER_RECHARGE_FORBIDDEN=true
PAIR_TO_SCALAR_HOST_ADAPTER_PROVED=false
POST_MASK_REMAINS_SEPARATELY_CHARGED=true
```

## q23 exact target

For each frozen active principal cell prove the correct fixed-power positive lower bound for

```text
J1_G
 = sum_{lambda in Lambda}
     sum_{f*n=W1(lambda)} R_q17(lambda;f,n),
```

where `lambda` is an already-filtered first-layer tau3 witness and `R_q17` retains all already-frozen q17 reciprocal-CRT packet conditions. The target must preserve the charged theorem measure.

Two variants are noninterchangeable:

```text
scalar Lambda,
polynomial outer-pair Lambda(E,m).
```

No q23 result may be promoted to the residual root/canonical/post-column post-mask without a new exact adapter.
