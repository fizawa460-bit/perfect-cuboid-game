# Stage12-N1b scope

This branch tests whether the shared-p convolution can absorb the primitive correction exactly.

The core identity under audit is

`gcd(x,y,c)=gcd(gcd(x,y),c)`.

The branch does not claim an asymptotic estimate or an improvement of the Stage11 lower bound.
