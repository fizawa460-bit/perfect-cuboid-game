# Stage32 Arsenal Harvest 4 — hostile dedup

Status: **HOSTILE DEDUP COMPLETE / REGISTRATION AND STABLE-ID ASSIGNMENT NOT PERFORMED**.

This phase compares the 21 temporary Harvest 4 abstraction units against the current routed Arsenal contracts and repository-wide promotion/route policies. Matching is semantic, not keyword-based: object/population, field/marking, canonicalization, inputs, operation, outputs, quantifiers, credit layer, and adapter hypotheses must agree before an abstraction is called duplicate or an extension.

The dedup phase does not edit `docs/arsenal/index.json`, generated cards/catalog, Stage32 authority, or mathematical credit.

## Result

| Classification | Count |
|---|---:|
| NEW_WEAPON | 7 |
| NEW_WORKFLOW | 2 |
| EXTEND_EXISTING | 3 |
| DUPLICATE_NO_REGISTER | 8 |
| BLOCKED unique interface | 1 |
| **Total** | **21** |

## NEW_WEAPON — retain for registration planning

### `ABS-S32-H4-01` — SUPPORT_MASS_CAP_NECESSARY_PRUNER

Retain as new. `S32-PW01` can count/index an exact family but does not derive a zero-loss necessary support/mass/capacity inequality. The Stage32 numerical constants and observed saving remain provenance only.

### `ABS-S32-H4-08` — FINITE_STABILIZER_CAPACITY_PARITY_ELIMINATOR

Retain as new. `S30-W01` identifies finite actions and `S30-W03` classifies marked descent states; neither supplies the exact two-stage eliminator “capacity first, exhaustive residual parity second”. Credit remains only on the exact finite stabilizer population.

### `ABS-S32-H4-09` — NORMALIZATION_CONDUCTOR_EVEN_CORRECTION_COUPLER

Retain as new. Existing branch-profile/cover cards do not provide the necessary coupling

```text
Disc(pi) = Br + 2 A
```

with conductor correction and ramification supported at the same base points. This is a necessary-condition weapon, not an existence theorem.

### `ABS-S32-H4-10` — CELLULAR_SMITH_COKERNEL_ASSEMBLY_OBSTRUCTION

Retain as new. The shared use of Smith normal form does **not** make this `S32-PW04`: PW04 classifies affine-lattice fractional classes for a quadratic bound, whereas this unit computes cokernel obstruction classes/torsion for a finite cellular gluing assembly.

### `ABS-S32-H4-13` — WEIGHTED_LOCAL_DEBT_AND_FACTORWISE_RAMIFICATION_SPLIT

Retain only the positive mathematical core as new: weighted divisor debt plus exact factorwise/nonnode ramification accounting. The generic “do not double charge a dependent aggregate equation” part is already covered by repository firewalls and is not a second new contribution.

### `ABS-S32-H4-18` — VALUATION_SEPARATED_SECTION_LOWER_BOUND_CERTIFIER

Semantically new: source-locked valuations prove newly constructed sections independent of an existing section subspace, yielding a certified lower bound on section-space dimension. No routed card with that input/output contract was found. Registration remains blocked until the currently `PROVISIONAL_CLOSED_UNMERGED` source receives the required retained/audited authority.

### `ABS-S32-H4-CF21` — SOURCE_BOUND_ORBIT_CHARACTER_WITH_ABSOLUTE_MARKING_FIREWALL

Retain as new. Harvest 3 already classified PR #1643 as a **genuinely new interface**, but authority was not recoverable at that freeze. Harvest 4 recovered hostile PASS `5123545511` on exact head `8550ab88e12cbbfd42b2d1e07c8f42be124de1a6`.

The new contract remains narrow:

```text
source-bound finite action + exhaustive source-labelled probes
-> source-bound abstract character/direction
-> residual absolute marking torsor remains explicit
```

`chi_u -> delta_0inf` still does **not** identify `delta_0inf` with an absolute retained line in `W`; no Q602/O210 arithmetic exclusion follows.

## NEW_WORKFLOW — retain for registration planning

### `ABS-S32-H4-07` — SYMBOLIC_FREE_AXIS_EXACT_UNSAT_BLOCK_PARTITION

Retain as a workflow, not a theorem weapon. Existing certificate replay validates results but does not prescribe partitioning only a finite outer rank while keeping an independent coordinate symbolic through every exact UNSAT leaf.

### `ABS-S32-H4-15` — CANONICAL_GEOMETRIC_IDENTITY_RECONSTRUCTION_STACK

Retain as a workflow. Existing cards require source-derived semantic anchors and immutable replay, but do not provide the general reconstruction chain

```text
source equations/Jacobian
-> exact singular objects
-> model-renaming equivalence
-> collision-free canonical projective fingerprints
-> runtime index -> canonical geometric identity
```

Runtime enumeration order is never semantic identity. This workflow grants infrastructure credit only.

## EXTEND_EXISTING — no new stable ID

### `ABS-S32-H4-02` -> `S32-PW01`

Extend the existing exact compression/indexer card with **secondary filtered survivor rank/unrank while preserving the old canonical completeness authority**. Creating another card would recreate the same-object split that previously caused `S32-PW02` to be retired into PW01.

### `ABS-S32-H4-06` -> `S32-PW03`

Extend the HNF lattice-image gate with an exact functional-augmentation ladder: add independent source-derived observables, detect dependent/dominated functionals, and pass forced derived quantities to later inequalities. The underlying object/output remains HNF image membership. Source promotion waits on the N260/N280 audit boundary.

### `ABS-S32-H4-12` -> `S30-W01` / `S30-WF01`

Extend the finite-equivariant identification interface to explicitly retain the strongest partial marking **and** the residual finite torsor/phase ambiguity. This does not justify a separate weapon: `S30-W01/WF01` already forbid finite survivors or gauge representatives from becoming canonical semantic markings without a source anchor.

## DUPLICATE / NO REGISTER

- `ABS-S32-H4-03`: generic immutable manifest / exact leaf certificate / fail-closed completeness separation routes to `S30-WF02`, `S30-WF03`, and the existing Stage32 canonical evidence chain. Source re-audit remains required, but no new workflow ID is justified.
- `ABS-S32-H4-04`: exact post-filter counts/order statistics are downstream uses of `S32-PW01`; route prioritization is operational policy, not a new proof weapon.
- `ABS-S32-H4-05`: the saturated all-ones vector is a strict endpoint/special case of H4-01’s support/mass/capacity mechanism. Keep it inside that future card rather than minting a micro-card.
- `ABS-S32-H4-11`: source-population exclusion + separately audited population identity before transfer is exactly the existing typed semantic-transfer firewall (`S30-WF03` and repository policy).
- `ABS-S32-H4-14`: ordinary automorphism orbit versus field/Galois-conjugate class is already governed by formal `S30-W03` (and semilinear setup by `S30-W02`). Outside the ordinary orbit alone is not a descent obstruction.
- `ABS-S32-H4-16`: explicit `PROVES/DOES_NOT_PROVE` claim edges are an implementation of the formal typed credit DAG `S30-WF03`.
- `ABS-S32-H4-17`: bounded route-family statuses, reopen conditions, no silent discard and no-recharge are already the repository-wide Cycle Exploration Safety Protocol candidate-ledger/parking contract.
- `ABS-S32-H4-19`: labelled-node support/span import requires exact identity and semantic adapters; that is the existing Stage32 `SOURCE_LOCKED_ADAPTER_WALL` plus `S30-WF03`. Label coincidence alone remains forbidden.

## BLOCKED unique interface

### `ABS-S32-H4-CF20` — EQUIVARIANT_QUOTIENT_NONINVARIANCE_TO_COMMUTATOR_CHAIN

Do **not** discard as duplicate. Harvest 3 already identified the repaired quotient-stabilizer -> noninvariance -> equivariant injective pullback -> conditional correspondence-commutator chain as genuinely new.

It remains unregistrable because no hostile re-audit receipt was recovered for exact head

```text
80de2c23979a691a8255baa221e31ad4b6f93f49
```

The withdrawn strict-transform exceptional-multiplicity shortcut remains permanently forbidden. Classification is therefore `BLOCKED`, not `DUPLICATE`.

## Hostile checks that changed the naive abstraction result

1. H4-05 was collapsed into H4-01 instead of becoming a trivial one-lemma card.
2. H4-02 and H4-06 were routed to existing Stage32 cards rather than creating same-object duplicates.
3. H4-03, H4-11, H4-16 and H4-19 were rejected as generic adapter/credit workflows already covered by formal Stage30/policy contracts.
4. H4-14 was rejected because formal `S30-W03` already separates ordinary finite orbits from marked/Galois arithmetic classes.
5. H4-13 was narrowed: only weighted debt/ramification accounting survives as new; generic no-double-charge language does not.
6. PR #1643 changed from Harvest-3 promotion-blocked to a new registration candidate because its exact hostile PASS is now recoverable.
7. PR #1570 did **not** change: uniqueness is retained, but audit provenance is still insufficient.

## Registration queue

New weapons:

```text
ABS-S32-H4-01
ABS-S32-H4-08
ABS-S32-H4-09
ABS-S32-H4-10
ABS-S32-H4-13
ABS-S32-H4-18
ABS-S32-H4-CF21
```

New workflows:

```text
ABS-S32-H4-07
ABS-S32-H4-15
```

Existing-card extensions:

```text
ABS-S32-H4-02 -> S32-PW01
ABS-S32-H4-06 -> S32-PW03
ABS-S32-H4-12 -> S30-W01 / S30-WF01
```

Blocked unique candidate:

```text
ABS-S32-H4-CF20
```

No stable IDs are assigned in this phase.

## Firewalls

- hostile dedup is not Arsenal registration;
- no `docs/arsenal/index.json` edit;
- no generated card/catalog edit;
- no provisional source is upgraded merely because its semantic interface is unique;
- no Stage32 MAIN/EX authority change;
- no numerical/receiver/route/theorem/endpoint credit;
- no Perfect Cuboid existence/nonexistence claim;
- no merge authorization.

Machine ledger: `docs/arsenal/stage32-harvest4-hostile-dedup.json`.

Next phase: **`STAGE32_HARVEST4_REGISTRATION_PLANNING`**.
