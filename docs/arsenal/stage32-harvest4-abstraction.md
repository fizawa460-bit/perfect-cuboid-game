# Stage32 Arsenal Harvest 4 — abstraction

Status: **ABSTRACTION COMPLETE / HOSTILE DEDUP, REGISTRATION, STABLE-ID ASSIGNMENT NOT PERFORMED**.

This phase compresses the 25 raw Harvest 4 discoveries into reusable semantic contracts. These `ABS-S32-H4-*` keys are temporary Harvest keys, not Arsenal IDs.

## Frozen source boundary

The abstraction uses exactly the discovery freeze:

- previous Harvest 3 upper bound: `714bd143f0c20082edcbb81c3905a86b1a56b4bf`;
- merged-source upper bound: `bf2890ec0b8168f70db803de876024aa6b6d1f6d`;
- PR #1753 is read only through frozen snapshot head `1b171f5f45ded8937f51a666e606d88b26a96402` and is not treated as repository-main authority.

Later Stage32 movement does not enter Harvest 4 automatically.

## Compression result

- raw discoveries: **25**;
- new reusable abstraction units: **19**;
- Harvest 3 carry-forward abstractions: **2**;
- total abstraction units entering hostile dedup: **21**.

The following raw strengthening chains are intentionally bundled instead of being registered as near-duplicate weapons:

- A06 + A07 + A08 -> exact functional-augmentation ladder: baseline HNF negative result, normal-mass augmentation, then dependent-functional extraction;
- B05 + B06 -> partial marking plus residual torsor certificate;
- B07 + B08 -> weighted local debt plus ramification-split/no-double-counting accounting;
- B09 + B10 -> finite-node action plus field-involution orbit diagnostic;
- C01 + C02 -> source-model reconstruction plus runtime-index-to-canonical-geometry identity stack.

## Main reusable contracts

1. `ABS-S32-H4-01` — derive zero-loss necessary pruning from support, mass and capacity constraints.
2. `ABS-S32-H4-02` — add survivor random access without replacing canonical completeness rank.
3. `ABS-S32-H4-03` — fail closed between structural filtered coverage and actual numerical leaf completeness.
4. `ABS-S32-H4-04` — exact post-filter census for route prioritization without claiming new pruning.
5. `ABS-S32-H4-05` — saturated support + exact mass forces an all-ones integral vector.
6. `ABS-S32-H4-06` — progressively augment integer-lattice tests with exact functionals, while recording dominated/dependent tests as anti-loop evidence.
7. `ABS-S32-H4-07` — partition only finite outer ranks while retaining one free axis symbolically through exact UNSAT proof.
8. `ABS-S32-H4-08` — finite stabilizer elimination by coarse capacity followed by exact parity branching.
9. `ABS-S32-H4-09` — normalization/conductor discriminant coupling with the required even correction.
10. `ABS-S32-H4-10` — cellular Smith-cokernel obstruction for finite assembly/gluing classes.
11. `ABS-S32-H4-11` — exclusion transfer only after a separately audited population-identity adapter.
12. `ABS-S32-H4-12` — recover the strongest partial marking while certifying the unresolved finite torsor/phase.
13. `ABS-S32-H4-13` — weighted local debt accounting plus explicit detection of dependent aggregate ramification equations.
14. `ABS-S32-H4-14` — finite automorphism-orbit versus field-conjugate-class diagnostic, without promoting outside-orbit to descent failure.
15. `ABS-S32-H4-15` — reconstruct canonical geometric identities and bridge runtime enumeration to them via exact fingerprints.
16. `ABS-S32-H4-16` — fail-closed claim-DAG promotion workflow with explicit scope and audit transitions.
17. `ABS-S32-H4-17` — bounded route-family exhaustion ledger with named re-entry conditions, not a global impossibility statement.
18. `ABS-S32-H4-18` — valuation separation gives a certified lower bound on a section-space dimension, not full `H^0`.
19. `ABS-S32-H4-19` — typed import adapter for external labelled-node support/span data.

## Carry-forward recheck

### `ABS-S32-H4-CF20` — PR #1570

The repaired reusable mathematical shape is clear: blow-down quotient noninvariance + equivariant finite-cover pullback injectivity can feed a conditional correspondence-commutator obstruction. The old strict-transform exceptional-multiplicity shortcut remains forbidden.

However, the current GitHub record queried in this phase did not expose a hostile re-audit receipt for exact head `80de2c23979a691a8255baa221e31ad4b6f93f49`; the PR body itself ends at `STOP pending hostile re-audit`. Therefore this abstraction remains `BLOCKED_AUDIT_RECEIPT` and must not be promoted during dedup.

### `ABS-S32-H4-CF21` — PR #1643

The previously missing maturity evidence is now recoverable: hostile audit PASS review/comment `5123545511` covers exact head `8550ab88e12cbbfd42b2d1e07c8f42be124de1a6`.

The reusable contract is deliberately narrow: a source-bound H-character probe may recover the abstract `chi_u -> delta_0inf` direction while preserving the finite marking torsor. It still does **not** identify `delta_0inf` with one absolute retained line in `W`, and therefore grants no Q602/O210 arithmetic exclusion.

This unit may enter hostile dedup as audited source-bound infrastructure.

## Explicit firewalls

This abstraction does not:

- classify any unit as `NEW_WEAPON`, `EXTEND_EXISTING`, or `NEW_WORKFLOW`;
- compare the 21 abstractions against current Arsenal cards;
- allocate stable Arsenal IDs;
- edit `docs/arsenal/index.json`;
- edit generated catalog/cards;
- change Stage32 MAIN/EX authority;
- convert provisional/audit-required source results into audited results;
- grant numerical, receiver, theorem, endpoint, existence/nonexistence, or Perfect Cuboid credit;
- authorize merge.

Machine ledger: `docs/arsenal/stage32-harvest4-abstraction.json`.

Next phase: **`STAGE32_HARVEST4_HOSTILE_DEDUP`**.