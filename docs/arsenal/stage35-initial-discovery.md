# Stage35 / Stage35-EX Arsenal initial harvest discovery ledger

`DISCOVERY + ABSTRACTION COMPLETE`

`HOSTILE DEDUP NOT YET COMPLETE`

`ARSENAL PROMOTION NOT PERFORMED`

Frozen range: Stage35 inception `43b5cc4a655cf68e9bdefad22800071d6f8d9fa0`; Stage35-EX inception `7423eab4f153df7e58c3c9aa7ef3ecdf3f53c4f8`; upper bound `9184c7ab694415592cc428a675c0ebed27cac510`. Scope is not widened by later main movement.

Machine abstraction: `docs/arsenal/stage35-initial-abstraction.json`, containing all 36 abstraction sheets with object type, hypotheses, transformation, output, failure contract, marking/source lock, reuse test, and source locator. Harvest 1 exact per-candidate ledger is retained immutably at blob `c8f5c36315981589417fb3e0282c5c6194bbfaa9`; `docs/arsenal/stage35-initial-discovery.json` binds that H1 record to H2.

H2 classes: `ABSTRACTABLE=7` (`A02,A03,A05,A09,A10,B01,B05`); `PARTIALLY_ABSTRACTABLE=14` (`A01,A04,A06,A07,A08,A11,B02,B03,B04,B06,B07,F01,F02,F03`); `STAGE35_SPECIFIC=3` (`D01-D03`); `NEGATIVE_ONLY=6` (`E01-E06`); `WORKFLOW_ONLY=6` (`C01-C06`).

Key decomposition: upstream primitive/Pythagorean extraction remains source-dependent; `A01` keeps only the coprime product-rectangle core; `B01` is a separate product-square receiver; `A02/A03` are downstream dynamic-support/squareclass-graph contracts. Product-square is not factorwise square, and A02/A03 do not yet yield the fixed finite exhaustive family required by `S34-W01`.

Gaussian: `D03` is Stage35-specific/classical; `A04` is only the source-locked joint-orientation compatibility contract after unit/conjugation control. Elliptic/Kummer: keep `A05-A08` separate as quotient, source-lift normal form, completion, and reciprocal compression; `B07` gives only quotient structure, not specialization-new point control. `S31-W01` remains the separate quartic/elliptic birational adapter.

Picard/Brauer: `B03/B04` are marked and `S33-PW04/PW07` adjacent; `D02` is Stage35-specific; `F01` is an incomplete open-boundary adapter; `F03` requires a genuine global unramified class and exact local evaluations. Proper Brauer is not open Brauer; a Brauer candidate is not an obstruction.

Most promising reusable contracts beyond those above: `A09` primitive pair-gcd skeleton, `A10` finite-census + bounded-genus Weil completion, `B05` forced-prime support + symmetry/Burnside compression. `A11` is only partially abstractable because its retained local theorem is prime/residue-pattern specific.

Harvest 3: keep the range frozen; hostile-dedup math candidates `A01-A11,B01-B07,F01-F03`, workflows `C01-C06`, and never promote `D01-D03,E01-E06` as mathematical weapons. Compare against `S34-W01,S34-W03,S34-WF01,S33-PW04,S33-PW07,S31-W01,S30-WF03` on object type, field, quantifiers, markedness, hypotheses, transformation, output, and failure contract. `A01` only at rectangle-core level; `A02/A03` only pre-enumeration; `A04` only joint orientation; keep `A05-A08` separate unless semantically identical; preserve proper/open firewall and dedup workflows against Research OS.

Forbidden remains unchanged: no stable IDs, no Arsenal promotion, no `index.json`/cards/catalog change, no Stage35/Stage35-EX authority or progression change, no merge.
