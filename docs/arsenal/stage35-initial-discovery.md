# Stage35 / Stage35-EX Arsenal initial harvest discovery ledger

`DISCOVERY + ABSTRACTION COMPLETE`

`HOSTILE DEDUP NOT YET COMPLETE`

`ARSENAL PROMOTION NOT PERFORMED`

Harvest 1 + Harvest 2 use the same frozen range and same PR. The exact lower/upper bounds remain:

- Stage35 inception: `43b5cc4a655cf68e9bdefad22800071d6f8d9fa0` (PR `#1506` merge).
- Stage35-EX inception: `7423eab4f153df7e58c3c9aa7ef3ecdf3f53c4f8` (`stage35-ex: add bounded startup charter`).
- frozen upper bound: `9184c7ab694415592cc428a675c0ebed27cac510`.
- no later-main result is imported.

Artifacts:

- `docs/arsenal/stage35-initial-discovery.json` — H1 registry/status plus immutable pointer to the full pre-H2 H1 36-record ledger blob `c8f5c36315981589417fb3e0282c5c6194bbfaa9`;
- `docs/arsenal/stage35-initial-abstraction.json` — 36 H2 abstraction sheets with object type, hypotheses, transformation, output, failure contract, marking/source-lock, reuse test, and source locator;
- this file — human H1/H2 summary and H3 handoff.

No `docs/arsenal/index.json`, generated card, catalog, Stage35 MAIN state, or Stage35-EX authority is modified. No stable ID, theorem/receiver/endpoint/E1/R29/FIB2/J12/Stage35/perfect-cuboid credit, or merge authorization is added.

## Harvest 2 reclassification

| Class | Count | Temporary IDs |
|---|---:|---|
| `ABSTRACTABLE` | 7 | `A02,A03,A05,A09,A10,B01,B05` |
| `PARTIALLY_ABSTRACTABLE` | 14 | `A01,A04,A06,A07,A08,A11,B02,B03,B04,B06,B07,F01,F02,F03` |
| `STAGE35_SPECIFIC` | 3 | `D01,D02,D03` |
| `NEGATIVE_ONLY` | 6 | `E01-E06` |
| `WORKFLOW_ONLY` | 6 | `C01-C06` |
| **Total** | **36** | |

## Product-square chain

The original chain is deliberately split rather than promoted as one weapon. Primitive/Pythagorean extraction remains source-dependent. `A01` retains only the exact coprime product-rectangle core: from `RS=UV` with `gcd(R,S)=gcd(U,V)=1`, prime powers split into four pairwise-coprime cells giving `R=alpha beta`, `S=gamma delta`, `U=alpha gamma`, `V=beta delta`. `B01` is a separate product-square transformation. `A02` and `A03` are downstream support/graph methods.

Firewall: **product-square is not factorwise square**.

## Squareclass

`A02` abstracts exact pairwise-gcd information into a dynamic reservoir incidence table, including separate 2-adic bookkeeping. `A03` abstracts complete pair-product squareclass relations into a compatibility graph with independent reservoir labels. Neither supplies a fixed finite exhaustive squareclass branch family, so `S34-W01` remains a later hostile-dedup/extension question rather than automatic credit.

## Gaussian / norm

The three concrete Gaussian face factorizations `D03` are Stage35-specific; the generic use of `Z[i]` is classical. `A04` survives only partially at the **joint-orientation** level: two genuinely distinct, source-locked norm orientations may yield a new squareclass compatibility condition after exact unit/conjugation control. A mirror orientation related by source symmetry is not independent.

Firewall: Gaussian factorization or a necessary norm condition is not a global obstruction.

## Elliptic / Kummer

`A05-A08` stay split:

- `A05`: involution quotient + fixed/boundary accounting + converse reconstruction;
- `A06`: rational-source lift predicate + source-faithful Kummer normal form;
- `A07`: internalize residual source square(s) into a complete finite Kummer receiver;
- `A08`: reciprocal common-factor compression of an already-complete receiver.

`B07` is only partially abstractable: sign involutions can yield elliptic quotient inventories, but quotient structure does not classify the high-genus source or specialization-new points. `S31-W01` remains the separate quartic-to-elliptic exact birational adapter.

Firewalls: finite quotient is not point classification; genus-one/elliptic model is not MW closure; generic function-field information is not an all-specialization theorem.

## Picard / Brauer

`B03/B04` are partially abstractable only with an exact source-marked full Picard G-module and therefore strongly overlap `S33-PW04/PW07`. `D02` remains the Stage35-specific proper-surface result. `F01` is the still-incomplete open-boundary schema: boundary divisor module -> proper Picard map -> `Pic(Ubar)` / localization / algebraic Brauer. Proper `H^1(G,Pic)` or proper `Br_1` cannot be transferred to the open receiver by relabeling. `F03` cannot become Brauer-Manin credit without one exact global unramified class and source-bound local evaluations.

Firewalls: Brauer candidate is not Brauer obstruction; proper Brauer is not open Brauer.

## Other strongest reusable contracts

- `A09`: primitive triple pair-gcd skeleton `A=xy*a`, `B=xz*b`, `C=yz*c`, with only genuinely forced coprimalities transported.
- `A10`: finite exceptional-prime classification by exact small-prime census plus bounded-genus auxiliary-curve/Weil completion for sufficiently large primes.
- `B05`: finite forced-prime support assignment followed by symmetry/Burnside orbit compression; a routing receiver, not global squareclass exhaustion.
- `A11`: p-adic valuation-cone/signed-gap method remains partially abstractable because the retained theorem is prime/residue-pattern specific.

## Marking / source lock

Marking remains load-bearing for `A04`, `A06-A08`, `B03`, `B06`, `F01`, and `F03`. The core contracts in `A01`, `A02`, `A03`, `A05`, `A09`, and `A10` can be stated without Stage35 variable names once their exact hypotheses are retained.

## Harvest 3 exact handoff

Keep the range frozen at `9184c7ab694415592cc428a675c0ebed27cac510`. Hostile-dedup mathematical scope is `A01-A11`, `B01-B07`, `F01-F03` (21 candidates). Workflow scope is `C01-C06`. `D01-D03` and `E01-E06` must not be promoted as mathematical weapons.

Compare against `S34-W01`, `S34-W03`, `S34-WF01`, `S33-PW04`, `S33-PW07`, `S31-W01`, `S30-WF03`, using object type, field, quantifiers, markedness, hypotheses, transformation, output, and failure contract. Dedup `A01` only at rectangle-core level; test `A02/A03` only as pre-enumeration support/graph contracts; test `A04` only at joint-orientation level; keep `A05-A08` split unless hostile comparison proves identical semantics; compare `B03/B06` against `S33-PW04` and `B04/F03` against `S33-PW07`; preserve proper/open separation; dedup `C01-C06` against Arsenal workflows and Research OS; retain `E01-E06` as negative history.

No stable IDs, Arsenal promotion, index/cards/catalog change, Stage authority change, or merge is authorized.
