# Stage35 / Stage35-EX Arsenal initial harvest discovery ledger

`DISCOVERY + ABSTRACTION COMPLETE`

`HOSTILE DEDUP NOT YET COMPLETE`

`ARSENAL PROMOTION NOT PERFORMED`

Frozen bounds remain Stage35 `43b5cc4a655cf68e9bdefad22800071d6f8d9fa0`, Stage35-EX `7423eab4f153df7e58c3c9aa7ef3ecdf3f53c4f8`, and upper bound `9184c7ab694415592cc428a675c0ebed27cac510`. No later-main result is imported.

Machine records: `docs/arsenal/stage35-initial-discovery.json` binds the frozen H1 ledger/history and H2 status; `docs/arsenal/stage35-initial-abstraction.json` contains all 36 H2 abstraction sheets.

H2 classification: `ABSTRACTABLE=7` (`A02,A03,A05,A09,A10,B01,B05`); `PARTIALLY_ABSTRACTABLE=14` (`A01,A04,A06,A07,A08,A11,B02,B03,B04,B06,B07,F01,F02,F03`); `STAGE35_SPECIFIC=3` (`D01-D03`); `NEGATIVE_ONLY=6` (`E01-E06`); `WORKFLOW_ONLY=6` (`C01-C06`). Total 36.

The product-square chain is split: upstream primitive/Pythagorean extraction remains source-dependent; `A01` retains only the coprime product-rectangle core; `B01` is a separate product-square transformation; `A02/A03` are downstream support/graph methods. `product-square != factorwise-square`.

Squareclass: `A02` is dynamic gcd-reservoir support and `A03` is a parametric squareclass compatibility graph. Neither gives the fixed finite exhaustive family required by `S34-W01`.

Gaussian: concrete `D03` formulas are Stage35-specific/classical. `A04` survives only as source-locked joint-orientation compatibility after exact unit/conjugation control. A mirrored orientation is not independent. A necessary norm condition is not a global contradiction.

Elliptic/Kummer: keep `A05-A08` separate: involution quotient; rational-source lift normal form; Kummer receiver completion; reciprocal common-factor compression. `B07` is only a partial sign-involution quotient method. `S31-W01` separately owns quartic-to-elliptic birational certification. Finite quotient is not point classification, and a genus-one/elliptic model is not MW or all-specialization closure.

Picard/Brauer: `B03/B04` remain source-marked and `S33-PW04/PW07`-adjacent; `D02` is specific; `F01` is an incomplete open-boundary adapter; `F03` needs one exact global unramified class with source-bound local evaluations. Proper Brauer is not open Brauer, and a Brauer candidate is not an obstruction.

Other strongest reusable contracts are `A09` primitive pair-gcd skeleton, `A10` finite-census + bounded-genus Weil completion, and `B05` forced-prime support + symmetry/Burnside compression. `A11` remains only partially abstractable because its retained local theorem is prime/residue-pattern specific.

Harvest 3 exact handoff: keep upper bound frozen; hostile-dedup mathematical candidates `A01-A11,B01-B07,F01-F03` and workflows `C01-C06`; do not promote `D01-D03,E01-E06` as mathematical weapons. Compare against `S34-W01,S34-W03,S34-WF01,S33-PW04,S33-PW07,S31-W01,S30-WF03` on object type, field, quantifiers, markedness, hypotheses, transformation, output, and failure contract. Dedup `A01` only at rectangle-core level; test `A02/A03` only as pre-enumeration contracts; test `A04` only jointly; keep `A05-A08` split unless semantically identical; compare `B03/B06` to `PW04` and `B04/F03` to `PW07`; preserve proper/open separation; dedup `C01-C06` against workflows/Research OS; retain `E01-E06` as negative history.

No stable IDs, Arsenal promotion, `index.json`/cards/catalog change, Stage authority change, Stage35 progression, or merge is authorized.
