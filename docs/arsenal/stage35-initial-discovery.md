# Stage35 / Stage35-EX Arsenal initial harvest — Harvest 1 + Harvest 2 + Harvest 3

`DISCOVERY + ABSTRACTION + HOSTILE DEDUP COMPLETE`

`IMPLEMENTATION NOT YET PERFORMED`

`ARSENAL PROMOTION NOT PERFORMED`

Frozen bounds are unchanged: Stage35 inception `43b5cc4a655cf68e9bdefad22800071d6f8d9fa0`; Stage35-EX inception `7423eab4f153df7e58c3c9aa7ef3ecdf3f53c4f8`; upper bound `9184c7ab694415592cc428a675c0ebed27cac510`. Later main movement is outside this harvest.

Machine artifacts: Harvest 2 abstraction sheets are `docs/arsenal/stage35-initial-abstraction.json`; Harvest 3 hostile dedup and frozen implementation metadata are `docs/arsenal/stage35-initial-hostile-dedup.json`. H1 exact 36-candidate ledger remains immutable at blob `c8f5c36315981589417fb3e0282c5c6194bbfaa9` and is referenced by `docs/arsenal/stage35-initial-discovery.json`.

## Harvest 2 abstraction boundary

H2 reclassification completed all 36 candidates. The main firewalls retained into H3 are: product-square is not factorwise square; a parametric squareclass graph is not a fixed finite squareclass family; a quotient/model is not rational-point or Mordell-Weil closure; a necessary local condition is not a global contradiction; a Brauer candidate is not a Brauer obstruction; proper Picard/Brauer does not transfer to an open receiver without a boundary/localization adapter.

## Harvest 3 hostile dedup — final classification

| Final class | Count | Candidates |
|---|---:|---|
| `NEW_WEAPON` | 5 | `A03,A05,A06,A08,A10` |
| `EXTEND_EXISTING` | 3 | `A02,A04,A09` |
| `NEW_WORKFLOW` | 1 | `C05` |
| `HISTORICAL_OR_NEGATIVE` | 9 | `E01-E06,F01-F03` |
| `STAGE35_SPECIFIC` | 7 | `A07,A11,B02,B07,D01-D03` |
| `REJECT_DUPLICATE` | 11 | `A01,B01,B03,B04,B05,B06,C01,C02,C03,C04,C06` |
| **Total** | **36** | |

Dedup was by source/target object, population, field, quantifiers, primitive/coprime assumptions, squareclass assumptions, markedness, local/global scope, input/output strength, reconstruction, finiteness, failure mode, semantic credit boundary, and source-lock requirements. Principal hostile comparisons were StageA1/A2 history; formal Stage30/31/34; provisional Stage32/33; Stage14 Gaussian lineage; and Research OS workflows.

### NEW_WEAPON freeze

`DISC-S35-A03 — PARAMETRIC_SQUARECLASS_COMPATIBILITY_GRAPH`. Input is a finite nonzero factor system with complete pair-product squareclasses/shared-prime channels. It outputs an exact edge-labelled squareclass graph and vertex labels in live reservoirs. This is **not** `S34-W01`: it deliberately allows parameter-dependent reservoirs and makes no finite-exhaustive-squareclass claim. Source PR `#1511`, exact head `3642810596eb1aa6e58a59fd6805e872b2ac8bc1`; source `stages/stage35-ex/35ex-09/bridge-squareclass-graph.md`, blob `1cbd3fdf4891ffabc1911ae19632f593a87b5d14`; certificate blob `000f32599d404e5b5ea0a3a3b8495d14405a8e5e`.

`DISC-S35-A05 — EXACT_RECEIVER_INVOLUTION_QUOTIENT_ADAPTER`. Exact involution on the **full receiver**, invariant-coordinate quotient, fixed/boundary audit, and exact converse reconstruction. It is not semilinear Galois descent (`S30-W02`) and not merely a model/j-invariant quotient. Source PR `#1567`, exact head `d836c743628b47d62e4db18c344981be8fe839f4`; certificate blob `ec3df8ed179295097878b1d8817edbf446418f2a`.

`DISC-S35-A06 — RATIONAL_SOURCE_LIFT_PRESERVING_KUMMER_NORMAL_FORM`. From an exact quotient receiver, materialize the additional rational-source lift as an iff square/discriminant condition and convert receiver + lift into a Kummer-style square system with forward/converse source reconstruction. It is not Stage33 `S33-PW09`, whose Kummer lift is a source-marked cohomological `H^2(mu2)`/Brauer binding. Source PR `#1571`, exact head `dc1930632304d2c47e5583e4d8cb324cbbd73e15`; certificate blob `df6637ce8b5a4127edb70d5d1f1d973ee300a9ca`.

`DISC-S35-A08 — RECIPROCAL_SHARED_FACTOR_RECEIVER_COMPRESSION`. Exact elimination exposes a common reciprocal factor in a completed simultaneous square receiver; explicit forward/inverse square-root scalings replace the source system by a smaller iff receiver. No point-set or physical-descent credit follows. Source PR `#1579`, exact head `21ce592d3f30fd10b421ed0d3be68a702c26c65a`; source blob `b663ef8b27503b10263d882c7e831b9dba3c05a2`; certificate blob `2f9f89d0571753e5885cf37b64de5f9367146d4d`; verifier blob `39a9d709d75788f7e9176dffceb37efcc6f23d5f`.

`DISC-S35-A10 — FINITE_EXCEPTIONAL_PRIME_CLASSIFICATION_BY_CENSUS_WEIL_COMPLETION`. Exhaust small primes under an exact finite-field predicate, then use a smooth bounded-genus auxiliary family plus explicit bad-point count and Weil lower bound to prove nondegenerate witnesses for every prime beyond a certified threshold. Output is an exact exceptional-prime set, not a `Q_p` or global theorem. Source PR `#1633`, exact head `fcedffa7f2d768ee8b1bc78b04611e1f0a401e77`; certificate blob `58ffdb975e95ba1d636ab8ff3b364d5042fe94a7`; verifier blob `37723cd995ad207e1a7899796a2b2f193f735729`.

All five are frozen at maturity `PROVISIONAL`; stable IDs are not assigned in Harvest 3.

### EXTEND_EXISTING freeze

`A02 -> S34-W01`: add an explicit dynamic-reservoir preflight/failure contract. Exact pairwise gcd + 2-adic support may be complete while fixed finite prime support remains unproved; the method must stop before finite squareclass enumeration. PR `#1511`, exact head `3642810596eb1aa6e58a59fd6805e872b2ac8bc1`, certificate blob `380d2ab51c4b1a646b4b5c3ba6aae5914ba02252`.

`A04 -> Stage14 Gaussian AR-017/AR-018 lineage`: add a multi-orientation independence contract. A second Gaussian orientation counts only after exact relabeling/mirror domination and unit/conjugation ambiguity are removed; then a joint squareclass condition may be extracted. Generic Gaussian factorization is not new. PR `#1515`, exact head `6a2193c19cce6d9022764c2daf6a2431e2348c1f`, source blob `196d2be1bbfbcb2b416535d7bb051a9b2ec93104`, verifier blob `7a5122ad30921548a18d8b7fba3af7af2cbd5214`.

`A09 -> S34-W01`: add the primitive three-object pair-gcd skeleton as an upstream normalization, with explicit prohibition on unsupported coprimalities/universal-torsor credit. PR `#1603`, exact head `51ae29a044d0e2524285e56237cf0e32269a54cf`, certificate blob `d0cd03a5ff744d5f6536b6d2784c0e0d543fea48`, verifier blob `d228ecc608ba7c4c26a7e7ba5b70d29186367b6e`.

No new IDs are needed for these three extensions.

### NEW_WORKFLOW freeze

`DISC-S35-C05 — ENDPOINT_RETURN_CIRCULARITY_FIREWALL`. When a quotient/descent/compression chain algebraically returns to endpoint-like variables, freeze the original source contract and classify every returned predicate as inherited, newly derived, or forbidden-to-reuse; require an explicit reverse/source adapter before re-importing primitive/orientation/source-only hypotheses. This is distinct from `S30-WF03`: that workflow controls upward semantic credit, while C05 specifically prevents circular re-import after returning to an earlier model. Source PR `#1581`, exact head `00d6199c0df611b0606b15b8a46897629363cb10`; source blob `e6db9d64941faafa8c906389f99ec3089e425576`; certificate blob `3057e494c8dbea2bfce8d636ead0fbcb962efff4`. Maturity `PROVISIONAL`.

### Rejections and demotions

`A01` and `B01` are rejected as separate IDs: coprime product-rectangle bookkeeping and the four-factor product-square receiver sit inside the exact factor/gcd input interface already owned by `S34-W01`; importantly, neither is allowed to imply factorwise squares or finite squareclass exhaustion. `B03/B06` are `S33-PW04` instances; `B04` is covered by the existing marked/Brauer stack and its concrete proper-surface result remains D02; `B05` is ordinary finite equivariant/orbit classification; `C01/C02` are already Research OS Cycle Exploration Safety Protocol; `C03` is existing source-lock/provenance workflow; `C04` is exactly `S34-WF01`; `C06` is immutable exact-head replay/provenance already covered by Stage30/repo audit workflows.

`A07,A11,B02,B07,D01-D03` are Stage35-specific after hostile abstraction: either their exact equation/fibration/prime payload is essential or the generic operation is already owned elsewhere. `E01-E06,F01-F03` remain searchable negative/unresolved route assets only; in particular unfinished open-surface Picard/Brauer or forced-prime parity/Hilbert routes receive no positive weapon credit.

## Harvest 4 implementation manifest

Harvest 4 has **6 proposed new stable-ID slots**: five provisional weapon roles `PARAMETRIC_SQUARECLASS_COMPATIBILITY_GRAPH`, `EXACT_RECEIVER_INVOLUTION_QUOTIENT_ADAPTER`, `RATIONAL_SOURCE_LIFT_PRESERVING_KUMMER_NORMAL_FORM`, `RECIPROCAL_SHARED_FACTOR_RECEIVER_COMPRESSION`, `FINITE_EXCEPTIONAL_PRIME_CLASSIFICATION_BY_CENSUS_WEIL_COMPLETION`, plus one provisional workflow `ENDPOINT_RETURN_CIRCULARITY_FIREWALL`. It has **3 extensions**: A02+A09 into `S34-W01`, and A04 into the existing Stage14 Gaussian orientation lineage. Exact HYPOTHESES/APPLICABILITY/DO_NOT_USE_FOR/source heads/blobs/verifiers/distinction rationales are frozen in `stage35-initial-hostile-dedup.json`.

Harvest 4 may then check the live ID namespace, assign stable IDs, edit the authoritative Arsenal source/`index.json`, generate cards/catalog, and run the sync checker. It must not promote any H3 historical/negative, Stage35-specific, or rejected duplicate item.

Forbidden remains: no Arsenal implementation in this Harvest 3, no Stage35/Stage35-EX authority or progression changes, and no merge.
