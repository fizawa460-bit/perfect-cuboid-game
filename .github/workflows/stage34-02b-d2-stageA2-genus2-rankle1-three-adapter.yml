name: Stage34-02b D2 StageA2 genus-two rank<=1 representative adapter

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'stages/stage34/runkeys/stage34-02b-d2-stageA2-genus2-rankle1-three-adapter.json'
      - 'stages/stage34/34-02/d2-stageA2-genus2-rankle1-gaussian-elliptic-quotient-lock.json'
      - 'stages/stage34/34-02/d2-stageA2-genus2-rankle1-gaussian-rank-index-lock.json'
      - 'stages/stage34/34-02/d2-stageA2-sign-involution-remaining30-pair-lock.json'
      - 'stages/stage34/34-02/d2-stageA2-sign-involution-remaining30-pair-certificate.json'
      - 'stages/stage34/34-02/probe_d2_stageA2_genus2_rankle1_gaussian_rank_index_chabauty.py'
      - '.github/workflows/stage34-02b-d2-stageA2-genus2-rankle1-three-adapter.yml'

permissions:
  contents: read

jobs:
  authorize:
    runs-on: ubuntu-latest
    outputs:
      authorized: ${{ steps.gate.outputs.authorized }}
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Check dedicated adapter run key advanced and armed
        id: gate
        env:
          BEFORE: ${{ github.event.before }}
        run: |
          KEY=stages/stage34/runkeys/stage34-02b-d2-stageA2-genus2-rankle1-three-adapter.json
          if [ -z "$BEFORE" ]; then echo 'authorized=false' >> "$GITHUB_OUTPUT"; exit 0; fi
          git fetch --no-tags --depth=1 origin "$BEFORE" || true
          if ! git cat-file -e "$BEFORE^{commit}" 2>/dev/null; then echo 'authorized=false' >> "$GITHUB_OUTPUT"; exit 0; fi
          if ! git diff --name-only "$BEFORE" HEAD | grep -Fxq "$KEY"; then echo 'authorized=false' >> "$GITHUB_OUTPUT"; exit 0; fi
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json,subprocess,os
          p='stages/stage34/runkeys/stage34-02b-d2-stageA2-genus2-rankle1-three-adapter.json'
          cur=json.load(open(p))
          before=subprocess.check_output(['git','show',f'{os.environ["BEFORE"]}:{p}'],text=True)
          old=json.loads(before)
          ok=(cur.get('schema')=='STAGE34_02B_D2_STAGEA2_GENUS2_RANKLE1_THREE_ADAPTER_RUNKEY_V1'
              and cur.get('armed') is True
              and isinstance(cur.get('generation'),int)
              and cur['generation']>int(old.get('generation',-1))
              and cur.get('expected_targets')==2
              and cur.get('planned_heavy_jobs')==1
              and cur.get('effective_heavy_concurrency')==1
              and cur.get('maximum_external_magma_requests_at_once')==1
              and cur.get('artifact_max_bytes')==1048576
              and cur.get('retention_days')==1)
          print('authorized='+('true' if ok else 'false'))
          PY

  probe:
    needs: authorize
    if: needs.authorize.outputs.authorized == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Run Gaussian exact rank, 2-index and elliptic Chabauty targets
        run: python3 stages/stage34/34-02/probe_d2_stageA2_genus2_rankle1_gaussian_rank_index_chabauty.py
      - name: Enforce compact diagnostic artifact cap
        if: always()
        run: |
          test -f stages/stage34/34-02/d2-stageA2-genus2-rankle1-gaussian-rank-index-chabauty-probe.json
          test $(wc -c < stages/stage34/34-02/d2-stageA2-genus2-rankle1-gaussian-rank-index-chabauty-probe.json) -le 1048576
          test $(wc -c < stages/stage34/34-02/d2-stageA2-genus2-rankle1-gaussian-rank-index-chabauty-stdout.txt) -le 1048576
      - name: Upload compact diagnostic evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage34-02b-d2-stageA2-genus2-rankle1-gaussian-rank-index-chabauty
          path: |
            stages/stage34/34-02/d2-stageA2-genus2-rankle1-gaussian-rank-index-chabauty-probe.json
            stages/stage34/34-02/d2-stageA2-genus2-rankle1-gaussian-rank-index-chabauty-stdout.txt
          if-no-files-found: warn
          retention-days: 1
