name: Stage34-02 D2 StageA2 pair quotient PARI classification

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'stages/stage34/runkeys/stage34-02-d2-stageA2-pair-quotient-pari.json'
      - 'stages/stage34/34-02/d2-stageA2-pair-quotient-pari-classification-lock.json'
      - 'stages/stage34/34-02/run_d2_stageA2_pair_quotient_pari_classification.py'
      - 'stages/stage34/34-02/run_d2_stageA2_factor_branch_local.py'
      - 'stages/stage34/34-02/refine_d2_stageA2_factor_branch_support.py'
      - 'stages/stage34/34-02/run_d2_stageA2_full_support_projective.py'
      - 'stages/stage34/34-02/validate_d2_stageA2_pair_quotient_pari_runkey.py'
      - '.github/workflows/stage34-02-d2-stageA2-pair-quotient-pari.yml'

permissions:
  contents: read

jobs:
  authorize:
    runs-on: ubuntu-latest
    outputs:
      authorized: ${{ steps.gate.outputs.authorized }}
      reason: ${{ steps.gate.outputs.reason }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch event-before commit if needed
        env:
          BEFORE: ${{ github.event.before }}
        run: |
          if [ -n "$BEFORE" ]; then git fetch --no-tags --depth=1 origin "$BEFORE" || true; fi
      - name: Validate dedicated run key against actual commit range
        id: gate
        env:
          BEFORE: ${{ github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: python3 stages/stage34/34-02/validate_d2_stageA2_pair_quotient_pari_runkey.py "$BEFORE" "$HEAD_SHA" >> "$GITHUB_OUTPUT"
      - name: Report authorization
        run: echo "authorized=${{ steps.gate.outputs.authorized }} reason=${{ steps.gate.outputs.reason }}"

  classify-pair-models:
    needs: authorize
    if: needs.authorize.outputs.authorized == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 105
    steps:
      - uses: actions/checkout@v4
      - name: Regenerate exact 92 d1 survivors
        run: |
          python3 stages/stage34/34-02/run_d2_stageA2_factor_branch_local.py
          python3 stages/stage34/34-02/refine_d2_stageA2_factor_branch_support.py
          python3 stages/stage34/34-02/run_d2_stageA2_full_support_projective.py
      - name: Install PARI GP
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends pari-gp
          dpkg-query -W pari-gp
      - name: Classify all 48 pair quotient Jacobians
        run: python3 stages/stage34/34-02/run_d2_stageA2_pair_quotient_pari_classification.py
      - name: Enforce compact artifact cap
        if: always()
        run: |
          test -f stages/stage34/34-02/d2-stageA2-pair-quotient-pari-classification.json
          test -f stages/stage34/34-02/d2-stageA2-pair-quotient-pari-classification-stdout.txt
          test $(wc -c < stages/stage34/34-02/d2-stageA2-pair-quotient-pari-classification.json) -le 4194304
          test $(wc -c < stages/stage34/34-02/d2-stageA2-pair-quotient-pari-classification-stdout.txt) -le 4194304
      - name: Upload compact classification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage34-02-d2-stageA2-pair-quotient-pari
          path: |
            stages/stage34/34-02/d2-stageA2-pair-quotient-pari-classification.json
            stages/stage34/34-02/d2-stageA2-pair-quotient-pari-classification-stdout.txt
          if-no-files-found: warn
          retention-days: 1
