name: Stage32 scratch post1648T KRR ancillary marked-g bounded negative

on:
  workflow_dispatch:
permissions:
  contents: read

jobs:
  krr-ancillary-marked-g-bounded-negative:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Verify KRR ancillary marked-g bounded negative
        run: python3 -B stages/stage32/residual-32-01-production/verify_stage32_post1648t_krr_official_ancillary_marked_g_bounded_negative.py
      - name: Diff check
        run: git diff --check
