name: Stage32 178 TD02 integer-lattice bounded capacity temporary verification

# lifecycle: ACTIVE_AUTO
# Branch-local lightweight bounded replay for the current 178 TD02 frontier.
# Remove after exact-head evidence is retained.
on:
  push:
    branches:
      - stage32-01-178-topdown-grf02-hpadj08
    paths:
      - stages/stage32/32-01-178/topdown-02/verify_td02_integer_lattice_capacity_bounded.py
      - .github/workflows/stage32-178-td02-integer-capacity-bounded-temp.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Verify bounded integer-lattice capacity LP
        run: python stages/stage32/32-01-178/topdown-02/verify_td02_integer_lattice_capacity_bounded.py
