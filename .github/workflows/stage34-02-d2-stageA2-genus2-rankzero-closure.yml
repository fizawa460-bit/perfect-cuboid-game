name: Stage34-02 D2 StageA2 genus-two rank-zero closure

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'stages/stage34/runkeys/stage34-02-d2-stageA2-genus2-rankzero-closure.json'
      - 'stages/stage34/34-02/d2-stageA2-genus2-rankzero-closure-lock.json'
      - 'stages/stage34/34-02/run_d2_stageA2_genus2_rankzero_closure.py'
      - 'stages/stage34/34-02/validate_d2_stageA2_genus2_rankzero_closure_runkey.py'
      - '.github/workflows/stage34-02-d2-stageA2-genus2-rankzero-closure.yml'

permissions:
  contents: read

jobs:
  authorize:
    runs-on: ubuntu-latest
    outputs:
      authorized: ${{ steps.gate.outputs.authorized }}
      reason: ${{ steps.gate.outputs.reason }}
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Fetch event-before commit if needed
        env: {BEFORE: '${{ github.event.before }}'}
        run: 'if [ -n "$BEFORE" ]; then git fetch --no-tags --depth=1 origin "$BEFORE" || true; fi'
      - name: Validate dedicated run key against actual commit range
        id: gate
        env:
          BEFORE: ${{ github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: python3 stages/stage34/34-02/validate_d2_stageA2_genus2_rankzero_closure_runkey.py "$BEFORE" "$HEAD_SHA" >> "$GITHUB_OUTPUT"
      - name: Report authorization
        run: echo "authorized=${{ steps.gate.outputs.authorized }} reason=${{ steps.gate.outputs.reason }}"

  prove-eight:
    needs: authorize
    if: needs.authorize.outputs.authorized == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Regenerate exact residual triple quotient table
        run: |
          python3 stages/stage34/34-02/run_d2_stageA2_factor_branch_local.py
          python3 stages/stage34/34-02/refine_d2_stageA2_factor_branch_support.py
          python3 stages/stage34/34-02/run_d2_stageA2_full_support_projective.py
          python3 stages/stage34/34-02/run_d2_stageA2_rank1_cover_selection.py
          python3 stages/stage34/34-02/probe_d2_stageA2_triple_quotient_models.py
      - name: Replay rank-zero genus-two complete pointsets and receiver pullback
        run: python3 stages/stage34/34-02/run_d2_stageA2_genus2_rankzero_closure.py
      - name: Enforce compact proof artifact cap
        if: always()
        run: |
          test -f stages/stage34/34-02/d2-stageA2-genus2-rankzero-closure.json
          test $(wc -c < stages/stage34/34-02/d2-stageA2-genus2-rankzero-closure.json) -le 1048576
          test $(wc -c < stages/stage34/34-02/d2-stageA2-genus2-rankzero-closure-stdout.txt) -le 1048576
      - name: Upload compact proof evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage34-02-d2-stageA2-genus2-rankzero-closure
          path: |
            stages/stage34/34-02/d2-stageA2-genus2-rankzero-closure.json
            stages/stage34/34-02/d2-stageA2-genus2-rankzero-closure-stdout.txt
          if-no-files-found: warn
          retention-days: 1
