# Stage28-60-r2 — post-merge causal deepening ledger

```text
CHECKPOINT=60
PARENT_PR=1280
PARENT_AUDIT=PASS
PARENT_MERGED=true
POST_MERGE_DEEPENING=true
```

The parent checkpoint60 already froze the common-host causal adapter, the two positive-divergent interaction invariants, the mechanism comparison, and the double-charge firewall.  This r2 batch tests only materially new post-merge questions.

## R15 — exact interaction quotient algebra

**SUCCESS.**

\[
\frac{I_{face}}{I_{sp}}
=\frac{M_3N_1}{M_2N_2}
=\frac{M_3}{N_2}\frac{N_1}{M_2}.
\]

This gives a direct legal bridge between the two interaction ladders and the Stage28 target ratio without using the perfect-cuboid intersection.

## R16 — asymptotic intermediate-population normalizer

**SUCCESS.**

Stage21 plus the `M1` asymptotic gives

\[
N_1\sim \frac{\kappa}{24\pi}B(\log B)^3,
\]

hence

\[
M_2/N_1\sim (24\pi C_{M_2}/\kappa)(\log B)^2.
\]

This yields the normalized curvature theorem

\[
M_3/N_2\sim (24\pi C_{M_2}/\kappa)K_{28}.
\]

## R17 — geometric origin of the log-squared threshold

**SUCCESS AS EXPLANATION, NOT AS NEW SAVING.**

`S25-W06` records `N1:(a,b)=(1,4)` and `M2:(1,6)`.  Their common `a=1` and two-step `b` gap explains the known `(log B)^2` normalizer.  The same ledger explicitly forbids fake Picard-rank subtraction across the K3 cover.

## R18 — compare current interaction corridors to the critical threshold

**NEGATIVE CERTIFICATE.**

The current bounds imply

\[
J_{28}\gg_\varepsilon B^{-1/6-\varepsilon}(\log B)^{-2}
\]

and

\[
J_{28}=o(B^{3/4}(\log B)^{3-\delta}),
\]

so the critical `(log B)^(-2)` scale remains deep inside the allowed corridor.  Positivity/divergence of both individual interactions does not resolve their relative size.

## R19 — literal/common-host completion reformulation

**NO STRONGER ORDERING.**

Checkpoint10/60 already supplies the legal common host `H_ge2=M2+M3`, with exact shares `Sigma19=N2/H_ge2` and `Phi20=M3/H_ge2`.  Rewriting the new curvature identity through these shares changes semantics presentation but produces no stronger bound.  No extra host factor is charged.

## R20 — branch-sensitive literature rematch

**NO DIRECT DISCHARGE FOUND IN BOUNDED REMATCH.**

Huang's generic toric thin-cover theorem remains branch-profile-insensitive at the needed strength.  Kummer counting results and degree-two K3 rational-point infinitude results do not match the two present covers plus the physical `R<=B` height strongly enough to compare `J_28` to `(log B)^(-2)`.

## R21 — joint/covariance endpoint shortcut

**REJECTED BY STAGE SCOPE.**

A direct product of the Stage19 space selector and Stage20 third-face selector counts the simultaneous completion locus, i.e. the deferred perfect-cuboid endpoint.  It cannot be used to estimate `J_28` inside Stage28.

## r2 stop boundary

The new successful algebra does not solve the bridge ordering, but it converts the remaining qualitative branch-sensitive receiver into one exact quantitative threshold problem:

```text
TARGET=J_28=I_face/I_sp
CRITICAL_SCALE=(log B)^(-2)
EQUIVALENT_NORMALIZED_TARGET=K_28=(log B)^2 J_28
BRIDGE_EQUIVALENCE=M3/N2~(24*pi*C_M2/kappa)K_28
```

Any further repo-native manipulation of the already known local sieve, generic degree-two cover, construction floors, or positive interaction signs reproduces an input already represented above.  Further progress needs a genuinely branch-sensitive physical-height marginal comparison theorem or a stronger independent population bound.

```text
MATERIALLY_DISTINCT_R2_ROUTES=7
SUCCESSFUL_NEW_DEDUCTIONS=3
NEGATIVE_OR_FIREWALL_ROUTES=4
RESEARCH_REQUEST_READY=true
CHECKPOINT60_R2_BOUNDED_DEEPENING_COMPLETE_CANDIDATE=true
AUDIT_REQUIRED=true
```