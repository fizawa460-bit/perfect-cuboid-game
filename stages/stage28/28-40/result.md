# Stage28-40 — strongest upper ledger and deep bridge attack

```text
TASK_ID=Stage28-40
CHECKPOINT=40
PARENT_ROADMAP=docs/stage16-29-population-roadmap.md
COMPARISON=Stage19 -> Stage20
STATUS=SUBMITTED_PENDING_FRESH_AUDIT
EVIDENCE_LEVEL=PROVED_REUSE_PLUS_NEW_DERIVED_LOCAL_COMPARISON_PLUS_LITERATURE_REMATCH
```

## Certified upper surface

The strongest currently certified upper control on the Stage28 bridge remains the audited checkpoint30 statement

\[
\boxed{
\frac{M_3(B)}{N_2(B)}
=o\!\left(B^{3/4}(\log B)^{5-\delta}\right)
}
\qquad \text{for every fixed }0<\delta<1/46.
\]

No stronger compatible whole-family `M3` upper, `N2` lower, or direct published `M3/N2` theorem was found in the bounded repo-wide / StructureRadar / current-literature rematch.

```text
CHECKPOINT40_NUMERIC_UPPER_IMPROVED=false
CURRENT_BRIDGE_UPPER=o(B^(3/4)(log B)^(5-delta)), 0<delta<1/46
SOURCE_TARGET_ASYMPTOTIC_ORDERING_IDENTIFIED=false
TRUE_N2_EXPONENT_IDENTIFIED=false
TRUE_M3_EXPONENT_IDENTIFIED=false
```

## New Stage28 comparative theorem: equal first-order local sieve dimension

The deep attack does produce one new relative structural conclusion.

Let `alpha_p` be the Stage19 space-square local acceptance and `beta_p` the Stage20 third-face local acceptance on the shared two-face local model. For odd primes,

\[
\log\frac{\alpha_p}{\beta_p}
=-\frac{2\chi_4(p)}p+O(p^{-2}).
\]

Since `sum_p chi_4(p)/p` converges, the relative local products have no polynomial or first-order logarithmic drift. Both systems have sieve dimension `2`.

Thus the existing local blocker laws do **not** provide a mechanism by which one Stage28 population should acquire an extra power of `B` or an extra first-order power of `log B` relative to the other.

This is not a global ratio theorem. Stage19 still lacks the growing-modulus uniformity needed to put its local product on the same `z=z(B)` scale as the Stage20 Selberg sieve.

```text
SPACE_SIEVE_DIMENSION=2
THIRD_FACE_SIEVE_DIMENSION=2
LOCAL_DIMENSION_DIFFERENCE=0
LOCAL_FIRST_ORDER_ORDERING_MECHANISM=ABSENT
M3_OVER_N2_THETA_ONE_PROVED=false
LOCAL_TO_GLOBAL_TRANSFER_PROVED=false
```

## What remains genuinely capable of improving Stage28

A strict bridge-upper improvement now requires genuinely global information, for example:

1. a same-host relative theorem comparing `Phi20=M3/H_ge2` directly with `Sigma19=N2/H_ge2`;
2. a joint correlation/energy theorem for the third-face and space-square completion indicators on the exact physical two-face measure;
3. a uniform common-height theorem comparing the K3 third-face cover with the moving Kummer/genus-one space cover;
4. effective Stage19 growing-modulus uniformity strong enough to support a relative sieve comparison at `z=z(B)`.

A theorem that only repeats the known fixed-prime local densities, fixed-surface rational-point estimates, or frozen Stage27 endpoint attacks is insufficient.

```text
OPEN_GATE_40=GLOBAL_SAME_HOST_RELATIVE_COMPLETION_THEOREM
OPEN_GATE_40_ACCEPTABLE_SPECIES=relative_host_share|joint_correlation|uniform_two_cover_height|stage19_growing_modulus
OPEN_GATE_POPULATION=primitive canonical two-face physical host / matched Stage19 and Stage20 shares
OPEN_GATE_CUTOFF=R<=B
OPEN_GATE_REQUIRED_STRENGTH=strictly improve current bridge upper or resolve asymptotic ordering
OPEN_GATE_RESEARCH_REQUEST_READY=true
```

## External rematch verdict

Recent/general rational-point literature checked in this batch does not change the numerical upper ledger:

- effective `B^(1+epsilon)` bounds for fixed degree-4/5 conic-bundle del Pezzo surfaces are not stronger than the current Stage20 bound on the exact physical population and do not compare against `N2`;
- current perfect-cuboid elliptic/genus-3/fiberwise obstruction papers provide structural and computational information, not a whole-family primitive-canonical `M3/N2` count;
- ambient primitive cuboid asymptotics count the wrong population.

## Checkpoint40 handoff

The canonical checkpoint40 role is satisfied: the strongest upper theorem is frozen, materially distinct upper routes were explored, a new relative local-sieve obstruction certificate was derived, and the remaining global theorem receiver is precise.

Because the local-sieve comparison is a new mathematical claim, this batch stops for independent audit before checkpoint50.

```text
CHECKPOINT40_COMPLETE_AS_SUBMISSION=true
DEEP_EXPLORATION_RULE_SATISFIED=true
AUDIT_REQUIRED=true
AUDIT_STATUS=PENDING
MERGE_ALLOWED=false
ADVANCE_ALLOWED=false
NEXT_CHECKPOINT_AFTER_PASS=50
NEXT_EXPECTED_COMMAND=Stage28-audit
CODEX_REQUIRED=false
PERFECT_CUBOID_CONCLUSION=NONE
```