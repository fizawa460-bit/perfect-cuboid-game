# Stage27-20-r302v-an

This checkpoint40 batch resumes from the independently audited and merged `r302p-u` batch (#1247) and advances the coefficient-specific MAIN quadratic-form route through the exact primitive Fourier/Gauss geometry already available from StructureRadar.

Core reductions:

- `r302v-w`: primitive Gauss magnitude flattens the admissible Gram diagonal and collapses the coefficient-specific diagonal target to one product `(D_d/E_packet) theta_d`.
- `r302x-z`: expose the squared-frequency phase difference and compute odd prime-power square-collision multiplicity.
- `r302aa-ac`: absorb regular collisions into the diagonal scale and convert singular odd collision energy to parity-preserving projected physical coset/block energy.
- `r302ad-ae`: shear `u^2-v^2` to `(u-v)(u+v)` with no coefficient Frobenius loss.
- `r302af-ai`: identify the primitive unit-group Ramanujan main term, reduce it to square-fiber energy, and expose discrepancy/character routes without claiming theorem applicability.
- `r302aj-ak`: show fixed-power inverse-a equidistribution is stronger than necessary on the odd local factor.
- `r302al-am`: globalize to the joint odd/2-primary primitive unit pushforward and convert the joint singular square-label energy to projected physical block concentration.
- `r302an`: final sufficient package has exactly one fixed-power input and two `B^o(1)` zero-loss adapters.

Final frontier:

```text
A = MAINWallPrimitiveInverseFrequencyAdmissibleClassDiagonalEnergyProductDeficit
    [fixed power]

B = MAINWallJointPrimitiveProjectedFineBlockMultiplicitySubpowerAdapter
    [zero fixed-power loss]

C = MAINWallJointPrimitiveUnitFiberSubpowerNonconcentration
    [zero fixed-power loss]
```

No one of A/B/C is proved here. No external large-sieve/Kloosterman/Kuznetsov theorem is asserted directly applicable. The original `H_phys^MAIN` measure, correlated modulus, gcd descent, primitive 2-primary factor, common-parent weights, physical masks, and checkpoint/exponent firewalls remain retained.

```text
CURRENT_CHECKPOINT=40
ADVANCE_TO_CHECKPOINT50=false
STRICT_SUB_SQRT_UPPER_PROVED=false
NEW_MU_LT_HALF_PROVED=false
TRUE_N2_EXPONENT_IDENTIFIED=false
FREEZE_FOR_STRUCTURE_RADAR=false
NEXT_DERIVED_ROUTE=27-20-r302ao
NEXT_EXPECTED_COMMAND=Stage27-20-r302-audit
```