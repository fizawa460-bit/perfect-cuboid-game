# Stage27-40ab — T averaged-theorem exceptional-set adapter attack

```text
TASK_ID=Stage27-40ab
OWNER_STAGE=Stage27
TRIGGER_CHECKPOINT=40
ROUTE_KIND=UPPER_ONLY
ROUTE_LABEL=T_AVG_ADAPTER
STATUS=SUBMITTED_PENDING_CONSOLIDATED_FRESH_AUDIT
ADVANCE_TO_CHECKPOINT50=false
```

## Source boundary

Stage14-t157/tH33 freezes one actual long fixed-U packet as one odd squarefree ordinary modulus `d=B^o(1)`, one invertible Gaussian residue `beta mod d`, one strict sector and fixed-power long headroom. The required conclusion is a pointwise principal-density lower ratio. tH33 certified that Kai/Mitsui covers only the smaller individual-modulus envelope and that BV/BDH/large-sieve inputs are averaged statements not directly chargeable to one frozen physical packet.

## Exact adapter formulation

Let `P` be the physical critical-wall packet set in one retained principal cell. Let

\[
\pi:P\to\mathcal C,
\qquad p\mapsto c(p)=(d(p),\beta(p),\text{sector/endpoint decorations})
\]

be the exact T target-class map, and write

\[
w(c)=\#\{p\in P:\pi(p)=c\}.
\]

Suppose an averaged prime theorem supplies a good-class set `G` and exceptional set `E=C\G`. Such a theorem transfers to the Stage27 physical measure only if the exceptional preimage is chargeable:

\[
\boxed{\sum_{c\in E}w(c)\ll B^{-\delta+o(1)}\sum_{c\in\mathcal C}w(c)}
\]

for some fixed `delta>0` after the frozen Stage14 capacity/post-mask ledger, or with an equivalent deficit sufficient to cross the half-power host.

Merely knowing

\[
|E|\ll B^{-\eta+o(1)}|\mathcal C|
\]

or an unweighted/standard-modulus mean-square discrepancy does **not** imply this: the physical pushforward weight `w` may concentrate on `E`.

## Sufficient weighted forms

Any of the following would be a legal crossing input:

1. a direct weighted exceptional-set theorem in the exact pushforward measure `w`;
2. a uniform preimage bound together with a lower bound for the physical image large enough that the exceptional-cardinality saving survives the exponent ledger;
3. an `L^2` adapter

\[
\sum_c w(c)^2\le B^{o(1)}\frac{(\sum_c w(c))^2}{|\mathcal C|}
\]

paired with an exceptional-set cardinality power saving strong enough that Cauchy--Schwarz leaves a fixed power;
4. a BDH/BV theorem already weighted by the actual Stage14 target-class incidence measure.

No such bound is present in the frozen t-chain. `d=B^o(1)` and the residue-group size `q_d=B^o(1)` do not by themselves control the distribution of `w(c)` across the averaged family.

## Outcome

The T-AVG route is therefore reduced to an exact same-measure pushforward anti-concentration theorem. Replacing the missing pointwise prime theorem by an ordinary averaged theorem without this adapter is illegal; no fixed-power Stage27 saving follows.

This is a genuine narrowing of the route, not a proof that no future averaged theorem can work. A future theorem satisfying one of the weighted contracts above may reopen T immediately.

```text
T_AVG_ADAPTER_ATTACK_EXECUTED=true
AVERAGED_THEOREM_ALONE_SUFFICIENT=false
PHYSICAL_PUSHFORWARD_WEIGHT_DEFINED=true
EXCEPTIONAL_CLASS_CARDINALITY_ALONE_CHARGEABLE=false
WEIGHTED_EXCEPTIONAL_PREIMAGE_FIXED_POWER_BOUND_PROVED=false
T_EXACT_REOPEN_GATE=PHYSICAL_TARGET_CLASS_PUSHFORWARD_ANTICONCENTRATION_OR_WEIGHTED_BDH
STRICT_SUB_SQRT_UPPER_PROVED=false
NEW_MU_LT_HALF_PROVED=false
NEXT_CHECKPOINT=40
NEXT_DERIVED_ROUTE=27-40ac
AUDIT_STATUS=PENDING_CONSOLIDATED
MERGE_ALLOWED=false
PERFECT_CUBOID_CONCLUSION=NONE
```
