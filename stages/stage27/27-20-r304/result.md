# Stage27-20-r304 — balanced-wall squareclass / eliminant bypass

```text
TASK_ID=Stage27-20-r304
ROUTE_KIND=UPPER_BYPASS
PARENT_CONTEXT=Stage27-20-r301z + frozen Stage27-20-r302
STATUS=SUBMITTED_PENDING_FRESH_AUDIT
R302_REMAINS_FROZEN=true
ADVANCE_TO_CHECKPOINT50=false
```

## Purpose

Attack the unresolved fixed-width balanced wall without reopening the frozen r302 same-`H_phys^MAIN` quadratic-form/collision theorem gate.

The legal StructureRadar weapons are:

- `SR-STR-164`: exact squareclass-collision identity / square-lift collision cover;
- `SR-STR-166`: dual-root-line eliminant dichotomy.

The route is legal only if the balanced-wall packet equations yield a genuinely new arithmetic factor after all already-charged common-core/root conditions are removed.

## Required derivation order

1. Retain the exact Stage14/Stage27 physical wall packet and all masks.
2. Form the two root-line relations on the same physical packet.
3. Eliminate the shared root/common-core variable exactly once.
4. Audit diagonal, symmetry and singular loci before using a square/polynomial sieve.
5. Inspect the eliminant for one of two genuinely new outputs:
   - a determinant/divisor factor not already charged in the host count;
   - an exact zero-loss inverse-fraction transform with a new modulus/sample family.

If neither output occurs, the route must stop. A re-expression of the same common-core spacing is not a new saving.

## Success receiver

A sufficient outcome is a fixed-power estimate on the nonproportional fixed-width wall slab

\[
|P_{\mathrm{wall},\eta_0}(B)|\ll B^{1/2-\delta+o(1)}
\]

for fixed `eta_0,delta>0`, which then glues with the already-audited off-wall saving from r301.

No such estimate is claimed here.

```text
WALL_SQUARECLASS_BYPASS_OPENED=true
SR_STR_164_IMPORTED=true
SR_STR_166_IMPORTED=true
NEW_DETERMINANT_FACTOR_PROVED=false
NEW_SECOND_MODULUS_PROVED=false
ZERO_LOSS_INVERSE_FRACTION_TRANSFORM_PROVED=false
STRICT_SUB_SQRT_UPPER_PROVED=false
NEXT_DERIVED_ROUTE=27-20-r304a
```
