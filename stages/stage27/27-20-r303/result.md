# Stage27-20-r303 — T weighted prime-distribution reentry

STATUS=SUBMITTED_PENDING_AUDIT
CHECKPOINT=40
ROUTE_KIND=UPPER_REENTRY_T_WEIGHTED_ADAPTER
PARENT_FROZEN_ROUTE=Stage27-20-r302

## Scope

Keep r302 frozen. Re-enter the strict sub-square-root upper problem through the independent T route identified in Stage27-40ab.

Let P be the retained physical critical-wall packet incidences and pi:P->C the exact T target-class map. Write w(c)=#{p in P:pi(p)=c}. An averaged prime-distribution theorem on classes is useful only if its exceptional set E is chargeable in this physical pushforward measure:

sum_{c in E} w(c) <= B^{-delta+o(1)} sum_c w(c).

Class-count sparsity alone is insufficient.

## First reduction

There are two genuinely different legal crossings:

1. INDIVIDUAL-MODULUS: prove the required Gaussian-prime occupancy/lower-ratio theorem directly for each frozen physical target class. This bypasses w altogether.
2. WEIGHTED-AVERAGE: retain an averaged theorem, but prove an L2/nonconcentration adapter for w strong enough that the exceptional-class saving survives Cauchy-Schwarz and the half-power ledger.

These routes must not be conflated.

## StructureRadar import

The StructureRadar Gaussian-prime work is imported only at its audited strength: it supplies exact normalization of ordinary Gaussian residue class, primary-generator convention, fixed angular sector, and the individual-modulus theorem target. It does not manufacture a theorem beyond its proved range and does not turn average-modulus information into physical-weight information.

## Frontier

The promising new direction is therefore to calculate the pushforward fiber multiplicity of pi from the Stage14 T parametrization. If sup_c w(c) or sum_c w(c)^2 admits a B^{o(1)}-loss relative to uniform mass, ordinary averaged prime theorems become legally chargeable; if not, the route collapses back to the individual-modulus theorem gate.

STRICT_SUB_SQRT_UPPER_PROVED=false
ADVANCE_TO_CHECKPOINT50=false
NEXT_DERIVED_ROUTE=27-20-r303a
