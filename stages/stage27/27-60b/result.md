# Stage27-60b — unresolved-ordering firewall

```text
TASK_ID=Stage27-60b
PARENT=Stage27-60a
ROUTE_KIND=MAINLINE_ORDERING_FIREWALL
STATUS=SUBMITTED_PENDING_FRESH_AUDIT
```

The interval calculus yields an exact firewall for comparisons against other certified exponent data.

If another stage only provides a lower bound `gamma_-`, then `N2` is certified asymptotically smaller only when the N2 upper endpoint is below that lower bound. If another stage only provides an upper bound `gamma_+`, then `N2` is certified asymptotically larger only when the N2 lower endpoint exceeds that upper bound.

In particular the currently retained Stage26 fact

\[
M_3(B)\gg B^{1/3-o(1)}
\]

cannot order `N2` and `M3`, because `1/3` lies strictly inside `[1/4,1/2]`. Neither `N2=o(M3)` nor `M3=o(N2)` follows.

This remains true even if finite-range diagnostics suggest a slope near `0.42`: finite fitted slopes are non-theorem diagnostics and may not collapse the interval.

The correct downstream object is therefore a partially ordered exponent ledger with unresolved relations explicitly retained.

```text
ORDERING_FIREWALL_PROVED=true
N2_VS_M3_ORDERING=UNRESOLVED
FINITE_SLOPE_PROMOTION_FORBIDDEN=true
PARTIAL_ORDER_LEDGER_REQUIRED=true
NEXT_DERIVED_ROUTE=27-60c
```
