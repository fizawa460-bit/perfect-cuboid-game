# Stage27-19-r5aa–r5ac fresh audit R01

```text
AUDIT_TARGET_HEAD=5ee1fb60316306cfbeb15e4b6ad514fb84ba5441
AUDIT_VERDICT=PASS
MATHEMATICAL_AUDIT=PASS
CI_AUDIT=SCOPED_PASS
PR_MERGEABLE=true
MERGE_ALLOWED=true
ADVANCE_TO_CHECKPOINT50=false
CURRENT_CHECKPOINT=40
NEXT_CHECKPOINT=40
NEXT_DERIVED_ROUTE=27-19-r5ad
```

Fresh hostile review independently re-derived the load-bearing identities before allowing the next theorem branch.

1. With `d=gcd(n,s)`, `n=dn0`, `s=ds0`, `M=m^2+n^2`, `K=r^2-s^2`, primitive slope coprimality removes every cross gcd except `h=gcd(M,K)`, giving exactly `g=d^2h`, `p=s0^2a`, `q=n0^2b` for `M=ha`, `K=hb`.
2. The frozen Stage19 final interface explicitly locks the physical shared-edge incidence to a unique positive toric representation after the chamber convention, so the square-divisor reconstruction in r5aa does not hide a physical-to-parameter multiplicity.
3. Direct expansion confirms `U=hJ`, `V=d^2h(p+q)` with `J=abh+d^2(p-q)`. Hence the space condition is exactly `sf(J)=sf(p+q)` and the two diagonal quadrics/core quartic receiver are algebraically exact.
4. For r5ac, `p` and `g` satisfy the two-squares prime criterion because `a,h` divide the primitive sum of two squares `M`. For every odd prime dividing `kappa=sf(p+q)`, the second diagonal equation produces a square root of `-1`, forcing the prime to be `1 mod 4`. The mod-8 contradiction excluding `2|kappa` is valid.
5. No route promotes these support restrictions to a fixed-power saving; the half-power Stage27 wall remains intact.

The dedicated `Stage27-19-r5aa core factorization` workflow is SUCCESS at the audited head. Unrelated legacy workflow failures on the same commit are not on this route's changed paths and are excluded from the scoped CI verdict.

This audit clears the theorem gate for `r5ad`; all post-audit changes require a new fresh audit before exponent promotion or merge of the extended batch.
