# Stage27 checkpoint40 — aa/ac upper-exploration audit trigger

## Why audit now

The hostile r401a audit mandated three independent continuations before further upper exploration could honestly be assessed:

1. MAIN-CRT2;
2. T-AVG-ADAPTER;
3. WALL-STRUCTURE.

All three have now been executed as candidate reductions in `27-40aa`, `27-40ab`, and `27-40ac`.

## Candidate synthesis

### MAIN

Because reciprocal witness multiplicity is `B^o(1)`, every fixed witness moment is fixed-power equivalent to reciprocal support. Fixed second/higher moment reweighting is not a new exponent source. The remaining MAIN gate is a fixed-power deficit in simultaneous nested two-level CRT **support mass**.

### T

Averaged prime distribution cannot be transferred merely from a small exceptional class set. The physical target-class pushforward weight can concentrate on the exceptional set. A legal T crossing needs a weighted exceptional-preimage bound / target-class anti-concentration theorem / exactly weighted BDH-BV statement strong enough to survive the half-power ledger.

### fully balanced wall

Common-core existence, same-root re-expression, and `B^o(1)` reconstruction multiplicity are exponent-neutral once already charged. A legal wall crossing needs polynomial large-core forcing, fixed-power low-core sparsity, a genuinely independent second modulus, or a direct support-correlation deficit.

## Current theorem surface

Nothing in aa/ac proves

\[
N_2(B)\ll_\varepsilon B^{1/2-\delta+\varepsilon}
\]

for fixed `delta>0`. The global upper remains

\[
N_2(B)\ll_\varepsilon B^{1/2+\varepsilon}.
\]

The off-wall fixed-width saving from audited r401a remains valid, so the unresolved mass is still confined to arbitrarily thin fixed neighborhoods of the balanced wall.

## Hostile audit questions

A fresh audit should now test:

1. whether the fixed-moment equivalence in aa is valid under the exact Stage14 witness-fiber quantifiers;
2. whether ab correctly distinguishes class-count exceptional savings from physical pushforward-weight savings, and whether a known frozen theorem already supplies one of the weighted adapters;
3. whether ac accidentally recharges a root/core condition already used in the half-power host, or overlooks a genuinely independent reduced-column modulus;
4. whether aa/ac together missed any repo-native fixed-power theorem already present after Stage14;
5. whether the mandatory-r401 continuation set may be marked executed without claiming absolute repository exhaustiveness.

The audit should not authorize checkpoint50 merely because these three candidates are exhausted. Checkpoint50 requires either a new strict sub-half theorem, a separately defined research-phase closure rule, or an explicit controller decision consistent with the Stage27 roadmap.

```text
AUDIT_TRIGGER=NOW
AUDIT_SCOPE=Stage27-40aa+27-40ab+27-40ac
MANDATORY_R401_CONTINUATIONS_EXECUTED_CANDIDATE=true
ABSOLUTE_REPO_NATIVE_EXHAUSTIVENESS_CLAIMED=false
STRICT_SUB_SQRT_UPPER_PROVED=false
NEW_MU_LT_HALF_PROVED=false
CURRENT_CHECKPOINT=40
ADVANCE_TO_CHECKPOINT50=false
MERGE_ALLOWED=false
NEXT_EXPECTED_COMMAND=Stage27-audit
PERFECT_CUBOID_CONCLUSION=NONE
```
