# Stage27-20-r302t — actual-coefficient off-diagonal correlation receiver

STATUS=BATCH_SUBMITTED_PENDING_FRESH_AUDIT
CHECKPOINT=40
ROUTE_KIND=UPPER_REENTRY_MAIN_HIGH_OCCUPANCY
PARENT_ROUTE=Stage27-20-r302s
SOURCE_STAGE=Stage20

If the continuation uses the coefficient-specific branch, there is likewise no need to retain the stronger all-vector bound

```text
||G_d-diag(G_d)||op <= B^{-2delta} E_packet.
```

The original Stage27 expression only needs the off-diagonal form evaluated on the exact physical Fourier vector `c_b=W_hat(b)/q`. A sufficient bound is therefore

```text
| sum_{b!=b'} c_b conj(c_{b'}) G_d(b,b') |
 <= B^{-2delta_off+o(1)} E_packet
    * sum_{b:d|b} |c_b|^2
```

uniformly over every retained MAIN packet and gcd stratum.

This receiver is strictly weaker than an operator norm estimate on `R_d=G_d-diag(G_d)`: it asks only for cancellation for the coefficient vectors actually generated by the physical residue weight. It still preserves the exact `H_phys^MAIN` measure, correlated modulus, gcd descent, common-parent allocation, masks, and quantifier order.

Combining this bound with the r302s diagonal exceptional-mass conclusion yields

```text
Q_d(c)
 <= B^{-2 min(delta_diag,delta_off)+o(1)} E_packet ||c||_2^2
```

for the actual coefficient vector, even if the stronger all-`c` operator theorem fails because some basis-vector diagonal is large.

The StructureRadar large-sieve/Kuznetsov/Kloosterman-fraction literature audit does not currently supply this exact same-measure physical off-diagonal form without an additional kernel/weight adapter. The benefit of this reduction is that a future theorem no longer has to control arbitrary `c`; it may exploit the exact Fourier origin of the coefficient sequence if that structure can be matched rigorously.

```text
FIRST_MISSING_LEMMA=MAINWallActualFourierCoefficientOffDiagonalSameMeasureCorrelationDeficit
```

STAGE27_20_R302T_STATUS=BATCH_SUBMITTED_PENDING_FRESH_AUDIT
ACTUAL_COEFFICIENT_OFFDIAGONAL_RECEIVER_DERIVED=true
FULL_REMAINDER_OPERATOR_NORM_REQUIRED=false
ACTUAL_COEFFICIENT_OFFDIAGONAL_DEFICIT_PROVED=false
BAD_MODE_FOURIER_ENERGY_DEFICIT_PROVED=false
COEFFICIENT_SPECIFIC_FULL_QUADRATIC_FORM_DEFICIT_PROVED=false
MAIN_ARITHMETIC_HOST_CORRELATION_POWER_DEFICIT_PROVED=false
STRICT_SUB_SQRT_UPPER_PROVED=false
NEW_MU_LT_HALF_PROVED=false
TRUE_N2_EXPONENT_IDENTIFIED=false
CURRENT_CHECKPOINT=40
NEXT_CHECKPOINT=40
ADVANCE_TO_CHECKPOINT50=false
NEXT_DERIVED_ROUTE=27-20-r302u
NEXT_BATCH=Stage27-20-r302-main-batch
