# Stage27-20-r302p-u

This batch repairs the hostile-audit failure in r302n/o and continues the checkpoint40 reduction as far as the currently imported StructureRadar identities allow.

- r302n/o repaired: the false baseline-subtraction escape is removed. The all-`c` receiver necessarily implies a power-small Gram diagonal by testing `c=e_b`.
- r302p: primitive Gauss completion shows inverse-frequency phase cancellation cannot save the diagonal.
- r302q: isolates the uniform diagonal normalization ratio and the legal coefficient-specific fallback.
- r302r: returns, when necessary, to the exact physical Fourier vector `c_b=W_hat(b)/q` rather than silently weakening the all-`c` theorem.
- r302s: derives a bad-diagonal-mode exceptional Fourier-energy receiver.
- r302t: weakens the off-diagonal operator target to the exact physical coefficient form.
- r302u: records the two honest closure packages and the next mathematical attack.

No fixed-power MAIN saving is claimed. Checkpoint50 remains blocked. The reduction chain itself is not frozen for StructureRadar.

NEXT_EXPECTED_COMMAND=Stage27-20-r302-audit
