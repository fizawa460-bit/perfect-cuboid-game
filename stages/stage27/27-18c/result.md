# Stage27-18c — StructureRadar / Arsenal rematch on the Stage18 source measure

```text
TASK_ID=Stage27-18c
ROLE=ARSENAL_REMATCH
STATUS=COMPLETE_ROUTING
```

The receiver has changed materially from the frozen Stage27-20 wall problems: the host is now the full settled Stage18 source measure, and the additional predicate is the exact squareclass equality

`sf(A)=sf(D)`

for the paired Gaussian norms from 27-18b.

Re-match verdict:

1. **Same-measure local split-prime parity sieve — GREEN, already certified.**  Stage19 already contains an independent causal proof that the survivor ratio tends to zero on this literal Stage18 measure.  It is a valid survival tool, but at its currently certified strength it gives zero density rather than a new fixed-power exponent beyond the inherited `1/2` numerator bound.

2. **Squareclass/Gaussian factorization structure — GREEN_STRUCTURAL.**  The exact `A=kP^2, D=kQ^2` decomposition is the correct localization coordinate.  It should be used to stratify the Stage18 mass by squarefree kernel `k`, reduced-direction/core height, and dyadic Gaussian norm ranges before any stronger analytic weapon is charged.

3. **Support-energy / moment tools — AMBER.**  They become useful only after the localized Stage18 mass is written on the exact physical source measure.  Ambient multiplicity or a larger Gaussian host cannot be substituted for source mass.

4. **Quadratic/Hecke large-sieve and determinant/incidence tools — AMBER_CONDITIONAL.**  They require a legal separated coefficient or new determinant adapter after localization.  The adapter gates frozen in Stage27-20 are not silently inherited as solved merely because the host measure changed.

5. **Super-Kai individual-modulus Gaussian-prime occupancy / exceptional-zero gates — RESERVE_ONLY.**  They are not prerequisites for 27-18d. Reopen only if a localized receiver lands exactly in their theorem shape.

The useful consequence of this rematch is routing: first localize the known `B(log B)^5` Stage18 mass in the exact `(k, core, direction, norm-band)` coordinates.  Only then ask for fixed-power suppression.  Reproving the Stage18 absolute asymptotic by Gaussian/divisor methods is explicitly forbidden.

```text
DIRECT_NEW_FIXED_POWER_SURVIVAL_THEOREM_FOUND=false
GREEN_EXISTING_ZERO_DENSITY=true
GREEN_STRUCTURAL_SQUARECLASS=true
LOCALIZATION_REQUIRED_BEFORE_NEW_ANALYTIC_CHARGE=true
NEXT_DERIVED_ROUTE=Stage27-18d
```
