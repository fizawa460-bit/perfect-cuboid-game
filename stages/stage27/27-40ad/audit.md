# Stage27-40ad hostile intermediate audit

## Verdict

PASS.

The fixed-U target-class universe is genuinely subpolynomial on the frozen Stage14 T interface. Stage14-t135 records `h_d=B^o(1)` for the ordinary Gaussian residue coset, and Stage14-t157 records `d=B^o(1)` for the terminal super-Kai individual-residue receiver. Hence `|R_d|<=d^2=B^o(1)`, and every fixed-U residue/projective class family inherited from this modulus is `B^o(1)`.

For fixed eta>0, a bound

`|E_U| <= B^(-eta+o(1)) |C_U|`

with `|C_U|=B^o(1)` has right side `B^(-eta+o(1)) -> 0`; integrality therefore forces `|E_U|=0` for all sufficiently large B. Thus a fixed-power exceptional-class fraction on the fixed-U class universe collapses asymptotically to the missing pointwise theorem rather than giving an intermediate averaged rescue.

Likewise, anti-concentration or collision-energy gains whose only denominator is `|C_U|=B^o(1)` are exponent-neutral at fixed-power scale. This does not rule out averaging over a genuinely polynomial-size outer physical packet family with the actual Stage14 pushforward weights.

The route is therefore accepted as a negative/narrowing result only. It proves no strict sub-square-root bound and does not identify the true N2 exponent.

Historical Stage27-40 and Stage27-40aa regression failures on the submission head were lifecycle-verifier failures caused by later checkpoint40 child-route state. Their verifiers were made successor-route state-aware during audit; the dedicated Stage27-40ad workflow itself was already SUCCESS.

```text
AUDIT_VERDICT=PASS
DISCOVERY_AUDIT_VERDICT=PASS
STAGE27_40AD_STATUS=INTERMEDIATE_AUDITED_PASS_AWAITING_MERGE
FIXED_U_CLASS_UNIVERSE_SUBPOLYNOMIAL_ACCEPTED=true
FIXED_POWER_EXCEPTION_FRACTION_IMPLIES_EVENTUAL_ZERO_EXCEPTIONS_ACCEPTED=true
FIXED_U_CLASS_ENERGY_FIXED_POWER_SAVING=false
T_AVERAGED_ROUTE_REQUIRES_POLYNOMIAL_OUTER_PHYSICAL_FAMILY_ACCEPTED=true
OUTER_PHYSICAL_WEIGHTED_AVERAGING_THEOREM_PROVED=false
STRICT_SUB_SQRT_UPPER_PROVED=false
NEW_MU_LT_HALF_PROVED=false
TRUE_N2_EXPONENT_IDENTIFIED=false
AUDIT_CLOSE_STAGE=false
ADVANCE_TO_CHECKPOINT50=false
CONTINUE_UPPER_EXPLORATION_AFTER_PASS=true
CURRENT_CHECKPOINT=40
NEXT_CHECKPOINT=40
MERGE_ALLOWED=true
PERFECT_CUBOID_CONCLUSION=NONE
NEXT_EXPECTED_COMMAND=merge PR #1029; then Stage27-main-batch with checkpoint40 upper exploration retained
```
