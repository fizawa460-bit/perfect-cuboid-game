# Stage27-19-r7a — SR-STR-166 eliminant/determinant novelty preflight

```text
TASK_ID=Stage27-19-r7a
PARENT=Stage27-19-StructureRadar-r402h-rematch
CARD=SR-STR-166
CHECKPOINT=40
CURRENT_MU=1/2
STATUS=SUBMITTED_PENDING_FRESH_AUDIT
```

The rematched receiver is

\[
A=s^2(m^2+n^2)=pg,\qquad D=n^2(r^2-s^2)=qg,
\]

with reduced `(p,q)=1`, all Stage19 physical masks retained, and fixed-core representation multiplicity already bounded by `B^{o(1)}` in r6d.

SR-STR-166 requires a charged-once eliminant preflight before importing determinant/lattice machinery.  For fixed `(p,q,g)` the two defining equations immediately give

\[
s^2\mid pg,\qquad n^2\mid qg,
\]

and, after selecting positive divisors `s,n`,

\[
\boxed{m^2=pg/s^2-n^2},\qquad
\boxed{r^2=qg/n^2+s^2}.
\]

Thus the natural elimination does not produce a new irreducible bilinear/trilinear hypersurface with an independent determinant factor.  It collapses to the same divisor-many fixed-core reconstruction already used in r6d.  Clearing denominators yields only identities generated by the original two equations, for example

\[
s^2(m^2+n^2)-pg=0,\qquad n^2(r^2-s^2)-qg=0,
\]

and combinations thereof.  Any determinant obtained by arranging these two equations as a 2x2 linear system in `pg,qg` is a coordinate artifact; it introduces no new arithmetic divisor, spacing modulus, or independent vanishing condition on physical points.

The shared variables `(n,s)` therefore do not create a new SR-STR-166 determinant branch at this level.  The fiber over `(p,q,g)` remains divisor/subpower and was already charged in r6d.  A Reuss-type lattice theorem would only re-count the same fixed-core reconstruction unless a further independent physical identity is added that produces a genuinely new determinant factor.

Verdict:

```text
SR166_ELIMINANT_PREFLIGHT=PASS
NEW_IRREDUCIBLE_BILINEAR_TRILINEAR_ELIMINANT_FOUND=false
NEW_DETERMINANT_OR_HYPERDETERMINANT_FACTOR_FOUND=false
ELIMINANT_COLLAPSES_TO_R6D_FIXED_CORE_RECONSTRUCTION=true
REUSS_DETERMINANT_IMPORT_CURRENTLY_LEGAL=false
SR166_R402H_QUANTITATIVE_BRANCH=RED_CURRENT
STRICT_SUB_SQRT_UPPER_PROVED=false
NEW_MU_LT_HALF_PROVED=false
ADVANCE_TO_CHECKPOINT50=false
NEXT_DERIVED_ROUTE=27-19-r7b
NEXT_TARGET=SR169_R402H_CORRELATION_ADAPTER_WITH_SUBPOWER_PUSHFORWARD_WEIGHTS
```

This is a route no-go, not a theorem that no other independent eliminant can ever exist.  Reopen SR-STR-166 only if a new Stage19 identity adds an independent coupled relation not generated by the two core equations and previously paid squareclass/common-core constraints.
