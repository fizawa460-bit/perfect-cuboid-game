# Stage30-05 — hostile audit of common `Q(i)` modular/cuboid anchor

```text
AUDITED_PR=1331
AUDITED_SUBMISSION_HEAD=368dd977f8fce5931873523048b45b2515de34f5
AUDIT_VERDICT=PASS_AFTER_BOUNDED_HAUPTMODUL_GAUGE_BRIDGE_REPAIR
```

## Verdict

The common-model claim is accepted. Testa--Stoll Section 4 gives the same cuboid surface over `Q(i)` as

```text
Sbar_Q(i) ~= (X(8) x X(8))/Delta G0,
G0=ker(PSL2(Z/8)->PSL2(Z/4)) ~= (Z/2)^3,
```

with

```text
XY=TZ,
X=a1+c,
Y=-a1+c,
T=a2+i*a3,
Z=a2-i*a3.
```

Thus the modular and seven-line/sign-cover descriptions can genuinely be compared on one function field; this is not an abstract `S4 ~= S4` identification.

The exact checker calculation is also correct in its displayed `X(4)` gauge:

```text
rho(S_mod)=a06=(A1 A2)(B1 B2)
rho(T_mod)=a21=(A1 C)(B2 B3)
|ker rho|=4
ker rho=V_mod={g04,g06,g12,g14}
|im rho|=6
```

Fresh algebraic reconstruction of the seven quadratic branch functions modulo `XY=TZ` reproduces these two generator permutations and their `+/-1` multipliers.

## Bounded Hauptmodul-gauge bridge repair

The submission cited Chai--Lin--Wang for the Hauptmodul transformation laws

```text
S(h)=(1-h)/(1+h),
T(h)=i*h,
```

and then used those formulas directly on the Testa--Stoll coordinate `t=x/y`.

A literal identification `h=t` was not source-locked. The missing bridge is finite and exact:

1. On the Testa--Stoll `X(4)=X(8)/G0` coordinate `t=x/y`, the six cusps are

```text
C6={0,infinity,+1,-1,+i,-i},
```

because the cusp divisor on `X(8)` is `uvw=0` with

```text
u^2=2xy,
v^2=x^2-y^2,
w^2=x^2+y^2.
```

2. Chai--Lin--Wang Corollary 4.4 gives a degree-one Hauptmodul `h:X(4)->P1` whose six cusp values are the same set `C6`; Proposition 4.3 gives the displayed `S,T` action.

3. Therefore the two Hauptmodul coordinates differ by a Möbius transformation

```text
m in Aut(P1,C6).
```

The transformations generated by

```text
S(t)=(1-t)/(1+t),
T(t)=i*t
```

preserve `C6` and form the full octahedral group of order 24, i.e. `Aut(P1,C6) ~= S4`.

4. Hence the actual Testa--Stoll `t`-action is conjugate to the submitted gauge action. The displayed image subgroup in the arrangement `S4` may be conjugated, but

```text
ker rho = V_mod,
|ker rho|=4,
|im rho|=6
```

are invariant under this gauge change.

Accordingly, the exact IDs `a06/a21` and the displayed six image IDs are retained as **chosen-gauge representatives**, not canonical arithmetic labels. The load-bearing Stage30 consequence is the invariant quotient shape

```text
1 -> V_mod ~= V4 -> PSL2(Z/4) ~= S4 -> S3_branch -> 1.
```

## Stage30-04 reinterpretation

The audited 24 Stage30-04 objects remain exact simultaneous finite-action relabellings. They do not become false. Stage30-05 proves that they are not source-geometric `S4 -> S4` adapters: on the actual common model the modular residual `S4` projects to the seven branch-square action through an `S3` quotient with kernel `V4`.

```text
STAGE30_04_24_CANDIDATES_VALID_FINITE_RELABELINGS=true
STAGE30_04_24_CANDIDATES_GEOMETRIC_ADAPTERS=false
```

## Reclassification

```text
L30-QI-FINITE-EQUIVARIANT-SEARCH
  = CLASS1_DISCHARGED_STAGE30_04

L30-COMMON-GEOMETRIC-OR-MODULI-ANCHOR
  = CLASS1_DISCHARGED_EXACT_COMMON_MODEL_PLUS_GAUGE_INVARIANT_ACTION_PROJECTION

L30-V4-SIGN-DECK-GALOIS-LIFT
  = CLASS2_STAGE30_06

NEW_CLASS3_THEOREM_GATE
  = NONE
```

No hidden Class-1 leaf remains at this gate. Stage30-06 must now lift `V_mod` from the branch-square quotient into the endpoint sign deck and derive the exact `Gal(Q(i)/Q)` semilinear/cocycle relation before any Codex verification is delegated.

## Firewalls

```text
COMMON_QI_MODEL_ANCHOR=VERIFIED
MODULAR_TO_BRANCH_KERNEL_EQUALS_V_MOD=true
MODULAR_TO_BRANCH_KERNEL_ORDER=4
MODULAR_TO_BRANCH_IMAGE_ORDER=6
DISPLAYED_IMAGE_S3_CANONICAL=false
DISPLAYED_GENERATOR_IDS_CANONICAL=false
V4_SIGN_DECK_LIFT_VERIFIED=false
Q_GALOIS_COCYCLE_VERIFIED=false
DEFECT_ELIMINATION_COUNT=0
R29_KUM5_DISCHARGED=false
NEW_THEOREM_ASSUMED=false
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```

## Final audit state

```text
AUDIT_REQUIRED=false
REPAIR_REQUIRED=false
MERGE_ALLOWED=true
ADVANCE_ALLOWED=true
NEXT_ITEM=30-06_QI_OVER_Q_DESCENT_COCYCLE_DERIVATION
NEXT_EXPECTED_COMMAND=Stage30-main-batch
```
