# Codex Task A — Stage30 modular S4 exact action extraction

Treat this file as the complete task specification. Do not infer stronger mathematical claims from names such as `S4`.

Repository: `fizawa460-bit/perfect-cuboid-game`

## Objective

Produce exact, dependency-light finite certificates for the two concrete Stage30 action objects defined in

- `stages/stage30/30-01/input-manifest.json`
- `stages/stage30/30-01/source-lock.md`

This task **does not** identify the arrangement S4 with the modular residual S4. It only materializes and independently verifies both action tables in common deterministic formats.

## Mandatory reads before coding

Read these exact repository files first:

```text
stages/stage30/30-01/input-manifest.json
stages/stage30/30-01/source-lock.md
stages/stage29/29-02ha/arrangement_check.py
stages/stage29/29-02ha/arrangement-check-output.md
stages/stage29/29-02g/result.md
stages/stage29/29-02g/exact-q-moduli-adapter.md
stages/stage29/29-02g/torsion-descent-defect.md
stages/stage29/29-02g/defect_orbits.py
stages/stage29/29-15/bounded-execution.md
stages/stage29/29-15/verify_bounded_execution.py
```

Reuse existing exact code and conventions before writing replacement logic.

## Frozen arrangement input

The seven line labels and forms are fixed by the Stage30-01 manifest. Independently rerun/reuse the existing arrangement checker and certify the frozen generators

```text
s_arr=(A1 A2)(B1 B2)
t_arr=(A1 A2 A3 C)(B1 B3)
```

from the displayed exact dual matrices. Do not accept the permutation strings without checking the matrices against the seven line forms.

Required arrangement action outputs:

```text
G_arr order
closure generated by s_arr,t_arr
permutation table on all seven labels
restricted table on Omega_arr_4={A1,A2,A3,C}
restricted table on Omega_arr_3={B1,B2,B3}
orbit and stabilizer tables
Q-liftable subgroup membership labels if recoverable from existing checker
```

## Frozen modular input

Work exactly with

```text
G_mod = PSL2(Z/4)=SL2(Z/4)/{+/-I}
S_mod=[[0,-1],[1,0]] mod 4
T_mod=[[1,1],[0,1]] mod 4
V_mod=ker(PSL2(Z/4)->PSL2(F2)).
```

Use a deterministic canonical representative for each projective matrix class. Explain and implement that normalization.

Do not simply import an abstract `S4` implementation and relabel it. Construct `SL2(Z/4)`, quotient by `+/-I`, and reduction mod 2 explicitly.

Certify from exact enumeration:

```text
|G_mod|=24
<S_mod,T_mod>=G_mod
|V_mod|=4
V_mod normal
V_mod ~= V4
```

Then construct exactly the Stage30-01 action sets

```text
Omega_mod_3 = V_mod - {identity}
Omega_mod_4 = {H <= G_mod : |H|=6, H intersect V_mod={1}, H*V_mod=G_mod}
```

and certify their actual cardinalities. Do not force the expected values if enumeration disagrees; report a counter-certificate instead.

For `Omega_mod_4`, enumerate subgroups exactly from the concrete group. Do not use a pre-labelled S4 point-stabilizer list.

Required modular action outputs:

```text
canonical G_mod element ledger
S_mod/T_mod multiplication and inverse witnesses
reduction-to-PSL2(F2) ledger
V_mod element ledger
Omega_mod_3 ledger
Omega_mod_4 subgroup ledger
conjugation action of S_mod,T_mod on Omega_mod_3 and Omega_mod_4
full orbit/stabilizer tables
verified group relations actually satisfied by the chosen generators
```

## Frozen marked data — label only in Task A

Carry, but do not use to claim Q-descent compatibility:

```text
D4=diag(1,-1) mod 4
K8=ker(SL2(Z/8)->SL2(Z/4)), |K8|=8
SIGMA_ACTION_ON_K8=TRIVIAL_AUDITED
MARKED_ARITHMETIC_DEFECT_CLASS_COUNT=8
```

Task A must not:

- identify `D4` as an element of `G_mod`;
- classify the eight defects under the arrangement action;
- claim a `Q(i)/Q` cocycle;
- claim the two S4 actions are already identified;
- eliminate any defect.

Those belong to later Stage30 units.

## Output files

Create under `stages/stage30/30-02C/`:

```text
result.md
action-tables.json
orbit-stabilizer.json
verify_actions.py
repro-manifest.json
```

`action-tables.json` must contain deterministic labels for every point/subgroup and explicit generator images. `repro-manifest.json` must record input paths/blob hashes where available, Python/runtime assumptions, and the exact command to rerun the checker.

`verify_actions.py` must use exact integer/modular arithmetic and Python standard library if reasonably possible. It must independently fail on malformed/altered output rather than merely regenerate the same JSON and compare it to itself.

Minimum successful checker output:

```text
ARRANGEMENT_GENERATORS=PASS
ARRANGEMENT_GROUP_ORDER=24
ARRANGEMENT_4_PLUS_3_ACTION=PASS
MODULAR_PROJECTIVE_GROUP_ORDER=24
MODULAR_GENERATORS=PASS
MODULAR_REDUCTION_KERNEL_ORDER=4
MODULAR_OMEGA3_COUNT=3
MODULAR_OMEGA4_COUNT=4
MODULAR_ACTION_TABLES=PASS
ORBIT_STABILIZER_CERTIFICATE=PASS
TASK_A_Q_DESCENT_CREDIT=false
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```

If any expected finite identity fails, do not patch around it. Record the exact failing object and stop with `TASK_A_VERDICT=COUNTERCERTIFICATE`.

## Repository discipline

Do not merge any PR. Do not modify Stage29 historical files. Keep all new result code/data under `stages/stage30/30-02C/` except for minimal controller/result linkage if explicitly required by the current repository workflow.

Final task status must be one of:

```text
TASK_A_VERDICT=PASS_FINITE_ACTION_CERTIFICATE_READY_FOR_STAGE30_AUDIT
TASK_A_VERDICT=COUNTERCERTIFICATE
TASK_A_VERDICT=BLOCKED_WITH_EXACT_REASON
```
