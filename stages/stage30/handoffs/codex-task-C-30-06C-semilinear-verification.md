# Codex Task C — Stage30-06C exact semilinear/cocycle verification

> **DO NOT EXECUTE UNTIL THE Stage30-06 PR HAS PASSED `Stage30-audit`.**
>
> This is the sole remaining Codex-owned Stage30 unit. Do not expand scope to Stage30-07 or later work.

## Repository / target

Repository:

```text
fizawa460-bit/perfect-cuboid-game
```

Target:

```text
Stage30-06C
K16-C2-MODULAR-S4-ACTION
R29-KUM5
```

Your job is **verification, not discovery**. Stage30-06 has already mathematically frozen the objects, the V4 sign-deck lift, the Q(i)/Q coordinate cocycle and the semilinear identity. You must independently reconstruct and exhaustively verify them using exact arithmetic.

## Mandatory reads

Read all of these before writing code:

```text
stages/stage30/30-06/source-lock.md
stages/stage30/30-06/cocycle-derivation.md
stages/stage30/30-06/semilinear-spec.json
stages/stage30/30-06/check_frozen_generators.py

stages/stage30/30-05/audit.md
stages/stage30/30-05/audit-state.json
stages/stage30/30-05/common-anchor.json
stages/stage30/30-05/verify_common_anchor.py

stages/stage30/30-02C/action-tables.json
stages/stage30/30-02C/verify_actions.py

stages/stage29/29-02ha/exact-sign-cover-model.md
stages/stage29/29-02g/exact-q-moduli-adapter.md
stages/stage29/29-15/bounded-execution.md

stages/stage30/ownership-amendment-2026-08-22.md
stages/stage30/codex-handoff-contract.md
stages/stage30/controller.json
```

Treat all prior claims as authoritative **only inside their audited scope**.

## Frozen mathematical objects — do not redefine

### Residual modular group

```text
H=PSL2(Z/4), |H|=24
S_mod=[[0,-1],[1,0]]
T_mod=[[1,1],[0,1]]
D4=diag(1,-1) mod 4
```

Use the same projective canonicalization/IDs as audited Task A.

Define

```text
theta(g)=D4*g*D4^-1.
```

Frozen expected facts:

```text
theta(S_mod)=S_mod
theta(T_mod)=T_mod^-1
V_mod={g04,g06,g12,g14}
theta fixes V_mod pointwise
```

### Endpoint projective lifts

Coordinate order:

```text
(a1,a2,a3,b1,b2,b3,c)
```

Frozen `S_hat`:

```text
a1 -> -a2
a2 -> -a1
a3 -> -a3
b1 ->  b2
b2 ->  b1
b3 ->  b3
c  ->  c
```

Frozen `T_hat`:

```text
a1 -> -c
c  -> -a1
a2 ->  i*a2
a3 ->  i*a3
b1 ->  i*b1
b2 ->  b3
b3 ->  b2
```

Equality is projective in `PGL7(Q(i))`.

### Frozen V4 sign-deck lift

```text
j(g04)=identity
j(g12)=negate {a2,a3,b1}
j(g06)=negate {a1,a3,b2}
j(g14)=negate {a1,a2,b1,b2}
```

### Frozen Q(i)/Q coordinate cocycle

```text
c_sigma=delta_a3
```

meaning `a3 -> -a3`, all other canonical coordinates fixed.

Expected cocycle relation:

```text
c_sigma * sigma(c_sigma)=1.
```

### Frozen semilinear identity

For the 24-element projective representation `alpha_hat` generated by `S_hat,T_hat`, verify for **every** `g in H`:

```text
sigma(alpha_hat(g))
 = c_sigma * alpha_hat(theta(g)) * c_sigma^-1
```

where `sigma` is coefficient conjugation `i -> -i`.

## Required independent reconstruction

Do not merely call the Stage30-06 generator checker or copy its booleans.

Independently:

1. enumerate all determinant-one matrices over `Z/4`;
2. quotient by `±I` using Task-A canonicalization;
3. reconstruct the 24-element multiplication table and IDs;
4. compute `theta` on all 24 elements;
5. generate the projective endpoint subgroup from `S_hat,T_hat` without importing a precomputed 24-row table;
6. establish a deterministic bijection from all 24 modular IDs to all 24 generated projective endpoint transformations;
7. test the cuboid quadrics are preserved by the generators and, preferably, all 24 generated transformations;
8. identify exactly which four generated transformations lie in the sign deck and compare them to the frozen `j(V_mod)` table;
9. verify the quadratic cocycle identity for `c_sigma`;
10. verify the semilinear identity for all 24 modular elements.

All arithmetic must be exact. No floating point and no numerical sampling.

## Projective equality

Your checker must compare projective linear transformations correctly. Matrices differing by one nonzero common scalar in `Q(i)` represent the same element of `PGL7(Q(i))`.

Do not accidentally compare raw matrices only.

## Required outputs

Create a new Stage30-06C directory. Minimum files:

```text
stages/stage30/30-06C/semilinear-certificate.json
stages/stage30/30-06C/verify_semilinear_cocycle.py
stages/stage30/30-06C/result.md
stages/stage30/30-06C/repro-manifest.json
```

The JSON certificate must include at least:

```text
all 24 modular IDs
theta image of every ID
projective endpoint representative for every ID
V_mod IDs
sign-deck intersection IDs
four explicit sign patterns
c_sigma
semilinear PASS/FAIL per modular ID
```

Use deterministic ordering.

The verifier must fail closed on:

```text
wrong group order
ID mismatch
projective relation failure
missing modular element
duplicate endpoint representative
wrong V4 kernel
wrong sign pattern
wrong theta image
cocycle failure
any one of the 24 semilinear failures
```

## Strict scope prohibitions

You must **not**:

- invent or replace `c_sigma`;
- change `S_hat`, `T_hat`, `D4`, `theta`, or the V4 sign patterns;
- use an abstract statement `S4 ~= S4` instead of the concrete 24-element check;
- promote the Stage30-04 24 relabellings back into geometric adapters;
- classify or eliminate the 8 `K8` arithmetic defects;
- perform Stage30-07 work;
- infer `E` descends to `Q`;
- infer endpoint existence or nonexistence;
- edit earlier audited Stage29/Stage30 artifacts merely to make the checker pass.

If a frozen claim fails exact verification, report the failure with the smallest counterexample and stop. Do not repair the mathematics silently.

## Required result block

End `result.md` with exactly scoped machine-readable lines like:

```text
CODEX_TASK=C_30_06C
INPUT_SOURCE_LOCK_COMPLETE=true|false
EXACT_ARITHMETIC_ONLY=true|false
MODULAR_GROUP_ORDER=24
MODULAR_ID_COVERAGE_COMPLETE=true|false
THETA_ALL24_VERIFIED=true|false
THETA_FIXES_V_MOD_POINTWISE=true|false
ENDPOINT_PROJECTIVE_GROUP_ORDER=24
V4_SIGN_DECK_INTERSECTION_VERIFIED=true|false
C_SIGMA=delta_a3
C_SIGMA_COCYCLE_VERIFIED=true|false
SEMILINEAR_ALL24_VERIFIED=true|false
FAILED_ELEMENT_COUNT=<integer>
CHECKER_PRESENT=true|false
CHECKER_PASS=true|false
K8_DEFECT_CLASSIFICATION_EXECUTED=false
DEFECT_ELIMINATION_COUNT=0
NEW_THEOREM_ASSUMED=false
R29_KUM5_DISCHARGED=false
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```

Even `CHECKER_PASS=true` is unverified external computational input until `Stage30-audit` consumes the 30-06C result.
