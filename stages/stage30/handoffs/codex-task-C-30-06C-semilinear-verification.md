# Codex Task C — Stage30-06C exact semilinear/cocycle verification

> **DO NOT EXECUTE UNTIL THE Stage30-06 PR HAS PASSED `Stage30-audit`.**
>
> This is the sole remaining Codex-owned Stage30 unit. Do not expand scope to Stage30-07 or later work.

## Repository / target

```text
fizawa460-bit/perfect-cuboid-game
Stage30-06C
K16-C2-MODULAR-S4-ACTION
R29-KUM5
```

Your job is **verification, not discovery**. Stage30-06 has mathematically frozen the residual group, the source-derived diagonal modular endpoint lifts, the V4 sign-deck restriction, the Q(i)/Q coordinate cocycle and the semilinear identity. Independently reconstruct and exhaustively verify them using exact arithmetic.

## Mandatory reads

```text
stages/stage30/30-06/source-lock.md
stages/stage30/30-06/cocycle-derivation.md
stages/stage30/30-06/source-action-lift-audit-repair.md
stages/stage30/30-06/semilinear-spec.json
stages/stage30/30-06/check_frozen_generators.py
stages/stage30/30-06/audit.md
stages/stage30/30-06/audit-state.json

stages/stage30/30-05/audit.md
stages/stage30/30-05/common-anchor.json
stages/stage30/30-05/verify_common_anchor.py
stages/stage30/30-02C/action-tables.json

stages/stage29/29-02ha/exact-sign-cover-model.md
stages/stage29/29-02g/exact-q-moduli-adapter.md
stages/stage29/29-15/bounded-execution.md
stages/stage30/ownership-amendment-2026-08-22.md
stages/stage30/codex-handoff-contract.md
stages/stage30/controller.json
```

The audit repair is authoritative over the pre-audit generator signs in `cocycle-derivation.md` where they conflict.

## Frozen residual modular group

```text
H=PSL2(Z/4), |H|=24
S_mod=[[0,-1],[1,0]]
T_mod=[[1,1],[0,1]]
D4=diag(1,-1) mod 4
theta(g)=D4*g*D4^-1

theta(S)=S
theta(T)=T^-1
V_mod={g04,g06,g12,g14}
theta|V_mod=id
```

## Source-derived endpoint projective lifts

Coordinate order:

```text
(a1,a2,a3,b1,b2,b3,c)
```

These are not arbitrary S4 lifts. They are the diagonal quotient actions derived from the Testa--Stoll X(8) equations and the audited Stage30-05 X(4) gauge.

Frozen `S_hat`:

```text
a1 -> -a2
a2 -> -a1
a3 -> -a3
b1 -> -b2
b2 -> -b1
b3 ->  b3
c  ->  c
```

Frozen `T_hat`:

```text
a1 -> -c
c  -> -a1
a2 ->  i*a2
a3 ->  i*a3
b1 ->  i*b1
b2 -> -b3
b3 -> -b2
```

Source bridge to verify independently:

```text
X(8): u^2=2xy, v^2=x^2-y^2, w^2=x^2+y^2

S on one factor may be lifted as
x'=(-x+y)/sqrt(2), y'=(x+y)/sqrt(2), u'=i*v, v'=i*u, w'=w.

T on one factor may be lifted as
x'=i*x, y'=y, u'=zeta_8*u, v'=i*w, w'=i*v.
```

Apply each lift diagonally to `X(8)xX(8)`, pass to

```text
U=u1u2, V=v1v2, W=w1w2,
X=x1x2, Y=y1y2, T=x1y2, Z=x2y1,
```

then use

```text
U=2b1, V=2b2, W=2b3,
X=a1+c, Y=-a1+c,
T=a2+i*a3, Z=a2-i*a3.
```

The diagonal quotient must reproduce exactly the frozen `S_hat,T_hat` above. `sqrt(2)` / `zeta_8` must cancel from the quotient action, leaving a Q(i)-defined projective action.

The pre-audit submission used a sign-deck-twisted section differing by

```text
S twist = negate {b1,b2}
T twist = negate {b2,b3}
```

Do not use that old section as the source-derived modular action.

## Frozen V4 sign-deck lift

The audit repair does **not** change the V4 restriction:

```text
j(g04)=identity
j(g12)=negate {a2,a3,b1}
j(g06)=negate {a1,a3,b2}
j(g14)=negate {a1,a2,b1,b2}
```

## Frozen Q(i)/Q coordinate cocycle

```text
c_sigma=delta_a3
```

meaning `a3 -> -a3`, all other canonical coordinates fixed. Verify

```text
c_sigma * sigma(c_sigma)=1.
```

## Frozen semilinear identity

For the 24-element representation `alpha_hat` generated by the **source-derived** `S_hat,T_hat`, verify for every `g in H`:

```text
sigma(alpha_hat(g))
 = c_sigma * alpha_hat(theta(g)) * c_sigma^-1
```

in `PGL7(Q(i))`, where sigma is coefficient conjugation `i -> -i`.

## Required independent reconstruction

Do not call the Stage30-06 checker as your proof. Independently:

1. enumerate `SL2(Z/4)` and quotient by `±I` with the Task-A ID convention;
2. reconstruct all 24 elements and `theta`;
3. derive the displayed endpoint generators from the X(8) diagonal quotient formulas above;
4. verify the cuboid quadrics are preserved;
5. generate the full projective endpoint subgroup and establish a deterministic 24-element correspondence with modular IDs;
6. verify projective orders `2,4,3` for `S,T,ST` and group order 24;
7. identify the exact four-element sign-deck intersection and compare with `j(V_mod)`;
8. verify `c_sigma` and its quadratic cocycle identity;
9. verify the semilinear identity for all 24 elements.

All arithmetic must be exact. Projective matrices differing by one nonzero common scalar are equal.

## Required outputs

Create:

```text
stages/stage30/30-06C/semilinear-certificate.json
stages/stage30/30-06C/verify_semilinear_cocycle.py
stages/stage30/30-06C/result.md
stages/stage30/30-06C/repro-manifest.json
```

The certificate must include all 24 modular IDs, every theta image, every endpoint representative, V_mod/sign-deck intersection, four sign patterns, `c_sigma`, and per-element semilinear PASS/FAIL.

The verifier must fail closed on any source-action derivation mismatch, wrong group order/ID, duplicate endpoint representative, wrong V4 lift, wrong theta image, cocycle failure, or any semilinear failure.

## Strict prohibitions

Do **not**:

- replace the source-derived `S_hat,T_hat`;
- invent or replace `c_sigma` or `theta`;
- use abstract `S4 ~= S4` as a substitute for the concrete source action;
- classify/eliminate the 8 `K8` arithmetic defects or do Stage30-07 work;
- infer elliptic-curve descent, endpoint existence, or endpoint nonexistence;
- edit prior audited artifacts to force PASS.

If a frozen claim fails, report the smallest exact counterexample and stop.

## Required result block

```text
CODEX_TASK=C_30_06C
INPUT_SOURCE_LOCK_COMPLETE=true|false
EXACT_ARITHMETIC_ONLY=true|false
SOURCE_DERIVED_DIAGONAL_LIFTS_VERIFIED=true|false
MODULAR_GROUP_ORDER=24
MODULAR_ID_COVERAGE_COMPLETE=true|false
THETA_ALL24_VERIFIED=true|false
THETA_FIXES_V_MOD_POINTWISE=true|false
ENDPOINT_PROJECTIVE_GROUP_ORDER=24
V4_SIGN_DECK_INTERSECTION_VERIFIED=true|false
C_SIGMA=delta_a3
C_SIGMA_COCYCLE_VERIFIED=true|false
SEMILINEAR_ALL24_VERIFIED=true|false
FAILED_ELEMENT_COUNT=<integer>
CHECKER_PRESENT=true|false
CHECKER_PASS=true|false
K8_DEFECT_CLASSIFICATION_EXECUTED=false
DEFECT_ELIMINATION_COUNT=0
NEW_THEOREM_ASSUMED=false
R29_KUM5_DISCHARGED=false
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```

Even `CHECKER_PASS=true` is unverified external computational input until a later `Stage30-audit` consumes the 30-06C result.
