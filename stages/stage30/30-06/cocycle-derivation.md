# Stage30-06 — exact V4 sign-deck lift and Q(i)/Q semilinear cocycle

```text
STAGE=30-06
STATUS=SUBMITTED_PENDING_STAGE30_AUDIT
OWNER=ChatGPT/main-batch
CODEX_CREDIT=NONE_YET
```

Let

```text
K=Q(i),
Gamma=Gal(K/Q)={1,sigma},
H=PSL2(Z/4) ~= S4.
```

All equalities of endpoint automorphisms below are in `PGL7(K)` unless stated otherwise.

## 1. Common-model descent cocycle

Use the Testa--Stoll common-model isomorphism

```text
Phi:(X(8) x X(8))/Delta G0 -> Sbar_K
```

with

```text
X=a1+c,
Y=-a1+c,
T=a2+i*a3,
Z=a2-i*a3,
U=2*b1,
V=2*b2,
W=2*b3.
```

Conjugating the coefficients of the inverse coordinate formula

```text
a3=(T-Z)/(2i)
```

changes only the sign of `a3`. Therefore

```text
Phi^sigma = delta3 o Phi,
```

where

```text
delta3:
a3 -> -a3
```

and all other canonical coordinates are fixed.

This agrees with the independent primary-source statement that factor exchange on `X x X` descends to `a3 -> -a3` on `Sbar`.

Thus the coordinate-isomorphism descent cocycle is

```text
c_sigma = delta3 in G_sign.
```

It is Q-defined and has order two, so

```text
c_sigma * sigma(c_sigma)=1.
```

This `c_sigma` is **not** the eight-torsion arithmetic defect `kappa=psi^sigma psi`.

## 2. Residual modular Galois automorphism

The exact level-4 Q-datum retains

```text
D4=diag(1,-1) mod 4.
```

Therefore complex conjugation transports a residual basis-change class by

```text
theta(g)=D4*g*D4^(-1),
g in H.
```

For the frozen Stage30 generators

```text
S_mod=[[0,-1],[1,0]],
T_mod=[[1,1],[0,1]]
```

in `PSL2(Z/4)`, exact reduction gives

```text
theta(S_mod)=S_mod,
theta(T_mod)=T_mod^(-1).
```

For the audited kernel

```text
V_mod={g04,g06,g12,g14}=ker(H -> S3_branch),
```

one has

```text
theta(v)=v for every v in V_mod.
```

Thus the residual `V4` is pointwise fixed by the Q(i)/Q semilinear action.

## 3. A source-compatible projective lift of H to the endpoint

In the audited Stage30-05 gauge, choose the following projective lifts on canonical coordinates ordered as

```text
(a1,a2,a3,b1,b2,b3,c).
```

Define `S_hat` by

```text
a1 -> -a2
a2 -> -a1
a3 -> -a3
b1 ->  b2
b2 ->  b1
b3 ->  b3
c  ->  c
```

and define `T_hat` by

```text
a1 -> -c
c  -> -a1
a2 ->  i*a2
a3 ->  i*a3
b1 ->  i*b1
b2 ->  b3
b3 ->  b2.
```

Their squared-coordinate action is exactly the Stage30-05 chosen-gauge branch action:

```text
S_hat -> (A1 A2)(B1 B2),
T_hat -> (A1 C)(B2 B3),
```

with the load-bearing multipliers for `T_hat`

```text
A2 -> -A2,
A3 -> -A3,
B1 -> -B1.
```

Direct substitution in the four cuboid quadrics shows both transformations preserve `Sbar_K`. Their projective relations are

```text
S_hat^2=1,
T_hat^4=1,
(S_hat*T_hat)^3=1 in PGL7(K).
```

Hence they realize the octahedral `(2,4,3)` presentation of the residual `S4` on the same endpoint surface.

Gauge changes may conjugate this displayed subgroup inside the endpoint automorphism group. The invariant statements below are the kernel lift and semilinear compatibility, not the literal choice of generator labels.

## 4. Exact lift V_mod -> G_sign

With the Task-A IDs and the chosen generators,

```text
g12 = T_mod^2,
g06 = S_mod*T_mod^2*S_mod^(-1),
g14 = g12*g06.
```

Their endpoint lifts are pure Q-defined sign-deck elements:

```text
j(g04)=identity

j(g12):
  negate {a2,a3,b1}

j(g06):
  negate {a1,a3,b2}

j(g14):
  negate {a1,a2,b1,b2}.
```

All omitted coordinates are fixed. These four projective sign patterns are distinct modulo the common projective sign and form a Klein four subgroup of `G_sign`.

Therefore

```text
j:V_mod -> G_sign
```

is an explicit injective lift of the Stage30-05 branch-kernel.

In particular,

```text
V4_SIGN_DECK_LIFT_DERIVED=true.
```

## 5. Exact semilinear relation

Let

```text
alpha_hat:H -> Aut_K(Sbar_K)
```

be the projective representation generated by `S_hat,T_hat`. Coefficient conjugation satisfies on generators

```text
sigma(S_hat) = c_sigma * S_hat * c_sigma^(-1),

sigma(T_hat) = c_sigma * T_hat^(-1) * c_sigma^(-1).
```

Since `theta(S)=S`, `theta(T)=T^(-1)` and both sides are homomorphisms in `g`, the exact all-element relation is

```text
sigma(alpha_hat(g))
  = c_sigma * alpha_hat(theta(g)) * c_sigma^(-1)
for every g in H.
```

This is the Stage30 semilinear/cocycle identity to be verified exhaustively in 30-06C.

On the kernel `V_mod`, all pieces simplify:

```text
theta|V_mod = identity,
j(V_mod) is Q-defined,
c_sigma and j(V_mod) lie in the abelian sign deck.
```

Hence

```text
sigma(j(v))=j(v)=c_sigma*j(v)*c_sigma^(-1)
for all v in V_mod.
```

So the V4 sign-deck lift itself has no residual field-of-definition ambiguity.

## 6. What remains for Codex 30-06C

The formulas above are now frozen mathematically. Codex is not allowed to choose a different cocycle or redefine the objects. It must independently reconstruct and exhaustively verify:

1. the 24-element concrete `PSL2(Z/4)` ledger;
2. `theta(g)=D4*g*D4^-1` for all 24 elements;
3. the 24 projective endpoint lifts generated by `S_hat,T_hat`;
4. the exact four-element intersection with the sign deck and the listed `j(V_mod)` patterns;
5. `c_sigma=delta3` and the quadratic cocycle identity;
6. the semilinear identity for all 24 residual elements.

## 7. Arithmetic defect firewall

The later eight marked states remain

```text
K8=ker(SL2(Z/8)->SL2(Z/4)),
|K8|=8,
SIGMA_ACTION_ON_K8=TRIVIAL_AUDITED.
```

Stage30-06 does not identify, merge or eliminate any one of these eight states. That transport belongs to Stage30-07 after an audited 30-06C certificate.

```text
K8_DEFECT_CLASSIFICATION_EXECUTED=false
DEFECT_ELIMINATION_COUNT=0
R29_KUM5_DISCHARGED=false
NEW_CLASS3_THEOREM_GATE=NONE
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```
