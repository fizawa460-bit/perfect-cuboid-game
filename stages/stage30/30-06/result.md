# Stage30-06 — V4 sign-deck lift and Q(i)/Q cocycle derivation

```text
STAGE=30-06
STATUS=SUBMITTED_PENDING_AUDIT
OWNER=ChatGPT/main-batch
```

Stage30-06 completes the mathematical derivation that must precede the sole remaining Codex verification unit.

## Derived exact structure

The audited Stage30-05 residual kernel

```text
V_mod={g04,g06,g12,g14}
```

lifts in the chosen common-model gauge to the following Q-defined sign-deck subgroup:

```text
g04 -> identity
g12 -> negate {a2,a3,b1}
g06 -> negate {a1,a3,b2}
g14 -> negate {a1,a2,b1,b2}
```

Thus

```text
V4_SIGN_DECK_LIFT_DERIVED=true
V4_SIGN_DECK_LIFT_ORDER=4
```

The common-model coordinate isomorphism has Galois cocycle

```text
c_sigma=delta_a3,
```

because conjugation of `a3=(T-Z)/(2i)` changes the sign of `a3`, matching the Testa--Stoll factor-swap descent statement.

The retained level-4 sign matrix

```text
D4=diag(1,-1)
```

defines

```text
theta(g)=D4*g*D4^-1
```

on `PSL2(Z/4)`, with

```text
theta(S)=S
theta(T)=T^-1
theta|V_mod=id.
```

For the frozen projective endpoint representation `alpha_hat` generated by the explicit `S_hat,T_hat`, the derived semilinear identity is

```text
sigma(alpha_hat(g))
 = c_sigma * alpha_hat(theta(g)) * c_sigma^-1
```

for all residual modular elements. Generator-level exact checks pass; exhaustive all-24 verification is deliberately reserved for Codex Stage30-06C.

## Important object separation

```text
c_sigma != kappa
V_mod != K8
```

Here `c_sigma` is the common-model coordinate descent cocycle in the endpoint sign deck. The later arithmetic defect remains

```text
kappa=psi^sigma psi in K8,
|K8|=8.
```

No K8 state has been classified or eliminated by this substage.

## Stage30-06P

A complete copy/paste-ready Codex verification task has been generated at

```text
stages/stage30/handoffs/codex-task-C-30-06C-semilinear-verification.md
```

It is explicitly marked **DO NOT EXECUTE UNTIL Stage30-06 HAS PASSED `Stage30-audit`**.

Codex scope is only:

```text
30-06C exact exhaustive verification of the frozen all-24 semilinear identities.
```

It may not proceed into 30-07 defect classification.

## Current gate

```text
30_06_MATHEMATICAL_DERIVATION=SUBMITTED_PENDING_AUDIT
30_06P_CODEX_PROMPT_CREATED=true
30_06P_CODEX_PROMPT_AUDIT_APPROVED=false
WAITING_EXTERNAL_CODEX_RESULT_C=false
AUDIT_REQUIRED=true
AUDIT_VERDICT=PENDING
MERGE_ALLOWED=false
ADVANCE_ALLOWED=false
NEXT_ITEM_AFTER_AUDIT_PASS=30-06C_EXTERNAL_CODEX_VERIFICATION
NEXT_EXPECTED_COMMAND=Stage30-audit
```

```text
Q_GALOIS_COCYCLE_DERIVED=true
Q_GALOIS_COCYCLE_EXHAUSTIVELY_VERIFIED=false
K8_DEFECT_CLASSIFICATION_EXECUTED=false
DEFECT_ELIMINATION_COUNT=0
R29_KUM5_DISCHARGED=false
NEW_CLASS3_THEOREM_GATE=NONE
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```
