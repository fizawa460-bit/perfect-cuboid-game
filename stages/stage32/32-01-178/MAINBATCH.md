# stage32-01-178 MAINBATCH

Mission target: complete Stage32 32-01 FULL178 numerical Picard census with machine-checkable completeness while preventing duplicate work and blocked-route loops.

## Execution mode

`stage32-01-178-mainbatch` is a **single-researcher breadth-cycle** command dedicated to sustained FULL178 research.

The same researcher owns route generation, literature/repository lookup, mathematical attack, algorithmic redesign, blocker recording, route rotation, synthesis, and checkpointing. Do not fan the mission out into `-a/-b/...` child chats unless the user explicitly asks to re-enable parallel dispatch.

Generation-1 lanes `a` through `f` already returned before this mode switch. Their retained states are historical inputs to MAINBATCH, not active child lanes. Consume useful results, preserve blockers and audit history, and do not rerun equivalent work under new names.

## Startup and routing synchronization

Read only, in this order:

1. `AGENTS.md`;
2. `stages/stage32/COMMANDS.md`;
3. `stages/stage32/MAIN-STATE.json` — current Stage32 routing authority;
4. `stages/stage32/32-01-178/MISSION.json` — local/historical mission DAG;
5. state files required by the current unresolved FULL178 frontier;
6. exact source/evidence paths referenced by those states.

Before any new mathematics, compare the mission's local state with current Stage32 `MAIN-STATE.json`. If they differ, current Stage32 routing wins. Preserve old mission nodes as history; do not restart an older N-node merely because it is still present in `MISSION.json`.

Use Research OS policies only on explicit triggers.

## One batch

1. Revalidate the current FULL178 receiver and unresolved frontier against current Stage32 MAIN authority.
2. Search existing mission/repository results before inventing a new route.
3. Read historical Generation-1 returns only as needed; extract their useful lemmas, blockers, interfaces, and audit findings.
4. Work on the strongest current FULL178 route directly in this chat. Multiple mathematical viewpoints and route rotation are allowed inside the same batch.
5. Freeze every failed route with a stable identity, exact blocker, and reopen condition. Do not rediscover or rerun it without new input.
6. When the same receiver survives repeated attacks, perform breadth/BLIND_REDISCOVERY under the Cycle Exploration Safety Protocol and continue with a materially distinct route.
7. Treat EX5 as an external producer. PR #1765 / BC2-24 is merged history; consume retained/audited EX5 outputs only through an explicit current-target adapter. If EX5 opens BC2-25 or a later producer leaf, do not duplicate that exact Picard64/node-support work here.
8. Heavy/artifact-producing compute still requires the repository heavy-workflow authorization gate.
9. Retain exact mathematical/algorithmic results with scope and replay path; update mission-local state at coherent checkpoints without overriding Stage32 MAIN authority.
10. Do not self-grant hostile-audit credit and do not merge without explicit user authorization.

## Current authority relationship

Stage32 MAIN currently remains the controller/integrator and may itself research MAIN-owned, cross-lane, or unowned mathematics. This 178 command is not a second controller: it is the sustained FULL178 specialist research surface.

Thus:

- MAIN chooses/integrates the Stage32 frontier;
- 178 attacks sustained FULL178 census mathematics;
- EX5 produces sustained Picard64/node-support obstruction results;
- no two surfaces should independently own the same semantic leaf.

## Human interface

The normal operator command is simply:

`stage32-01-178-mainbatch`

Use `stage32-01-178-audit` only when MAINBATCH reaches a new exact retained audit boundary. No additional lane commands are required in single-researcher mode.
