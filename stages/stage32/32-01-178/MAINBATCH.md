# stage32-01-178 MAINBATCH

Mission target: complete Stage32 32-01 FULL178 numerical Picard census with machine-checkable completeness while preventing duplicate work and blocked-route loops.

## Execution mode

`stage32-01-178-mainbatch` is a **single-researcher breadth-cycle** command.

The same researcher owns route generation, literature/repository lookup, mathematical attack, algorithmic redesign, blocker recording, route rotation, synthesis, and checkpointing. Do not fan the mission out into `-a/-b/...` child chats unless the user explicitly asks to re-enable parallel dispatch.

Generation-1 lanes `a` through `f` already returned before this mode switch. Their retained states are historical inputs to MAINBATCH, not active child lanes. Consume useful results, preserve blockers and audit history, and do not rerun equivalent work under new names.

## Startup

Read only:

1. `AGENTS.md`;
2. `stages/stage32/32-01-178/MISSION.json`;
3. state files required by the current unresolved frontier and returned Generation-1 evidence;
4. exact source/evidence paths referenced by those states.

Use Research OS policies only on explicit triggers.

## One batch

1. Revalidate the current FULL178 receiver and unresolved frontier.
2. Search existing mission/repository results before inventing a new route.
3. Read the historical Generation-1 returns only as needed; extract their useful lemmas, blockers, interfaces, and audit findings.
4. Work on the strongest current route directly in this chat. The researcher may examine multiple mathematical viewpoints within the same batch and may rotate routes when blocked.
5. Freeze every failed route with a stable identity, exact blocker, and reopen condition. Do not rediscover or rerun it without new input.
6. When the same receiver survives repeated attacks, perform breadth/BLIND_REDISCOVERY under the Cycle Exploration Safety Protocol and continue with a materially distinct route.
7. Treat EX5 as an external producer. Do not duplicate its active BC2-02 work; consume only retained/audited outputs through an explicit current-target adapter.
8. Heavy/artifact-producing compute still requires the repository heavy-workflow authorization gate.
9. Retain exact mathematical/algorithmic results with scope and replay path; update mission state at coherent checkpoints.
10. Do not self-grant hostile-audit credit and do not merge without explicit user authorization.

## Immediate inherited frontier

Generation-1 returned:

- N101 through N105: retained/closed noncredit research inputs;
- N106: repaired after a hostile-audit FAIL but still `AUDIT_REQUIRED`;
- N150: EX5 external-consumption gate remains BLOCKED pending a current-target promotable EX5 result;
- N190: reconciliation remains downstream of N106.

MAINBATCH should use these as context, not as a requirement to continue the old six-lane decomposition. It may repair/replace/demote routes as the mathematics requires, while preserving exact audit history.

## Human interface

The normal operator command is simply:

`stage32-01-178-mainbatch`

Use `stage32-01-178-audit` only when MAINBATCH reaches an exact retained audit boundary. No additional lane commands are required in single-researcher mode.
