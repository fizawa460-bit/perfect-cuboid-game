# Stage32 MB104 — `000707000f0f` e=2 even norm-form conductor baselocus

Status: **RETAINED CONDITIONAL NORM-FORM / OPPOSITE-SHEET GLUING SUPPORTED ON AMBIENT EIGENSECTION BASE LOCUS / EXACT INTERSECTION RECONCILIATION / e=2 OPEN / NO CREDIT**

## Scope

Continue only the active leaf

```text
MB104-GENUS1-SPAN5-BALANCED16-000707-E2-RESIDUAL-LIFT-CONDUCTOR-PAIR-MAP.
```

Assume the already-retained candidate ambient-H1 linearization used by the half-hyperplane factorization. Specialize to

```text
l=2m,  m>=1.
```

Let

```text
pi:Y->S
```

be the residual double cover with branch half-line `L=L_abs`. The retained even-l factorization gives

```text
Lambda_(2m) ~= pi^*A,
A=O_S(7mH),
```

and effective conjugate divisors

```text
G1=tau(G2),
O_Y(G1)=O_Y(G2)=pi^*A.
```

The purpose here is to write this pencil in the standard double-cover algebra and connect its base locus directly to the conductor residual-sheet character.

## 1. Double-cover eigensection decomposition

For a double cover defined by `2L~B_abs`,

```text
pi_*O_Y ~= O_S direct_sum L^(-1).
```

Therefore every section defining `G1` decomposes uniquely as

```text
s1 = u + xi*v,
```

where

```text
u:=u in H^0(S,A),
v in H^0(S,A tensor L^(-1)),
```

and `xi` is the local anti-invariant Kummer generator. The deck involution gives

```text
s2=tau(s1)=u-xi*v,
```

with zero divisor `G2`.

On the etale open set `S\B_abs`, choose the retained rational Kummer trivialization

```text
xi^2=f_t,
f_t=(t-i)/(t+i).
```

Then the norm is

```text
N(s1)=s1*s2=u^2-f_t*v^2.                       (NORM)
```

Globally `(NORM)` is a section of `A^2=O_S(14mH)`; the displayed rational formula is interpreted in the retained rational trivialization of `L`.

## 2. The carrier is the residual factor of the norm

The exact effective divisors from the half-hyperplane leaf satisfy

```text
G1+G2
 =pi^*(C+8m sum_(j in Sigma) E_j).
```

Consequently the downstairs norm divisor is exactly

```text
div N(s1)
 = C + 8m sum_(j in Sigma) E_j.                (C-NORM)
```

Thus every even-l realization in this candidate eigensection framework writes the hypothetical carrier as the residual divisor of the ambient norm form

```text
u^2-f_t*v^2
```

after removing the exact supported-exceptional factor of multiplicity `8m`.

This is stronger than merely knowing that the normalization square class is trivial: it gives an ambient pair of eigensections whose norm cuts out the carrier plus the prescribed exceptional fixed part.

## 3. The normalization square root is the ambient ratio u/v

Away from the common zero locus of `u` and `v`, equation `(C-NORM)` gives on the carrier

```text
(u/v)^2=f_t.                                    (ROOT)
```

Hence the square root `g` appearing in the retained ambient-character/conductor formulation can be taken branchwise as the restriction of the ambient ratio

```text
g=u/v.
```

Let `p` be a singular point of the carrier away from `B_abs`, with two or more normalization branches over it. If `(u(p),v(p))!=(0,0)`, then `(ROOT)` extends through the ambient point and every normalization branch has the same limiting value of `u/v`. Therefore all such branches carry the same residual-sheet trivialization at `p`.

Thus a nontrivial conductor sign can occur only over the ambient eigensection base locus

```text
Z(u,v)={u=v=0}.                                (BASE)
```

Equivalently:

```text
opposite-sheet conductor gluing
  => ambient square root u/v is indeterminate 0/0 at the carrier singularity.
```

This is an exact support statement for the missing gluing character; it does not yet identify which base-locus points are actual conductor pairs.

## 4. Remove the forced supported-exceptional common vanishing

Use the retained even-l allocation variables

```text
b_j=x_j/2-4m,
r_j=|b_j|,
mu_j=4m-r_j,
q_j=2r_j.
```

The invariant and anti-invariant eigensections have common supported-exceptional vanishing `mu_j`. Put

```text
M=sum_j mu_j E_j,
A_mu=A-M,
B_mu=A-L-M.
```

After removing this common divisorial part, the residual base-locus intersection number is

```text
A_mu.B_mu
 =784m^2-2 sum_j mu_j^2.                       (I0)
```

Using

```text
R1=sum_j r_j,
Q=sum_j b_j^2,
sum_j mu_j^2=224m^2-8mR1+Q,
```

we obtain

```text
A_mu.B_mu
 =336m^2+16mR1-2Q.                             (I1)
```

## 5. Exact exceptional cancellation correction

Near one supported exceptional `E`, the line `L` is formally trivial. After removing the common order `mu`, the two conjugate combinations have residual orders

```text
0 and q
```

along `E`. In a local equation `e=0`, write the two residual combinations as

```text
k,
e^q h.
```

The corresponding eigensection ideal is generated by the same two functions. Since the non-exceptional residual combination has intersection

```text
(k=0).E=2mu,
```

the part of the common-zero intersection forced purely by the `q`-fold exceptional cancellation is

```text
2mu*q.                                         (LOCAL-CORR)
```

Any further common zero of `k` and `h` is residual and belongs to the lifted-component intersection rather than to this fixed exceptional correction.

For the active variables,

```text
2mu_j q_j
 =4 r_j(4m-r_j).
```

Summing over the fourteen supported nodes gives

```text
I_exc
 =16mR1-4Q.                                    (EXC)
```

## 6. The residual base locus is exactly the retained weighted cut numerically

Subtract `(EXC)` from `(I1)`:

```text
I_res
 :=A_mu.B_mu-I_exc
 =336m^2+2Q.                                    (IRES)
```

The retained A1 lift-energy identity for `l=2m` is

```text
y/2=336m^2+2Q.                                 (CUT)
```

Therefore

```text
I_res = y/2.                                    (BRIDGE)
```

So the previous square-energy conductor cut has an exact norm-form interpretation:

```text
weighted opposite-sheet cut
 = residual common-zero intersection of the two downstairs eigensections
   after removing the forced supported-exceptional cancellation contribution.
```

Wolfram arithmetic independently checks the symbolic cancellation

```text
(784m^2-2(224m^2-8mR1+Q))-(16mR1-4Q)
 =336m^2+2Q.
```

The retained proof is the exact intersection calculation above; the external computation is only a convenience check.

## Route consequence

The active missing conductor map has now been narrowed from an arbitrary branchwise sign system to an ambient base-locus problem.

For even `l`, within the candidate eigensection framework:

1. the carrier is the residual norm divisor `u^2-f_t v^2=0` after the fixed exceptional factor;
2. the normalization square root is the ambient ratio `u/v` wherever defined;
3. opposite-sheet conductor gluing is supported only where `u=v=0`;
4. after the exact exceptional correction, the intersection mass of that base locus is the retained weighted cut `y/2`.

What remains is to determine the **actual scheme/rank structure** of the residual common-zero locus for pairs `(u,v)` satisfying the adaptive cancellation conditions. A successful exact simultaneous evaluation could now attack the conductor cut directly rather than reconstructing independent local sheet labels.

## Firewalls

- All conclusions inherit the candidate status of the ambient-H1/half-hyperplane linearization.
- `(BASE)` is a necessary support condition; not every base-locus point is asserted to be a conductor identification.
- No individual conductor pair is assigned a residual sign.
- No new upper bound for `y/2` is claimed; `(BRIDGE)` reconciles two retained exact formulas.
- No existence of a nondegenerate pair `(u,v)` is proved by Riemann--Roch alone.
- `e=2`, `e=4`, `000707000f0f`, MB104, span5, finite-window, receiver, effectivity, theorem, endpoint and Perfect-Cuboid credit remain open/zero.
- No heavy compute is armed.
- No merge or rebase authorization.
