# MB104 Z5' — size-48 CM inert-7 archive bridge — 2026-09-19

Status: **PARALLEL RETAINED-RESULT BRIDGE / DANGEROUS-PACKET FRONTIER REDUCTION / NO NEW CREDIT**

## Purpose

The current compact restart had retained the three geometric balanced support orbits after Z33G,
but did not carry forward one stronger archived Z5' result:

```text
GENUS1-SPAN5-BALANCED16-SIZE48-CM-INERT7-OBSTRUCTION.md
```

This bridge restores its exact scope without changing theorem/receiver credit.

## Retained archived result

For either size-48 balanced support

```text
0000770000ff
00007b0000ff
```

assume an actual normalization-genus-one carrier realizes the MB104 dangerous equality packet

```text
r_i=M_i=8l at every supported node,
all branch contacts m=1,
r_odd=R8=M=d=112l.
```

All fourteen supported nodes have one Beauville singular stabilizer type.

For each connected product-cover component degree

```text
e in {1,2,4},
```

quotient by that singular involution produces a genus-one source (B_1) and two equal-degree
isogenies

```text
alpha_1,alpha_2:B_1 -> E_0,
deg alpha_1=deg alpha_2=14 e l,
```

to the fixed CM elliptic quotient

```text
E_0:y^2=x^3-x,
End^0(E_0)=Q(i).
```

The rational prime (7) is inert in (Q(i)).  Since (7mid 14el), equality of degrees forces
the two isogenies to share nontrivial 7-primary kernel.  Hence their pair map has generic degree
divisible by 7.

The box/Kummer quotient identifies the same generic degree as one of

```text
e or 2e,  with e in {1,2,4},
```

hence a power of two.  Contradiction.

Therefore no dangerous equality-packet carrier exists on either size-48 support orbit for any
(l>=1).

## Current-frontier consequence

Geometrically, the size-48 Picard rays are not declared empty: different exceptional branch
partitions are outside the archived theorem.

But MB104's dangerous equality-packet frontier is now exactly

```text
000707000f0f   orbit size 768.
```

The two size-48 orbits should not receive further equality-packet work.

This is precisely a successful use of the original **Z5' packet-sensitive arithmetic** direction.

## Source lock

Historical archive head:

```text
ae6ce8e2feb8233886dec1ca21b6db0ec8dbbb11
```

Archived note blob:

```text
5e1f7d4d6db10cd823532b1ac44290621ee2aa99
```

No mathematical statement is strengthened beyond that archived result.

## Firewalls

```text
size48_dangerous_packet_excluded=true
size48_geometric_uniform_ray_empty=false
dangerous_packet_remaining_orbit=000707000f0f
dangerous_packet_remaining_support_count=768
MB104_complete=false
receiver_credit=false
theorem_credit=false
endpoint_credit=false
merge_authorized=false
```
