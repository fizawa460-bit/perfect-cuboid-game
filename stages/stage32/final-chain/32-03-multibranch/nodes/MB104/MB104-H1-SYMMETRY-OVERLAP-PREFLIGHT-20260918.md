# Stage32 MB104 — H1 symmetry-amplified translate-overlap preflight — 2026-09-18

Status: **H1 SHALLOW GATE FAIL / PARKED / H2 NEXT / NO MATHEMATICAL CREDIT**

## Question tested

The Class-3 roadmap asks whether a carrier C in D_l = 7l H - 4l sum_(p in Sigma) E_p, with |Sigma|=14, can be contradicted by comparing it with a symmetry translate gC for g in Aut(S) outside the support stabilizer. This run tests only the first shallow gate: the double-coset overlap budget and the scheme-theoretic local multiplicity forced without residual-sheet signs or unnamed tangent labels.

No conductor/Picard tunnel, U12/P6 continuation, or elliptic-classification deepening is reopened here.

## Source boundary

The finite replay uses the source-locked order-1536 node action generated by the nine coordinate substitutions in Stoll's verification source. The four canonical balanced support masks and retained stabilizers are:

    0000770000ff   orbit 48    |G_Sigma|=32
    00007b0000ff   orbit 48    |G_Sigma|=32
    000707000f0f   orbit 768   |G_Sigma|=2
    00070b000f0f   orbit 768   |G_Sigma|=2

The retained Picard interface is H^2=16, H.E_i=0, E_i.E_j=0 for i!=j, and E_i^2=-2. The archived static-landing wall is also load-bearing: at every supported node the packet has 8l multiplicity-one branches with pairwise distinct nonzero exceptional landing keys, and the first tangential jet is not fixed by the retained interface. Removing finitely many forbidden landing values still leaves infinitely many choices.

Exact immutable source identities are recorded in MB104-H1-SYMMETRY-OVERLAP-CERTIFICATE.json.

## Exact cross-class budget

For t=|Sigma cap gSigma|,

    D_Sigma(l).D_gSigma(l) = (784 - 32t) l^2.

The exact order-1536 replay gives:

| support | double cosets | non-stabilizer overlap values | max outside overlap | min cross coefficient |
|---|---:|---|---:|---:|
| 0000770000ff | 11 | 0,12,13 | 13 | 368 |
| 00007b0000ff | 12 | 0,12,13 | 13 | 368 |
| 000707000f0f | 400 | 0,2,3,4,5,6,7,8,12,13 | 13 | 368 |
| 00070b000f0f | 400 | 0,2,3,4,5,6,7,8,12,13 | 13 | 368 |

Thus every g outside G_Sigma leaves D_l.gD_l >= 368 l^2. The full double-coset size/overlap distribution is machine-checked by the verifier.

## Why the packet does not supply a quadratic excess

At a common supported A1 node, the singular-model intersection has the usual quadratic A1 baseline 32 l^2 because both curves carry exceptional mass 8l. Passing to the smooth resolution subtracts exactly that baseline, which is why the strict-transform cross-class formula loses 32l^2 per common support node.

To beat the strict-transform intersection, H1 needs additional forced coincidence or tangency beyond that A1 baseline. The allowed packet data do not provide it. The retained local interface permits finite landing sets to move away from finitely many forbidden values. If a supported node is fixed by g and the induced exceptional-line action is nontrivial, a generic finite set can avoid its image. Even in the worst identity-action case, the visible unavoidable coincidence is at most 8l landing points at that node. Across all fourteen supported nodes this is at most 112l, which has zero l^2 coefficient. The archived first-jet wall supplies no source-valid higher tangency multiplicity.

Therefore the source-complete quadratic lower bound available to this H1 gate has excess coefficient 0, while every non-stabilizer translate requires at least 368. Working downstairs does not rescue the argument: the forced 32t l^2 A1 contribution is exactly the correction between the ambient 784l^2 degree intersection and the resolved (784-32t)l^2 intersection.

## Decision

H1 fails its first shallow gate:

    H1 = PARKED_AFTER_SHALLOW_FAIL
    new adapter = none
    finite degree window = not proved
    MB104 closure = not proved

This is stronger than a numerical HOLD: every relevant double coset has a fixed positive quadratic deficit under the permitted source-valid local data. Continuing H1 would require a new named tangent/jet or residual-sheet adapter, which the roadmap does not authorize us to assume.

Per the Class-3 schedule, the next stage32mb-mainbatch shallow gate is H2: prove or refute the exact F/R scheme-theoretic incidence adapter before any large-degree computation.

## Verification

Run:

    python stages/stage32/final-chain/32-03-multibranch/nodes/MB104/verify_mb104_h1_symmetry_overlap_preflight.py

Expected terminal lines:

    PASS STAGE32_MB104_H1_SYMMETRY_OVERLAP_PREFLIGHT_V1
    AutS=1536 supports=4 max_nonstabilizer_overlap=13 min_cross_coeff=368
    H1=FAIL_PARKED quadratic_excess_coeff=0 next=H2_adapter_gate no_credit

No MAIN, receiver, theorem, effectivity, endpoint, Perfect-Cuboid, or merge credit is changed.
