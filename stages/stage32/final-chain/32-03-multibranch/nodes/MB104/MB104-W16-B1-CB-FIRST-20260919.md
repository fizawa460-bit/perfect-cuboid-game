# Stage32 MB104 — W16-B1 solo: Cayley–Bacharach first — 2026-09-19

Status: **B1 NARROWED TO CONDUCTOR DESCENT ONLY / SURFACE CB STILL OPEN / NO MATHEMATICAL CREDIT**

## Scope

This checkpoint advances W16-B1 only.

The retained A2 object is

`Z_red=(nu(R_phi))_red subset C subset S`,

with `length(Z_red)<=112l`, nonempty and zero-dimensional lci on the smooth cuboid resolution.

The task is to decide `CB(|D_l|)` directly.

## 1. Surface restriction is surjective

Stoll–Testa prove for the cuboid resolution

`q(S)=h^1(O_S)=0`.

For a hypothetical carrier `C in |D_l|`,

`0 -> O_S -> O_S(D_l) -> O_C(D_l) -> 0`

therefore gives a surjection

`H^0(S,O_S(D_l)) -> H^0(C,O_C(D_l))`.

Hence the Cayley–Bacharach question on the surface is exactly the evaluation question for the conductor-descended subspace

`H^0(C,O_C(D_l))`

inside the normalization linear system.

External source lock: Stoll–Testa, *The surface parametrizing cuboids*, Section 2: `p_g(S)=7`, `chi(O_S)=8`, `q(S)=0`.

## 2. Exact factor line bundles on the elliptic normalization

For the two retained full-`G` quotient maps write

`phi_i:E->P1`, `deg(phi_i)=56l`,

`M_i=phi_i^*O(1)`.

The full `G` quotient `C8->P1` has six branch values, all with inertia order two.

Let `B_6` be their reduced divisor on the base. The retained Hurwitz passport says:

- every ramification point of `phi_i` is simple and lies above `B_6`;
- the `112l` supported normalization branches are exactly the unramified points above `B_6`.

Therefore, as divisors on `E`,

`phi_i^*B_6 = U + 2R_i`,

where `U` is the reduced divisor of all supported normalization branches and `R_i` is the reduced ramification divisor.

Since `O(B_6)=O(6)` and Riemann–Hurwitz on the elliptic curve gives

`O_E(R_i)=M_i^2`,

we obtain

`O_E(U)=M_i^6 tensor O_E(-2R_i)=M_i^2`.

Thus

`M_1^2 ~= M_2^2`

and

`delta:=M_1 tensor M_2^(-1) in Pic^0(E)[2]`.

This two-torsion conclusion now holds directly from the common six-branch passport; it is not restricted to the previous e=2 factor-line leaf.

## 3. Canonical hyperplane restriction

Freitag–Salvati Manni identify the box degree-one coordinate ring with the diagonal invariant ring generated by products of one-factor theta coordinates. Their five one-factor theta coordinates give the canonical projective system on `C8`: multiplying them by the common theta factor produces five independent holomorphic differentials, and `g(C8)=5`.

Equivalently, the box hyperplane bundle pulls back to

`pr_1^*K_C8 tensor pr_2^*K_C8`

on `C8 x C8`.

For the full `G` quotient `pi_G:C8->P1`, the six order-two branch values give by Riemann–Hurwitz

`K_C8 ~= pi_G^*O(1)`.

Descending the two factor compositions to the normalization therefore yields

`nu^*O_S(H) ~= M_1 tensor M_2`.

Source locks:

- Freitag–Salvati Manni, Theorem 2.4: degree-one box coordinates are invariant products of the five one-factor theta coordinates;
- their five independent holomorphic differentials on `C8`;
- retained six-branch quotient geometry.

## 4. Exact line bundle of the active ray on E

On the normalization,

`nu^*O_C(D_l)`
` ~= (nu^*H)^(7l) tensor O_E(-4l U)`.

Using

`nu^*H=M_1 M_2`,
`O(U)=M_i^2`,
`delta=M_1 M_2^(-1)`, `delta^2=O`,

gives, for either factor `i`,

`nu^*O_C(D_l) ~= M_i^(6l) tensor delta^l`.

Since `O(R_i)=M_i^2`, equivalently

`nu^*O_C(D_l) ~= O_E(3l R_i) tensor delta^l`.   (L-R)

The degree is

`336l^2`,

as required.

This explains the previously noticed numerical identity

`336l^2=(56l)(6l)`

and makes it an exact line-bundle statement up to the single explicit two-torsion class `delta^l`.

## 5. The ramification divisor is NOT CB on the normalization

Fix `p in Supp(R_i)`. The retained passport makes `R_i` reduced of degree `112l`.

Put

`L_E=nu^*O_C(D_l)`.

Then

`deg L_E(-R_i+p)=336l^2-112l+1 >0`,

`deg L_E(-R_i)=336l^2-112l >0`.

On an elliptic curve every positive-degree line bundle has

`h^1=0`, `h^0=degree`.

Therefore

`h^0(L_E(-R_i+p)) - h^0(L_E(-R_i)) = 1`.

So for every ramification point `p` there exists a normalization section which vanishes at every point of `R_i\{p}` but not at `p`.

Hence

`R_i is not Cayley–Bacharach for the full normalization linear system`

for any `l>=1`.

The conclusion is independent of the two-torsion twist `delta^l`.

## 6. Exact remaining B1 gate

This eliminates the hoped-for possibility that the ramification divisor itself forces CB by elliptic/factor geometry.

If the surface scheme `Z_red` is CB for `|D_l|`, the reason must now be entirely singular:

`normalization one-point-separating sections`
` -> killed by conductor/gluing descent`
` -> only the subspace H^0(C,O_C(D_l)) survives`.

Collisions under `nu` may add further identifications, but neither the ramification divisor nor the factor line bundles themselves impose the required evaluation dependency.

Thus B1 has been reduced to one precise map:

`H^0(E,nu^*O_C(D_l)) -> conductor/gluing quotient`,

restricted to the subspaces `H^0(L_E(-R_i+p))`.

To prove surface CB one must show that, for every image point, every normalization section that separates it fails the conductor descent conditions.

To disprove surface CB it is enough to produce one separating normalization section lying in the descent kernel.

That conductor evaluation map is not yet materialized in the retained chain.

## B1 disposition

`W16-B1 = NARROWED_TO_CONDUCTOR_DESCENT_ONLY_OPEN`.

W16 remains open. The next solo branch is `W16-H`: use Hilbert/Quot/conductor module machinery specifically to materialize the missing descent map, rather than merely repackaging `nu_*O_Rphi` as in A1.

## Firewalls

- No claim that `Z_red` is or is not CB on the surface.
- No claim that `delta` is trivial.
- No locally-free Serre extension is proved.
- No `l>=2` exclusion.
- No MB104/finite-window/receiver/effectivity/theorem/endpoint/Perfect-Cuboid credit.
- No merge authorization.
