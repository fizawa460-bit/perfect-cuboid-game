# Stage32 MB104 — Beauville node-type quotient on the current balanced16 survivors

Status: **RETAINED CURRENT-FRONTIER GLOBAL REDUCTION / 000707 EXCLUDES e=1 / SIZE48 ONE-TYPE GEOMETRY / NO CLOSURE / MB104 INCOMPLETE / NO CREDIT**

## Scope correction

The earlier `(5,5,4)` stabilizer-type distribution belongs only to the original explicit formal F1-P5 support `S_P5_14`.  That explicit support is not the current balanced16 frontier.  It was already excluded earlier by negative `c=0` conic intersections.

This leaf recomputes the Beauville singular-stabilizer types for the **actual three surviving balanced support orbits**:

```text
0000770000ff   size 48,
00007b0000ff   size 48,
000707000f0f   size 768.
```

The general equality-rigidity theorem remains applicable to a hypothetical actual realization of the uniform ray because it uses only

```text
g=1,
d=r_odd=112l.
```

## Node-type convention

The modular source adapter identifies the three box-node stabilizer types with the unique zero among

```text
b1=Z1,
b2=Z2,
b3=Z3.
```

The verifier reconstructs the retained 48-node model in its canonical index order and decodes the support masks directly.

## Exact current-survivor counts

The results are

```text
mask             b1=0  b2=0  b3=0   distinct types
0000770000ff       14     0     0          1
00007b0000ff       14     0     0          1
000707000f0f        7     7     0          2
```

For comparison, the already-closed fourth balanced orbit has

```text
00070b000f0f        7     7     0          2.
```

Thus the current size-48 survivors are **one-type** supports, while the current size-768 survivor is a **two-type** support.

## Component-stabilizer consequence on the product cover

Let

```text
G ~= (Z/2)^3,
G0 ~= (Z/2)^2,
P=C8 x C8 -> X=P/G0
```

be the retained modular/product-cover groups.  For a hypothetical actual uniform-ray carrier, let `Y` be the connected Beauville pullback and `Z` a connected component of the normalized pullback to `P`.  Put

```text
e=deg(Z->Y) in {1,2,4}.
```

By equality rigidity, both projections `Z->C8` are etale of degree

```text
14 e l.
```

At an odd branch point of a supported box-node type `s in G\G0`, the corresponding singular stabilizer fixes the whole four-point `P->X` fiber.  Every connected component over `Y` meets that fiber, so `s` stabilizes every component.

Therefore the component stabilizer contains the subgroup generated by all node-stabilizer types occurring in the support.

### One-type supports

For either size-48 mask, only one outside involution `s` occurs.  Its generated subgroup has trivial intersection with `G0`.  Hence this alone gives no improvement over

```text
e in {1,2,4}.
```

The exact equality correspondence bidegrees remain

```text
(14l,14l), (28l,28l), or (56l,56l).
```

### Two-type support `000707000f0f`

Let the two distinct outside involutions be `s1,s2`.  Their product

```text
h=s1*s2
```

is a nontrivial element of the index-two subgroup `G0`.  Since both `s1` and `s2` stabilize every component, so does `h`.

Thus the component stabilizer inside `G0` has order at least `2`, and consequently

```text
e in {2,4}.
```

In particular the `e=1` equality realization is impossible for `000707000f0f`.

The only remaining product-cover bidegrees are

```text
(28l,28l)  if e=2,
(56l,56l)  if e=4.
```

## Consequence

This is current-frontier progress, unlike the historical `(5,5,4)` explicit-support reduction:

- size-48 survivors: one Beauville node type; equality correspondence still allows `e=1,2,4`;
- `000707000f0f`: two Beauville node types; `e=1` is excluded and only `e=2,4` remain.

The next useful leaf is to quotient the `000707` correspondence by its component stabilizer separately in the `e=2` and `e=4` cases.  Both cases descend to genus-one covers of `P^1` with finitely many involutory branch values, so Riemann--Hurwitz can turn the `112l` supported branches into exact finite passport data.

## Firewalls

- No statement from the historical `(5,5,4)` explicit support is transferred to the current survivors without this recomputation.
- `e=1` is excluded only for `000707000f0f`, not for the two size-48 orbits.
- No actual etale correspondence is constructed.
- No current surviving support orbit is closed by this leaf.
- Whole span5, unequal Picard coefficients, P6 sectors, MB104, receiver, theorem, endpoint, and Perfect-Cuboid credit remain open/zero.
- No merge authorization.
