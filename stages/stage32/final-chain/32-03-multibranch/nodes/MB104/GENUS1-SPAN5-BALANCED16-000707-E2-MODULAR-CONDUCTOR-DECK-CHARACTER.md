# Stage32 MB104 — `000707000f0f` e=2 modular conductor deck character

Status: **RETAINED EXACT CHARACTER REDUCTION / CONDUCTOR TRANSITION ELEMENT STILL MISSING / e=2 OPEN / NO CREDIT**

## Scope

Continue the active MB104 residual-lift conductor-pair leaf without assigning any conductor sign.

The retained ambient Kummer chain already gives a fixed degree-two character

```text
alpha_abs : pi_1(U) -> Z/2,
```

and in the `e=2` case the normalization pullback is split.  The missing datum is the relative deck transition at normalization preimages identified by the conductor.

This note makes the residual deck bit explicit in the modular presentation of the Beauville cover.

## 1. Published modular deck character

Freitag--Salvati Manni, *Parametrization of the box variety by theta functions*, Section 4, define an index-two subgroup

```text
Gamma'[4] < Gamma[4]
```

containing `Gamma[8]`, generated by `Gamma[8]`, `TT'`, and `TR`.  They give the equivalent congruence description

```text
M = [[a,b],[c,d]] in Gamma[4]
M in Gamma'[4]  <=>  a+b+c == 1 (mod 8).       (FSM-CHAR)
```

They also prove

```text
H := Gamma'[4]/Gamma[8] ~= (Z/2)^2
```

acts freely on `C8=H*/Gamma[8]`, and the inclusion `Gamma'[4] < Gamma[4]` gives the Beauville degree-two cover of the box variety.

With

```text
G := Gamma[4]/Gamma[8] ~= (Z/2)^3,
```

define the quotient character

```text
chi_B : G -> G/H ~= Z/2.
```

Then for a representative `M in Gamma[4]`,

```text
chi_B([M]) = 0  <=>  M in Gamma'[4]
                  <=>  a+b+c == 1 (mod 8).     (DECK)
```

The congruence is unchanged after multiplying the representative by an element of `Gamma[8]`, so `(DECK)` is a well-defined test on `G`.

Existing Stage32 source locks `BEAUVILLE-PRODUCT-COVER-SOURCE-NOTE.md` and `FREITAG-SALVATI-MANNI-RESIDUAL-KUMMER-COORDINATE-SOURCE-NOTE.md` already retain the cover and theta geometry; the new point here is the explicit index-two membership test from Section 4.

## 2. Conductor descent interpretation

Ferrand, *Conducteur, descente et pincement*, Bull. Soc. Math. France 131 (2003), Sections 2 and 5, supplies the standard conductor/fibre-product descent framework: a singular object obtained by pinching is reconstructed from normalization-side data together with compatible descent data over the conductor.  For invertible modules the load-bearing extra datum is the gluing isomorphism along that conductor square.

This is used only as a descent framework.  Ferrand does **not** identify the Stage32 gluing isomorphism or choose a Beauville sheet.

In the retained `e=2` case, fix the split normalization pullback of the residual double cover.  Let two normalization preimages `u,v` be identified by the singular carrier conductor.  If a source-locked level-eight modular transition

```text
[M_(u,v)] in G = Gamma[4]/Gamma[8]
```

between compatible lifts of `u` and `v` is materialized, then the relative conductor bit is

```text
epsilon(u)+epsilon(v) = chi_B([M_(u,v)]).       (PAIR)
```

Thus the former unspecified same-sheet/opposite-sheet decision becomes the explicit finite-group test `(DECK)`.

Changing the global split section on the normalization flips all individual sheet labels simultaneously but leaves the pairwise value `(PAIR)` unchanged.

## 3. Weighted-cut form

For conductor branches `beta_i,beta_j` over a singular point `p`, write `M_(p;i,j)` for the corresponding source-locked modular transition when it exists.  The retained cross-sheet identity would then read

```text
y/2
 = sum_p sum_(i<j) I_p(beta_i,beta_j) * chi_B([M_(p;i,j)]).
```

The retained Hodge obstruction requires

```text
sum_p sum_(i<j) I_p(beta_i,beta_j) * chi_B([M_(p;i,j)])
 >= 84*l^2.
```

No value of this sum is asserted here because the transition elements are not yet retained.

## 4. Arsenal check

The closest current Arsenal routing card is provisional `S32-PW12`, `SOURCE_BOUND_ORBIT_CHARACTER_WITH_ABSOLUTE_MARKING_FIREWALL`.  Its contract permits extraction of a source-bound abstract orbit character while explicitly refusing promotion to an absolute retained marking.  It therefore does not supply `M_(u,v)` or a conductor sheet assignment for MB104.

No Arsenal credit or authority is imported from that card.

## 5. Exact remaining input

The active missing object is now sharpened to either of the equivalent forms

```text
conductor pair (u,v)
  -> [M_(u,v)] in Gamma[4]/Gamma[8]
  -> chi_B([M_(u,v)]) in Z/2,
```

or a directly source-locked evaluation of the same relative deck character.

It is enough to materialize, for every load-bearing conductor pair, one compatible modular transition representative `M_(u,v)`; `(FSM-CHAR)` then evaluates the residual sheet bit by a finite congruence test.

The current retained node type, local A1 equation, `(1,1)` contact, multiplicity, and unordered product-lift pair do not determine `M_(u,v)`.

## Sources

- E. Freitag and R. Salvati Manni, *Parametrization of the box variety by theta functions*, Michigan Math. J. 65 (2016), 675--691; arXiv:1303.6495, especially Section 4.
- D. Ferrand, *Conducteur, descente et pincement*, Bull. Soc. Math. France 131 (2003), 553--585, especially Sections 2 and 5, DOI 10.24033/bsmf.2455.
- `STACKS-NORMALIZATION-PICARD-GLUING-SOURCE-NOTE.md`.
- `STACKS-KUMMER-AMBIENT-ETALE-MONODROMY-SOURCE-NOTE.md`.

## Firewalls

- no conductor transition `M_(u,v)` is claimed to be known;
- no conductor sign is assigned;
- no weighted-cut upper bound is claimed;
- no `e=2` or `e=4` closure is claimed;
- `000707000f0f` remains open;
- MB104/span5/finite-window/receiver/theorem/endpoint/Perfect-Cuboid credit remains zero;
- no heavy compute is armed;
- no merge authorization.
