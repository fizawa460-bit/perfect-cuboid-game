# Stage32 MB104 — wide shallow closure scan Round A — 2026-09-18

Status: **ROUND A COMPLETE / 5 ROUTES SCREENED / W4 ONLY SURVIVES TO SECOND SCAN / NO MATHEMATICAL CREDIT**

## Method

This run follows the wide-scan override. Five materially different routes receive one cheap kill-test each. No missing adapter is developed.

Target:

```
Sigma = 000707000f0f,
D_l=7lH-4l sum_(p in Sigma)E_p,
|Sigma|=14,
D_l^2=336l^2,
K.D_l=112l,
Delta=168l^2+56l.
```

## W1 — Bogomolov--Reider superabundance trap: DROP

The standard Reider/Serre construction attached to a full zero-dimensional genus-defect cluster would have

```
c1=D_l-K,
c2=Delta.
```

Here

```
(D_l-K)^2
 =336l^2-224l+16,
4Delta
 =672l^2+224l,
```

so

```
c1^2-4c2
 =-336l^2-448l+16 < 0
```

for every `l>=1`.

The classical Bogomolov--Reider obstruction architecture needs the opposite discriminant sign to force an unstable rank-two bundle and hence a small destabilizing divisor. Thus the full source-complete defect package is too large, not too small.

A new canonical subcluster with much smaller `c2` could define a different route, but wide-scan mode does not build that missing object.

**Disposition: DROP.**

## W2 — symbolic-power / Waldschmidt effectivity: DROP

This route dies more decisively: the exact class is already effective for every `l>=1`.

Riemann--Roch gives

```
chi(O(D_l))
 =8+(D_l^2-K.D_l)/2
 =168l^2-56l+8 >0.
```

Also

```
H.(K-D_l)=16-112l<0.
```

Since `H` is nef, `K-D_l` is not effective; hence `h^2(O(D_l))=0`. Therefore

```
h^0(O(D_l))
 = chi(O(D_l))+h^1(O(D_l))
 >0.
```

This calculation depends only on the fourteen orthogonal A1 exceptional classes, not on their arrangement. An effectivity-only fat-point/Waldschmidt obstruction cannot close the active ray.

**Disposition: DROP.**

## W3 — Aut(S)-norm / invariant-section obstruction: DROP

The active support orbit has size `768`. Aut(S) is transitive on the 48 nodes, so double counting gives

```
768*14/48=224
```

translated supports through every node.

The norm of a hypothetical section therefore has divisor class

```
5376l H -896l sum_(all 48 nodes)E
 =896l A,
A=6H-sum E.
```

The base invariant class is numerically large:

```
A^2=480,
K.A=96,
chi(O(A))=200.
```

It also pairs positively with every retained low-degree test family:

```
6-node conic:             A.Q=6,
8-node elliptic quartic:  A.Q=16,
rank-3 genus-5 fiber:     A.F=40.
```

Thus symmetry amplification does not create an obvious invariant-section vanishing overload; it moves the problem to a big positive class. A much finer representation-theoretic statement about the full invariant ring would be a different theorem.

**Disposition: DROP.**

## W4 — coordinate K3 quotient pushdown: PASS TO SECOND SCAN

Stoll--Testa's coordinate-sign quotients provide K3 surfaces and the exact push-pull identity

```
pi^*pi_*D
 =D+sigma(D)+sum_(E in E_pi)(D.E)E.
```

For the active support let

```
t=|Sigma intersect sigma(Sigma)|,
f=# supported nodes fixed/contracted by sigma.
```

The correction cancels the exceptional coefficient at every fixed supported node. A direct intersection calculation gives

```
(pi_*D_l)^2
 =(1120-32t+64f)l^2,
deg(pi_*D_l)=112l.
```

Replaying the seven coordinate-sign involutions on the exact 48-node model gives:

| quotient | t | f | square / l^2 |
|---|---:|---:|---:|
| a1 | 7 | 7 | 1344 |
| a2 | 7 | 7 | 1344 |
| a3 | 8 | 8 | 1376 |
| b1 | 13 | 7 | 1152 |
| b2 | 13 | 7 | 1152 |
| b3 | 12 | 0 | 736 |
| c | 6 | 6 | 1312 |

The degree `112l` satisfies the K3 even-degree parity condition. All squares are positive and below the Hodge-index ceiling

```
(112l)^2/8=1568l^2.
```

So the first numerical test does **not** close the route.

However, W4 survives because the quotient comes with something the previous parked routes did not: a fixed rank-20 K3 Picard lattice generated by explicit curves and 15 elliptic fibrations. One more shallow test can check the seven displayed rays against that fixed curve/fibration inventory without recovering any normalization branch lift.

**Disposition: PASS-TO-SECOND-SCAN.**

## W6 — modular-form vanishing / valence: DROP

Freitag--Salvati Manni identify every box coordinate with an invariant product of weight-`1/2` theta generators on `X(8)xX(8)`, so a modular zero-count route is structurally legitimate.

But on the active support the two zero-pairing elliptic quartics are already Satake-boundary components. Each contains seven supported nodes, and

```
D_l.Q
 =7l*(H.Q)-4l*7
 =28l-28l
 =0.
```

The retained product-cover saturation calculation sharpens this: for each zero quartic, the two relevant factor-quotient fibers are completely reduced and completely occupied by the supported packet:

```
e=2: degree 28l on each saturated fiber,
e=4: degree 56l on each saturated fiber.
```

Thus the natural factorwise valence budget is met at exact equality, not exceeded. A successful modular continuation would need a genuinely coupled relation between the two factor maps, not a pure weight-versus-zero-count inequality.

**Disposition: DROP.**

## Round A result

```
W1  DROP
W2  DROP
W3  DROP
W4  PASS-TO-SECOND-SCAN
W6  DROP
```

No route becomes DEEP.

Next invocation:

1. broadly scan Round B: W5 higher symmetric differentials, W8 simultaneous sign-quotient genus budget, W10 ambient fat-point Hilbert function, W11 elliptic-normalization linear-series;
2. in parallel give W4 exactly one second shallow K3 lattice/fibration kill-test;
3. do not build adapters for any dropped route.

No MB104, receiver, theorem, endpoint, Perfect-Cuboid, or merge credit changes.
