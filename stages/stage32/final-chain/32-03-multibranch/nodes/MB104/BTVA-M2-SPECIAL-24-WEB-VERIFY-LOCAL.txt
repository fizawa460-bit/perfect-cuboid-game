Stage32 MB104 special-24 m=2 web local symbolic replay

Status: PASS (independent algebra replay in the operator environment; NOT GitHub Actions / exact-head hostile-audit credit)

Source formulas:
- BTVA Table 1 / ancillary perfectcuboid_script.m
- representative section omega*=omega1-i*omega2-omega3+i*omega4+i*omega5+omega6

Affine chart x1=1:
  c=x2, v=x3,
  y1^2=c^2+v^2,
  y2^2=v^2+1,
  y3^2=c^2+1,
  z^2=c^2+v^2+1.

Stereographic substitution:
  u=c/(z+1), w=v/(z+1),
  c=2u/(1-u^2-w^2),
  v=2w/(1-u^2-w^2),
  z=(1+u^2+w^2)/(1-u^2-w^2),
  t=u+i*w.

Exact transformed quadratic coefficients were replayed as
  S_uu = (2-2i)/((u+i*w)(u+i*w-1)(u+i*w+i)),
  S_uw / S_uu = 2i,
  S_ww / S_uu = -1.

Hence
  omega* = S_uu * (du+i*dw)^2
         = (2-2i)/(t(t-1)(t+i)) * (dt)^2.

Independent checks:
- quadratic discriminant S_uw^2 - 4*S_uu*S_ww = 0;
- scalar numerator is the nonzero constant 2-2i;
- t=(x2+i*x3)/(z+x1);
- t=lambda gives the hyperplane x2+i*x3=lambda*(z+x1);
- hyperplane class H=K_S, H^2=16, so every nonexceptional irreducible fiber component has canonical degree <=16.

Credit firewall:
- this replay certifies only the special 24-support residual algebra;
- it is not a population-wide finite-window proof;
- m=2 extension-kernel zero is not curve nonexistence;
- receiver/theorem/endpoint credit remains zero;
- merge unauthorized.
