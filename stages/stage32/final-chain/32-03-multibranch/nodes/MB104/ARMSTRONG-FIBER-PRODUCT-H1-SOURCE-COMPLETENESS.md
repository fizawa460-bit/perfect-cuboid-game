# Stage32 MB104 — Armstrong fiber-product fixed-relation completeness

Status: **RETAINED GROUP-THEORETIC COMPLETENESS LEMMA / AMBIENT H1 ROUTE ONLY / NO CONDUCTOR SIGN / NO CREDIT**

For the fiber product `F=Delta x_H Delta`, every fixed-point element has the form
`(g x_i g^-1, h y_j h^-1)`, with the two cone generators having the same nonzero inertia label.

Choose a second-factor lift `u` with `phi(u)=phi(g)`. Conjugation by `(g,u) in F` reduces this to
`(x_i,u^-1 h y_j h^-1 u)`. Write `u^-1h=k t_delta`, where
`k in ker(phi)` and `delta in H`. Conjugation by `(1,k)` then reduces it to
`(x_i,t_delta y_j t_delta^-1)`.

Conversely every such element lies in `F` and fixes a product point. Therefore the normal closure of all fixed-point elements is generated by the representatives indexed by two common inertia types, four cone generators in each factor, and four relative H-cosets: `2*4*4*4=128`.

Thus the existing (459 x 64) relation matrix uses the complete Armstrong fixed-point normal closure, not a sample. Its unimodular minor consequently proves only the retained ambient `H1` statement. It does not evaluate a conductor pair or grant any Stage32 credit.
