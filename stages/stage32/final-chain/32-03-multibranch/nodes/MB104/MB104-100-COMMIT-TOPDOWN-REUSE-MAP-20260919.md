# MB104 — 100-commit top-down reuse map — 2026-09-19

Status: **PRE-AUDIT PORTFOLIO SYNTHESIS / CROSS-ROUTE REUSE / NO CREDIT**

## Scope

This note re-reads the latest 100 commits on the active MB branch with one constraint:

```
do not duplicate the active 32-01 search itself.
```

In particular, MB does not reopen the degree-10 / degree-12 fibration-shell search that 32-01 is
already pursuing.  Fibration results are imported only as exact cuboid-surface measuring devices.

The governing MB objective remains:

```
finite l-window or direct elimination of the dangerous equality frontier.
```

## 1. Current dangerous frontier

The archived CM inert-7 obstruction already excludes the dangerous equality packet on both size-48
support orbits for every l>=1.

Hence the dangerous equality frontier is

```
Sigma = 000707000f0f
orbit size = 768.
```

Recent size-48 canonical-cover / tangent-cone work is therefore auxiliary only and remains parked.
The hostile cleanup also leaves parts of that late analytic chain provisional, so it should not
drive MB routing.

## 2. Strong exact MB inputs recovered from the 100-commit review

### Equality packet

```
C in |lP|,
M=R=O=d=112l,
all supported exceptional contacts m=1.
```

### Support hyperplane

```
div_E(nu^*h)=B_node
```

with B_node reduced of degree 112l.

### Conductor / different

```
length(Z_cond)=delta(C)=168l^2+56l,
deg Delta=336l^2+112l,
Supp(Z_cond) lies in the smooth interior.
```

The conductor scheme uniquely recovers the carrier inside |lP| through the retained adjoint
adapter.

### Null geometry / contraction

The complete balanced P-null locus is known and finite formal Picard gluing is exhausted on
000707.

The contraction package gives

```
O_S(P)=phi^* O_Y(3K_Y),
3K_Y ample Cartier.
```

### Six fibration measuring system imported from 32-01

The six explicit genus-five fibrations have true fiber intersections on 000707

```
P.G_j = (40,40,56,44,44,56).
```

Their combined differential is injective at every smooth non-node point of the original cuboid
surface.

For the elliptic normalization E and g_j=f_j o nu,

```
A := gcd(R_0,...,R_5)
```

has coefficient m_q-1 on each normalization branch, and

```
deg A <= 80l.
```

This is retained as a measuring constraint.  MB will not search for better fibration classes while
32-01 is doing that work.

## 3. What the 100 commits say NOT to repeat

The following are already exhausted, superseded, or poor primary routes on the actual dangerous
frontier:

```
ordinary/finitely-thickened Picard gluing,
numerical Picard membership of P itself,
single-fibration polar degree comparison,
search among the known 28 fibrations for smaller P.G,
current common-G/Kummer/Abel recombinations,
standard Serre/Reider/Bogomolov from conductor length alone,
size48 non-lc local analytic classification as the main finite-window route.
```

The 28-fibration audit is useful as a closure statement, not as a new search program.

## 4. Composition candidate A — conductor x six-polar residual ideal

This is the strongest reuse candidate after the 100-commit review.

Local Piene-type conductor/Jacobian theory predicts, for each fibration direction,

```
div_E(polar_j)=Delta+R_j.
```

The degree identity is already exact:

```
deg Delta + deg R_j
 = C.(C+K_S+2G_j).
```

The six differential-spanning result gives

```
gcd_j(R_j)=A,
deg A<=80l.
```

Therefore the six polar pullbacks have a common divisor of the form

```
Delta + A
```

where

```
deg Delta = Theta(l^2),
deg A = O(l).
```

The useful object is not another ramification bound.  It is the **surface-side ideal generated by
the six polar sections after the conductor factor is accounted for**.

A source-valid globalization could turn this into a finite-rank degeneracy/base-locus problem:

```
six explicit cuboid differential directions
 -> six polar sections
 -> common conductor scheme Z_cond
 -> residual Jacobian/ramification scheme of only linear complexity.
```

Because the six maps satisfy the fixed cuboid quadratic relations, this route may have much
stronger syzygies than a generic collection of six projections.

### Missing adapter

The load-bearing missing statement is:

```
globalize the local Piene conductor/Jacobian identity on the smooth cuboid surface
and identify the surface ideal / Fitting ideal generated by the six relative polars.
```

Only after this adapter exists should one compute Chern/base-locus bounds.

### Why this is preferred

It combines three independent exact assets:

```
quadratic conductor scheme,
six-direction differential spanning,
linear common ramification defect.
```

No single one closes MB, but their scales are genuinely different.

Disposition:

```
PRIMARY SHALLOW COMPOSITION TARGET.
Do not yet deepen to long syzygy calculations.
```

## 5. Composition candidate B — Severi/equigeneric x six-fibration defect

There is an attractive numerical gap:

```
delta(C)=168l^2+56l,
RR leading part for |lP|=168l^2-56l,
gap=112l.
```

However Dedieu--Sernesi deformation theory shows that equigeneric families on surfaces with positive
canonical class can be obstructed/superabundant, and the normalization normal sheaf can have
torsion supported on the ramification divisor.

For our genus-one normalization,

```
Nbar_nu ~= nu^*omega_S^{-1}(-Z)
```

has negative degree because K_S.C=112l>0.

Thus a small or zero equigeneric tangent space does not imply nonexistence of an isolated carrier.

The imported 80l ramification-defect bound therefore does not by itself convert the 112l RR gap
into a contradiction.

Disposition:

```
KEEP AS SECONDARY THEOREM-HUNT ONLY.
Do not promote the 112l-vs-80l comparison by itself.
```

## 6. Composition candidate C — six-projection branch semigroup / jet collision

The exact six-map system gives, at every branch, six projection orders constrained by the four
cuboid quadrics.

Generic two-coordinate semigroup estimates are too weak: a plane branch can have fixed small
multiplicity but arbitrarily large conductor through high characteristic exponents.

A viable local theorem would have to use the **simultaneous six orders plus the cuboid quadratic
relations** to control either

```
intrinsic branch conductor,
or
pairwise smooth-branch contact.
```

Global ramification budgets are linear in l, so any local inequality summing conductor/contact
against those six orders with a uniform constant would immediately attack the quadratic Delta.

Disposition:

```
SECOND PRIMARY SHALLOW TARGET.
Search for a finite local-algebra statement before any global construction.
```

## 7. Packet-specific e=4 assets

For the e=4 000707 packet there is additional exact structure:

```
two degree-56l factor maps,
two saturated seven-node fibers,
nu^*O_S(H) ~= M^2,
support hyperplane section factors as the two saturated fibers,
O_E(Delta) ~= M^(6l+2).
```

This is strong, but the current common-G/Kummer/Abel architecture has already been audited as
non-closing.

Use these facts only if a new conductor/polar or semigroup adapter naturally consumes the
distinguished factorization.  Do not restart the old Kummer/Abel route on its own.

## 8. 32-01 interaction rule

The active 32-01 work may continue to classify or exclude improved fibration classes.

MB should consume such results only as updates to the numerical measuring layer, e.g.

```
min P.F,
available differential directions,
fiber support patterns.
```

MB should NOT independently redo:

```
degree-10 residual enumeration,
degree-12 primitive nef isotropic enumeration,
full fibration classification.
```

## 9. Updated top-down priority

After the 100-commit review:

```
A. six-polar conductor/Fitting globalization       HIGH
B. six-order local semigroup/jet inequality        HIGH
C. Severi/equigeneric superabundance theorem hunt  MEDIUM/SHALLOW
D. Z4' generic conductor regularity                MEDIUM, only with new theorem
E. Z12 general-m primitive Hilbert                  HIGH UPSIDE / HIGH BUILD COST
PARK size48 local analytic chain and exhausted Picard/Kummer recombinations.
```

The next MB cycle should compare A and B at shallow depth before choosing either for deeper work.

## Firewalls

```
reviewed_commit_window=100
duplicate_32_01_fibration_search=false
size48_local_route_primary=false
dangerous_frontier_000707_only=true
six_fibration_data_used_as_measurement_layer=true
finite_degree_window_proved=false
dangerous_000707_excluded=false
MB104_complete=false
receiver_credit=false
theorem_credit=false
endpoint_credit=false
merge_authorized=false
```
