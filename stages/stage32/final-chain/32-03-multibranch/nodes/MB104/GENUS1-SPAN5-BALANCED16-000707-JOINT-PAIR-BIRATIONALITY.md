# Stage32 MB104 — `000707000f0f` joint pair birationality

Status: **RETAINED CURRENT-FRONTIER GLOBAL REDUCTION / JOINT FACTOR-QUOTIENT MAP BIRATIONAL FOR e=2,4 / NO CLOSURE / MB104 INCOMPLETE / NO CREDIT**

## Scope

Assume conditionally that an integral normalization-genus-one carrier realizes the retained dangerous minimal-branch equality packet on

```text
Sigma = 000707000f0f,
node-type counts = (7,7,0),
d=r_odd=112l.
```

The retained equality/Hurwitz leaves leave two product-cover cases only:

```text
e=2  or  e=4.
```

Let the two occurring singular deck involutions be `s1,s2` and put

```text
H=<s1,s2> ~= (Z/2)^2.
```

For `e=2`, the component stabilizer is `K=H`; for `e=4`, `K=G~=(Z/2)^3` and `H` is its order-four subgroup generated by the two used singular types.

Let `Z subset C8 x C8` be the connected product-cover component and set

```text
Q=Z/H,
R=C8/H.
```

The retained Hurwitz calculation gives

```text
g(Q)=1,
R ~= P1.
```

The two etale product projections descend to

```text
psi1,psi2: Q -> R
```

of common degree

```text
e=2: 28l,
e=4: 56l.
```

Write

```text
Psi=(psi1,psi2): Q -> R x R.
```

## Relative deck symmetry and the generic degree

The diagonal subgroup `H` acts trivially after passing from `Z` to `Q` and from each factor `C8` to `R`.

Suppose two generic points of `Q` have the same image under `Psi`. Choose lifts `z,z' in Z`. Equality of the two factor-quotient coordinates means that for some `h1,h2 in H`,

```text
z'=(h1,h2) z
```

inside `C8 x C8`.

Modulo the diagonal `H` action this is equivalent to the relative transformation

```text
(1,r),  r=h1^{-1}h2 in H.
```

Because `Z` is irreducible, if `(1,r)` sends one generic open subset of `Z` back to `Z`, then

```text
(1,r)Z=Z.
```

Define

```text
A={r in H : (1,r)Z=Z}.
```

Then `A` is a subgroup of `H`, acts generically freely on `Q`, and the generic degree of `Psi` onto its image is exactly

```text
m=|A|.
```

Thus the only possible source of non-birationality is a nontrivial relative deck symmetry.

## Relative deck symmetries descend to type-preserving box automorphisms

The full modular deck group `G` is elementary abelian, so `(1,r)` commutes with every diagonal element of `G`. Therefore `(1,r)` descends through

```text
C8 x C8 -> B=(C8 x C8)/diag(G)
```

to an automorphism `beta_r` of the box surface. For `r!=1` this descended automorphism is nontrivial: `(1,r)` is not a diagonal deck transformation.

Because `(1,r)` stabilizes `Z`, `beta_r` stabilizes the downstairs carrier and hence its 14-node support `Sigma`.

Moreover `beta_r` preserves each singular stabilizer type individually. Indeed, if a product point has diagonal stabilizer `<s_j>`, commutativity gives

```text
s_j (1,r) = (1,r) s_j,
```

so its image still has stabilizer `<s_j>`.

Hence a nontrivial element of `A` would produce a **nontrivial type-preserving automorphism of the support `Sigma`**.

## Exact support stabilizer replay

The retained exact `Aut(S)` node action has order `1536`. Replaying it on

```text
Sigma=000707000f0f
```

gives a support stabilizer of order exactly `2`.

The verifier additionally identifies its unique nonidentity element with the first retained generator

```text
[a1,a2,a3,b1,b2,b3,c]
 -> [a2,a1,a3,b2,b1,b3,c].
```

Thus the unique nontrivial support symmetry exchanges

```text
b1=0  <->  b2=0,
```

i.e. it swaps the two node-stabilizer types occurring in `Sigma`.

Consequently the type-preserving subgroup of the exact support stabilizer is trivial.

Therefore no nontrivial `r in H` can lie in `A`:

```text
A={1}.
```

## Conclusion

For both surviving Beauville equality cases

```text
e=2 and e=4,
```

the joint pair map is generically degree one:

```text
Psi: Q -> P1 x P1
```

is birational onto its image.

Its image therefore has exact bidegree

```text
e=2: (28l,28l),
e=4: (56l,56l),
```

and normalization genus one.

This removes the possibility that the large one-factor degrees are artifacts of an additional common relative deck quotient. Any remaining realization must be a genuine high-bidegree genus-one correspondence.

## Next lever

For `e=2`, each vertical or horizontal fiber has degree `28l`. Since every supported box node carries `8l` distinct minimal branches, four supported special grid points in one row or column would force at least `32l` intersections with that fiber and give an immediate contradiction. The next leaf is therefore the exact `4 x 4` special-grid placement of the seven supported nodes of each of the two used stabilizer types.

For `e=4`, the analogous fiber capacity is `56l`, so the same first-order argument is weaker and will require the joint grid/Nielsen data.

## Firewalls

- Birationality of the joint quotient pair map does not by itself exclude `e=2` or `e=4`.
- No unsupported concentration of all branches at one quotient branch value is assumed.
- No claim is made for arbitrary branch partitions in the Picard class.
- `000707000f0f`, MB104, span5, P6 sectors, receiver, theorem, endpoint, and Perfect-Cuboid credit remain open/zero.
- No merge authorization.
