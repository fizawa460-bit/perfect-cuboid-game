# MB104 P6B — full-span survivor known-curve / Kc quotient gate — 2026-09-19

Status: **PRE-AUDIT EXACT FINITE GATE / NO CREDIT**

## Target

Hostile P6A survivor:

```
Sigma = 0000093f442e
nodes = {1,2,3,5,10,14,16,17,18,19,20,21,24,27}
D_l = 7l H - 4l sum_(i in Sigma) E_i
```

The support spans `P^6`.

This gate asks whether already-retained/source-locked test curves or coordinate-sign K3 quotient root walls force a fixed component.

## Surface known-curve library: all 92 curves

Use the immutable Stoll--Testa verification source

```
repository = MichaelStollBayreuth/Verification
commit     = 51233ed5ef2bf228fac9416c66db9adc0ebcaadd
path       = Cuboids/cuboids.magma
blob       = 0422b69847f2afb97cb7b3ed02ebef91279f61b1
```

Its known-curve set on the cuboid surface consists of

```
32 conics,
12 genus-one degree-four b-curves,
48 further genus-one degree-four curves.
```

The exact 48-node replay gives, on the P6A survivor:

```
32 conics:
  max supported nodes = 3
  min D_l.C = 14l - 12l = 2l > 0

12 b-curves:
  max supported nodes = 5
  min D_l.C = 28l - 20l = 8l > 0

48 further genus-one degree-four curves:
  max supported nodes = 3
  min D_l.C = 28l - 12l = 16l > 0
```

So every one of the 92 known surface curves has strictly positive pairing with the hostile survivor ray.

The simple "known generator is a fixed component" route is exhausted on this support.

This does **not** prove nefness, because these 92 curves generate the Picard group but are not asserted to generate the full effective cone.

## Revival of W4: coordinate-sign K3 quotients

The retained W4 source adapter gives, for a primitive pushdown `P`,

```
H_K^2 = 8,
H_K.P = 112,
V=P-14H_K,
V^2=P^2-1568.
```

If an effective (-2)-root `R` of degree `d=H_K.R>0` has `P.R<0`,
Cauchy--Schwarz gives

```
d^2 < 16(1568-P^2)/P^2.
```

For the current support, exact coordinate-sign projection gives

```
coordinate  P^2   d^2 bound
a1          1376  96/43
a2          1376  96/43
a3          1184  192/37
b1          1184  192/37
b2           928  320/29
b3          1248  160/39
c            992  288/31
```

The old W4 values are not reused blindly; these are recomputed from the new support.

## Kc type: a1, a2, a3, c

Stoll--Testa prove for `K_c`:

- `Pic(K_c)` is generated by the known curves;
- every curve has even hyperplane degree;
- all conics are the 44 explicit known conics.

The exact 44-conic replay on the four Kc-type pushdowns gives maximum weighted quotient-node incidence

```
a1: 3
a2: 3
a3: 5
c : 6
```

while a negative conic pairing requires weighted incidence at least `8`:

```
P.R = 28 - 4 W < 0  <=>  W>=8.
```

The root-degree bounds plus even-degree parity now finish the four Kc-type quotients:

```
a1,a2:
  bound permits only d=1, but odd degree is impossible.

a3,c:
  only d=2 survives parity/bound;
  every conic is among the exact 44;
  max W < 8.
```

Therefore no Kc-type coordinate quotient has a negative effective (-2)-root against this P6 survivor pushdown.

## Kb type remains finite

The three Kb-type quotients are not silently identified with Kc.

The retained no-line proof applies, so the root bounds reduce the remaining W4 problem to:

```
b1: degree 2 only
b2: degree 2 or 3 only
b3: degree 2 only
```

Nothing of degree >=4 can produce the W4 fixed-root obstruction on the current survivor.

Thus W4 has revived from a broad soft-park to one finite exact residual problem:

```
Kb conics for b1,b2,b3
+
Kb cubics for b2.
```

## Disposition

```
surface known 92-curve fixed-component gate = CLOSED NEGATIVE
Kc quotient root wall = CLOSED NEGATIVE
Kb quotient root wall = FINITE OPEN
whole P6 = OPEN
```

The next leaf should attack only this finite Kb conic/cubic residual. If no source-complete classification is available and a bounded direct span/incidence argument does not close it, park W4 again and switch to a different packet-sensitive P6 invariant.

## Firewalls

```
finite_degree_window_proved=false
MB104_complete=false
R29_LG2_MB_discharged=false
receiver_credit=false
effectivity_final_milestone_credit=false
theorem_credit=false
endpoint_credit=false
perfect_cuboid_existence_claim=false
perfect_cuboid_nonexistence_claim=false
merge_authorized=false
```
