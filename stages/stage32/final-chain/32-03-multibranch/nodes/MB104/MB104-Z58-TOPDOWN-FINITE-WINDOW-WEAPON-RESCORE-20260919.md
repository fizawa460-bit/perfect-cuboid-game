# MB104 Z58 — top-down finite-window weapon rescore — 2026-09-19

Status: **PRE-AUDIT TOP-DOWN PORTFOLIO RESCORE / SIZE48 LOCAL ROUTE PARKED / NO CREDIT**

## Governing objective

The MB104 research objective remains a finite degree / finite l window for the hostile
normalization-genus-one sector, not analytic classification of local singularities for its own sake.

This checkpoint deliberately returns to the top-level question:

```
Which shallow weapon can still produce l <= L or eliminate the dangerous equality frontier?
```

No single route is allowed to consume a deep allocation here.

## 1. Dangerous equality frontier is already size-768 only

The retained archive bridge

```
MB104-Z5P-SIZE48-CM-INERT7-ARCHIVE-BRIDGE-20260919.md
```

source-locks the older CM inert-7 obstruction.

For either size-48 support orbit

```
0000770000ff
00007b0000ff
```

the dangerous equality packet is impossible for every l>=1.

Therefore the dangerous equality-packet frontier is already

```
000707000f0f   orbit size 768.
```

Consequently the recent size48 local singularity chain Z48--Z57 is not the primary finite-window
route.  Its exact results are retained as auxiliary analytic assets, but further size48
tangent-cone/canonical-cover depth is PARKED unless a future global theorem explicitly consumes it.

## 2. Shallow weapon A — Z12 general-m symmetric-differential/support Hilbert

Exact attractive feature:

```
ordinary BTVA support coefficient changes sign between N=13 and N=14,
c_14=-5/54.
```

A support-specific primitive graded module could in principle restore positive cubic growth and
yield a direct large-l obstruction.

Current missing adapter is large:

```
general-m W_m
+ local extension maps
+ multiplication
+ active-support kernel
+ quotient by known hyperplane-generated submodule
+ primitive Hilbert coefficient.
```

Disposition:

```
mathematically live,
direct finite-window shape,
but implementation distance is multiple interfaces.
KEEP SHALLOW / HIGH UPSIDE / HIGH BUILD COST.
```

## 3. Shallow weapon B — Z4' quadratic conductor postulation

Exact object already exists:

```
Z_cond subset S_smooth,
length(Z_cond)=168l^2+56l.
```

This has the correct quadratic scale to beat a linear/equality packet if a theorem forces the
conductor scheme onto lower-complexity ambient data.

Natural adjoint counting is exact equality and Serre/Bogomolov is a no-go.

A fresh shallow literature check again finds Eisenbud--Ulrich regularity as the closest theorem
shape, but its projectively/arithmetically Gorenstein hypotheses are not retained for the
P-polarized hypothetical carrier.

Thus the missing object is genuinely one stronger theorem/adapter:

```
intrinsic conductor postulation / regularity / generator bound
for an integral Cartier curve on the fixed smooth cuboid surface.
```

Disposition:

```
BEST CURRENT THEOREM-HUNT TARGET.
one theorem-shaped gap,
quadratic scale already exact,
do not build a local analytic adapter first.
```

## 4. Shallow weapon C — Picard/null-locus/formal gluing

On surviving 000707000f0f:

```
ordinary Pic0 gluing trivial,
first normal transition zero,
all finite formal Picard restrictions trivial.
```

The complete P-null locus has already been classified.

Disposition:

```
EXHAUSTED as an independent finite-window closer.
Retain only as helper geometry.
```

## 5. Shallow weapon D — global BMY/Sabatino plus local correction

For size768 the exact complete-null-boundary Sabatino margin is

```
28(l+9)/(3(l+1)) > 28/3.
```

This is much less favorable than the size48 4/3 margin.

Moreover available orbifold/local Euler formulas require lc/klt/quotient or an analytically
classified singularity, while the relevant contraction is non-lc.

Disposition:

```
POOR PRIMARY FINITE-WINDOW ROUTE FOR 000707.
Size48 local classification does not address the actual dangerous frontier.
PARK.
```

## 6. Shallow weapon E — common-G / Kummer / Abel / isogeny packet

The 000707 packet already has:

```
common labelled G-cover,
Kummer square classes,
different Abel class,
half-fiber factorization,
conductor first-jet compatibility.
```

The current architecture was re-audited and found to add no independent invariant: the square
classes recode the known common character covers.

Disposition:

```
EXHAUSTED unless genuinely new marked divisor data is discovered.
Do not recombine existing Kummer/Abel data.
```

## 7. Shallow weapon F — collision/tangency strictness (Z25 class)

The equality engine has

```
M=R=O=d,
all supported contacts m=1.
```

The exact conductor scheme is supported in the smooth interior and therefore does not perturb the
exceptional collision packet.

A revival requires a theorem stronger than ordinary simple-contact equality, coupling smooth
interior singularity mass to exceptional/node landing data.

Disposition:

```
NEW-THEOREM ONLY.
Keep as a shallow search direction, not a construction target.
```

## 8. Top-down priority

The next allocation is deliberately not a deep continuation.

```
Priority A: Z4' intrinsic conductor postulation theorem hunt.
Priority B: Z12 direct primitive-Hilbert shortcut hunt that avoids building all general-m modules.
Priority C: Z25-style strictness theorem hunt.
Park: size48 local singularity, Picard/formal, current Kummer/Abel recombinations.
```

The success criterion for the next cycle is a theorem-shaped adapter with a visible route to

```
l <= L
```

or direct elimination of 000707000f0f.

If no such adapter is found after shallow searches, do not deepen any one candidate automatically;
repeat the portfolio rescore.

## Firewalls

```
dangerous_frontier_size48=false
dangerous_frontier_000707_only=true
size48_local_route_primary=false
finite_degree_window_proved=false
whole_uniform_ray_closed=false
MB104_complete=false
receiver_credit=false
theorem_credit=false
endpoint_credit=false
merge_authorized=false
```
