# Stage32 MB104 — W5 solo: order-two active-support regularity — 2026-09-18

Status: **W5 ORDER-TWO EXHAUSTED / HIGHER ORDER OPEN / NO MATHEMATICAL CREDIT**

## Scope

This checkpoint advances W5 only. W20 and W16 are intentionally untouched.

The retained BTVA order-two reflexive symmetric-differential space has dimension 13. At every A1 node its exceptional principal-part map has rank three. The previous archive result proved only this local rank and the three-direction saturation; it did not intersect the kernels over the exact active fourteen-node support.

Active support:

`{0,1,2,3,8,9,10,11,24,25,26,32,33,34}`.

In the BTVA coordinate convention `(x1,x2,x3,y1,y2,y3,z)`, it splits exactly as

- `y1=0`: nodes `0,1,2,3,24,25,26`;
- `y2=0`: nodes `8,9,10,11,32,33,34`;
- overlap: none.

## Exact stacked principal-part computation

Use the explicit 13 BTVA generators from Table 7.1 / the ancillary computation, in the affine `x1=1` basis

`omega01,...,omega06, eta, x2*eta, x3*eta, y1*eta, y2*eta, y3*eta, z*eta`.

For each active node, pull the generators to the standard A1 double cover and retain the constant binary-quadratic exceptional principal part. For nodes with `x1=0`, first move to the `x2=1` chart and transform the tensor coefficients by the exact coordinate change. Stack the three principal-part equations for all fourteen nodes.

The resulting `42 x 13` system has exact rank

`12`.

Hence the common subspace regular at all fourteen active exceptional curves is one-dimensional.

A reduced row-echelon form can be chosen with pivots in columns 1--12 and the only free coefficient the `z*eta` coefficient. Its kernel is generated by

`(0,0,0,0,0,0,-1,-1,-i,0,0,0,1)`.

Therefore the unique order-two form regular at the full active support is, up to scalar,

`(-1-x2-i*x3+z)*eta`

on `x1=1`, i.e. globally

`(z-x1-x2-i*x3)*eta`.

## Interpretation

The linear factor

`z-x1-x2-i*x3=0`

is precisely the active support hyperplane. Thus the one-dimensional common regular subspace is not a new intrinsic-regularity phenomenon: it is exactly the already-retained support-hyperplane regularization architecture.

Consequently there is no second independent order-two form whose intrinsic exceptional regularity can be spent to improve the `1/2` vanishing/order ratio. At order two, the apparent 13-form portfolio collapses on the active fourteen-node packet to the same single hyperplane mechanism already known.

This is stronger than the previous pure-power wall: it exhausts the entire explicit order-two BTVA space on this exact support, not merely powers of `omega_7`.

## 2025 A_n local-Euler machinery

Bruin--Ilten--Xu (2025) gives an exact lattice-point description of local Euler characteristics and extension conditions for A_n singularities. For A1, however, the paper explicitly builds on the exact A1 computation already available in BTVA. It does not by itself supply new global perfect-cuboid symmetric differentials with better exceptional valuation.

Therefore the new local formula is useful as a checksum/engine for higher order, but it does not change the order-two conclusion above.

## What remains genuinely open in W5

W5 is **not** closed negatively. The computation above says only that order two is exhausted.

A positive continuation must produce a genuinely new higher-order global object, for example:

1. a primitive order `m>=4` reflexive symmetric differential not generated by the retained order-two hyperplane architecture;
2. a twisted higher-order section whose active-node extension cost is strictly below `m/2` on enough nodes;
3. higher-order principal-part cancellation among products that creates a new common-regular subspace not generated by powers/products of the unique support-hyperplane form.

The next exact finite experiment, if W5 is revisited, is order four: compute the active-support regular subspace inside the product-generated `Sym^2(V_13)` and then distinguish genuinely new primitive order-four sections from products. That computation is not claimed in this checkpoint.

## W5 solo disposition

`W5 = ORDER2_EXHAUSTED / HIGHER_ORDER_PRIMITIVE_OPEN`.

W5 remains reserve rather than a current closure route. This checkpoint does not change MB104, finite-window, receiver, effectivity, theorem, endpoint, or Perfect-Cuboid credit. No merge is authorized.

Remaining user-selected solo routes: `W20`, then `W16`.
