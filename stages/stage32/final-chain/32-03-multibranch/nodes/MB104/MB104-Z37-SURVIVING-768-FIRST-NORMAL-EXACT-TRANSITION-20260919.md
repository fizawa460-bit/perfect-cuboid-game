# MB104 Z37 — surviving-768 first-normal exact transition — 2026-09-19

Status: **PARALLEL PRE-AUDIT EXACT FIRST-NORMAL VANISHING / NO CREDIT**

## Target

Continue the original Z3 null-locus direction on the surviving balanced support

```text
Sigma = 000707000f0f.
```

Z33G proves that the two zero-pairing elliptic quartics

```text
A:
  b1=0,
  i*a2-a3=0,
  a1-c=0

B:
  b2=0,
  i*a3+a1=0,
  a2-c=0
```

meet at the two smooth points

```text
r_s=(1,1,i,0,0,s,1),  s in {+sqrt(2),-sqrt(2)},
```

and that the ordinary Pic^0 gluing holonomy of O(P) is trivial.

Z33H leaves exactly the first-normal cycle class uncomputed.  This note computes its local
transition coefficient.

## 1. Exact local coordinates on the surface

Work in the affine chart c=1 and set

```text
x=b1,
y=b2.
```

Near either r_s, the four cuboid quadrics solve analytically as

```text
a1 = sqrt(1-x^2),
a2 = sqrt(1-y^2),
a3 = i*sqrt(1-x^2-y^2),
b3 = s*sqrt(1-(x^2+y^2)/2).
```

The branches are chosen by their values at r_s.

Hence x,y are regular local parameters and

```text
A={x=0},
B={y=0},
U=A union B={xy=0}.
```

In particular the first infinitesimal kernel is locally generated by xy.

## 2. Exact ordinary transition

For the surviving support, Z33G uses

```text
L_A = b3 + i*b2 - 2*a2,
L_B = -b1 + i*b3 - 2*a3,
f_A = c^3 L_A/a2^4,
f_B = c^3 L_B/a3^4.
```

Let

```text
g=f_B/f_A.
```

At both r_s,

```text
L_A=s-2,
L_B=i(s-2),
g(r_s)=i.
```

Since s is +/-sqrt(2), s-2 is nonzero, so all these expressions are units in the local
rings at the two intersection points.

Scale the B-trivialization by i so that

```text
G:=g/i
```

has G(r_s)=1.

## 3. Conductor-normalized first-order transition

For a unit G(x,y) with G(0,0)=1, remove the restrictions to the two branches by

```text
Ghat(x,y)
 = G(x,y) / (G(x,0) G(0,y)).
```

Then

```text
Ghat(x,0)=Ghat(0,y)=1,
```

so

```text
Ghat-1
```

is divisible by xy.  The fiber coefficient of the corresponding first-normal gluing class at
the node is

```text
c_s
 = coefficient of xy in Ghat
 = (partial_x partial_y log G)(0,0).
```

This normalization is invariant under multiplying either component trivialization by a unit
depending only on its own branch coordinate.

## 4. The mixed coefficient is exactly zero

From the exact local formulas,

```text
L_A = b3 + i*y - 2*a2,
L_B = -x + i*b3 - 2*a3.
```

Because a2,a3,b3 depend only on x^2 and y^2 to first order,

```text
(L_A)_x(0,0)=0,
(L_A)_y(0,0)=i,
(L_A)_{xy}(0,0)=0,

(L_B)_x(0,0)=-1,
(L_B)_y(0,0)=0,
(L_B)_{xy}(0,0)=0.
```

Also

```text
partial_xy log(a2)=0,
partial_xy log(a3)=0
```

at the origin.

Therefore

```text
partial_xy log(L_B)=0,
partial_xy log(L_A)=0,
```

and hence

```text
c_s
 = partial_xy log[(L_B/L_A)(a2^4/a3^4)]
 = 0.
```

The calculation is independent of the sign s, so

```text
c_+=c_-=0.
```

## 5. First-normal consequence

The normalization exact sequence for

```text
O_U(-U)
```

shows that after the componentwise H^1 terms vanish, the only possible first-normal class is the
two-intersection cycle discrepancy.

The exact local conductor-normalized transition vector is now

```text
(c_+,c_-)=(0,0).
```

Therefore the surviving size-768 support has no first-normal line-bundle obstruction:

```text
O_{2U}(P) is compatible with the trivial restriction on U.
```

Equivalently, the Z33H first-normal interface closes in the negative direction.

This does not prove triviality on every higher formal neighborhood and does not construct a
carrier.

## 6. Z3 routing after this gate

The formal null-locus picture is now:

```text
00070b000f0f  (768):
  excluded already at ordinary Pic^0 holonomy.

000707000f0f  (768):
  ordinary holonomy trivial;
  first-normal cycle class also zero;
  any formal obstruction must begin at higher order.

0000770000ff  (48):
  all finite formal Picard neighborhood obstructions vanish
  by the separate size-48 tree calculation.

00007b0000ff  (48):
  same.
```

Thus another first-order/null-Picard computation should not be repeated.

If Z3 is continued, the only remaining formal direction is a genuinely higher-order obstruction
on 000707000f0f; otherwise rotate to a different global Z mechanism.

## Source locks

- Stoll--Testa computational source:
  `MichaelStollBayreuth/Verification`,
  commit `51233ed5ef2bf228fac9416c66db9adc0ebcaadd`,
  `Cuboids/cuboids.magma` blob `0422b69847f2afb97cb7b3ed02ebef91279f61b1`;
- Z33G note blob `14c1661c5a3b1975474c1f983ed08b29c2150731`;
- Z33G certificate blob `62374fbb19092a2a4a5630fb86b37ddb4924ca29`;
- Z33H note blob `a52a101290c9b2b7024be03858eebd69f67d206d`.

## Firewalls

```text
first_normal_obstruction_surviving_768=false
higher_formal_obstruction_computed=false
surviving_768_excluded=false
remaining_balanced_support_count=864
whole_uniform_ray_closed=false
finite_degree_window_proved=false
MB104_complete=false
receiver_credit=false
theorem_credit=false
endpoint_credit=false
merge_authorized=false
```
