# Stage32 MB104 — `000707000f0f` e=2 diagonal-`G` linearization obstruction

Status: **RETAINED CANDIDATE FINITE PICARD REFINEMENT / LINEARIZABLE FIXED-JACOBIAN KERNEL HAS EXACTLY FOUR CLASSES / 1024 FACTOR PAIRS REDUCED TO 128 / e=2 OPEN / NO CREDIT**

## Scope

Continue only the active leaf

```text
MB104-GENUS1-SPAN5-BALANCED16-000707-E2-RESIDUAL-LIFT-CONDUCTOR-PAIR-MAP.
```

This note consumes the retained candidate product/Picard chain

```text
Phi_Z=0,
O_(C8 x C8)(Zbar) ~= p1^*A tensor p2^*B,
deg A=deg B=28l,
A,B in Pic^(28l)(C8)^G,
Zbar ~ Tdiag(Zbar),
Hdiag stabilizes Zbar,
G=<H,T> ~= (Z/2)^3,
H=<T',TT'R>.
```

The retained explicit fixed-Jacobian computation gives

```text
J(C8)^G ~= (Z/2)^5,
```

so before the present refinement there are `32*32=1024` ordered invariant factor-class pairs `(A,B)` for each `l`.

The purpose here is to use the distinction between invariant and **linearized** line bundles to remove three relative obstruction bits.

## 1. The product line bundle is diagonally `G`-linearizable

Put

```text
P=C8 x C8,
L_Z=O_P(Zbar).
```

Because `Hdiag` preserves the actual divisor `Zbar`, `L_Z` has its canonical `Hdiag` divisor linearization.

The retained product-correspondence linearization gives a rational function `F` on `P` with

```text
div(F)=Zbar-Tdiag(Zbar).                       (F-DIV)
```

Applying `Tdiag` gives

```text
div(Tdiag^*F)=-div(F),
```

so

```text
Tdiag^*F=c/F
```

for some `c in C^*`. Rescaling `F` by a square root of `c` normalizes

```text
Tdiag^*F=1/F.                                  (T-RECIP)
```

Thus `(F-DIV)/(T-RECIP)` supplies a genuine order-two `Tdiag` linearization of `L_Z`.

It remains to check compatibility with `Hdiag`.

### Unsupported fixed points kill the two possible `H` characters

For `h in H`, the group is abelian and `hdiag` preserves both divisors in `(F-DIV)`. Hence

```text
hdiag^*F/F = c_h in C^*.
```

Since `h^2=1`,

```text
c_h in {+1,-1}.                                (H-CHAR)
```

It suffices to treat the two generators

```text
h=T',
h=TT'R.
```

Each corresponding box-node stabilizer type has exactly `16` nodes. The active `000707` support contains exactly seven nodes of each of these two used types. Therefore each generator has at least one **unsupported** box node of its own stabilizer type.

Choose a product-cover point `p` above such an unsupported node. Then

```text
hdiag(p)=p.
```

The active carrier misses every unsupported box node, so

```text
p notin Zbar union Tdiag(Zbar).
```

Therefore `F(p)` is finite and nonzero. Evaluating `(H-CHAR)` at the fixed point gives

```text
F(p)=hdiag^*F(p)=c_h F(p),
```

hence

```text
c_h=1.
```

Both generators act trivially on `F`, so

```text
hdiag^*F=F for every h in H.                  (H-INV)
```

Consequently the `Tdiag` linearization commutes with the canonical `Hdiag` linearization. Thus

```text
L_Z is diagonally G-linearizable.              (G-LIN-P)
```

This is stronger than the previously retained statement that its line-bundle class is diagonally `G`-invariant.

## 2. The linearizable part of `J(C8)^G` has size at most four

Use the standard equivariant-Picard obstruction

```text
obs_G: Pic(C8)^G -> H^2(G,C^*),
```

source-locked in `EQUIVARIANT-PICARD-LINEARIZATION-SOURCE-NOTE.md`. Its kernel consists exactly of classes represented by `G`-invariant divisors.

Let

```text
pi:C8 -> C8/G ~= P1
```

and let `R1,...,R6` be the six reduced ramification-orbit divisors over the six branch values. Each `R_i` has degree four and

```text
pi^*(a_i)=2R_i.
```

Every other `G`-orbit divisor is a full unramified fibre of degree eight, hence is linearly equivalent to `2R_i`. Therefore every degree-zero `G`-invariant divisor class is generated by differences among the six `R_i`.

The retained fixed-Jacobian leaf records the exact principal relations

```text
R1 ~ R2,
R3 ~ R4,
R5 ~ R6,
R1+R2-R3-R4 ~ 0,
R1+R2-R5-R6 ~ 0.                              (R-REL)
```

Hence every degree-zero invariant-divisor class is generated by

```text
lambda_1=O(R1-R3),
lambda_2=O(R1-R5),                             (LAM)
```

with

```text
2 lambda_1=2 lambda_2=0.
```

Therefore

```text
|Ker(obs_G | J(C8)^G)| <= 4.                  (KER-UP)
```

## 3. The kernel has exactly four classes

The retained exact topology gives

```text
J(C8)^G ~= (Z/2)^5,
|J(C8)^G|=32.                                  (FIX32)
```

For

```text
G ~= (Z/2)^3,
```

the source-locked Schur multiplier is

```text
H^2(G,C^*) ~= (Z/2)^3,
|H^2(G,C^*)|=8.                                (H2-8)
```

Therefore any homomorphism from the 32-element fixed Jacobian to this 8-element obstruction group has kernel size at least four:

```text
|Ker(obs_G | J(C8)^G)| >= 32/8 = 4.           (KER-LOW)
```

Combining `(KER-UP)` and `(KER-LOW)` gives the exact result

```text
Ker(obs_G | J(C8)^G)
 = <lambda_1,lambda_2>
 ~= (Z/2)^2.                                   (KER4)
```

In particular the five fixed-Jacobian bits split canonically by role into

```text
2 linearizable divisor bits,
3 genuine G-linearization obstruction bits.
```

Equivalently, the restricted obstruction map is surjective onto

```text
H^2(G,C^*) ~= (Z/2)^3.
```

This gives an algebraic meaning to the previous branch-divisor anti-shortcut: branch-orbit divisors see exactly the two-dimensional linearizable kernel, not all five fixed-Jacobian bits.

## 4. Diagonal linearization forces the factor obstruction classes to agree

For invariant line bundles `A,B` on the two factors, the diagonal obstruction of the external product is additive:

```text
obs_diag(p1^*A tensor p2^*B)
 = obs_G(A)+obs_G(B).                           (OBS-SUM)
```

By `(G-LIN-P)`, the left side is zero. Since the target `(Z/2)^3` has exponent two,

```text
obs_G(A)=obs_G(B).                              (OBS-EQ)
```

Hence the degree-zero relative factor class satisfies

```text
A tensor B^(-1) in Ker(obs_G | J(C8)^G).
```

Using `(KER4)`, necessarily

```text
A tensor B^(-1)
 in {O, lambda_1, lambda_2, lambda_1 tensor lambda_2}.   (REL4)
```

Thus for each of the 32 possible invariant classes `A`, there are at most four compatible invariant classes `B`.

The previous

```text
32*32 = 1024
```

ordered factor-class possibilities collapse to

```text
32*4 = 128.                                     (PAIR128)
```

This reduction is uniform in `l`.

An independent Wolfram finite replay gives

```text
fixed-Jacobian dimension       = 5,
obstruction dimension          = C(3,2)=3,
linearizable-kernel dimension  = 2,
compatible ordered pairs       = 2^5 * 2^2 = 128.
```

## 5. What remains

`(PAIR128)` is a finite Picard refinement, not an effectivity or conductor decision. A surviving pair still has to support an irreducible divisor `Zbar` with:

```text
bidegree (28l,28l),
diagonal H stabilizer,
000707 support passport,
common etale normalization,
retained conductor/residual-sheet data.
```

The immediate finite continuation is now sharper:

```text
128 obstruction-compatible factor pairs
 -> realize lambda_1,lambda_2 and the five fixed Jacobian bits in explicit theta/modular divisor coordinates
 -> test section/effectivity/support passport,
```

while the direct active route remains

```text
conductor loop -> 0 or gamma_Q in H_1(U,Z)=Z/2.
```

## Source locks / inherited boundaries

- `EQUIVARIANT-PICARD-LINEARIZATION-SOURCE-NOTE.md` — equivariant Picard obstruction and invariant-divisor kernel.
- `GENUS1-SPAN5-BALANCED16-000707-E2-PRODUCT-CORRESPONDENCE-LINEARIZATION.md` — `Zbar~Tdiag(Zbar)` and diagonal `H` geometry.
- `GENUS1-SPAN5-BALANCED16-000707-E2-EXTERNAL-PRODUCT-PICARD-REDUCTION.md` — `O(Zbar)=A box B` with `A,B` invariant.
- `GENUS1-SPAN5-BALANCED16-000707-E2-G-FIXED-JACOBIAN-EXPLICIT-BASIS.md` — exact 32-element fixed Jacobian and ramification-divisor relations.
- `GENUS1-SPAN5-BALANCED16-000707-E2-RESIDUAL-NODE-ORBIT-TABLE.md` — seven supported nodes of each used stabilizer type.
- `BEAUVILLE-NODE-STABILIZER-TYPE-SOURCE-NOTE.md` — sixteen box nodes of each stabilizer type.
- `GENUS1-SPAN5-BALANCED16-000707-E2-A1-LIFT-ENERGY-IDENTITY.md` — unsupported nodes are missed by the active carrier.

## Firewalls

- This note inherits the candidate/audit dependencies of the ambient-H1/product-linearization/Rosati-zero chain.
- `(G-LIN-P)` is a statement about the divisor line bundle on `C8 x C8`; it does not assign a residual `G/H` sheet to a conductor branch.
- `(KER4)` classifies the linearizable part of the fixed Jacobian; it does not realize the five retained topological basis vectors as explicit algebraic divisors.
- No claim that any of the 128 factor pairs is effective or supports the required irreducible `Zbar`.
- No conductor loop is assigned to `0` or `gamma_Q`.
- No weighted-cut upper bound or `e=2` closure is claimed.
- `e=4`, `000707000f0f`, MB104, span5, finite-window, receiver, effectivity, theorem, endpoint and Perfect-Cuboid credit remain open/zero.
- No heavy compute is armed.
- No merge or rebase authorization.
