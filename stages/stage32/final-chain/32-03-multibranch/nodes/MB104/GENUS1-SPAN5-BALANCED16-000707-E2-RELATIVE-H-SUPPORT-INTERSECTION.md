# Stage32 MB104 — `000707000f0f` e=2 relative-`H` support/intersection refinement

Status: **RETAINED EXACT RELATIVE-H TRANSLATE GEOMETRY / CONDUCTOR TRANSITION STILL OPEN / NO CREDIT**

## Scope

Continue only the active leaf

```text
MB104-GENUS1-SPAN5-BALANCED16-000707-E2-RESIDUAL-LIFT-CONDUCTOR-PAIR-MAP.
```

For the surviving support

```text
Sigma = 000707000f0f
      = {P0,P1,P2,P3,P8,P9,P10,P11,P24,P25,P26,P32,P33,P34},
D_Sigma = 7l H - 4l sum_(p in Sigma) E_p,
```

the used singular involutions are

```text
s1=T',
s2=TT'R,
H=<s1,s2> ~= (Z/2)^2.
```

This note computes the exact action of the two nontrivial relative factor translations induced by `s1,s2` on the 48-node model, and the resulting intersection numbers between the uniform Picard ray and its translates.

## 1. Relative `H` factor actions descend to two coordinate sign flips

For one level-eight factor, the retained theta source gives the sign actions on

```text
(a,e,b,c,d)
```

as

```text
T  : (+,-,+,+,+),
T' : (+,+,-,+,+),
R  : (+,+,+,-,-).
```

Act on the **second** factor of the box theta parametrization.

For `T'`, only `b=theta01` changes sign. Hence the box coordinates transform as

```text
(W1,W2,W3,Z1,Z2,Z3,C)
 -> (W1,W2,W3,-Z1,Z2,Z3,C).
```

In the Stage32 convention this is exactly

```text
beta_1: b1 -> -b1,
```

with all other projective coordinates fixed.

For `TT'R`, the factor signs are

```text
(a,e,b,c,d) -> (a,-e,-b,-c,-d).
```

The box coordinates therefore become

```text
(-W1,-W2,-W3,-Z1,+Z2,-Z3,-C).
```

After the immaterial common projective factor `-1`, this is exactly

```text
beta_2: b2 -> -b2.
```

Thus the three nonidentity relative-`H` transformations relevant here are

```text
beta_1,
beta_2,
beta_12=beta_1 beta_2.
```

These agree with the corresponding exact `Aut(S)` coordinate substitutions already source-locked in the repository.

## 2. Exact action on the active support

Replay on the exact 48-node model gives

```text
beta_1(Sigma)
 = {P0,P1,P2,P3,P8,P9,P10,P11,P24,P25,P26,P32,P34,P35},

beta_2(Sigma)
 = {P0,P1,P2,P3,P8,P9,P10,P11,P24,P26,P27,P32,P33,P34},

beta_12(Sigma)
 = {P0,P1,P2,P3,P8,P9,P10,P11,P24,P26,P27,P32,P34,P35}.
```

Therefore

```text
|Sigma cap beta_1(Sigma)|  = 13,
|Sigma cap beta_2(Sigma)|  = 13,
|Sigma cap beta_12(Sigma)| = 12.
```

More precisely the only support replacements are

```text
beta_1 : P33 <-> P35,
beta_2 : P25 <-> P27,
beta_12: both replacements.
```

The transformations also permute several retained support nodes internally; the verifier records the complete exact permutation table.

## 3. Exact Picard-class intersection budgets

Use the retained intersection conventions

```text
H^2=16,
H.E_p=0,
E_p.E_q=0 for p!=q,
E_p^2=-2.
```

For two uniform rays with supports `Sigma,Sigma'` of size `14` and overlap `k`,

```text
D_Sigma . D_Sigma'
 = 49*16*l^2 - 32*k*l^2
 = (784-32k) l^2.                              (INT)
```

The self-intersection is

```text
D_Sigma^2=336l^2.
```

Substituting the exact overlap sizes gives

```text
D_Sigma . beta_1(D_Sigma)  = 368l^2,
D_Sigma . beta_2(D_Sigma)  = 368l^2,
D_Sigma . beta_12(D_Sigma) = 400l^2.
```

Equivalently,

```text
(D_Sigma-beta_1 D_Sigma)^2  = -64l^2,
(D_Sigma-beta_2 D_Sigma)^2  = -64l^2,
(D_Sigma-beta_12 D_Sigma)^2 = -128l^2.
```

These numbers are exact global geometry of the active support orbit; they are not formal branch-allocation counts.

## 4. Relation to the active residual-lift leaf

The relative transformations above are generated by elements of `H`. Consequently they do **not** themselves represent the nontrivial residual `G/H` deck action. In particular this computation does not assign a residual `+/-` sheet to any normalization branch.

What it supplies is an exact global intersection budget for the three relative-`H` translates of any realized uniform packet carrier. A later conductor-descent argument may consume these budgets only after proving an adapter from a conductor identification / normalization-preimage relation to an intersection with the corresponding relative translate.

No such adapter is claimed here.

Therefore the active missing objects remain

```text
per-normalization-branch point in R=C8/H,
conductor transition M_(u,v) or equivalent fibre transport,
chi_res(M_(u,v)),
weighted opposite-sheet cut.
```

## Firewalls

- No conductor sign is assigned.
- No conductor pair is identified with an intersection against `beta_1(C)`, `beta_2(C)`, or `beta_12(C)`.
- No upper bound for the Hodge cross-sheet cut is proved.
- `e=2`, `e=4`, and `000707000f0f` remain open.
- The active leaf is unchanged.
- MB104/span5/finite-window/receiver/effectivity/theorem/endpoint/Perfect-Cuboid credit remains zero.
- No heavy compute is armed.
- No merge authorization.
