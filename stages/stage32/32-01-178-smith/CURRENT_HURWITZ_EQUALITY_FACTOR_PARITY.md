# Stage32 32-01-178 Smith — Hurwitz-equality factor-parity free subfrontier

Status: `PROVISIONAL_SMITH_ROUTE_CHECKPOINT / NO_NEW_MAIN_CREDIT / HOSTILE_AUDIT_REQUIRED_FOR_SMITH_PROMOTION`.

## Current audited inputs

The current N354 necessary interval uses

- `n1+n2=d`,
- `n_i <= e/2 + 2g - 2` for both retained factors,
- hence `d <= e+4g-4`.

The FULL178 manifest contains 178 rows, all with even degree: genus zero rows `d=8,10,...,176` and genus one rows `d=8,10,...,192`.

## Equality subfrontier

On the scalar-Hurwitz equality stratum

`d = e + 4g - 4`,

put `u=e/2+2g-2`. Then `d=2u`. Since both factor degrees satisfy `n1,n2<=u` and `n1+n2=2u`, necessarily

`n1=n2=u=d/2`.

Therefore the common factor-degree parity used by the Smith pair-mass reduction is fixed without exposing a new Picard coordinate:

`n1 mod 2 = n2 mod 2 = (d/2) mod 2`.

For each of the 178 FULL178 rows there is exactly one such equality value,

- genus zero: `e=d+4`,
- genus one: `e=d`.

Each lies in the manifest exceptional-mass interval and satisfies the N354 lower interval. Thus there are exactly **178 N354 equality strata**, one per FULL178 row. N355 full-prefix pruning leaves the stratum count unchanged at 17,128, so these equality strata remain represented on the current post-N355 frontier (possibly with fewer terminals).

## Smith consequence

The exact current Smith factor-parity replay studies the twelve marked pair-mass parities. Before factor parity the current prefix leaves residual parity rank two. On these 178 equality strata, factor parity is free data supplied by the audited Hurwitz equality itself rather than an additional terminal observable.

Subject to the current carrier-to-Smith semantic adapter checkpoint and the factor-parity rank replay, the equality subfrontier therefore needs at most the remaining one true pair-group parity before the complete three-single-transposition classification can be made.

This is a route reduction, not a pruning count. No claim is made that any equality stratum is eliminated merely from knowing `n1 mod 2`.

## Firewalls

No new N354/N355 authority is asserted. No Smith MAIN pruning, N350 registration, production completion, N104 release, FULL178 completion, theorem, receiver, endpoint, Stage32 closure, Perfect Cuboid existence/nonexistence, heavy-compute, or merge credit is claimed.
