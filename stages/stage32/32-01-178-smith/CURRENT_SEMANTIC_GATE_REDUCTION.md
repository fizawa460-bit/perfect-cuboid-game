# Stage32 32-01-178 Smith — current semantic gate reduction

Status: `PROVISIONAL_RESEARCH_CHECKPOINT / NO_MAIN_CREDIT / HOSTILE_AUDIT_REQUIRED_BEFORE_PROMOTION`.

This note narrows the remaining semantic obligation in the independent Smith lane. It does not grant MAIN pruning, FULL178 completion, theorem, receiver, endpoint, Stage32 closure, Perfect Cuboid existence/nonexistence, heavy-compute, or merge credit.

## Source locks

Current MAIN authority surface:

- Stage32 MAIN PR #1753 current branch: `stage32-main-final-chain-reentry`.
- N355 full-prefix hostile-audit PASS: review `5165895301`, audited exact head `3f3aadd2e5ada2a0a02a69490d6d659c02762682`.
- N355 special-fibre geometry: for each retained factor and each of its six special fibres,
  `F_b = 2 E_b + sum_{j in I_b} E_j`, with `|I_b|=8`; in each factor the six `I_b` partition exceptional labels `93..140`.

Smith source locks:

- `CURRENT_NUMERICAL_MARKING_CLOSURE.json`: numerical X8 marking gate closed.
- `CURRENT_SEMANTIC_BRIDGE_CONTRACT.json`: semantic carrier lift remains open.
- scratch arbitrary-contact parity lemma blob `5bc6cbc39697afef0ebcbb01d4c6ff0d3f580e22`.
- scratch boundary fixed-point accounting blob `73886257e8761e54f7283147c4774d0e19bf9e49`.

The two scratch notes remain unaudited and are used only as derivations to identify which hypotheses are actually independent.

## Reduction

Let `N` be the smooth normalization of an intended current retained carrier. Assume one structural hypothesis:

`H_current`:
`N` is source-locked to the same standard common degree-two pullback used by the Smith/S32-PW10 cellular construction, with the same fixed-X8 marked Weierstrass identification and H-equivariant integral cellular pullback assembly.

Then the two previously listed local semantic obligations are not independent additional hypotheses.

### 1. Arbitrary contact multiplicity parity follows

At a special-fibre contact with exceptional multiplicity `m >= 0` and boundary-elliptic contact `ell >= 0`, the audited current fibre coefficient pattern gives the local pullback exponent

`k = m + 2 ell`.

After normalizing the degree-two cover, a fixed point occurs exactly when `k` is odd, equivalently exactly when `m` is odd. For a block with contact partition

`M = sum_r m_r`, `n_odd = #{r : m_r odd}`,

one has

`M - n_odd = sum_r (m_r - (m_r mod 2)) in 2 Z`, hence

`n_odd == M (mod 2)`.

Thus higher tangency/contact multiplicity cannot alter the exceptional block parity read by the marked-pair mass.

### 2. Extra fixed-point contributions are exhausted by exceptional contacts

Under `H_current`, the double cover is etale away from the six branch/special fibres, so no tau-fixed point occurs there after pullback to the smooth normalization.

At a smooth special-fibre contact away from exceptional components, the local coefficient is the even coefficient `2 E_b`; hence the pullback exponent is even for every tangency order and normalization creates no fixed point.

At an exceptional contact the coefficient is `1` modulo `2`, so the pullback exponent has parity equal to the exceptional contact multiplicity. Therefore all tau-fixed contributions are exactly the odd exceptional contacts. No additional boundary-tangency parity term is available.

### 3. Current grouped masses are therefore sufficient once `H_current` is source-locked

N355 already supplies the current six-by-eight exceptional block partitions on both retained factor fibrations. The Smith numerical-marking closure already source-locks the current exceptional incidence/Weierstrass marking and the exact mod-2 signature readout.

Consequently, after `H_current` is proved/source-locked, neither

- a separate arbitrary-contact parity promotion, nor
- a separate fixed-point-exhaustion promotion

is needed. They become formal local consequences of the current N355 fibre divisor plus normalization parity.

## Remaining load-bearing gate

The remaining semantic gate is therefore reduced to the single structural source-lock:

> Prove/source-lock that every intended current FULL178 carrier to which the Smith cut is to be applied admits the same source-compatible fixed-X8 common-cover / H-equivariant integral cellular pullback assembly used by S32-PW10, with the current marked Weierstrass identification.

This note does **not** prove that structural lift. In particular, the audited EX1/S32-PW10 source theorem is for the V6 genus-1 carrier population and cannot be silently promoted to the current FULL178 population.

Only after that structural lift is established may the closed numerical marking/signature machinery be replayed on current survivors and submitted for hostile audit before MAIN credit.

## Checkpoint conclusion

`semantic_subobligations_before = {arbitrary_contact_parity, fixed_point_exhaustion, structural_common_cover_cellular_lift}`

`semantic_subobligations_after_reduction = {structural_common_cover_cellular_lift}`

The reduction is conditional and provisional; `semantic_carrier_lift = OPEN` and all downstream credit remains false.
