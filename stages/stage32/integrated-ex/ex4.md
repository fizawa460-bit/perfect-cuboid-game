# Integrated EX4 — absolute marking / Q602 residue discrimination

Status: `FULL_TARGET_CLOSURE_AUDITED` for the bounded frozen-source-package question.

Canonical evidence remains under `stages/stage32-ex4/`.

Audited result:

- `S32.EX4.FIXED_SOURCE_PACKAGE_AMBIGUITY_TERMINAL.V2`;
- the frozen source package cannot select a unique absolute W-line;
- the three retained W-lines remain symmetric at that bounded interface;
- this did not independently exclude residues 73, 97, or 235.

For the currently selected Stage32 route, Q602 geometric realization is already excluded by the consumed MAIN O210/Q602 chain, so EX4 is parked as provenance/anti-loop information. Re-entry requires genuinely new marked data or a new explicit routing contract.

This file is organizational only and grants no additional credit.
