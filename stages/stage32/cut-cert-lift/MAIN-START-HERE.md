# Stage32 CUT-CERT-LIFT startup

This is an on-demand independent research lane. It is intentionally **not** a new canonical ordinary command yet.

## Purpose

Use hostile-audited CUT193..CUT198 closure evidence as read-only input and search for a symbolic, parameterized obstruction explaining a large fraction of the repeated finite-ring/Picard64 infeasibility.

The target is not another bounded wave. The target is an exact implication of the form

```text
H(d,e,prefix invariants, Picard/HNF signature) => exact completion infeasibility.
```

A useful result must be solver-timeout independent and must carry an exact population adapter before any scope enlargement.

## Startup order

1. `AGENTS.md`
2. `stages/stage32/COMMANDS.md`
3. `docs/research-os/policies/cycle-exploration-safety-protocol.md`
4. this file
5. `MISSION.json`
6. `STATE.json`
7. `SOURCE-LOCKS.json`
8. only compact deterministic certificate material required by the active node

## Concurrency contract

This lane may run while CUT199 or EX5 BC2-36 heavy flows are active because it:

- starts from hostile-audited historical evidence only;
- does not mutate CUT/EX5/178/MB state;
- does not arm a heavy runkey;
- does not require an automatic Actions workflow;
- does not grant itself MAIN credit.

If a proposed step needs new heavy solving, stop and return that requirement instead of silently turning this lane into another CUT executor.

## Certificate semantics

Do **not** infer a symbolic law from timeout-sensitive execution-route labels. CUT193 already records that independent fresh replay may classify the same closed set by a slightly different modular/HNF route while reproducing the exact closure set. CERT-LIFT therefore treats the exact audited closure and the deterministic replay witness as authority; runtime route labels are diagnostic only.

For every candidate rule:

1. derive it from exact retained certificate data;
2. test it against all available audited nonclosed/residual controls;
3. record false positives explicitly;
4. only after a zero-false-positive exact implication exists, attempt symbolic proof;
5. only after symbolic proof, challenge a different wave and then the e10 scout through an exact adapter.

## Current active leaf

`CERTLIFT-01_CERTIFICATE_SIGNATURE_EXTRACTION`

Deliverable: a compact table keyed by exact block/terminal scope containing only stable pre-solve invariants plus the deterministic infeasibility witness signature. The table must include residual controls, not only successes.

## Firewalls

- six bounded e8 waves are not a global theorem;
- recent 97-98% closure rates do not imply the same rate on another e or stratum;
- no e10/general-e promotion without an exact semantic adapter;
- no UNKNOWN -> UNSAT relabeling;
- no MAIN/FULL178/effectivity/theorem/endpoint credit;
- no merge without explicit user authorization.
