# Stage32 MAIN scratch — V6 Galois field-adapter wall

Status: scratch only. No Stage32 MAIN authority, Q602/O210 exclusion, receiver/theorem/endpoint credit, hostile-audit credit, or merge.

## Deduplication correction

The previous MAIN tangent-capacity scratch did not open a new route boundary. The retained AM–BJ bundle already contains:

- AS: residual cusp inertia acts by sign on the exceptional coordinate and does not force collisions among FSM-minimal landings;
- AT: the conductor demand splits exactly as `7847 = 4497 + 3350` through the intermediate quotient and twelve blowdowns;
- AU: cusp multiplicity lower bounds alone cannot force a Bezout component in any positive bidegree.

Thus pure branch-count/tangent-slot and cusp-weight Bezout attacks are already retained scratch nonpruning routes.

## New cross-lane field consequence

BI source-locks complex conjugation on the retained Picard data and finds that V6 is not fixed by conjugation. All twelve moving exceptional conjugate pairs have unequal V6 masses; total exceptional mass splits as 126 on conjugation-fixed nodes and 140 on conjugation-moving nodes.

Therefore a divisor defined over `Q` cannot have exact Picard class V6: a `Q`-defined divisor has conjugation-fixed divisor class, while `cc(V6) != V6`.

BJ then exhausts the retained full automorphism group of order 1536 and finds no automorphism sending V6 to `cc(V6)`. Hence there is no class-level semilinear stabilizer of the simple form `g o cc` with `g in Aut(S)` that repairs this failure of descent.

This does **not** exclude a geometric V6 carrier over `C` or over a larger field. It is a field/descent wall only.

## Relation to the active same-member Q602 interface

The registered claim `S32.V6.MEMBER_LEVEL_Q602_LOCAL_IDENTITY.V2` requires one and the same actual V6 member and forbids changing population, model, field, or marking semantics.

BI/BJ therefore sharpen its blocker: a future adapter cannot simply treat V6 as a `Q`-defined class, nor repair conjugation by a holomorphic automorphism of the box surface. A valid future route must do one of the following explicitly:

1. work over a field where V6 is defined and base-change/source-lock the Q602 and local data to that exact same field;
2. replace the single-member interface by a typed Galois-pair/orbit or twisted-population adapter, which is a semantic change and therefore requires a new claim/adapter;
3. bypass the interface if EX1 later receives hostile-audited population-wide V6 genus-one nonexistence credit and that credit is explicitly promoted to MAIN.

## EX1 live status

PR #1728 is currently re-audit-ready/provisional at `beec62400f5f2b49d325083fd9b745c3fd66763f`. Its handoff reports all three prior hostile-audit blockers repaired, but there is no new hostile-audit PASS yet. MAIN therefore consumes no terminal EX1 credit.
