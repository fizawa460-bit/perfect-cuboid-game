# Stage32 MAIN scratch — EX1 uniform transvection meets EX4 absolute-marking obstruction

Status: **SCRATCH_ONLY / UNAUDITED / bounded tested-source-package result**.

This note does not change MAIN authority, EX1 authority, EX4 authority, the claim DAG, Q602/O210 status, or Stage32 closure. It records the next integration result after the scratch uniform EX1 `28 -> 3` transvection candidate.

Snapshot: `f2a89e613cdf91191a0aada9e90c9fc93373a6c6`.

## 1. Input from the preceding MAIN scratch

The preceding scratch adapter replays the basis-independent Weierstrass transvection predicate directly on the 28 EX1/common-cover Q602 residues and leaves exactly

`73, 97, 235`.

The same candidate argument applies to each of the 29 h=4 EX1 ramification states `Q_EX1=210,212,...,266`, so the coarse product ledger contracts

`29 x 28 = 812`

to

`29 x 3 = 87`.

No EX1 Q-state is thereby excluded. The three residues correspond to the three nonzero retained W-lines

- 73 -> L1 = span(r*e1),
- 97 -> L2 = span(r*e2),
- 235 -> L3 = span(r*e1+r*e2).

The remaining question is absolute marking: which retained line is the source-bound class

`delta_0inf=[P_0-P_infinity]`?

## 2. Source side is already fixed

The retained Hperp/H-deck diagnostic and post1648 localization bind

`normal label 9 -> chi_u -> Z3={0,infinity} -> delta_0inf`.

Thus the ambiguity is no longer on the source side. The missing arrow is

`delta_0inf -> {L1,L2,L3}`.

The fixed V6 box marking also records `Z3` at the retained boundary block carried by the box coordinate `b3`. None of this by itself supplies the marked ppav identification needed to turn that source label into one retained J[2] line.

## 3. Exact Cecotti/G12 ambiguity replay

Use the retained principal lattice generators over `Z[r]`, `r^2=-2`,

`b3=[[-1,-1],[1,0]]`,

`b4=[[1,1+r],[0,-1]]`,

and Cecotti's naming

`S=b4`, `T=-b3`.

The corrected differential trace calculation selects the **plus-r** ordered-generator orbit represented by

`(S,T^-1)`.

Exact enumeration of the group generated by `b3,b4` gives:

- `|G12|=48`;
- 24 distinct inner conjugates of `(S,T^-1)`;
- each ordered pair is represented by exactly two conjugators, differing by the central `±I`;
- modulo two these two conjugators act identically on W.

For the literal plus-r representative the source pair assignment is

`Z1 -> L1`, `Z2 -> L3`, `Z3 -> L2`.

Transporting this assignment through all 24 admissible inner conjugates gives

- `delta_0inf -> L1` for 8 pairs;
- `delta_0inf -> L2` for 8 pairs;
- `delta_0inf -> L3` for 8 pairs.

Therefore the trace-compatible Cecotti orbit is exactly transitive on the three current residues. It does not prefer 73, 97, or 235.

This independently replays the 8/8/8 count already recorded in `post1648j-cecotti-trace-orientation-correction.json`.

## 4. Why the explicit curve automorphisms still do not fix the marking

The inspected Cecotti Appendix B passage does three separate things:

1. gives the lattice generators `b3,b4` and renames them `S,T`;
2. identifies the principal ppav with the Jacobian of `y^2=x^5-x`;
3. lists explicit order-2, order-6 and order-8 automorphisms of that Bolza curve.

At the inspected source boundary it does not identify the displayed order-2/order-6 curve automorphisms with the named lattice elements `S,T` on one marked ppav identification.

Koziarz--Rito--Roulleau likewise state that the curve-induced group `H48` and the torus group `G48` are conjugate by an automorphism `g` of the Abelian surface, and explicitly allow changing the curve embedding by composing with `g` before identifying the two groups. The inspected passage does not supply the mod-2 action of that `g`.

Thus existence of a conjugacy is not the missing absolute marking; its marked action is the missing datum.

## 5. FSM/Stoll transposition alone is insufficient

The source-locked FSM element `S` acts on the three source lines as

`Z1 <-> Z3`, with `Z2` fixed.

If one **conditionally** identifies this FSM `S` with principal `b4`, whose retained W action fixes L1 and swaps L2/L3, there are still exactly two equivariant source-to-target bijections:

- `Z1->L2, Z2->L1, Z3->L3`;
- `Z1->L3, Z2->L1, Z3->L2`.

So even that additional transposition anchor leaves

`delta_0inf -> L2 or L3`.

A compatible order-3 generator anchor would remove the final twofold ambiguity. The current tested package does not source-bind that second generator to the retained lattice.

## 6. Bounded result

For the explicitly tested source package consisting of the retained EX1 uniform adapter, Hperp/chi_u source binding, post1648 marking localization, FSM/Stoll S action, principal Bolza lattice, gauge orbit, and Cecotti trace-corrected generator data:

`{73,97,235} -> no further contraction`.

Thus the current coarse EX1 ledger stays at

`29 x 3 = 87`.

This is **not** an EX4 terminal certificate because this MAIN scratch did not prove that the full EX4 frozen-source inventory is repository-wide exhaustive. It is also not a literature-wide absence claim.

## 7. Minimal useful re-entry datum

Any one of the following would genuinely advance the absolute marking:

1. the source-bound mod-2 action of the conjugating ppav automorphism `g` on W/J[2];
2. an explicit marked ppav isomorphism binding a named curve generator pair to a named retained lattice pair;
3. a direct source-bound identification `delta_0inf -> L1/L2/L3`;
4. a source-bound FSM transposition-to-retained-reflection identification **plus** one compatible order-3 generator identification.

Absent such a cross-anchor, repeating gauge choices, trace tests, group-order matches, or inner-conjugacy enumeration cannot reduce the three residues.

## 8. Next MAIN research fork

Two genuinely different routes remain useful:

- **marking route:** search specifically for one of the four cross-anchor data above;
- **member-level coupling route:** bypass absolute marking and couple the three residue/image-line alternatives directly to the EX1 state parameter `r`, local singularity type, or off-cusp projection ramification data for the same hypothetical member.

The second route may be preferable if no marked ppav source is found, because it could eliminate residue/state combinations without ever choosing a globally absolute W-line.
