# Stage32 post-1526 diagonal-theta H-orbit symmetry negative audit

## Scope

This leaf continues the hostile-audited conditional reduction merged in PR #1526. The fixed residual target remains `g1-d186`, `O=210`, `q'=4`, with required `Q(T)=602` for the Bolza correspondence operator `T=(f1)_*(f2)^*`.

PR #1526 proved only the conditional statement that if the actual `T` commutes with two independent Bolza automorphisms whose analytic common centralizer is scalar, then `Q(T)=602` is impossible by the exact CM norm congruence. It did not prove those commutators vanish.

This leaf tests one concrete source-locked mechanism for obtaining such commutation: diagonal modular/theta symmetries that preserve the four-component `H`-deck orbit of the recovered V6 carrier class. The result is negative. It does not exclude `O210`.

## Exact provenance repair after hostile audit

The hostile audit of PR #1529 accepted the finite enumeration but rejected the source locking of the five Stoll words. The repair is deliberately narrow.

For `U,S`, repo search found no retained asset or source-note with an explicit locator for the literal identifications `U -> g4*g5*g9` and `S -> g2*g5`. The compressed `stage32_picard_marking_retained.py` payload is therefore not mined further. Instead, `post1529-fsm-stoll-diagonal-action-source-lock.json` is a minimal transcription adapter for exactly these two actions and no others. It pins:

- Freitag--Salvati Manni, *Parametrization of the box variety by theta functions*, immutable arXiv `1303.6495v1`, Section 2, and DOI `10.1307/MMJ/1480734014`;
- Michael Stoll's `Verification` repository at commit `51233ed5ef2bf228fac9416c66db9adc0ebcaadd`, `Cuboids/cuboids.magma`, blob `0422b69847f2afb97cb7b3ed02ebef91279f61b1`, at the `substs := [...]` automorphism-generator definition.

After normalizing the projective box action by the scalar fixing `c`, the adapter records only

- `U=[[1,2],[0,1]]`: `(-a1,-a2,a3,b1,b2,-b3,c)`, equal to Stoll `g4*g5*g9`;
- `S=[[0,-1],[1,0]]`: `(a3,-a2,a1,b3,b2,b1,c)`, equal to Stoll `g2*g5`.

The diagnostic reconstructs these words from the adapter and checks the seven-coordinate substitutions against the pinned Stoll generator definitions before using the retained all-140 permutations. No broader claim about Freitag--Salvati Manni is introduced.

For the `H`-deck side, the exact retained asset is
`stages/stage32/residual-32-01-production/post1490-o210-q4-equivariant-beauville-deck-cross-exclusion.json`,
blob `9cd6d7122b8a3149b8ab79396946d72b986649df`, canonical SHA256
`8c32735092671d725034de8d14d09c09ac275517fa5f0e225791d2fc53eb5bf3`.
Its `equivariant_adapter.modular_to_stoll` field explicitly fixes

- `u = g7*g9`;
- `v = g7*g8`;
- `uv = g8*g9`.

The certifier hash-binds both the new two-action adapter and this retained post-1490 asset, and the diagnostic reconstructs all five permutations from those locked words.

## Source-locked finite data

The exact recovered V6 class is fixed by its `all140_pairings` vector in `stages/stage32/32-21/post1473-v6-witness-body-recovered.json`, canonical SHA256 `d0c1c8bddfe3950737ed6f87ffa74acd850c736298bd12ec1eceac609625b8a8`.

The retained Stage32 Picard marking provides the nine exact Stoll automorphism permutations on all 140 known classes. The generator order is the order of Stoll's pinned `substs` list. The two diagonal theta actions and three `H`-deck actions are no longer hard-coded independently: they are reconstructed from the source-lock assets described above.

On the Bolza holomorphic differential space, scalar factors are irrelevant for centralizers, and the corresponding projective matrices are represented by

- `U ~ diag(1,-1)`;
- `S ~ [[1,1],[1,-1]]`.

Their common centralizer in `M_2(C)` is scalar.

## Exact finite enumeration

The dedicated diagnostic composes the source-locked 140-class permutations exactly, with no floating-point arithmetic and no CAS. The subgroup generated by the diagonal `U` and `S` actions has order 8 on the retained 140-class interface.

Its eight enumerated words and projective differential matrices are:

| word | H-orbit match | differential matrix |
|---|---|---|
| `1` | `id` | `(1,0,0,1)` |
| `1S` | none | `(1,1,1,-1)` |
| `1U` | none | `(1,0,0,-1)` |
| `1SU` | none | `(1,-1,1,1)` |
| `1US` | none | `(1,1,-1,1)` |
| `1SUS` | none | `(0,2,2,0)` |
| `1USU` | none | `(1,-1,-1,-1)` |
| `1USUS` | none | `(0,2,-2,0)` |

Therefore exactly one element of `<U,S>` preserves the recovered V6 class up to `H`-deck translate, namely the identity. In particular there is no pair of nontrivial orbit-preserving diagonal theta symmetries whose analytic common centralizer could force `T` to be scalar.

The check is numerical-Picard exact: equality with an `H`-translate is tested by equality of the complete retained all-140 pairing vector. Since the recovered V6 numerical class is uniquely reconstructed from those pairings in the retained primitive Picard basis, a failed all-140 equality excludes equality of numerical divisor classes under this bounded action.

## Decision

`AUDITED_NEGATIVE_PENDING_HOSTILE_AUDIT`

This closes only the following route:

`<U,S> diagonal theta symmetry + preservation of the recovered V6 H-deck orbit => two commuting Bolza actions on T`.

The required orbit preservation fails maximally: the stabilizer inside the enumerated order-8 subgroup is trivial.

Consequently:

- actual commutation of `T` with the PR #1526 Bolza generators remains unproved;
- the PR #1526 conditional CM-norm exclusion is not promoted to an unconditional exclusion;
- `Q(T)=602` and `O210` remain open;
- `O212+` remains blocked;
- no controller advancement is authorized.

## Re-entry / next exact routes

Do not repeat this `<U,S>` / H-orbit enumeration. Re-entry requires genuinely new input, for example:

1. a different source-locked symmetry outside this diagonal order-8 subgroup that preserves the correspondence or its four-component orbit;
2. direct carrier-map equivariance giving a commutator identity without numerical class stabilization;
3. an exact divisor/correspondence identity on `C0 x C0` forcing one or more commutators to vanish;
4. an independent argument proving integral valence or another scalarity mechanism.

No effectivity, FULL178, receiver, route, theorem, endpoint, or perfect-cuboid existence/nonexistence credit is created by this negative leaf.
