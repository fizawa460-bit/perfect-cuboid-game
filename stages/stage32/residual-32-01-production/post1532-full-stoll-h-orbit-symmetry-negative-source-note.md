# Stage32 post1532 full-Stoll H-orbit symmetry negative source note

## Scope

This leaf answers one bounded post-#1529 re-entry question only:

> inside the already-retained, source-locked Stoll finite automorphism action on the all-140 Picard interface, is there any non-H element that sends the recovered V6 numerical class into its four-element H-deck orbit, or preserves that H-orbit setwise?

It does **not** establish correspondence equivariance, commutation of the actual Bolza correspondence operator `T`, scalarity of `T`, or exclusion of `Q(T)=602` / `O=210`.

## Exact retained inputs

1. Recovered V6 witness body:
   - `stages/stage32/32-21/post1473-v6-witness-body-recovered.json`
   - Git blob `dae90ed19395355bebeebe2a6aa6bb1c6e53c244`
   - canonical SHA-256 `d0c1c8bddfe3950737ed6f87ffa74acd850c736298bd12ec1eceac609625b8a8`
   - fixed row `g1-d186`; the exact `all140_pairings` vector is used.

2. Retained all-140 Picard / Stoll action interface:
   - `stages/stage33/33-07/stage32_picard_marking_retained.py`
   - Git blob `5a0708a4ddb171e30d85c5a768e0f14ee0eb05f7`
   - `aut_action.schema = STAGE32_AUT_PERM_SOURCELOCK_V1`
   - nine retained generator permutations on 140 labels.

3. Exact H-deck dictionary:
   - `stages/stage32/residual-32-01-production/post1490-o210-q4-equivariant-beauville-deck-cross-exclusion.json`
   - Git blob `9cd6d7122b8a3149b8ab79396946d72b986649df`
   - canonical SHA-256 `8c32735092671d725034de8d14d09c09ac275517fa5f0e225791d2fc53eb5bf3`
   - `u = g7*g9`, `v = g7*g8`, `uv = g8*g9`.

The Arsenal card `S32-PW05` was used only as a routing pointer to this retained exact finite-action asset. It is not authority for the result.

## Exact finite computation

`diagnose_stage32_post1529_full_stoll_h_orbit.py` closes the group generated by all nine retained Stoll permutations and obtains exactly `1536` elements, matching the retained full-action contract.

For the recovered V6 all-140 pairing vector `C`, the H-deck orbit has exactly four distinct numerical classes:

- `C`,
- `uC`,
- `vC`,
- `uvC`.

Across all 1536 retained Stoll elements:

- exactly four elements send `C` into this H-orbit;
- those four are exactly `1`, `g7*g8`, `g7*g9`, `g8*g9`, i.e. H itself;
- therefore the number of base-class hits outside H is `0`;
- the setwise stabilizer of the whole four-class H-orbit also has exactly four elements;
- those four are again exactly H;
- therefore the setwise H-orbit stabilizer outside H is `0`.

Diagnostic CI run `33853796734`, job `100962314922`, completed SUCCESS on PR #1532 head `7eeb501e8436f6468481cbfd08e5e48419738594` against base `ea51d06f3fe46b134e98a065332e9c70fcec57f0`.

## Decision

This is an **exact bounded negative route closure**:

> within the retained source-locked full Stoll finite action on the all-140 Picard interface, there is no non-H candidate preserving the recovered V6 four-component H-orbit, and no non-H element even sends the recovered V6 base numerical class into that orbit.

This closes only the reuse-existing-retained-Stoll-symmetry subroute opened after #1529.

## Firewalls / re-entry

No Stage32 authority is advanced by this leaf:

- `O=210` is not excluded;
- `Q(T)=602` is not excluded;
- actual `T`-commutation is not proved;
- the conditional #1526 CM-norm implication remains conditional;
- `O212+` remains blocked;
- no receiver / route / theorem / endpoint / FULL178 / effectivity credit is granted;
- `stages/stage32/controller.json` is not modified.

Re-entry requires genuinely new input outside this bounded retained action: a symmetry not represented by this Stoll finite action, direct carrier-map equivariance, an exact divisor/correspondence commutator identity, or an independent valence/scalarity argument.
