# Stage32 post-1536 intrinsic-b3 common-cover re-entry audit

Scope: fixed recovered V6 class `g1-d186`, `O=210`, `q'=4`, `Q=602`, after hostile-audited #1534 and #1536.

This leaf resolves one bounded re-entry question left after #1536:

> Does the retained common-double-cover / relative-V4 Cartesian identity already supply the missing intrinsic `b3` equivariance needed to prove `[T,b3]=0`?

The answer is **no at the retained source-locked level**. This is not a theorem that no future intrinsic carrier automorphism can exist.

## Exact acceptance criterion

By #1534, exact commutation with the principal Bolza automorphism is enough:

`[T,b3]=0 => Q(T) != 602`.

For the common-cover route to discharge the missing premise, retained evidence would need at least one exact semantic bridge of one of these forms:

1. an intrinsic automorphism `beta:Y->Y` with exact compatibility
   `f1 o beta = b3 o f1` and `f2 o beta = b3 o f2` (or an explicitly source-locked equivalent sufficient for the Jacobian correspondence);
2. an exact diagonal correspondence identity such as `(b3 x b3)^* Gamma = Gamma`;
3. an exact quotient-normalizer/lift identity whose proved consequence is one of the preceding equivariances.

Finite action matching, a common fixed subspace, or an ambient candidate without this semantic bridge is insufficient.

## Inspected retained source chain

The bounded corpus is the exact source chain already implicated by the live Stage32 authority and the #1534/#1536 re-entry condition:

- `post1503-o210-q4-relative-v4-torsor-mod2-coupling.json` and its source note;
- `post1500-common-cover-marked-branch-geometry-certificate.json` and audit note;
- `post1532-q602-single-b3-commutator.json` and source note;
- `post1536-b3-cusp-orbit-ambient-equivariance-negative-source-note.md`;
- `post1490-o210-q4-bolza-global-or-carrier-local-jet-information-boundary-source-note.md`;
- Arsenal `S30-W01` and provisional `S32-PW05`.

This is a bounded authority/source-chain audit, not a repository-wide absence search.

## What the Cartesian identity actually proves

The hostile-audited relative-V4 coupling fixes

`Z=X(8)`, `H~=F2^2`, `C0=Z/H`, `X=(Z x Z)/H_diag`

and, for the hypothetical carrier normalization `Y` with projections `f1,f2:Y->C0`, gives the exact torsor identity

`f1^* Z ~= f2^* Z`.

Passing to the character plane `W=image(H^* -> H^1(C0,F2))` yields

`T|W = identity mod 2` and `T^dagger|W = identity mod 2`.

That identity was already consumed in the audited Stage32 residue reductions. It is positive coupling evidence, but it contains no retained `b3` action on `Y`, no lift/normalizer for `b3` through the carrier, and no diagonal invariance statement for `Gamma`.

The older common-cover audit likewise explicitly rejects promoting shared-cover structure alone to a stronger correspondence conclusion.

## Exact separation from b3 commutation

The hostile-audited #1534 reduction checks the final Q602 residues `73,97,235` and finds

`b3_commuting_residues = []`.

Thus the retained common-cover-derived mod-2 constraints and all later audited refinements are compatible with three Q602 residue classes, while every one of those classes fails `b3` commutation modulo two.

This is an exact algebraic witness that the **currently extracted common-cover predicates** do not themselves imply `[T,b3]=0`.

It is not a counterexample to a stronger future geometric theorem about an actual effective carrier.

## Arsenal boundary

Formal `S30-W01` requires a common/source-derived semantic anchor before equivariant adapter credit. The missing anchor here is precisely the exact action of `b3` on the actual carrier/correspondence.

Provisional `S32-PW05` consumes an already validated finite action to reconstruct invariant tables and explicitly forbids semantic/geometric identification merely from reconstructed algebra. It cannot manufacture the missing carrier action.

## Decision / firewall

Close only:

`REUSE_RETAINED_COMMON_DOUBLE_COVER_CARTESIAN_IDENTITY_AS_B3_COMMUTATION_PROOF`.

Do **not** infer:

- `[T,b3] != 0` for an actual hypothetical carrier;
- nonexistence of an intrinsic automorphism of `Y`;
- nonexistence of a future quotient-normalizer or divisor identity;
- unconditional `Q(T) != 602`;
- exclusion of `O210`;
- authorization of `O212+`;
- effectivity, FULL178, receiver, route, theorem, endpoint, or perfect-cuboid credit.

The Stage32 controller remains unchanged.

Re-entry requires genuinely new exact information: an intrinsic carrier automorphism with source-locked action on both `f1,f2`, an exact `(b3 x b3)` correspondence/divisor identity, or an exact quotient-normalizer statement that proves the required commutation.
