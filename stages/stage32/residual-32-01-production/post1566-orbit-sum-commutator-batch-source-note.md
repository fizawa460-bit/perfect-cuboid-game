# Stage32 post-1566 orbit-sum / correspondence-commutator batch

Scope: fixed recovered V6 class `g1-d186`, `O=210`, `q'=4`, `Q=602`, after hostile-re-audited and merged #1566.

This batch does not reopen the ambient-symmetry search closed by #1566. It consumes that result together with the previously unmerged #1547 diagnostic idea, recomputes the exact H-orbit-sum stabilizer on current main, and then tests the two remaining #1566 lanes that can be decided from this information: direct correspondence invariance and valence/scalarity.

## Route A — current-main reconstruction of the H-orbit-sum class

The retained exact V6 witness supplies the all-140 intersection vector of the resolved box-surface class `C`. The hostile-audited relative-H adapter gives

- `u = g7*g9`,
- `v = g7*g8`,
- `uv = g8*g9`

inside the source-locked nine-generator Stoll action on all 140 classes.

The current-main diagnostic reconstructs the full retained Stoll group of order `1536`, the H subgroup of order `4`, and the numerical Picard pairing vector

`S_B = C + uC + vC + uvC`.

The full stabilizer of `S_B` in the retained 1536 action has order `4` and is exactly H. Hence every retained box automorphism outside H changes the numerical class `S_B`.

This is a current-main recomputation of the useful finite calculation that appeared historically in unmerged PR #1547; #1547 itself is not used as authority.

## Route B — consume #1566: principal b3 changes the orbit-sum class

Merged #1566 proves

`Aut(B) = retained_Stoll_1536`

and places the principal `b3` box automorphism `beta_B` in this group but outside H. No explicit Stoll word is needed for the following statement, because Route A has determined the complete stabilizer subgroup of `S_B`.

Therefore

`beta_B^*[S_B] != [S_B]`

in the retained numerical Picard group.

This is stronger than #1566's component-class statement `beta_B(C) notin H.C`: even the sum of the four H-translates is not preserved.

## Route C — transport to the actual correspondence pullback

The retained Beauville quotient square is

- `P = X(8) x X(8)`,
- `X = P/H_diag`,
- `B = P/G_diag`,
- `Q = P/(H x H) = C0 x C0`,
- `pi:X->B`,
- `q:X->Q`, with q finite etale of degree four.

For a hypothetical actual carrier in the exact V6 class, the post-1500 hostile-audit repair retains the exact divisor identity

`q^*Gamma = D + uD + vD + uvD`.

The exact Beauville adapter gives, on the resolved surfaces,

`Dtilde = pi_tilde^* C`

and the H-action is equivariant with `pi`. Consequently the numerical resolved pullback class of `q^*Gamma` is the pullback of `S_B`.

Merged #1556 constructs compatible automorphisms `beta_X` and `beta_B` from the same principal-b3 lift and proves `pi o beta_X = beta_B o pi`. Because the lift induces `b3` on each `C0=Z/H` factor, the other quotient square is also tautologically equivariant:

`q o beta_X = (b3 x b3) o q`.

If `(b3 x b3)^*[Gamma]=[Gamma]` in `NS(Q)`, then pulling back by q and then resolving would force `beta_B^*[S_B]=[S_B]`, contradicting Route B. Therefore, conditional on existence of the exact carrier/correspondence,

`(b3 x b3)^*[Gamma] != [Gamma]`

already at the numerical Neron-Severi level.

No injectivity of q-pullback is needed for this direction: equality downstairs would imply equality upstairs, and the latter is disproved.

## Route D — correspondence class to Jacobian commutator

External source lock:

Igor Dolgachev and Yuri G. Zarhin, *Endomorphisms of Complex Abelian Varieties*, April 8, 2025, Chapter 10 §10.1, official University of Michigan lecture notes.

Exact facts used:

1. Equation (10.2) identifies the correspondence group
   `Corr(C)=NS(C x C)/T`
   (T generated by the two fibral classes) with `End(J(C))`.
2. The identification is a ring isomorphism under composition of correspondences.
3. The switch of factors corresponds to Rosati; equations (10.3)-(10.4) give the same correspondence/Rosati form already retained by Stage32.
4. A correspondence has valence nu exactly when its induced Jacobian endomorphism is `[-nu]`.

For an automorphism `a` of C, direct pull-push functoriality in the definition of the correspondence action shows that diagonal transport `(a x a)^*Gamma` induces conjugation of the associated Jacobian endomorphism by `a` (the displayed conjugate may use `a` or `a^{-1}` depending on the Stage32 pull/push orientation; equality is equivalent in either convention).

The two fibral classes are fixed by `(b3 x b3)`. Also `Gamma` and `(b3 x b3)^*Gamma` have the same bidegree `(105,81)`. Thus their difference has bidegree `(0,0)`. A nonzero element of the fibral subgroup cannot have both bidegrees zero, so the nonzero Neron-Severi difference from Route C remains nonzero in `Corr(C0)`.

By the injective/isomorphic correspondence-to-endomorphism map,

`(b3 x b3)^*[Gamma] != [Gamma] in Corr(C0)`

implies the conjugated endomorphism differs from T. Equivalently,

`[T,b3] != 0`.

This is conditional only on the same hypothetical exact carrier/correspondence whose consequences Stage32 is testing. It is not an existence statement.

## Route E — valence/scalarity lane closes negative

A valence-nu correspondence has `T=[-nu]`, hence commutes with every endomorphism, in particular with b3. Route D proves the opposite for any hypothetical exact-V6 O210 correspondence. Therefore this actual correspondence, if it exists, is **not of valence** and its T is **not scalar**.

So the former #1522 conditional route

`integral valence => T=-nu id => Q(T)=2nu^2 => nu^2=301 impossible`

cannot be promoted to close O210: its premise is now refuted for the exact hypothetical correspondence rather than merely unproved.

This closes the `INDEPENDENT_VALENCE_OR_SCALARITY_THEOREM` re-entry lane for this fixed target unless genuinely new data invalidate one of the exact source-bound identifications above.

## Route F — Q602 compatibility check / firewall

The hostile-audited post1532 certificate leaves exactly residues `73,97,235` at Q602, and all three have nonzero principal-b3 commutator modulo 2. Therefore the new actual noncommutation theorem is compatible with every currently retained Q602 residue. It does **not** exclude Q602 or O210.

The correct effect of this batch is terminal lane reduction:

- direct principal-b3 Gamma invariance is refuted;
- actual `[T,b3]!=0` is proved conditionally on carrier existence;
- valence/scalarity is refuted for that hypothetical correspondence;
- all currently retained Q602 residues remain compatible with this noncommutation.

The only authorized re-entry after this batch is genuinely new non-automorphism geometry/arithmetic that distinguishes the three noncommuting Q602 possibilities or otherwise excludes sigma=1204 / Q=602.

## Firewalls

Do not infer:

- existence or nonexistence of the exact carrier from this batch alone;
- `Q(T)!=602` from `[T,b3]!=0` (the implication available from post1532 is in the opposite useful direction: commutation would exclude Q602);
- O210 exclusion;
- O212+ authorization;
- effectivity, FULL178, receiver, route, theorem, endpoint, or perfect-cuboid credit.

The Stage32 controller remains unchanged.