# Stage32 post1648S scratch source note — simultaneous inner-conjugacy invariants cannot select the absolute line

This leaf is scratch-only and grants no MAIN or arithmetic credit.

post1648R fixes the named B9/B8 ordered-pair constraints in the same homology/torus representation convention as post1648N and the retained `G12` lattice. The exact retained enumeration gives 24 compatible ordered pairs, all generating `G12`, with B9 fixed-line counts `L1:8, L2:8, L3:8`.

The present leaf proves the structural anti-loop statement behind those counts. Direct conjugation by all 48 elements of `G12` sends one compatible pair through exactly all 24 compatible pairs. Its stabilizer has size 2 and is exactly the center `{+I,-I}`. Independently, the `G12` action on the three nonzero lines of the retained Richelot plane `W` is transitive.

Therefore any predicate of the ordered pair that is invariant under simultaneous inner conjugacy is constant on the whole 24-element compatible set. Since the B9 fixed line assumes all three values on that set, such a predicate cannot select an absolute retained W-line.

This closes as selectors: additional orders, traces, determinants, characteristic polynomials of words, abstract relations, subgroup order, conjugacy-class labels, and any other statistic depending only on the simultaneous inner-conjugacy class. It does not close genuinely marked information such as an actual source-bound conjugating element, a labelled theta/half-period, or an absolute origin/basis normalization.
