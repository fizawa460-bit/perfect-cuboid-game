# Stage32 post1648Y scratch source note — Cecotti/KKK literal branch automorphism route is already subsumed

This leaf is scratch-only and grants no MAIN or arithmetic credit.

## External source boundary

Cecotti, *Symplectic Singularities, Color Confinement, and the Quantum Dirac Sheaf*, arXiv:2509.24605v1, Appendix B, equations (B.1)-(B.9):

- (B.1)-(B.4) give the retained Abelian-surface matrices and the generator convention `S=b4`, `T=-b3`, with `S^2=1`, `T^3=-1`, `(ST)^4=-1`.
- (B.5) gives the principal period matrix.
- (B.6) identifies the Abelian variety as the Jacobian of the Bolza curve `y^2=x^5-x`.
- The text then says only “In particular we have the following automorphisms” and lists one curve automorphism of each order 2, 6, and 8 in (B.7), (B.8), and (B.9).
- At this locator there is no equation or sentence identifying the individual curve maps (B.7), (B.8), (B.9) with `S`, `T`, `ST`, or with another explicit word in the retained lattice generators.

The order sequence 2,6,8 matches the orders of `S,T,ST`, but that coincidence is not an elementwise source lock and is not promoted to one.

The order-8 curve map (B.9) has `x -> i*x`. This is literally the same base-coordinate automorphism as Klein-Kokotov-Korotkin's named `mu1: z -> i*z` on the same Bolza model `y^2=z(z^4-1)`. The KKK homology action and the source half-period `delta_0inf=(0,0,1,1)` are already exact-locked by post1648N/post1648U.

## Exact retained consequence

post1648N exhausts all 48 polarized period-lattice isomorphisms from the KKK canonical period lattice to the retained G12 lattice. Under those 48 isomorphisms the exact KKK `mu1` action has six target G12 images, each with multiplicity 8, and its unique fixed retained W-line is distributed

`L1:16, L2:16, L3:16`.

Thus recognizing Cecotti (B.9) as the same literal curve map `x -> i*x` does not add a new target elementwise lattice label beyond what post1648N already tested. The 48-isomorphism torsor therefore does not shrink.

post1648J already computes the literal B.7/B.8 holomorphic-differential representation and records that Cecotti does not explicitly identify B.7 with `S` or B.8 with `T`/`T^-1`. post1648S proves that the surviving compatible B9/B8 pair set is one simultaneous-inner-conjugacy orbit, so order/trace/determinant/word-relation data cannot choose one absolute retained W-line.

Therefore the proposed `Cecotti literal curve maps + KKK branch labels` common-anchor route is not a new route: its B.9 content is subsumed by post1648N/U and its B.7/B.8 content is subsumed by post1648J/S. Treating the order-2/order-6/order-8 listing as an implicit `S,T,ST` table would violate the source-lock firewall.

A genuinely new common branch-labelled anchor must bind both sides: it must supply a source branch/half-period label and an explicit retained target lattice word, target half-characteristic, or equivalent marked ppav datum in the same source contract.

This is a bounded anti-loop closure of this specific Cecotti/KKK recombination. It is not a claim that no other source can supply an actual marked ppav isomorphism, an explicit curve-automorphism-to-lattice-word table, or another genuinely two-sided geometric anchor.
