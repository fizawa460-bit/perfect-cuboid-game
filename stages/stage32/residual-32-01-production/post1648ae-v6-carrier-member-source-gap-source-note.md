# Stage32 post1648AE — V6 carrier-member source gap

Scope: fixed `g1-d186`, `O=210`, `qprime=4`, `Q=602`, survivors `[73,97,235]`. This is a scratch bounded source-gap leaf. It grants no MAIN authority, exclusion, receiver, route, theorem, endpoint, or perfect-cuboid credit.

## Exact retained positive result

The recovered V6 class has `C^2=758` and canonical degree `K.C=186`. The hostile-audited Stage29 source lock gives `K^2=16`, `p_g=7`, `q=0`, and `K` nef. Hence

`chi(O)=8`,
`K.(K-C)=16-186=-170`.

If `K-C` were effective, nefness of `K` would force `K.(K-C)>=0`, contradiction. Thus `h^2(O(C))=h^0(O(K-C))=0`. Riemann--Roch gives

`chi(O(C)) = 8 + (758-186)/2 = 294`

and therefore `h^0(O(C))>=294`. So the retained V6 Picard class does contain an effective divisor. Adjunction gives arithmetic genus

`p_a(C)=(758+186)/2+1=473`.

An integral member with normalization genus 1 would therefore require total genus defect 472.

## Exact retained member-level gap

The hostile-audited post1556 source-gap certificate records no defining equation/ideal for the hypothetical carrier member, no distinguished defining section, and no uniqueness of an integral carrier in the fixed V6 class. post1648AC independently records that class-level data cannot materialize `C ∩ ct(C)` support, and post1648AD still has no actual carrier equation/ideal.

Therefore the correct boundary is:

- effective divisor existence: retained positive;
- distinguished integral irreducible genus-1 carrier member: not materialized;
- carrier defining ideal/section: not materialized;
- `C ∩ ct(C)` scheme support: not materialized;
- local `ct` fixed-point adapter: not materialized.

This is not a claim that no such carrier exists and not a repository-wide absence claim. It says only that the exact retained class/action/effectivity chain does not supply the member-level object needed by the current support route.

## Decision

`PASS_STAGE32_POST1648AE_V6_CARRIER_MEMBER_SOURCE_GAP_NONEXCLUSION`

No numerical/symmetry/node-span lane should be reopened. Re-entry requires genuinely new member-level input: an actual V6 carrier ideal, a distinguished section, or a constructive linear-system member decomposition with integral irreducible genus-1 certification. Only then should `C ∩ ct(C)` or local `ct` fixed points be materialized.
