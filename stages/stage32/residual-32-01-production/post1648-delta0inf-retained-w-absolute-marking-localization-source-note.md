# Stage32 post1648 — delta_0inf / retained-W absolute marking localization

## Scope

This leaf continues the hostile-audited and merged #1643 result without granting new arithmetic exclusion. It localizes the remaining adapter defect between the source-bound abstract class `delta_0inf` and the three retained nonzero lines of

`W = span_F2(r*e1, r*e2)`.

The exact audited survivor set remains `[73,97,235]`.

## Source-side chain is now fully retained-boundary marked

The #1643 H-deck/Picard probe gives

`normal label 9 -> chi_u`, with H-profile `(id,u,v,uv)=(0,1,0,1)`.

The retained abstract W/Weierstrass lock gives

`chi_u -> Z3 -> delta_0inf=[P_0-P_infinity]`.

The older source-locked Satake marking now makes `Z3` concrete on the retained box surface:

`Z3 = b3`, with retained Satake-boundary block labels `41,42,43,44`.

The boundary/Weierstrass adapter independently sends those four labels to Weierstrass IDs `4,4,2,2`, exactly the pair `{infinity,0}`.

Hence the source side is no longer merely an abstract Richelot-name statement:

`label 9 -> chi_u -> Z3=b3 -> retained boundary block 41..44 -> delta_0inf`.

What is still absent is the final map from that retained box/torsor marking into one absolute line of the retained Jacobian basis.

## A new exact normalizer datum

The source-locked Freitag–Salvati Manni action

`S=[[0,-1],[1,0]]`

has normalized box substitution

`(a1,a2,a3,b1,b2,b3,c) -> (a3,-a2,a1,b3,b2,b1,c)`,

so it swaps `Z1=b1` and `Z3=b3` and fixes `Z2=b2`.

The retained H naming is

`u = g7*g9`, `v = g7*g8`, `uv = g8*g9`,

with `g7` the `b1` sign, `g8` the `b2` sign, and `g9` the `b3` sign. Therefore conjugation by this `S` gives exactly

`u -> u`, `v <-> uv`.

Dualizing to `H^*` gives

`chi_v` fixed, `chi_u <-> chi_uv`.

Using the already locked character-to-Weierstrass pairs, the induced abstract W action is therefore

`delta_pmi` fixed,
`delta_0inf <-> delta_pm1`.

This is new exact localization credit. It does not identify the same `S` with any named principal G12 lattice generator in the retained `Z[sqrt(-2)]^2` basis.

## Retained W side and finite obstruction

The retained marked-gauge asset has

`L1=r*e1`, `L2=r*e2`, `L3=r*(e1+e2)`,

and

`73 -> L1`, `97 -> L2`, `235 -> L3`.

It also source-locks the principal actions

`b3: L1 -> L3 -> L2 -> L1`,
`b4: L1 fixed, L2 <-> L3`.

However, no current source lock identifies the modular/theta `S` above with retained principal `b4`, nor does it identify an order-3 curve/modular generator with retained principal `b3`. Matching cycle type is not a semantic adapter.

Consequently all `3! = 6` bijections

`{delta_pm1,delta_pmi,delta_0inf} -> {L1,L2,L3}`

remain admissible at the absolute-marking layer. In particular `delta_0inf` can still be any of `L1,L2,L3`, corresponding to residues `73,97,235`.

As a diagnostic only: if a future source lock proves `FSM S = retained principal b4` on this exact common marking, equivariance would force `delta_pmi -> L1` and reduce the ambiguity from 6 to 2, leaving `delta_0inf` as `L2` or `L3`. A compatible independent order-3 anchor would then make the equivariant bijection unique. This conditional count is not current mathematical credit.

## Arsenal firewall

Formal Arsenal weapon `S30-W01` explicitly requires a common/source-derived semantic anchor before finite equivariant matching can be promoted to an adapter, and explicitly forbids `gauge representative => canonical marking`.

Provisional `S32-PW05` reconstructs values after an action has already been validated; it does not create the missing semantic identification.

Therefore no abstract-group or cycle-type shortcut is allowed here.

## Decision

Obtained:

- `delta_0inf` is source-bound through `chi_u`, `Z3=b3`, and retained boundary labels `41..44`;
- the exact FSM `S` action on the abstract W plane is determined;
- the unresolved absolute adapter is localized to a six-element source/target marking ambiguity.

Not obtained:

- an absolute `delta_0inf -> Li` identification;
- any residue-specific commutator;
- arithmetic contraction of `[73,97,235]`;
- `Q602` or `O210` exclusion;
- controller/receiver/route/theorem/endpoint/perfect-cuboid credit.

Next exact route:

`SOURCE_BIND_COMMON_AUTOMORPHISM_ANCHOR_BETWEEN_MODULAR_BOX_H_CHARACTER_ACTION_AND_RETAINED_BOLZA_J2_W_ACTION_OR_DIRECTLY_BIND_Z3_DELTA_0INF_TO_ONE_RETAINED_W_LINE`.
