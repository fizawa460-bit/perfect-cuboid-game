# Stage36 MAIN constitution — Campedelli uniform H-torsor arithmetic

Stage36 is the dedicated attack on the Stage29 Class-3 kernel
`K16-C3-CAMPEDELLI-UNIFORM-TORSOR` / receiver `R29-CAMP2`.
Its purpose is to obtain exact arithmetic control of the audited Q-defined
Campedelli `H ~= (Z/2)^3` quotients sufficient to prove quotient emptiness, or
to prove an exact receiver-matched replacement theorem with the same endpoint
consequence.

## Ordinary startup: bounded by default

After confirming the live branch/PR head with the GitHub connector, read only:

1. `AGENTS.md`
2. this file
3. `stages/stage36/MAIN-STATE.json`
4. `stages/stage36/MAIN-BATCH-HANDOFF.md`
5. only the immediate files named by `current_leaf_working_set`

This list is the sole routine Stage36 startup enumeration authority.

Do not automatically read all Stage29 history, Stage32/33/34/35 controllers,
the full Campedelli literature, the full Arsenal, all ten kernels, or sibling
Brauer routes. Stage29 is consumed through exact Stage36 source interfaces and
expanded only when the current leaf requires it.

`ROADMAP.md` is planning provenance, not routine startup input.
`MAIN-BATCH-HANDOFF.md` is transient delta memory only. If it is `EMPTY`, read
no prior narrative from it.

## Targeted expansion

Expand beyond the ordinary working set only for a named reason:

- the current leaf needs an exact Stage29 source object not yet source-locked;
- a source path/SHA no longer matches;
- one of the three certified Q-symmetry representatives must be materialized;
- the physical-open quotient/boundary map must be reconstructed exactly;
- an explicit `H`-torsor/cocycle or Q-form adapter must be built;
- uniform ramification support must be proved or falsified;
- a finite Selmer-style twist family needs an exact exhaustiveness proof;
- an external theorem must be source-locked and hypothesis-checked;
- the active leaf has identified an existing-weapon type and must consult the
  canonical Arsenal router before fresh literature search or new route invention;
- hostile audit, claim promotion, heavy compute, closure, or release is being performed;
- the user explicitly requests broader history or audit.

Open the smallest relevant source, export the needed fact into a compact Stage36
certificate, then return to the bounded working set.

## Stage29 authority consumed by Stage36

Stage36 starts from the audited facts that:

```text
ROOT_KERNEL=K16-C3-CAMPEDELLI-UNIFORM-TORSOR
SOURCE_RECEIVER=R29-CAMP2
PARENT_ROUTE=Q11-CAMPEDELLI
H=(Z/2)^3
TEN_Q_DEFINED_KERNELS=true
CERTIFIED_Q_SYMMETRY_SPLIT=6+2+2
EXECUTION_REPRESENTATIVES=3
ENDPOINT_Q_POINT_PUSHES_TO_EVERY_AUDITED_C_H=true
QUOTIENT_Q_POINT_LIFTS_AUTOMATICALLY=false
```

The geometric `Q(i)` split `8+2` must not replace the certified Q-arithmetic
`6+2+2` split. Exact Q-isomorphism class count is not assumed.

## Formal Arsenal reuse

Stage36 does not preload the full Arsenal. Once an active leaf identifies an
exact missing weapon type, consult `docs/arsenal/index.json` and then only the
matching generated/formal card.

Pre-registered candidate routes are:

```text
S34-WF01 CLASS3_RECEIVER_REPLACEMENT_THEOREM_PIPELINE
S30-W02 SEMILINEAR_GALOIS_DESCENT_ADAPTER
S30-WF02 IMMUTABLE_LAYERED_CERTIFICATE_REPLAY
S30-WF03 ADAPTER_CREDIT_LAYER_FIREWALL
S34-W01 SUCCESSIVE_EXACT_FACTOR_SQUARECLASS_DESCENT
S34-W03 RECEIVER_RESTRICTED_INTERSECTION_EXCLUSION
S34-W02 GLOBAL_MORDELL_WEIL_CONGRUENCE_EXCLUSION
```

Each card is a reusable proof pattern, not an applicability claim. Re-check the
object, field, quantifiers, Q-form, exceptional locus, torsor adapter, physical
image, and endpoint implication before use.

Provisional active-stage cards are discovery/routing only and never override
Stage36 live authority.

## Stage36 proof firewall

Closure of `R29-CAMP2` requires more than writing down quotient geometry or
sampling twists. At minimum Stage36 must certify:

1. the exact audited Q-defined quotient representative;
2. the exact physical endpoint -> quotient map and all relevant boundary loci;
3. the exact `H`-torsor/lifting class or an exact receiver-matched replacement;
4. either uniform finite ramification support leading to an exhaustive finite
   Selmer-style family, or another theorem with the same quotient-emptiness quantifier;
5. exact discharge of every local/global survivor and exceptional class;
6. an audited implication back to `R29-CAMP2`.

The following shortcuts are forbidden:

```text
GEOMETRIC_QI_8_PLUS_2 != Q_ARITHMETIC_CLASSIFICATION
H1_Q_H_WRITTEN_DOWN != FINITE_TWIST_FAMILY
BOUNDED_TWIST_SEARCH != EXHAUSTIVE_SELMER_CONTROL
MOST_TWISTS_LOCALLY_BAD != QUOTIENT_EMPTINESS
CAMPEDELLI_BRAUER_DATA != CAMP2_CLOSURE
QUOTIENT_Q_POINT != ENDPOINT_Q_POINT
```

Do not promote Stage36 progress to `Q11-CAMPEDELLI`, endpoint closure, or
perfect-cuboid existence/nonexistence without a separate audited full implication.

## MAIN behavior

Advance exactly `current.next_exact_leaf` from `MAIN-STATE.json`.

The initial leaf is `36-01_SOURCE_AUTHORITY_LOCK`, not broad literature search,
not torsor enumeration, and not computation on all ten kernels.

After the source frontier is fixed, proceed through the three certified
Q-symmetry representatives. The first intended Class-3 wall is uniform
ramification support. Do not enumerate arbitrary `H^1(Q,H)` twists before that
support has been proved finite.

Once the exact missing weapon type is identified, consult the Arsenal router
before inventing a new proof route. Repeated bounded local searches are anti-loop
unless they test a named lemma, produce a counterexample, or certify a genuinely
finite exhaustive reduction.

`R29-CAMP4` / `K16-C2-BRAUER-EXPLICIT-CHAIN` is a sibling kernel. It may provide
source-locked assets when the current Stage36 leaf explicitly requires them, but
its authority is not silently merged into Stage36 and its completion does not
automatically close `R29-CAMP2`.

## Search-first repository discovery

Use the Research OS discovery order:

```text
known path -> direct fetch
known filename/symbol -> targeted search
source-lock locator -> exact follow
recursive tree traversal -> explicit exhaustive need only
```

A search miss is not evidence of absence.

## Cycle / anti-loop behavior

Use the repository Cycle Exploration Safety Protocol under the same conditions
as Stage35/35EX. If materially distinct routes repeatedly block, perform the
required `EXHAUSTIVE_VIEW_AUDIT + BLIND_REDISCOVERY` before parking or inventing
another route. Use the current policy's exact machine-readable enum values.

Do not silently revive dominated or frozen routes.

## Commit / audit granularity

Prefer:

```text
one mathematical claim or interface
-> one certificate
-> one verifier
-> one small commit or tightly bounded PR unit
```

Before heavy Actions compute use the repo-wide Actions evidence/safety policy.
Before receiver/theorem/endpoint promotion use the research-credit firewall.

At every hostile-audit boundary verify:

```text
exact current head
current-main freshness
immutable source locks
exact-head CI
no stale MAIN-STATE authority
explicit no-credit firewall
no merge until hostile audit PASS
```

At batch end, promote exact state only into `MAIN-STATE.json`; keep unpromoted
transient narrative only in `MAIN-BATCH-HANDOFF.md`.
