# 13-13fo builder

`build_r06_review_bundle.py` reads every embedded source from fixed commit `103dbc9bf241f8c306befc8dab0175e3ca4fb0f2`, writes the immutable R06 HTML target plus manifest/result, and computes the bundle SHA-256 from the exact HTML bytes.

The dedicated workflow re-computes the SHA and commits the generated target to the PR branch. External review starts only after these generated bytes are present.
