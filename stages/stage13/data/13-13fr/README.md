# Stage13-13fr audit data

No finite data file is used as proof. The dedicated audit recomputes the inert unit-state identities directly for several inert primes and checks the proof-facing lock tokens.
