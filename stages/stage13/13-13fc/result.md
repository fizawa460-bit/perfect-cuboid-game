# Stage13-13fc — R05 Gate C result

> STATUS: `COMPLETE_CURVED_REGION_ERROR_ACCUMULATION`
>
> INPUT: `13-13fb` complete; R04 immutable.
>
> NEXT: `13-13fd` — retained-harmonic conductor/log bookkeeping.

## Question

R04 reviewers objected that the curved-region passage used phrases such as “polylogarithmically many boxes” and “smaller than every fixed negative power of log B” without exposing the actual box count or showing how per-box errors survive global summation.

## Result

Gate C now fixes

```text
H0 = U = exp((log B)^(1/4))
eta = (log B)^(-8)
epsilon = 1/16
N = 64
```

and proves an explicit zero-mode accumulation ledger.

### Box count

Each coordinate lies in `[1,2B]`; a multiplicative mesh `e^eta` therefore needs `O((log B)^9)` intervals per coordinate. Hence

```text
BOX_COUNT=O((log B)^27)
```

for the full three-variable grid.

### Finite-order Perron remainders

The fixed `N=64` one-variable remainder gives a uniform per-box bound

```text
O(B (log B)^(-62)).
```

After all `O((log B)^27)` boxes,

```text
FINITE_REMAINDER_AFTER_ALL_BOXES=O(B(log B)^-35).
```

### Rectangle power tails

With `epsilon=1/16`, the core cutoff gives the explicit saving

```text
exp(-(3/16)(log B)^(1/4)).
```

Thus the total power tail is

```text
B (log B)^(C_rect+27) exp(-(3/16)(log B)^(1/4)),
```

which is smaller than `B(log B)^(-A)` for every fixed `A>0`.

### Wings, boundary and mesh

The zero-mode global ledger is

```text
small height:       O(B (log B)^(9/4))
small coordinate:   O(B (log B)^(5/2))
mixed log shifts:   O(B (log B)^2)
finite remainders:  O(B (log B)^(-35))
power tails:        stretched exponential after O((log B)^27) boxes
boundary shell:     O(B (log B)^(-5)) + lower-order ledger
Riemann mesh:       O(B (log B)^(-5))
```

Every term is `o(B(log B)^3)`.

The boundary argument is now separated cleanly: a box crossing `h(r^2+s^2)=2B` lies inside the multiplicative shell `2B e^{-3eta} <= h(r^2+s^2) <= 2B e^{3eta}`. Its main-polynomial mass is `O(eta B(log B)^3)`, while all analytic rectangle remainders are charged to the already accumulated global remainder ledger.

The full written proof is

```text
stages/stage13/13-13fc/curved-region-error-lemma.md
```

and the exponent substitutions are independently regenerated by

```text
stages/stage13/scripts/13-13fc/curved_region_error_audit.py
```

with checked-in output

```text
stages/stage13/data/13-13fc/curved_region_error_audit.json
```

## Scope

This closes only the zero-mode curved-region/box accumulation objection. It deliberately does **not** claim that the retained nonzero-harmonic estimate is closed; the old fixed `A=48` versus conductor-growth bookkeeping is Gate D (`13-13fd`).

The theorem statement is unchanged, and no Stage12 directional information is used to create the zero-mode direction factor.

```text
STAGE13_13FC=COMPLETE_CURVED_REGION_ERROR_ACCUMULATION
BOX_COUNT=O((log B)^27)
FINITE_REMAINDER_N=64
FINITE_REMAINDER_AFTER_ALL_BOXES=O(B(log B)^-35)
POWER_TAIL_SAVING=exp(-(3/16)(log B)^(1/4))
CURVED_BOUNDARY=O(B(log B)^-5)+lower-order-ledger
MESH_ERROR=O(B(log B)^-5)
THEOREM_CHANGED=false
THEOREM_CONTRACT_REOPEN_REQUIRED=false
R04_IMMUTABLE=true
R05_REQUIRED=true
NEXT=13-13fd
```
