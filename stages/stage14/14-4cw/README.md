# Stage14-4cw

Stage14-4cw now consumes merged `X12` together with the full Cayley-row quantifier correction.

X12 proves that a fourth root of the lost core `C/J` divides the endpoint-linear column cofactor product.  After that column fixes `M`, the Cayley row may still use the full already-fixed `C_Cayley` rather than only `J`.

The two remaining losses are therefore

```text
effective column:
  max(0,1/4-chi/4-3j/4),

full Cayley row N-lift:
  max(0,1/4-c_y),
```

with

```text
j>=chi-4a-2b,
c_y>=chi-2a-2b.
```

Together with

```text
E_H<=3phi-1/8-3a-3b
```

and the merged s/k counts, the three-case minimax gives

```text
V(B)<<B^(61/112+o(1)).
```

Unique saturation:

```text
theta=61/224,
phi=1/4,
chi=33/112,
a=3/112,
b=0,
j=3/16,
c_y=27/112.
```

At equality:

```text
effective column support = B^(1/28),
full-row N-lift          = B^(1/112).
```

Receiver:

```text
SixtyOneOneHundredTwelfthsLostCoreFourthRootFullCayleyRowAsymmetricShortLiftIncidence
```

No auxiliary H/tH theorem is required. Next: `Stage14-4cx`.