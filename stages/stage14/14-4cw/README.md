# Stage14-4cw

Stage14-4cw starts from merged X11/s7-37's current `19/34` theorem and strengthens the nonproportional row/column reconstruction.

The endpoint-linear column genuinely needs the residual/Cayley intersection `J`.  After it reconstructs `M`, however, the Cayley row CRT can use the full already-fixed Cayley-good core:

```text
J | C_Cayley | C,
j>=chi-4a-2b,
c_y>=chi-2a-2b.
```

This removes the fixed-power row `N=abcd` lift at the new equality profile and gives

```text
V(B)<<B^(11/20+o(1)).
```

Unique saturation:

```text
theta=11/40,
phi=1/4,
chi=3/10,
a=1/40,
b=0,
j=1/5,
c_y=1/4.
```

Only one `B^(1/20)` linear-column cofactor support remains; the full-row `N` lift is `B^o(1)`.

Receiver:

```text
ElevenTwentiethsFullCayleyRowUniqueNLinearShortCofactorIncidence
```

No auxiliary H/tH theorem is needed. Next: `Stage14-4cx`.