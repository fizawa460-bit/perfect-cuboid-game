# Stage14-4cw

Stage14-4cw combines merged `s7-35` with merged `4cv` on the same charged-once physical packet.

Main conclusions:

```text
nonproportional branch <= B^(19/34+o(1)),
proportional branch    <= B^(9/16+o(1)),
whole family           <= B^(9/16+o(1)).
```

The nonproportional gain comes from inserting

```text
g_star/H_star^2 | oddpart(omega_1*omega_2)=B^o(1)
```

into the exact row/column joint-core ledger and combining the fourth-power-root count with row/column reconstruction in the ratio `8:3`.

The new whole-family barrier is no longer a finite endpoint-linear product.  It is the proportional branch

```text
L_-=0,
z_1*r_2*s_2=z_2*r_1*s_1,
z_1=A_0*t,
z_2=B_0*t,
t=B^(1/8+o(1)),
```

where the common endpoint scale survives as the minimal rational coordinate gcd of a k residual Gaussian host.

Current receiver:

```text
NineSixteenthsProportionalEndpointScaleKResidualGaussianGcdIncidence
```

No auxiliary H/tH theorem is requested.  Next: `Stage14-4cx`.