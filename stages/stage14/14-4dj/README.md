# Stage14-4dj

Localizes the square-root principal-density obstruction left by merged Stage14-4diH. For each full-conductor root cell `c`, define the exact physical occupancy ratio `omega(c)=M(c)/M_max(c)` relative to the charged-once complete-coordinate majorant. Every fixed-power deficit layer `omega(c)=B^(-delta+o(1))` contributes at most `B^(1/2-delta+o(1))`. Thus square-root saturation can only come from near-maximal conditional principal cells with `omega(c)=B^(-o(1))`.

No new H is requested. Next mainline: `Stage14-4dk`.
