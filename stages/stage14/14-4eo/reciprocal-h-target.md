# Stage14-4eo immutable auxiliary target — fixed polynomial common-core off-diagonal projective collision dispersion

```text
AUDITED_THROUGH=Stage14-4eo
REQUESTED_OBJECT=FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
TARGET_FROZEN=true
```

Audit whether existing determinant-method, bilinear-incidence, multiplicative-character large-sieve, dispersion, modular-hyperbola, or related results imply a uniform fixed-power deficit for the following exact receiver.

Freeze one physical packet and one exponent cell with

```text
C=B^(kappa+o(1)), kappa>0,
|X_j|~R,
|Y_j|~S,
RS/C >= B^(eta+o(1)), eta>0,
gcd(X_j,Y_j)=1,
gcd(C,X_jY_j)=1.
```

The actual charged-once candidate weights `w(z)` are generated by the canonical balanced integer/Gaussian allocation and reciprocal physical packet. The live pair incidence is

```text
z1 != z2,
X1*Y2-X2*Y1 = q C,
q != 0,
|q| <= B^o(1) RS/C,
```

with exact rational proportional pairs removed and all original physical masks retained.

Wanted: some fixed `delta>0`, uniformly over every frozen packet, such that the genuine off-diagonal collision energy is bounded by

```text
OffDiagEnergy_C << M_C^2 B^(-delta+o(1)),
```

or an equivalent estimate ruling out exponent-zero centered-root discrepancy on the concentrated exact-modulus branch.

Do not:

```text
replace the physical candidate sequence by an unrestricted box;
assume q is independent of the candidate pair;
average over unrelated moduli instead of the exact correlated C;
recharge Gaussian splitting, root principal density, diagonal/proportional removal, or finite fibers;
delete balanced/squarefree/coprime/smooth-rough/range/angular/chart/reciprocal masks.
```

If no direct theorem applies, identify the closest theorem family and the exact missing weight/modulus adapter or provide a rigorous full-exponent countermechanism.

Required verdict fields:

```text
DIRECT_THEOREM_APPLICABLE=true|false
OFFDIAGONAL_COLLISION_FIXED_POWER_SAVING_PROVED=true|false
CERTIFIED_SAVING_EXPONENT=...
CANONICAL_PHYSICAL_WEIGHT_ADAPTER_PROVED=true|false
EXACT_CORRELATED_MODULUS_RETAINED=true|false
FULL_PHYSICAL_MASKS_RETAINED=true|false
MINIMAL_REMAINING_OBSTRUCTION=...
WHOLE_FAMILY_CROSS_PROMOTION_PROVED=false unless separately established
NEXT_H_NEEDED=true|false
```
