# Stage14-4cm

Stage14-4cm imports merged 4cl, s7-25, and X4.

Exact odd-part cancellation gives

```text
oddpart(H_k^-)=oddpart(R*J)*oddpart(u_res),
oddpart(H_xi^-)=oddpart(alpha*delta)*oddpart(v_res).
```

Hence all odd agreement support lies in the two linear factors of the minus terms.  The quadratic `i` branches of 4cl/X4 are physically empty, so nine cyclotomic branch types reduce to four signed linear-linear types.

On the only remaining saturation edge `theta=5/16`, the dominant linear moduli have exponents `phi` and `5/16`.  Their quotient variables have total raw support exponent at most `1/8`.

This support statement is not promoted to a whole-family saving because the switch-product-to-quotient fiber remains unproved.

```text
CURRENT_PHYSICAL_UPPER_BOUND_EXPONENT=7/8
MAINLINE_H_NEEDED=false
NEXT=Stage14-4cn
```
