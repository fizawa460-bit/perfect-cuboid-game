# Stage14-q15 summary

`14-q` gate: **RUN**.

Merged `4fs` materially changes q14's target. The heavy global/s branch is now split into

```text
U = bare short-unitary shadow sparsity
C = conditional canonical/reverse physical-completion deficit
```

with survival budget

```text
sigma-delta_c >= mu.
```

q15 therefore searches the exact mechanism-U object

```text
exists u||m in a transported short interval,
```

rather than the older squareclass reciprocal-divisor packet.

## Literature verdict

```text
DIRECT_FULL_OBSTRUCTION_THEOREM_COUNT=0
DIRECT_LOCALIZED_UNITARY_DIVISOR_SUPPORT_THEOREM_FOUND=false
```

Closest sources:

1. Kevin Ford, ordinary divisor in a given interval — `NEAR_HIGH_PRIORITY`, but no unitary/coprime-complement transfer.
2. Ford–Tenenbaum, multiple divisors in a short interval — background only because Stage14 inner witness multiplicity is already `B^o(1)`.
3. Kevin Ford, rough integers with a divisor in `(y,2y]` — conditional on both roughness and unitary-to-ordinary adapters.
4. **Drappeau–Mounier 2026**, localized-divisor natural density — new post-q14 near result and useful ordinary-shadow benchmark, but not direct for unitary divisors or shrinking Stage14 windows.
5. Mangerel/Hooley-Delta short-interval theory — wrong direction/quantifier for the required uniform outer-support deficit.

The direct search under modern `unitary divisor` literature did not locate a theorem counting outer integers admitting a localized unitary divisor with the required uniformity. Existing unitary-divisor papers found in the scan concern arithmetic functions/ranges rather than localized support.

## Main missing adapter

```text
u||m <=> u|m and gcd(u,m/u)=1.
```

The next useful test is therefore

```text
Q15_UNITARY_TO_ORDINARY_LOCALIZED_DIVISOR_TRANSFER_TEST
```

which must decide whether the coprime-complement restriction can be removed/imposed at `B^o(1)` distortion on a charged fixed-E packet. Only after that may Ford or Drappeau–Mounier be charged against the bare-shadow exponent.

A second required check is

```text
Q15_LOCALIZED_DIVISOR_WIDTH_COMPATIBILITY_TEST
```

to determine whether the Stage14 short window has fixed exponent width or shrinks with `B`; the new 2026 numerical localized-divisor result cannot be extrapolated from a fixed-width region to an `o(1)` exponent window.

Mechanism C stays completely separate:

```text
FORD_OR_LOCALIZED_DIVISOR_SAVING_CROSS_PROMOTED_TO_COMPLETION=false
```

Fixed-U is not reopened. Positive merged `tH31` already closes the Mitsui-safe long-headroom branch; the endpoint-short and beyond-Mitsui branches remain t/tH work.

## Locks

```text
STAGE14_Q15=COMPLETE_UNITARY_SHORT_DIVISOR_SHADOW_LITERATURE_RADAR
STAGE14_Q_GATE=RUN
FORD_2004_ORDINARY_DIVISOR_INTERVAL=NEAR_HIGH_PRIORITY
FORD_2019_ROUGH_DIVISOR_INTERVAL=NEAR_CONDITIONAL
DRAPPEAU_MOUNIER_2026_LOCALIZED_DIVISOR_DENSITY=NEW_NEAR_RESULT
DIRECT_LOCALIZED_UNITARY_DIVISOR_SUPPORT_THEOREM_FOUND=false
UNITARY_TO_ORDINARY_LOCALIZED_SHADOW_BOUNDED_DISTORTION=false
Q15_CANONICAL_REVERSE_COMPLETION_THEOREM_FOUND=false
TH31_POSITIVE_FIXED_U_BOUNDARY_RETAINED=true
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_FROM_LITERATURE_PROVED=false
```

q parks after q15 until the unitary-to-ordinary transfer / width-compatibility tests materially resolve, or another genuinely new stable obstruction or theorem appears.
