# Stage14-4cw

Stage14-4cw combines two merged exact reductions on the same nonproportional physical packet:

- X12: the fourth root of the lost core `D=C/J` divides the linear-column cofactor product;
- 4cr/4cv: after the `J`-column reconstructs `M`, the Cayley row can use the larger full `C_Cayley` modulus.

The complete count is

```text
E_FR4
 <=2phi
   +3a+(3/2)b-d
   +max(0,2a+2b-d),

d=chi-1/4,
E_H<=3phi-1/8-3a-3b.
```

Case A (`2a+2b<=d`) uses a `1:1` weighted min. Case B uses `5:3`. Together with merged `E_s,E_k` and the s7-37 proportional `7/16` bound:

```text
CURRENT_PHYSICAL_UPPER_BOUND_EXPONENT=61/112.
```

Unique equality profile:

```text
theta=61/224,
phi=1/4,
chi=33/112,
a=3/112,
b=0,
j=3/16,
C_Cayley exponent=27/112.
```

Remaining fixed-power supports:

```text
effective column = 1/28,
full-row N lift  = 1/112.
```

Receiver:

```text
SixtyOneOneHundredTwelfthsFullCayleyRowFourthRootColumnShortLiftIncidence
```

No mainline H/tH is required. Next: `Stage14-4cx`.
