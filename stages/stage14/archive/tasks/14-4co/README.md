# Stage14-4co

Stage14-4co consumes merged `14-4cn`, `14-s7-28`, and `14-X5`, attacks the physical singular branch `lambda=4`, and integrates the completed independent H audit of the smooth reciprocal Edwards proposal.

The singular relation

```text
D*(Q-P)=A*(Q+P)
```

combined with both original reciprocal equations yields exact primitive gcd-square identities

```text
G_k^2 = 4*r*s*epsilon_k*c_k^+*c_k^-,
G_x^2 = 4*X*Y*epsilon_x*c_x^+*c_x^-.
```

Hence

```text
sf(oddpart(v_res)) = sf(oddpart(r*s)),
sf(oddpart(u_res)) = sf(oddpart(X*Y)).
```

At fixed primitive root / endpoint-small data this reduces raw residual-pair support exponent from `1/4` to `1/8`, but no whole-family saving is promoted because the primitive agreement pair remains moving.  A synthetic infinite primitive singular family prevents an unjustified `B^o(1)` fiber claim.

The completed H audit records

```text
FIXED_LAMBDA_UNIFORM_POINT_BOUND=B^(7/32+phi/2+o(1))
OPTIMISTIC_FIXED_LAMBDA_SAVING_RANGE=phi<17/80
FULL_TOP_EDGE_DETERMINANT_SAVING_PROVED=false
REQUIRED_WORST_EDGE_DELTA_GT=3/22
REQUIRED_DELTA_CERTIFIED_BY_AVAILABLE_THEOREM=false
GENERIC_GENUS_ONE_RECEIVER_IS_MINIMAL=false
```

Therefore the preferred mainline receiver is the sharper merged s7-28 object

```text
TopThetaPrimitiveAgreementQuadraticValueDivisibilityIncidence.
```

```text
CURRENT_PHYSICAL_UPPER_BOUND_EXPONENT=7/8
MAINLINE_H_AUDIT_COMPLETE=true
MAINLINE_H_NEEDED=false
MAINLINE_BLOCKED_BY_H=false
NEXT=Stage14-4cp
```
