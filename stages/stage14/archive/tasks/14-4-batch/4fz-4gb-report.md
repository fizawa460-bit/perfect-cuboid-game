# Stage14-main-batch report — 4fz through 4gb

## Boundary

```text
STAGE14_MAIN_BATCH=COMPLETE
BATCH_START_MAIN_SHA=f9c3116fc82cacbcb494a055b40bb0daa825e19e
BATCH_PUBLICATION_MAIN_SHA=f9c3116fc82cacbcb494a055b40bb0daa825e19e
BATCH_FIRST_STAGE=Stage14-4fz
BATCH_LAST_STAGE=Stage14-4gb
BATCH_SUBSTANTIVE_WORK_UNIT_COUNT=3
BATCH_SUBSTANTIVE_STAGE_COUNT=3
BATCH_INTEGRATED_H_UNITS=NONE
BATCH_STOP_REASON=receiver_change
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
CURRENT_MAIN_RECEIVER=FixedComplementaryDilationPrimitiveEndpointOneDimensionalConditionalPhysicalCompletionSupport_OR_FixedComplementaryDilationTwoSidedPrincipalRectangularConditionalCanonicalReversePhysicalCompletionDeficitWithCapacityHeadroomKappaMinusMu_OR_PolynomialComplementaryDilationFixedPrimitiveProductOneDimensionalConditionalPhysicalCompletionSupport_OR_PolynomialComplementaryDilationPolynomialPrimitiveProductMovingOrdinaryDivisorOuterPairAbsoluteCapacityVersusConditionalPhysicalCompletionDeficit_OR_CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity_OR_FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion_OR_DiffusePolynomialComplementaryGaussianFactorCanonicalAllocationBilinearCorrelation
MAIN_ROUTE_H_NEEDED=false
MAIN_ROUTE_H_REQUEST=NONE
MAIN_ROUTE_H_TARGET=NONE
MAIN_ROUTE_H_BLOCKING=false
NEW_HEAVY_MAIN_H_NEEDED=false
NEXT=Stage14-4gc
```

## Merged sources consumed

The batch starts from latest merged main

```text
f9c3116fc82cacbcb494a055b40bb0daa825e19e
```

and consumes only merged theorem sources:

- mainline through `Stage14-4fy`;
- s-route through `Stage14-s7-107`;
- fixed-U through merged `Stage14-t143+tH32` only via the integrated no-cross-promotion boundary;
- `Stage14-Work-bxX36`;
- q15 as the already-merged literature-routing boundary.

No fixed-U theorem saving is cross-promoted to the global multiplication/completion packet.

## 4fz — multiplication collisions are only divisor-many

For one principal fixed-E rectangle define

```text
Q=#D #V=B^(kappa+o(1)),
r(m)=#{(d,v) in D x V:dv=m}.
```

Since every representation is indexed by a divisor of `m`,

```text
r(m)<=tau(m)=B^o(1).
```

Therefore

```text
#P(D,V)=Q*B^o(1)=B^(kappa+o(1)).
```

The multiplicative collision energy also satisfies

```text
E_x(D,V)=sum_m r(m)^2=B^(kappa+o(1)).
```

So there is no polynomial high-collision branch and no fixed-power multiplication-image compression.

## 4ga — primitive/unitary restriction also has zero fixed-power deficit

The actual bare unitary support is

```text
P_prim(D,V)={uv:u in D,v in V,gcd(u,v)=1}.
```

On a principal rectangle at least one interval cardinality has positive exponent. Holding the other factor fixed, Möbius inversion on the polynomial-cardinality interval gives

```text
#{v in V:gcd(u,v)=1}
 = #V*phi(u)/u+O(tau(u)),
phi(u)/u=B^(-o(1)),
tau(u)=B^o(1),
```

or symmetrically with `D,V` interchanged. Consequently the number of coprime pairs is

```text
#D #V * B^(-o(1)).
```

The product map has `B^o(1)` fibers, hence

```text
#P_prim(D,V)=B^(kappa+o(1)).
```

Thus ordinary rectangular product support and actual unitary/coprime bare support have identical fixed-power exponent on this exact fixed-E packet. No global arbitrary unitary-divisor bounded-distortion theorem is claimed.

## 4gb — completion-only receiver

Let the physical accepted support have exponent

```text
S_phys=B^(tau_phys+o(1))
```

inside `P_prim(D,V)`, and define

```text
delta_comp=kappa-tau_phys.
```

A surviving heavy cell requires exactly

```text
kappa-delta_comp>=mu.
```

Thus the fixed-E two-sided branch no longer has an independent bare-arithmetic fixed-power mechanism. Its minimal receiver is

```text
FixedComplementaryDilationTwoSidedPrincipalRectangularConditionalCanonicalReversePhysicalCompletionDeficit
WithCapacityHeadroomKappaMinusMu.
```

If `kappa=mu+o(1)`, survival forces `delta_comp=o(1)`. If `kappa>=mu+eta`, a future completion theorem must beat the actual headroom `kappa-mu`.

This is a material receiver change, so the batch stops after three substantive work units.

## H decision

No new heavy H is opened. The completion Boolean still bundles canonical/root-origin/allocation and reverse/post-column predicates. `Stage14-4gc` should open that Boolean and identify its minimal arithmetic species before a clean-room theorem target is frozen.

Existing non-heavy mainline H gates remain separate and pending; they do not block this internal heavy route.

```text
PUBLICATION_MAIN_RECHECK_COMPLETE=true
NEW_MERGED_CONSUMER_AFTER_BATCH_START=false
```
