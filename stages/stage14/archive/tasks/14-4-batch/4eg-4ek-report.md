# Stage14-4 batch — 4eg through 4ek

This batch advances five substantive mainline stages from merged `Stage14-4ef` and the merged negative applicability audit `Stage14-sH71`.

Batch start main:

```text
eb93b1c3ca28637c2d3dd3ffead4271cb3e56478
```

Publication recheck reached latest merged main

```text
7bcb1b56d11c92249c826e7c6cbe9b86f2f9b3a4
```

and consumed newly merged `Stage14-s7-72..74` as confirming theorem sources. Those stages independently establish exactly the common-core scale stratification, principal/centered root decomposition, and low-core/allocation versus polynomial-core/discrepancy dichotomy used in `4eg..4ei`. The batch therefore treats `4eg..4ei` as mainline integration of the now-merged s-route results, not as competing theorem claims. Newly merged fixed-U `Stage14-t109..111` remains non-cross-promotable; it changes no global theorem statement used below.

## Stage summary

`Stage14-4eg` consumes sH71's recommended internal reduction and, now consistently with merged s7-72/73, stratifies the reciprocal common-core modulus as `C0=B^(kappa+o(1))`. It writes the exact root selector as principal density

```text
rho(C0)=2^omega(C0)/phi(C0)=B^(-kappa+o(1))
```

plus centered discrepancy. Polynomial `C0` has power-small principal density; subpolynomial `C0` does not.

`Stage14-4eh` proves, consistently with merged s7-73/74, that on every fixed positive-`kappa` block square-root saturation forces an exponent-zero **positive centered root discrepancy**. The polynomial-core reciprocal receiver is no longer raw occupancy but `PolynomialCommonCoreCanonicalAllocationCenteredGaussianRootLineDiscrepancy`.

`Stage14-4ei` integrates merged s7-74 with merged 4ef. The global obstruction splits into two exponent regimes:

```text
low C0=B^o(1):
  canonical integer/Gaussian three-divisor allocation correlation;

polynomial C0:
  centered correlated Gaussian root-line discrepancy.
```

The allocation theorem gate from 4ef remains open; no new discrepancy H is opened yet.

`Stage14-4ej` goes beyond merged s7-74 and performs exact multiplicative-character Fourier inversion on the unit ratio `X0/Y0 mod C0`. The centered root indicator is exactly the nonprincipal character sum, with all physical masks retained in the candidate weight and with the actual `C0`-candidate correlation untouched.

`Stage14-4ek` squares the fixed-modulus character receiver. Orthogonality gives the exact projective collision identity

```text
C | X1*Y2-X2*Y1.
```

But an exact modulus cannot always be frozen: polynomially many `C` values may carry the discrepancy. The polynomial-core receiver therefore splits again into:

```text
A. concentrated exact-modulus projective collision energy;
B. diffuse variable-modulus canonical-allocation norm-divisor graph discrepancy.
```

The concentrated energy still contains diagonal/proportional pairs, and the diffuse branch may be carried by a polynomial family of correlated moduli. Therefore no fresh determinant or collision saving is claimed.

## Current arithmetic obstruction

After this batch, possible square-root saturation is localized to the union of three explicit mechanisms:

```text
LOW COMMON CORE:
  CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity

POLYNOMIAL COMMON CORE / CONCENTRATED MODULI:
  ConcentratedExactCommonCoreProjectiveCollisionEnergy

POLYNOMIAL COMMON CORE / DIFFUSE MODULI:
  DiffusePolynomialCommonCoreCanonicalAllocationNormDivisorGraphDiscrepancy
```

No strict sub-square-root whole-family theorem is proved.

## H decisions

The allocation-side H need from merged 4ef remains unchanged:

```text
MAINLINE_ALLOCATION_H_NEEDED=true
MAINLINE_ALLOCATION_H_TARGET=CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
```

Merged sH71 is a completed negative applicability certificate and is now fully consumed. No new reciprocal/discrepancy H is frozen in this batch because one more internal separation is required:

```text
concentrated branch -> diagonal/proportional vs off-diagonal mover collisions;
diffuse branch -> persistent-modulus / norm-divisor-graph full-exponent mechanism.
```

## Frozen batch boundary

```text
STAGE14_4_BATCH=COMPLETE_FIVE_STAGE_RUN
BATCH_START_MAIN_SHA=eb93b1c3ca28637c2d3dd3ffead4271cb3e56478
BATCH_PUBLICATION_MAIN_SHA=7bcb1b56d11c92249c826e7c6cbe9b86f2f9b3a4
BATCH_FIRST_STAGE=Stage14-4eg
BATCH_LAST_STAGE=Stage14-4ek
BATCH_SUBSTANTIVE_STAGE_COUNT=5
BATCH_STOP_REASON=requested_stage_limit_reached
MERGED_SH71_CONSUMED=true
MERGED_S7_72_74_CONSUMED=true
MERGED_T109_111_RECHECKED=true
T109_111_SAVING_CROSS_PROMOTED=false
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
SQRT_OBSTRUCTION_SPLIT_INTO_LOW_CORE_ALLOCATION_OR_POLYNOMIAL_CORE_DISCREPANCY=true
POLYNOMIAL_CORE_DISCREPANCY_SPLIT_INTO_CONCENTRATED_OR_DIFFUSE_MODULUS=true
MAINLINE_ALLOCATION_H_NEEDED=true
MAINLINE_ALLOCATION_H_TARGET=CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
NEW_RECIPROCAL_H_NEEDED=false
NEXT_H_NEEDED=false
NEXT=Stage14-4el
```
