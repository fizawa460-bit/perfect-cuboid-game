# Stage14-toolbox-az — unique-corner consumer audit and 19/34 receiver refresh

## Status

`COMPLETE_UNIQUE_CORNER_CONSUMER_AUDIT_AND_NINETEEN_THIRTY_FOURTHS_REFRESH`

Stage14-toolbox-ay requested an audit of the first merged consumers of `4ct`,
`X10`, and `t72`, together with the eventual `tH19` outcome.  This stage
performs that audit on merged main through `s7-37` and `t75`.  It proves no new
arithmetic theorem and does not use draft work as a source.

The audited chains are

```text
4ct -> 4cu / s7-33 -> s7-34 -> s7-35 -> s7-36 -> X11 -> s7-37
X10 -> 4cu / s7-33 -> s7-34 -> s7-35 -> s7-36 -> X11 -> s7-37
t72 -> t73 -> t74 -> tH20 -> t75
```

The strongest merged whole-family theorem is now

```text
V(B) << B^(19/34+o(1)).
```

The remaining global receiver is

```text
NineteenThirtyFourthsSingleCrossRootJointCoreTwinShortRowColumnIncidence.
```

The fixed-`U` receiver is distinct and is now

```text
SharedUSmallOddKappaFixedTagSmallAngularGcdBalancedShortCoverTypeIIDispersionEnergy.
```

## 1. First consumers of 4ct and X10

Merged `4ct` makes the xi residual Gaussian host primitive up to `B^o(1)` on
the old unique `5/8` corner.  Merged `X10` independently imposes small root gcd,
a dominant Cayley orientation, and a short cofactor.  Their first merged
consumers are `4cu` and `s7-33`.

`4cu` compares the residual and Cayley orientations through the endpoint
linear product and proves `19/32`.  `s7-33` identifies the common-core Gaussian
orientation with the primitive root line, forbids double charging, and records
a physical counterexample to the stronger canonical `S/T` associate split.
Thus the ay intersection label was a valid necessary-filter receiver, but is no
longer the current minimal object.

```text
FIRST_4CT_CONSUMERS_AUDITED=true
FIRST_X10_CONSUMERS_AUDITED=true
FOUR_CT_X10_FILTER_INTERSECTION_VALID_AT_AY=true
FOUR_CT_X10_SAVINGS_MULTIPLIED=false
CANONICAL_ST_ASSOCIATE_SPLIT_REUSED=false
```

## 2. Merged global supersession chain

The subsequent merged certificates are ordered as follows:

| Stage | merged exponent | live obstruction at that stage |
|---|---:|---|
| toolbox-ay / X10 | `5/8` | unique top corner |
| 4cu | `19/32` | joint core / linear product |
| s7-34 | `47/80` | fourth-power root transfer |
| 4cv | `7/12` | row/column twin cofactors |
| s7-35 | `4/7` | single cross-root joint core |
| s7-36 | `9/16` | proportional common-`z` branch |
| X11 | `19/34` | nonproportional twin short row/column incidence |

These are successive complete bounds on the same whole-family universe.  They
may be compared by strength; their savings are not multiplied.

X11 lowers the proportional branch to `13/24`.  Its first merged s-consumer
`s7-37` strengthens this further to `7/16` by proving `K^2|u_res`.  Hence
the global obstruction is the nonproportional equality profile inherited from
s7-36:

```text
theta=19/68, phi=1/4, chi=21/68,
eta_star=3/136, eta_other=0, rho=3/68, j=15/68,
|h_-h_+|<=B^(1/34+o(1)),
row CRT lifts<=B^(1/34+o(1)),
H^4|q_xi.
```

The gap to square-root scale is `1/17`.  The old `5/8`, `19/32`, `47/80`,
`7/12`, `4/7`, and `9/16` receivers are historical certificates, not current
barriers.

## 3. First consumer of t72 and tH outcome

The first merged consumer of `t72` is `t73`.  It proves:

1. the `kappa=1` branch is an elementary coprime difference-of-squares branch;
2. fixed denominator tags determine the t72 CRT sign at `B^o(1)` cost;
3. fixed `(kappa,beta,Pminus)` norm fibers are uniformly `B^o(1)`.

It consumes the negative `tH19` audit and requests `tH20` only for the moving
canonical-largest-prime norm values with all physical filters retained.

Merged `t74` then removes that norm value as an independent variable.  Writing
`c=odd(Pminus/ell)`, it proves

```text
c=H*R_V,
ell*c<2B,
ell*g*c<2B,
odd(r*t)=g*c/H,
r,t<sqrt(ell),
h*ell*(r^2+t^2)<=4B.
```

Merged `tH20` finds no off-the-shelf uniform fixed-power sieve theorem for this
post-t74 packet.  Largest-prime, Pell, friable-value, and generic norm-form
sieves are no longer minimal.  Angular divisor switching is directly useful,
but alone proves no fixed-power saving.  No `tH21` is requested; the t route is
to continue elementarily with `t75`.

```text
FIRST_T72_CONSUMER_AUDITED=true
TH19_CONSUMED_BY_T73=true
TH19_CURRENT_REQUIREMENT=false
TH20_CONSUMED=true
TH20_OFF_THE_SHELF_FIXED_POWER_SAVING=false
TH20_CROSS_PROMOTED_TO_GLOBAL=false
TH21_NEEDED=false
T_ROUTE_BLOCKED=false
```

## 4. Cross-route and supervisor decision

The X/global receiver counts a moving whole-family common-core/root allocation.
The t receiver conditions on fixed `U`, small `kappa`, and a denominator tag.
No exact adapter identifies their coefficient spaces, so tH20 contributes no
global exponent saving.

No toolbox-H line is needed.  Both live objects already expose unused exact
arithmetic, and neither is blocked on an external theorem.  The next toolbox
audit should wait for actual consumers of `s7-37` and `t75`; it must not
reopen tH19 or the ay unique-corner receiver.  The first merged tH20 consumer `t75`
splits the angular gcd across the two coprime cover columns, makes the highly
unbalanced branch Type-I, and leaves only the balanced small-`g` Type-II energy
named above.  No `tH21` is needed before `t76` exhausts that exact packet.

## Locked boundary

```text
STAGE14_TOOLBOX_AZ=COMPLETE_UNIQUE_CORNER_CONSUMER_AUDIT_AND_NINETEEN_THIRTY_FOURTHS_REFRESH
MERGED_TOOLBOX_AY_IMPORTED=true
FIRST_4CT_CONSUMERS_AUDITED=true
FIRST_X10_CONSUMERS_AUDITED=true
FIRST_T72_CONSUMER_AUDITED=true
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=19/34
CURRENT_GAP_TO_SQRT=1/17
PROPORTIONAL_BRANCH_UPPER_BOUND_EXPONENT=7/16
CURRENT_GLOBAL_TOOLBOX_RECEIVER=NineteenThirtyFourthsSingleCrossRootJointCoreTwinShortRowColumnIncidence
CURRENT_FIXED_U_RECEIVER=SharedUSmallOddKappaFixedTagSmallAngularGcdBalancedShortCoverTypeIIDispersionEnergy
GLOBAL_AND_FIXED_U_RECEIVERS_EQUIVALENT=false
TH19_CURRENT_REQUIREMENT=false
TH20_CONSUMED=true
TH20_CROSS_PROMOTED_TO_GLOBAL=false
TH21_NEEDED=false
TOOLBOX_H_CONTINUATION_NEEDED=false
TOOLBOX_ROUTE_BLOCKED=false
NEW_AZ_WHOLE_FAMILY_POWER_SAVING_PROVED=false
SQRT_B_UPPER_BOUND_PROVED=false
NEXT=Stage14-toolbox-ba audit first s7-37 and t75 consumers after they merge
```
