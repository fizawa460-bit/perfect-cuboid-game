# Stage14-s4c — collective activation reverse-count boundary

Stage14-4ak closes the complete fixed physical Q-rational M-degree-four mechanism. Stage14-s4c therefore treats the observed active-base signal as necessarily collective unless a different non-fixed mechanism is found.

For an M-degree `d` fixed rational stratum, the Stage14 physical height polarization gives a point-count scale at most

```text
B^(2/d+o(1)).
```

Thus if an eventual law

```text
V(B) ~ c B^(1/2)
```

were true and were explained by a growing family of degree-`d` strata, their number must satisfy the necessary reverse-count relation

```text
N_d(B) >= B^(1/2 - 2/d - o(1)).
```

In particular:

```text
d=8   -> N_8(B)  >= B^(1/4-o(1))
d=12  -> N_12(B) >= B^(1/3-o(1))
d=16  -> N_16(B) >= B^(3/8-o(1))
```

This is only a necessary exponent budget. It neither proves existence of such strata nor the `sqrt(B)` asymptotic.

The finite s4a/s4b census reinforces the collective interpretation: among 490 active oriented first-face vertices, 483 exact Kummer square-class triples and 393 explainable coarse arithmetic signatures occur; the largest coarse cluster has size four and 326 signatures are singletons. This finite dispersion does not itself prove geometric proliferation, but it rules out using a few repeatedly identical descent classes as the observed finite explanation.

The dedicated audit also regenerates `A(B)`, the eligible primitive oriented Pythagorean-base count, and `V(B)`, the first-hit active count, at frozen cutoffs, matching the collective activation measure used by Stage14-4al.

```text
STAGE14_S4C=COMPLETE_COLLECTIVE_ACTIVATION_REVERSE_COUNT_BOUNDARY
FIXED_M4_MECHANISM_CLOSED_BY_4AK=true
COLLECTIVE_PROLIFERATION_NECESSITY_FORMULA_LOCKED=true
DEGREE8_REQUIRED_COUNT_EXPONENT=1/4
DEGREE12_REQUIRED_COUNT_EXPONENT=1/3
FINITE_S4B_DISPERSION_IMPORTED=true
SQRT_B_ASYMPTOTIC_PROVED=false
HIGHER_DEGREE_STRATA_EXISTENCE_PROVED=false
NEXT=Stage14-s5 seek a uniform arithmetic small-point/rank-jump counting theorem compatible with the collective activation measure
```
