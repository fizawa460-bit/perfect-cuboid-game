# Stage14-4cp

Stage14-4cp promotes the merged s7-29 primitive common-core root-line estimate into the Stage14 mainline and intersects it with merged X6.

The old `7/8` barrier is superseded:

```text
V(B) << B^(3/4+o(1)).
```

The exponent comes from

```text
C~B^c residual/common-core support: B^(c+1/4+o(1)),
primitive (U,V) on one C-root line: B^(2phi-c+o(1)),
```

so the common-core exponent cancels and each block costs

```text
B^(2phi+1/4+o(1)) <= B^(3/4+o(1)).
```

Only `theta=5/16, phi=1/4` can saturate the new bound.

Merged X6 also eliminates the `lambda=4` singular branch asymptotically and supplies the surviving primitive pair with real/twisted four-root quadratic-value masks.  The `alpha*delta` and `S*T` moduli are generated by those quadratic values, so they cannot be charged a second time as independent determinant-spacing moduli.

The new mainline receiver is

```text
QuarterPhiCommonCorePrimitiveFourRootQuadraticValueEnergy.
```

The completed generic genus-one H audit is not the minimal route to this receiver.

```text
CURRENT_PHYSICAL_UPPER_BOUND_EXPONENT=3/4
NEW_WHOLE_FAMILY_POWER_SAVING_PROVED=true
MAINLINE_H_NEEDED=false
NEXT=Stage14-4cq
```
