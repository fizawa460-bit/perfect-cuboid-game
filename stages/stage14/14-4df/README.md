# Stage14-4df

Stage14-4df consumes merged `Stage14-4de` and merged `Stage14-s7-46`.

The active square-root receiver is now the s7-46 finite-fiber mixed-root/cofactor packet

```text
SquareRootQuarterScaleMixedFourthRootDualBalancedXiCofactorSplitPhysicalAdmissibilityDensity
```

with

```text
Q_mix=C_*u_*=B^(1/4+o(1)),
M_+=oddpart(D^2+A^2)/C_*=oddpart(S*T)*B^o(1),
M_-=oddpart(D^2-A^2)/u_*=oddpart(R*J)*B^o(1).
```

Define the same-side overlaps

```text
W_+=gcd(C_*,M_+),
W_-=gcd(u_*,M_-).
```

After `(C_*,u_*)` and the mixed tuple are fixed, s7-46 reconstructs `M_+,M_-` with `B^o(1)` multiplicity; therefore `W_+,W_-` carry no independent fixed-power choice cost. But

```text
C_*W_+ | D^2+A^2,
u_*W_- | D^2-A^2,
```

so the actual root modulus lifts to

```text
Q_eff=Q_mix*W_+*W_-.
```

For `W_+=B^(w_++o(1))`, `W_-=B^(w_-+o(1))`, primitive root-line spacing gives

```text
E_4df(w_+,w_-) <= 1/2-w_+-w_-.
```

Thus any square-root-saturating packet must have

```text
gcd(C_*,M_+)=B^o(1),
gcd(u_*,M_-)=B^o(1).
```

Together with 4de plus/minus cross-coprimality, the four blocks `C_*`, `M_+`, `u_*`, `M_-` are pairwise separated at fixed-power scale.

New receiver:

```text
SquareRootQuarterScalePairwiseSeparatedMixedFourthRootDualBalancedXiCofactorSplitPhysicalAdmissibilityDensity
```

The s route is already active after merged s7-46. No reactivation decision is made in 4df. The next s stage remains `Stage14-s7-47`; reactivation checks resume only after a future s-stage closes the route again.

Mainline exponent remains `1/2`. Next mainline stage: `Stage14-4dg`.