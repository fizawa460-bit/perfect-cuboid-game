# Stage14-main-batch report — 4gc through 4ge

## Boundary

```text
STAGE14_MAIN_BATCH=COMPLETE
BATCH_START_MAIN_SHA=a0c01e4f1236e2a3c1f718f056fee6d4f1c73e20
BATCH_PUBLICATION_MAIN_SHA=a0c01e4f1236e2a3c1f718f056fee6d4f1c73e20
BATCH_FIRST_STAGE=Stage14-4gc
BATCH_LAST_STAGE=Stage14-4ge
BATCH_SUBSTANTIVE_WORK_UNIT_COUNT=3
BATCH_SUBSTANTIVE_STAGE_COUNT=3
BATCH_INTEGRATED_H_UNITS=NONE
BATCH_STOP_REASON=receiver_change
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
CURRENT_MAIN_RECEIVER=FixedEEndpointConditionalCompletion_OR_FixedETwoSidedPrincipalRectangularReciprocalDivisorCRTSupportDeficitVersusConditionalRootCanonicalPostColumnCompletionDeficitWithCapacityHeadroomKappaMinusMu_OR_PolynomialEFixedProductConditionalCompletion_OR_PolynomialEPolynomialProductFiberedConditionalPhysicalLift
MAIN_ROUTE_H_NEEDED=false
MAIN_ROUTE_H_REQUEST=NONE
MAIN_ROUTE_H_TARGET=NONE
MAIN_ROUTE_H_BLOCKING=false
NEXT=Stage14-4gf
```

This batch starts from merged `Stage14-4fz..4gb` and publication-consumes merged `Stage14-q16`, merged `Stage14-s7-108..110`, merged `Stage14-t144..146` only through the integrated `Stage14-Work-byX37` no-cross-promotion boundary, and merged `Stage14-Work-byX37` itself.

## 4gc — restore the exact primitive representation before opening completion

Merged 4gb expresses the fixed-`E` two-sided survivor as an outer-product completion support.  Stage14-4gc pulls that Boolean back to the actual primitive pair

```text
(u,v) in D x V,
gcd(u,v)=1,
m=u*v.
```

The multiplication fiber over fixed `m` is at most `tau(m)=B^o(1)`.  Therefore the accepted primitive-pair support and accepted outer-product support have the same fixed-power exponent.  The completion deficit `delta_comp` and survival budget

```text
kappa-delta_comp>=mu
```

are unchanged; no multiplication saving is recharged.

## 4gd — exact reciprocal divisor/CRT completion system

On fixed `E=E0`, fixed ray `(x,y)` and fixed agreement pair `(U,V)`, the primitive pair fixes

```text
m=u*v,
h=d0*E0*m,
Xrec=h*x,
Yrec=h*y,
|Xr|=alpha*E0*u^2,
|Yr|=beta*E0*v^2.
```

The second reciprocal identity reduces to the already-frozen coefficient identity times `(uv)^2`; it is not a new pair-dependent selector.

The remaining bare reverse reconstruction is explicit:

```text
p|Xrec,
q|Yrec,
c=Xrec/p,
d=Yrec/q,
W1=4*r*s*epsilon_k*p*q,
F_-*F_+=W1,
F_+ + F_- == 0 (mod 2U),
F_+ - F_- == 0 (mod 2V).
```

The corresponding `a,b` are reconstructed by

```text
a=(F_++F_-)/(2U),
b=(F_+-F_-)/(2V).
```

All candidate layers are divisor-bounded, so each fixed primitive pair has only `B^o(1)` bare reciprocal candidates.  Every root-origin, canonical allocation/orientation and post-column condition not used in the bare system remains in one exact residual Boolean `R_post`.

The X13 column parameter is simultaneously restricted to the fixed square class

```text
M=M0*m^2.
```

## 4ge — nested reciprocal-support versus residual-completion ledger

Define

```text
T_rec={primitive pairs with at least one bare reciprocal divisor/CRT candidate},
#T_rec=B^(sigma_rec+o(1)),

delta_rec=kappa-sigma_rec,

delta_post=sigma_rec-tau_phys.
```

Then exactly

```text
delta_comp=delta_rec+delta_post,
heavy survival:
kappa-delta_rec-delta_post>=mu.
```

This is a nested-support identity, not an independence assumption.

In particular, on a near-threshold rectangle `kappa=mu+o(1)`, survival forces both

```text
delta_rec=o(1),
delta_post=o(1).
```

Thus an opaque completion deficit is replaced by two explicit mechanisms:

```text
(R) fixed-agreement radial-linear two-level divisor/CRT reciprocal solvability;
(P) conditional root-origin/canonical/post-column acceptance inside that support.
```

The resulting fixed-`E` two-sided receiver is

```text
FixedComplementaryDilationTwoSidedPrincipalRectangular
ReciprocalDivisorCRTSupportDeficitVersusConditionalRootCanonicalPostColumnCompletionDeficit
WithCapacityHeadroomKappaMinusMu.
```

This is a material receiver change, so the batch stops after three work units.

## H and scope locks

No new heavy H is opened yet.  The next internal stage must first test whether the explicit reciprocal selector itself already has full fixed-power exponent by a direct divisor-choice/CRT construction on the principal cells.  Only if that fails and leaves a stable theorem-shaped divisor-correlation receiver should an independent heavy H be frozen.

The existing non-heavy mainline H gates remain pending.  No q16 product-set result, s-route completion result, or fixed-U Gaussian-prime theorem is cross-promoted into the reciprocal completion support.

```text
COMPLETION_DEFICIT_EXACTLY_SPLITS_AS_DELTA_REC_PLUS_DELTA_POST=true
NEAR_THRESHOLD_SURVIVAL_FORCES_RECIPROCAL_DEFICIT_ZERO_AT_FIXED_POWER=true
NEAR_THRESHOLD_SURVIVAL_FORCES_POST_MASK_DEFICIT_ZERO_AT_FIXED_POWER=true
WORK_BYX37_REVISIT_TRIGGER_4GE_REACHED=true
NEW_HEAVY_MAIN_H_NEEDED=false
EXISTING_NONHEAVY_MAIN_H_GATES_PENDING=true
Q16_RESULT_RECHARGED=false
SAVING_CROSS_PROMOTABLE=false
```