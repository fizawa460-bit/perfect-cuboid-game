# Stage14-4 batch — 4eq through 4eu

Five substantive stages were completed from latest merged main

```text
34d855de880c68a6e04ca6db650a14e7fe802e93
```

which includes merged `Stage14-4el..4ep`, merged `Stage14-s7-78..80`, merged `Stage14-t115..117`, and merged `Stage14-q13`.

## Stage summary

`Stage14-4eq` consumes s7-80's heavy-ray split. For one fixed exact radial value `h`, the raw reciprocal pair `(X,Y)` is fixed. The second reciprocal difference-of-squares identity makes `(Xr,Yr,U,V)` divisor-many from fixed `W2=X^2-Y^2`; this reconstructs only `B^o(1)` possible `(U,V,M)` packets. Merged X13 can then be used legally in its proved direction, closing the fixed-`h` reverse-background fiber. Thus radial concentration is closed, while radial diffusion remains live.

`Stage14-4er` returns to the independent diffuse norm-factor branch and freezes the complementary quotient scale in

```text
N=X0^2+Y0^2=C*m.
```

It splits exactly into subpolynomial `m=B^o(1)` and genuinely polynomial `m=B^(lambda+o(1)), lambda>0` branches.

`Stage14-4es` closes the subpolynomial-`m` branch. Since `C=B^(kappa+o(1))` with `1/6<=kappa<=1/4`, there are at most `B^(1/4+o(1))` norm values `N=Cm`; merged 4ep gives `B^o(1)` physical fiber over `N`. Hence this branch is strictly sub-square-root.

`Stage14-4et` factors the remaining polynomial-quotient diffuse candidate in `Z[i]` as

```text
z=epsilon*alpha*beta,
N(alpha)=C,
N(beta)=m,
```

where `alpha` is selected by the physical common-core Gaussian root orientation. No coprimality of `C,m` is assumed. The factor pair has only `B^o(1)` reverse physical fiber; the remaining issue is the joint canonical-allocation / Gaussian-factor correlation.

`Stage14-4eu` freezes the new diffuse H target

```text
DiffusePolynomialComplementaryGaussianFactorCanonicalAllocationBilinearCorrelation.
```

Merged q13 supplies receiving-test literature leads but no direct theorem import.

## Current survivors

```text
LOW COMMON CORE:
  CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
  -> existing allocation H gate

POLYNOMIAL COMMON CORE / HEAVY RAY / RADIAL DIFFUSION:
  FixedPrimitiveReciprocalRayDiffuseRadialScalePhysicalIncidence
  -> internal route remains

POLYNOMIAL COMMON CORE / GENUINE MOVER:
  FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
  -> existing 4eo mover H gate

POLYNOMIAL COMMON CORE / DIFFUSE MODULUS / POLYNOMIAL m:
  DiffusePolynomialComplementaryGaussianFactorCanonicalAllocationBilinearCorrelation
  -> new 4eu diffuse H gate
```

Thus the whole mainline is not blocked by H; the heavy-ray radial-diffusion branch remains an internal target.

## H decisions

```text
MAINLINE_ALLOCATION_H_NEEDED=true
RECIPROCAL_MOVER_H_NEEDED=true
NEW_DIFFUSE_H_NEEDED=true
NEW_DIFFUSE_H_TARGET=DiffusePolynomialComplementaryGaussianFactorCanonicalAllocationBilinearCorrelation
ALL_CURRENT_MAINLINE_SURVIVORS_THEOREM_GATED=false
WHOLE_MAINLINE_BLOCKED_BY_H=false
NEXT_H_NEEDED=true
```

## Frozen batch boundary

```text
STAGE14_4_BATCH=COMPLETE_FIVE_STAGE_RUN
BATCH_START_MAIN_SHA=34d855de880c68a6e04ca6db650a14e7fe802e93
BATCH_PUBLICATION_MAIN_SHA=34d855de880c68a6e04ca6db650a14e7fe802e93
BATCH_FIRST_STAGE=Stage14-4eq
BATCH_LAST_STAGE=Stage14-4eu
BATCH_SUBSTANTIVE_STAGE_COUNT=5
BATCH_STOP_REASON=requested_stage_limit_reached_with_radial_diffusion_internal_survivor_and_new_diffuse_H_gate
MERGED_S7_78_80_CONSUMED=true
MERGED_T115_117_RECHECKED=true
T115_117_SAVING_CROSS_PROMOTED=false
MERGED_Q13_CONSUMED_AS_LITERATURE_RADAR=true
HEAVY_RAY_RADIAL_CONCENTRATION_BRANCH_CLOSED=true
HEAVY_RAY_RADIAL_DIFFUSION_BRANCH_RETAINED=true
DIFFUSE_SMALL_QUOTIENT_BRANCH_CLOSED=true
NEW_DIFFUSE_H_NEEDED=true
WHOLE_MAINLINE_BLOCKED_BY_H=false
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
NEXT=Stage14-4ev
```
