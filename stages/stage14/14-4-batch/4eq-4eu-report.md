# Stage14-4 batch — 4eq through 4eu

Five substantive mainline stages were completed from merged main

```text
2fd569305e0f8c1dc1acafe1c3b7a3c635aec0e4
```

which includes corrected merged `Stage14-4el..4ep`, merged `Stage14-s7-75..77`, merged `Stage14-t112..114`, and merged `Stage14-Work-bnX26`.

Publication recheck remained on the same latest main; no newer merged s/t/Work result superseded this batch.

## Stage summary

`Stage14-4eq` closes the heavy primitive-ray survivor.  For the opposite reciprocal raw products

```text
X=Q_xi+P_xi,
Y=Q_xi-P_xi,
```

the radial gcd divides `2*gcd(P_xi,Q_xi)`.  Merged cross-root structure and the square-root equality boundary give radial scale `B^o(1)`.  Fixed raw `(X,Y)` has divisor-many `(c,p,d,q)` factorizations; its fixed second reciprocal difference of squares gives divisor-many `(U,V,M)` data; merged X13 then reconstructs the full physical packet with `B^o(1)` multiplicity.  Hence

```text
GLOBAL_PRIMITIVE_RAY_MULTIPLICITY_BOUND=Bo1
```

and the heavy-ray quadratic energy mechanism is impossible.

`Stage14-4er` feeds the radial closure back into the diffuse branch.  The primitive reciprocal coordinates remain quarter-scale, so

```text
N=X0^2+Y0^2=B^(1/2+o(1)).
```

With equality-band common core

```text
C=B^(kappa+o(1)), 1/6<=kappa<=1/4,
```

the complementary quotient is forced to

```text
m=B^(1/2-kappa+o(1)), 1/4<=1/2-kappa<=1/3.
```

Thus the `m=B^o(1)` near-full-core escape is empty.

`Stage14-4es` sets `G=gcd(C,m)`.  On a fixed `G=B^(gamma+o(1))` stratum,

```text
G^2|N,
N=B^(1/2+o(1)),
```

so the charged-once norm-value count is at most

```text
B^(1/2-gamma+o(1)).
```

Every fixed-power overlap is strict sub-square-root; saturation requires `gcd(C,m)=B^o(1)`.

`Stage14-4et` peels that overlap and uses primitive sum-of-two-squares support plus the already-frozen common-core root orientation to factor

```text
z=X0+iY0=epsilon*alpha*beta,
N(alpha)=C,
N(beta)=m.
```

The product-to-physical fiber is `B^o(1)`.  The bare Gaussian bilinear ledger is exactly

```text
kappa+(1/2-kappa)=1/2,
```

so only the actual canonical physical correlation can save a power.

`Stage14-4eu` freezes the diffuse theorem target

```text
DiffuseCoprimePolynomialGaussianNormFactorCanonicalAllocationBilinearCorrelation
```

in `14-4eu/diffuse-h-target.md`.

## Current survivors

The heavy primitive-ray branch is closed.  The remaining arithmetic branches are exactly

```text
LOW COMMON CORE:
  CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
  -> existing mainline allocation H target

POLYNOMIAL COMMON CORE / CONCENTRATED GENUINE MOVER:
  FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
  -> existing 4eo mover H target

POLYNOMIAL COMMON CORE / DIFFUSE C:
  DiffuseCoprimePolynomialGaussianNormFactorCanonicalAllocationBilinearCorrelation
  -> new 4eu diffuse H target
```

Every current survivor is now theorem-gated.  No theorem result is assumed or cross-promoted in this batch.

## H decisions

```text
MAINLINE_ALLOCATION_H_NEEDED=true
MAINLINE_ALLOCATION_H_TARGET=CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity

RECIPROCAL_MOVER_H_NEEDED=true
RECIPROCAL_MOVER_H_TARGET=FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion

NEW_DIFFUSE_H_NEEDED=true
NEW_DIFFUSE_H_TARGET=DiffuseCoprimePolynomialGaussianNormFactorCanonicalAllocationBilinearCorrelation

ALL_CURRENT_MAINLINE_SURVIVORS_THEOREM_GATED=true
WHOLE_MAINLINE_BLOCKED_BY_H=true
```

A material new merged s/t/Work identity may reopen an internal route and must be publication-rechecked before any future H conclusion is consumed.

## Frozen batch boundary

```text
STAGE14_4_BATCH=COMPLETE_FIVE_STAGE_RUN
BATCH_START_MAIN_SHA=2fd569305e0f8c1dc1acafe1c3b7a3c635aec0e4
BATCH_PUBLICATION_MAIN_SHA=2fd569305e0f8c1dc1acafe1c3b7a3c635aec0e4
BATCH_FIRST_STAGE=Stage14-4eq
BATCH_LAST_STAGE=Stage14-4eu
BATCH_SUBSTANTIVE_STAGE_COUNT=5
BATCH_STOP_REASON=requested_stage_limit_reached_and_all_survivors_theorem_gated
HEAVY_PRIMITIVE_RAY_BRANCH_CLOSED=true
DIFFUSE_QUOTIENT_ALWAYS_POLYNOMIAL=true
DIFFUSE_SATURATION_REQUIRES_CORE_QUOTIENT_GCD=Bo1
DIFFUSE_GAUSSIAN_PRODUCT_FACTORIZATION_PROVED=true
NEW_DIFFUSE_H_NEEDED=true
ALL_CURRENT_MAINLINE_SURVIVORS_THEOREM_GATED=true
WHOLE_MAINLINE_BLOCKED_BY_H=true
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
NEXT_H_NEEDED=true
NEXT=Stage14-4ev
```
