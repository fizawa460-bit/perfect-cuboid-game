# Stage14-4 batch — 4el through 4ep

The requested approximately five-stage mainline batch completed five substantive stages from merged main

```text
c6c4136d21bc75bd14a92156d774c680feaa63bb
```

which includes merged `Stage14-4eg..4ek` and merged `Stage14-Work-bnX26`.

## Stage summary

`Stage14-4el` audits the concentrated exact-common-core projective energy. On a saturating polynomial-mass exact modulus, diagonal mass and exact rational-proportional primitive-vector mass are only `M_C B^o(1)`, while large centered discrepancy forces energy `M_C^2 B^(-o(1))`. Therefore genuine off-diagonal nonzero determinants are forced.

`Stage14-4em` writes every genuine collision uniquely as

```text
X1*Y2-X2*Y1=qC,
q!=0.
```

After one dyadic vector cell is frozen, `|q|<=B^o(1)RS/C`. The range `C>RS B^o(1)` is empty. The concentrated branch splits into near-maximal `RS/C=B^o(1)` and polynomially separated `RS/C>=B^(eta+o(1))` regimes.

`Stage14-4en` closes the near-maximal regime. For fixed primitive first vector and frozen `q,C`, the determinant equation is one affine integer line

```text
(X2,Y2)=qC(A,B)+t(X1,Y1),
X1*B-A*Y1=1.
```

A same-scale dyadic box contains only `O(1)` values of `t`; even after all charged finite decorations the collision degree is `B^o(1)`, too small to carry exponent-zero energy.

`Stage14-4eo` freezes the genuinely surviving concentrated receiver

```text
FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
```

with exact polynomial `C`, polynomial determinant quotient range, genuine off-diagonal pairs, and all canonical allocation / reciprocal physical weights retained. This branch now needs an independent theorem audit. The gate blocks only the concentrated subroute.

`Stage14-4ep` continues independently on the diffuse-modulus branch. Large positive centered discrepancy implies exponent-zero actual accepted incidences

```text
N=X0^2+Y0^2=C*m.
```

Divisor multiplicity, primitive two-square representation multiplicity, and reverse physical candidate multiplicity are all `B^o(1)`, so projection to `N` loses no power. The diffuse variable-modulus graph therefore contracts to

```text
DiffuseCanonicalAllocationPrimitiveGaussianNormFactorCorrelation,
```

with quotient scale `m=B^(lambda+o(1))` frozen for the next internal split.

## Current survivors

```text
LOW COMMON CORE:
  CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
  -> existing mainline H target from 4ef / Work-bnX26

POLYNOMIAL COMMON CORE, CONCENTRATED EXACT C:
  FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
  -> new 4eo auxiliary theorem target

POLYNOMIAL COMMON CORE, DIFFUSE C:
  DiffuseCanonicalAllocationPrimitiveGaussianNormFactorCorrelation
  -> internal quotient-scale split remains
```

No positive H result is consumed in this batch and no saving is cross-promoted from fixed-U work.

## H decisions

```text
MAINLINE_ALLOCATION_H_NEEDED=true
MAINLINE_ALLOCATION_H_TARGET=CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
NEW_RECIPROCAL_H_NEEDED=true
NEW_RECIPROCAL_H_TARGET=FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
NEW_RECIPROCAL_H_BLOCKING_CONCENTRATED_BRANCH=true
NEW_DIFFUSE_H_NEEDED=false
WHOLE_MAINLINE_BLOCKED_BY_H=false
```

The new reciprocal audit should test determinant/bilinear/character-dispersion/incidence theorems against the exact correlated physical weight at fixed polynomial `C`; unrestricted-box or unrelated-modulus results are advisory only.

## Frozen batch boundary

```text
STAGE14_4_BATCH=COMPLETE_FIVE_STAGE_RUN
BATCH_START_MAIN_SHA=c6c4136d21bc75bd14a92156d774c680feaa63bb
BATCH_FIRST_STAGE=Stage14-4el
BATCH_LAST_STAGE=Stage14-4ep
BATCH_SUBSTANTIVE_STAGE_COUNT=5
BATCH_STOP_REASON=requested_stage_limit_reached_with_new_concentrated_H_gate
MERGED_WORK_BNX26_CONSUMED=true
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
NEAR_MAXIMAL_CONCENTRATED_COLLISION_BRANCH_CLOSED=true
CONCENTRATED_SURVIVOR_IS_POLYNOMIAL_Q_OFFDIAGONAL_DISPERSION=true
DIFFUSE_SURVIVOR_IS_PRIMITIVE_GAUSSIAN_NORM_FACTOR_CORRELATION=true
MAINLINE_ALLOCATION_H_NEEDED=true
NEW_RECIPROCAL_H_NEEDED=true
NEW_DIFFUSE_H_NEEDED=false
NEXT_H_NEEDED=true
NEXT=Stage14-4eq
```
