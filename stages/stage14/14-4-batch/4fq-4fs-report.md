# Stage14-main-batch report — 4fq through 4fs

## Boundary

```text
STAGE14_MAIN_BATCH=COMPLETE
BATCH_START_MAIN_SHA=3af02c764300db002cce3e3bdf7da1236548ecbd
BATCH_PUBLICATION_MAIN_SHA=PENDING_RECHECK
BATCH_FIRST_STAGE=Stage14-4fq
BATCH_LAST_STAGE=Stage14-4fs
BATCH_SUBSTANTIVE_WORK_UNIT_COUNT=3
BATCH_SUBSTANTIVE_STAGE_COUNT=3
BATCH_INTEGRATED_H_UNITS=NONE
BATCH_STOP_REASON=receiver_change
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
CURRENT_MAIN_RECEIVER=ComplementaryDilationBareShortUnitaryShadowExponentVersusConditionalCanonicalReverseCompletionDeficitBudget_OR_CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity_OR_FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion_OR_DiffusePolynomialComplementaryGaussianFactorCanonicalAllocationBilinearCorrelation
MAIN_ROUTE_H_NEEDED=false
MAIN_ROUTE_H_REQUEST=NONE
MAIN_ROUTE_H_TARGET=NONE
MAIN_ROUTE_H_BLOCKING=false
NEXT=Stage14-4ft
```

## Result

This batch consumes merged mainline through `4fp`, merged `s7-96..98`, merged `Work-buX33`, and q14 only as its already-merged literature-routing boundary.

### 4fq — fixed-E bare shadow / completion ledger

For exact `E=E0`, the merged physical support predicate

```text
A_E0(m)=1
```

is nested inside the bare unitary shadow

```text
B_E0(m)=1{exists u||m in U_E0(m)}.
```

Writing

```text
S_bare=B^(sigma+o(1)),
S_phys=B^(tau+o(1)),
delta_c=sigma-tau,
```

a heavy survivor satisfies exactly

```text
sigma-delta_c=tau>=mu.
```

This prevents an illegal conclusion that the conditional physical-completion density must be exponent-zero.

### 4fr — polynomial-E bare shadows

The same decomposition is performed while retaining the complementary local mask `m_E(E)`.

Merged `s7-98` is consumed as a same-packet refinement:

- `m=B^o(1)`: freeze exact `(m0,u0)`, leaving one-dimensional outer coordinate `E`;
- polynomial `m`: retain the genuine two-dimensional outer pair `(E,m)`.

Both branches receive exact bare-support exponent / conditional-completion-deficit ledgers.

### 4fs — material receiver change

All live complementary-dilation branches now have the same nested-support budget

```text
bare short-unitary shadow exponent sigma_j
-
conditional canonical/reverse completion deficit delta_j
>= mu.
```

This separates two different possible saving mechanisms:

1. bare short-unitary witness sparsity;
2. conditional physical-completion sparsity among bare witnesses.

q14/Ford can only be compared to mechanism 1 on the fixed-E branch. It proves neither the required branch-exact power estimate there nor mechanism 2.

The heavy receiver materially changes to

```text
ComplementaryDilationBareShortUnitaryShadowExponentVersusConditionalCanonicalReverseCompletionDeficitBudget.
```

No new heavy H is opened. The next stage is `Stage14-4ft`, testing the exact fixed-E unitary-to-ordinary divisor shadow transfer while retaining the Stage14 exponent threshold.
