# Stage14-4eu — theorem gate for the diffuse coprime Gaussian bilinear correlation

## Status

`COMPLETE_DIFFUSE_COPRIME_GAUSSIAN_BILINEAR_CORRELATION_H_GATE`

Consumes batch-local `Stage14-4eq..4et`, merged `Stage14-4ep`, merged `Stage14-Work-bnX26`, and the existing H boundaries from merged 4ef and 4eo.

```text
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
```

## 1. Internal diffuse reductions are exhausted

The diffuse branch has now been reduced to

```text
z=alpha*beta in Z[i],
N(z)=B^(1/2+o(1)),
N(alpha)=C=B^(kappa+o(1)),
1/6<=kappa<=1/4,
N(beta)=m=B^(1/2-kappa+o(1)),
gcd(N(alpha),N(beta))=B^o(1),
```

with `alpha` carrying the frozen common-core root orientation and all canonical allocation / reciprocal physical masks retained in the actual candidate weight.

The following possible elementary gains are already discharged:

```text
heavy primitive-ray concentration,
radial scale freedom,
small complementary quotient,
fixed-power gcd(C,m),
Gaussian split-prime support,
finite representation/orientation fibers,
standalone divisor multiplicity.
```

The bare factor ledger remains exactly

```text
kappa+(1/2-kappa)=1/2.
```

Hence another divisor switch or unrestricted product count cannot improve the exponent.

## 2. Freeze the exact theorem target

The theorem-ready diffuse object is

```text
DiffuseCoprimePolynomialGaussianNormFactorCanonicalAllocationBilinearCorrelation.
```

The immutable contract is recorded in

```text
stages/stage14/14-4eu/diffuse-h-target.md.
```

A positive theorem must produce some fixed `delta>0` such that, uniformly on every frozen physical exponent packet, the actual diffuse physical mass satisfies schematically

```text
DiffuseMass(B)
 << B^(1/2-delta+o(1)),
```

or an equivalent bilinear/dispersion estimate strong enough to rule out exponent-zero occupancy of the correlated Gaussian product family.

The theorem must retain the actual canonical background and may not replace it by arbitrary independent Gaussian coefficients.

```text
NEW_DIFFUSE_H_NEEDED=true
NEW_DIFFUSE_H_TARGET=DiffuseCoprimePolynomialGaussianNormFactorCanonicalAllocationBilinearCorrelation
NEW_DIFFUSE_H_BLOCKING_DIFFUSE_BRANCH=true
```

## 3. Relation to the two existing mainline H gates

The low-common-core branch already has the independent theorem target

```text
CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
```

from merged 4ef / Work-bnX26.

The concentrated genuine-mover branch already has

```text
FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
```

from merged 4eo.

Stage14-4eq closes the only non-H concentrated alternative, the heavy primitive ray.  Stage14-4eu now puts the diffuse branch behind its own exact theorem gate.

Therefore every currently surviving arithmetic branch is theorem-gated:

```text
LOW C:
  three-divisor allocation correlation H;

CONCENTRATED POLYNOMIAL C:
  off-diagonal projective mover dispersion H;

DIFFUSE POLYNOMIAL C:
  coprime Gaussian bilinear correlation H.
```

```text
ALL_CURRENT_MAINLINE_SURVIVORS_THEOREM_GATED=true
WHOLE_MAINLINE_BLOCKED_BY_H=true
```

This blocking statement is conditional only on the current latest merged receiver.  A material new merged s/t/Work identity may reopen an internal route and must be checked before consuming any future H conclusion.

## 4. Required audit discipline

The diffuse H audit should test Gaussian bilinear/Type-II, dispersion, Hecke-character large sieve, norm-form divisor correlation, and related theorem families only under an exact mask-preserving adapter.

The following do not qualify by themselves:

```text
unrestricted Gaussian product counts,
independent coefficient Type-II estimates,
averaging over unrelated moduli,
ordinary split-prime density,
recharging gcd(C,m) sparsity,
recharging primitive sum-of-two-squares representation multiplicity.
```

A negative audit should identify the minimal missing coefficient/physical-background adapter or an explicit full-exponent countermechanism.

## Boundary

```text
STAGE14_4EU=COMPLETE_DIFFUSE_COPRIME_GAUSSIAN_BILINEAR_CORRELATION_H_GATE
HEAVY_PRIMITIVE_RAY_BRANCH_CLOSED=true
NEW_DIFFUSE_H_NEEDED=true
NEW_DIFFUSE_H_TARGET=DiffuseCoprimePolynomialGaussianNormFactorCanonicalAllocationBilinearCorrelation
NEW_DIFFUSE_H_BLOCKING_DIFFUSE_BRANCH=true
MAINLINE_ALLOCATION_H_NEEDED=true
MAINLINE_ALLOCATION_H_TARGET=CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity
RECIPROCAL_MOVER_H_NEEDED=true
RECIPROCAL_MOVER_H_TARGET=FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion
ALL_CURRENT_MAINLINE_SURVIVORS_THEOREM_GATED=true
WHOLE_MAINLINE_BLOCKED_BY_H=true
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
NEXT_H_NEEDED=true
NEXT=Stage14-4ev
```
