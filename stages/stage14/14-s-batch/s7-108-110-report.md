# Stage14-s-batch report — s7-108 through s7-110

```text
STAGE14_S_BATCH=COMPLETE
BATCH_START_MAIN_SHA=f9c3116fc82cacbcb494a055b40bb0daa825e19e
BATCH_PUBLICATION_MAIN_SHA=f9c3116fc82cacbcb494a055b40bb0daa825e19e
BATCH_FIRST_STAGE=Stage14-s7-108
BATCH_LAST_STAGE=Stage14-s7-110
BATCH_SUBSTANTIVE_WORK_UNIT_COUNT=3
BATCH_SUBSTANTIVE_STAGE_COUNT=3
BATCH_INTEGRATED_H_UNITS=NONE
BATCH_STOP_REASON=receiver_change
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
CURRENT_S_RECEIVER=FixedComplementaryDilationFixedPrimitiveEndpointOneDimensionalConditionalPhysicalCompletionSupport_OR_FixedComplementaryDilationTwoSidedPrincipalRectangularDistinctProductCapacityVersusConditionalPhysicalLiftDeficit_OR_PolynomialComplementaryDilationFixedPrimitiveProductOneDimensionalConditionalPhysicalCompletionSupport_OR_PolynomialComplementaryDilationPolynomialPrimitiveProductPrincipalFiberedDistinctProductOuterPairCapacityVersusConditionalPhysicalLiftDeficit_OR_CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity_OR_FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion_OR_DiffusePolynomialComplementaryGaussianFactorCanonicalAllocationBilinearCorrelation
S_ROUTE_H_NEEDED=false
NEXT=Stage14-s7-111
```

Consumes merged `s7-105..107`, merged mainline `4fw..4fy`, merged `Work-bxX36`, and q15 only as its already-merged routing boundary.

`Stage14-s7-108` first consumes the merged fixed-E supersession: the two-sided fixed-E branch is already the principal rectangular distinct-product receiver from 4fy and is not re-counted.  It then straightens the polynomial `(E,m)` ordinary-divisor envelope **fiberwise**.  On one root/exponent chart define

```text
D_E = Z_{>0} intersect sqrt(I_X/(alpha E)),
V_E = Z_{>0} intersect sqrt(I_Y/(beta E)),
E_K = {E:m_K(E)=1}.
```

Then exactly

```text
O_ord,pair(E,m)=1
<=>
(E,m) in P_fib,

P_fib={(E,dv):E in E_K,d in D_E,v in V_E},
```

and therefore

```text
#P_fib=sum_E #P(D_E,V_E).
```

The fixed-E rectangle is not silently transferred: `D_E,V_E` vary with E.

`Stage14-s7-109` localizes the fiber cardinalities at `B^o(1)` cost.  On one E-bin

```text
#E_*=B^(kappa_E+o(1)),
#D_E<=B^(kappa_D+o(1)),
#V_E<=B^(kappa_V+o(1)),
```

so the deterministic ambient capacity is

```text
#P_fib(E_*) <= B^(kappa_E+kappa_D+kappa_V+o(1)).
```

Every cell with `kappa_E+kappa_D+kappa_V<=mu-eta` is discharged before correlation machinery.  A survivor must have principal ambient capacity at least `B^(mu-o(1))`.

`Stage14-s7-110` writes on a principal fibered cell

```text
#P_fib(E_*)=B^(pi_fib+o(1)),
S_phys,fib=B^(tau_fib+o(1)),
delta_lift,fib=pi_fib-tau_fib,
```

with exact heavy-survival ledger

```text
pi_fib-delta_lift,fib>=mu.
```

Thus the polynomial-pair branch is reduced to two noninterchangeable mechanisms:

```text
P_fib: fiberwise distinct-product compression;
C_fib: conditional physical-lift deficit.
```

The material s receiver becomes

```text
FixedComplementaryDilationFixedPrimitiveEndpointOneDimensionalConditionalPhysicalCompletionSupport
OR
FixedComplementaryDilationTwoSidedPrincipalRectangularDistinctProductCapacityVersusConditionalPhysicalLiftDeficit
OR
PolynomialComplementaryDilationFixedPrimitiveProductOneDimensionalConditionalPhysicalCompletionSupport
OR
PolynomialComplementaryDilationPolynomialPrimitiveProductPrincipalFiberedDistinctProductOuterPairCapacityVersusConditionalPhysicalLiftDeficit.
```

No generic divisor density, multiplication-table saving, unitary/ordinary distortion, cross-E collision saving, fixed-fiber multiplicity, reverse-completion multiplicity, q15 near theorem, or fixed-U theorem saving is recharged.  No new `sH` is opened.  `s7-110` reaches the s component of merged Work-bxX36's normal revisit condition; the next s-local step is collision-energy / near-injective localization on the principal fibered product receiver.
