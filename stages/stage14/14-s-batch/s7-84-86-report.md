# Stage14-s-batch report — s7-84 through s7-86

```text
STAGE14_S_BATCH=COMPLETE
BATCH_START_MAIN_SHA=95e98cbd6626bc8f50a1397be881d04722b271ff
BATCH_PUBLICATION_MAIN_SHA=95e98cbd6626bc8f50a1397be881d04722b271ff
BATCH_FIRST_STAGE=Stage14-s7-84
BATCH_LAST_STAGE=Stage14-s7-86
BATCH_SUBSTANTIVE_WORK_UNIT_COUNT=3
BATCH_SUBSTANTIVE_STAGE_COUNT=3
BATCH_INTEGRATED_H_UNITS=NONE
BATCH_STOP_REASON=receiver_change
CURRENT_PHYSICAL_WHOLE_FAMILY_EXPONENT=1/2
STRICT_SUBSQRT_POWER_SAVING_PROVED=false
CURRENT_S_RECEIVER=FixedPrimitiveRayFixedAgreementPairShortRadialScaleMassCapacityGap_OR_CanonicalBalancedIntegerGaussianThreeDivisorCorrelationDensity_OR_FixedPolynomialCommonCoreCanonicalAllocationOffDiagonalProjectiveCollisionDispersion_OR_DiffusePolynomialComplementaryGaussianFactorCanonicalAllocationBilinearCorrelation
S_ROUTE_H_NEEDED=false
NEXT=Stage14-s7-87
```

This batch starts and publishes against the same latest merged main `95e98cbd6626bc8f50a1397be881d04722b271ff`, which already contains merged `s7-81..83`, merged mainline `4ey..4fa`, and merged `Work-bpX28`.

`Stage14-s7-84` consumes the newly available `4fa` agreement-pair compression. After freezing the divisor-many large-overlap data, the total `(U,V)` value support on one heavy ray is only `B^o(1)`. Therefore the polynomial factor mobility forced by s7-82 cannot live on the agreement side; it must occur in `Xr` or `Yr`. One exact agreement pair can be frozen at `B^o(1)` cost, leaving

```text
Xr*Yr=r0*h^2
```

with fixed rational `r0`.

`Stage14-s7-85` writes

```text
|Xr|=kappa_x*a^2,
|Yr|=kappa_y*b^2
```

and uses the fixed product squareclass to prove that, with

```text
J=gcd(kappa_x,kappa_y),
kappa_x=J*A,
kappa_y=J*B,
```

the noncommon split satisfies the exact fixed relation

```text
A*B=K_Z.
```

Thus `(A,B)` has only divisor-many choices and all polynomial kernel diffusion is a shared squarefree overlap `J`, not independent one-factor squarefree density.

`Stage14-s7-86` substitutes the root normal form back into the radial identity. Since `r0/K_Z` is a fixed rational square, there are fixed coprime positive integers `c0,d0` with

```text
d0*J*a*b=c0*h.
```

For one exact `h`, the possible `(J,a,b)` are bounded by `d_3(c0*h)=B^o(1)`. Hence the kernel-diffusion and fixed-kernel square-part branches of s7-83 are merely divisor-many descriptions of the same radial support. Merged 4fa then gives

```text
#h <= B^(1/24+o(1)).
```

The heavy-ray obstruction therefore contracts to

```text
FixedPrimitiveRayFixedAgreementPairShortRadialScaleMassCapacityGap.
```

No strict saving follows yet because the concentrated exact-`C` collision ledger provides only a required mass exponent `eta>0`; no merged uniform `eta>1/24` is known. No new `sH` is opened. The next internal task is to audit that mass exponent against the `1/24` support capacity or freeze the exact remaining atomic-capacity obstruction.

```text
PUBLICATION_MAIN_RECHECK_COMPLETE=true
NEW_MERGED_CONSUMER_AFTER_BATCH_START=false
```
