# Stage35-EX Goal4V — full Picard Galois-module source lock

## Exact surface identity inherited from Goal4U

Goal4U already certifies that the Stage35-EX projective cuboid surface in coordinates

`(h,x,y,z,q,p,w)`

is literally the Stoll/Testa cuboid surface under

`(a1,a2,a3,b1,b2,b3,c) = (h,x,y,z,q,p,w)`.

The four quadratic defining equations match after this substitution; this is not merely a birational comparison. The retained surface has the same 48 A1 singularities, so its minimal resolution is the same smooth surface `S` used below.

## Theoretical Picard-generation lock

Source: Michael Stoll and Damiano Testa, *The surface parametrizing cuboids*, arXiv:1009.0388v2 [math.AG], revised 2025-02-24.

Immutable version locator: `https://arxiv.org/abs/1009.0388v2`.

Load-bearing statements from v2:

- Theorem 2: the geometric Picard group of the minimal desingularization has rank 64, is generated by the explicitly listed known curves together with the exceptional divisors, and has intersection discriminant `-2^28`.
- Proposition 7: the 140 known divisor classes have intersection rank 64 and generate a finite-index subgroup.
- Theorem 8: that finite-index subgroup is saturated; the classes of the known curves generate the full geometric Picard group. The proof reduces the only possible saturation defect to 2-divisibility of the canonical/hyperplane class and excludes it using Lemma 5.
- In the proof of Theorem 8, all generating divisors are defined over `L = Q(i,sqrt(2))`; hence the absolute Galois action on the full Picard group factors through `Gal(L/Q) ~= C2 x C2`.

Goal4V uses Theorem 8 only for the integral saturation/full-generation step. It does not import Theorem 10 or any Brauer/H1 conclusion.

## Computational action lock

Pinned computational source:

- repository: `MichaelStollBayreuth/Verification`
- commit: `51233ed5ef2bf228fac9416c66db9adc0ebcaadd`
- path: `Cuboids/cuboids.magma`
- git blob: `0422b69847f2afb97cb7b3ed02ebef91279f61b1`

Stage33 retained exact transport:

- `stages/stage33/33-09/marked-picard-basis-source.json`
  - git blob `c9eb0e195e95263f6753fb29099ffa6d5d74dc13`
  - canonical sha256 `0a1863928608c2698051b4d22d0ac1b92128164825dbdb7edfb82fe941a05c8f`
- `stages/stage33/33-09/marked-picard-basis-bridge-certified.json`
  - git blob `77b16e2ee80c33af27f7a5a04e1c465e9fc1acea`
  - canonical sha256 `039e3792e950ac5bf94adf6538c229640da231000a5e1b159a80e2323a812a92`
- `stages/stage33/33-09/stage33-09-closure.json`
  - git blob `0e388cac1d5287ee5b2a3288c10153c16e99fb2a`
  - canonical sha256 `6c3ff8f7ca7d1bbd4084da0cc77ca6d43b31b32566a3bbb2c2103b7c2e9548b7`
- `stages/stage33/33-09/certify_stage33_09_exit.py`
  - git blob `efad25bf2e5d5550104150b83580b2ae2208b367`

The Stage33 certifier replays over `Z` the marked 64x64 bridge, inverse, determinant, Gram transport, and the integral actions named `cc`, `ct`, seven coordinate-sign actions, `swap12`, and `swap13`. Goal4V invokes that existing certifier runner-side; it does not expand the large certified bridge or retained encoded Picard payload into assistant context.

## Exact Goal4V consequence

Because Theorem 8 identifies the known divisor lattice with the full geometric Picard group, and because Goal4U identifies the Stage35-EX surface with that exact cuboid surface, the Stage33 marked integral basis is now a basis of the actual `Pic(S_bar)` for Stage35-EX.

Since all Picard generators are defined over `Q(i,sqrt(2))`, the full absolute Galois action factors through the biquadratic group generated by the two exact Stage33 integral matrices `cc` and `ct`. Therefore Goal4V may certify the full integral marked Picard Galois module.

## Credit firewall

Goal4V does **not** compute or claim:

- `H^1(Q, Pic(S_bar))`;
- the algebraic Brauer group;
- a nonconstant Stage35 Brauer class;
- a Brauer-Manin obstruction;
- E1;
- `R29-PESCH-E1` closure;
- Stage35 closure;
- perfect-cuboid existence or nonexistence.

Those remain separate downstream obligations.
