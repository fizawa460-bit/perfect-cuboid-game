# Stage35-EX Goal4BZ source lock — post-bridge-quartic fresh-route audit

Scope: consume Goal4BY and execute the Cycle Exploration Safety Protocol after the Goal4BT material receiver change. Goal4BU/BV/BW/BX/BY exhausted several quadratic/quartic reformulations of the now-source-known bridge reservoir `e`; Goal4BZ therefore runs a blind breadth pass, then targeted historical/Arsenal dedup, before choosing the next route. Mathematical authority remains V74 / Goal4AK. No merge.

## 1. Exact parent and material change

The retained parent before this audit is

```text
head = a55b0517af61650dc6d9c936d93af9ff18eec779
Goal4BY dedicated run = 34422378913 SUCCESS
```

Goal4BT changed the pre-Goal4BS parking information by proving that for every Master-Hit

```text
H = sqrt((V1*U2)^2+(U1*V2)^2)/(c*q),
e = gcd(c,H)
```

are source-computable before any E1-failure assumption. Goal4BU--BY then established:

- the three source-known quadratic reservoirs `cross,T,e` do not universally close by quadratic reciprocity;
- every odd `ell|e` has a source-selected Gaussian orientation and quartic datum;
- the E1 equations fix the relative local root but leave one residual Legendre bit;
- the product of residual bridge phases compresses to one Jacobi bit, not forced by the bridge-congruence layer;
- the exact integer factor graph source-fixes the `e12/e34` allocation, but its apparent quartic unit-character adapter reduces algebraically to the already-known source quartic datum.

Thus another quadratic/quartic character evaluation of the same bridge carrier is not a legal fresh route.

## 2. Blind rediscovery from the exact current receiver

Do not consult the old route names in this section. Work from a Master-Hit and the source-known quantities

```text
c,p,q,D=U1/c,T=U2/c,H,e=gcd(c,H).
```

The exact bridge identity under a hypothetical E1 counterexample is

```text
(q*H)^2 + (c*D*T)^2 = (p*w)^2.
```

After dividing by `e^2`, define the entirely source-computable coprime legs

```text
X = q*H/e,
Y = c*D*T/e,
gcd(X,Y)=1,
B_e = X^2+Y^2.                                      (BZ-Be)
```

A hypothetical E1 counterexample forces

```text
B_e = (p*w/e)^2.                                    (BZ-square)
```

The following distinct lenses arise.

### BZ-A — odd bridge-prime valuation parity of the source norm

For every odd source-known prime `ell|e`, the integer valuation

```text
nu_e(ell)=v_ell(B_e)                                (BZ-nu)
```

is source-computable. `(BZ-square)` forces

```text
nu_e(ell) is even.                                  (BZ-parity)
```

This is not merely the Goal4BT Legendre condition. There are genuine Master-Hits for which the quadratic bridge test passes while `(BZ-parity)` fails. Thus the valuation parity is a new source-only E1 kill predicate at the bridge support.

The next leaf can sharpen it using the exact cases

```text
v_ell(c) != v_ell(H)  => v_ell(B_e)=0,
```

so only the balanced source stratum

```text
v_ell(c)=v_ell(H)=v_ell(e)
```

can carry positive bridge-norm valuation. On that stratum the valuation is the `ell`-adic depth of one source-selected factor of `X^2+Y^2` after Hensel lifting a root of `-1`.

Classify: `LIVE`.

### BZ-B — bridge parameter-pair descent

The conditional bridge parameter pair `(alpha,beta)` and

```text
(alpha-beta)*(alpha+beta)=U1*U2/(c*e)
```

remain exact, but 35EX-09 already proved that no admissible size-decreasing successor preserving the Master-Hit and E1-counterexample structure has been constructed. Source-knowledge of `e` changes evaluability, not the missing successor map.

Classify: `BLOCKED_EXISTING_DESCENT_MAP_GAP`.

### BZ-C — finite-support S-unit/Thue completion

For a fixed hit the source support is finite, but 35EX-12 proved that the support varies with the Master-Hit and no fixed finite `S` or fixed finite Thue family is available. Goal4BT source-knowledge of `e` does not bound the primes of `c`, `T`, or the cross reservoir uniformly.

Classify: `BLOCKED_DYNAMIC_SUPPORT`.

### BZ-D — second Euclid / alternate norm completion

Trying to use the unspent second-Euclid completion separately reproduces the alternate-norm/index-swap system of 35EX-13 and its later Gaussian coupling route. It is not a new consequence of Goal4BT.

Classify: `EQUIVALENT_HISTORICAL_ALTERNATE_NORM_ROUTE`.

### BZ-E — global height / adelic realization

The Goal4BQ boundary-depth invariant still lacks the global rational-realization/height adapter frozen at Goal4BS. Nothing in Goal4BT--BY supplies it.

Classify: `BLOCKED_MISSING_GLOBAL_REALIZATION_ADAPTER`.

### BZ-F — another finite/deeper Gaussian character evaluation

Goal4BY already closes the early bridge quartic rewrite as source-redundant. Goal4BJ--BO separately close mechanical deeper `(1+i)`-ray character evaluation on the endpoint Gaussian carrier because detection lacks an independent source-fixed functional value.

Classify: `BLOCKED_OR_EQUIVALENT_CHARACTER_RECHARGE`.

## 3. Historical and Arsenal dedup after blind generation

Targeted repository discovery confirms:

- 35EX-10/11 work only at the quadratic residue/routing layer and do not state the source norm valuation parity `(BZ-parity)`;
- 35EX-12 is a dynamic-support S-unit/Thue blocker, not an odd bridge-prime valuation theorem;
- Goal4BO concerns the ramified prime `1+i` and an unbounded endpoint ray-character tower, not the varying odd split primes `ell|e` of the source bridge norm;
- Goal4BS parked the pre-Goal4BT state precisely because no new source-fixed E1 invariant was then available;
- bounded repository search found no retained Hensel/higher-adic bridge valuation weapon that subsumes `(BZ-parity)`.

Therefore BZ-A is not erased by historical dedup. It is a new local theorem species produced specifically by the Goal4BT source reclassification.

No Arsenal route is charged here: the active object is an exact source integer valuation, and no discovered card supplies or subsumes the needed odd-prime parity theorem. A search miss is not a repository-wide nonexistence statement; this classification is only the bounded active-leaf discovery result.

## 4. Nonredundancy witness already visible at the audit layer

Use the genuine Branch-L Master-Hit

```text
(a,b,m,n)=(13,4,96,91).
```

Its source data are

```text
(U1,V1,W1)=(153,104,185),
(U2,V2,W2)=(935,17472,17497),
c=17,p=1,q=104,
H=1513,e=17,D=9,T=55.
```

Goal4BU already records that the three source-channel quadratic predicates are simultaneously admissible. In particular

```text
Legendre(p*q,17)=+1.
```

But

```text
X=q*H/e=9256,
Y=c*D*T/e=495,
B_e=9256^2+495^2=85918561,
v_17(B_e)=1.                                        (BZ-witness)
```

Hence this Master-Hit is excluded from being an E1 counterexample by `(BZ-parity)` even though its bridge quadratic test passes. The valuation route therefore adds a genuine new per-hit pruning predicate beyond Goal4BT/BU.

This witness does not prove a universal E1 theorem.

## 5. Candidate ledger

```text
LIVE:
  SOURCE_BRIDGE_NORM_ODD_VALUATION_PARITY

UNTESTED:
  SOURCE_BRIDGE_NORM_HIGHER_ADIC_LEADING_UNIT_AFTER_PARITY

BLOCKED:
  BRIDGE_PARAMETER_ADMISSIBLE_DESCENT_MAP
  FIXED_S_SUNIT_THUE_COMPLETION
  BQ_GLOBAL_RATIONAL_REALIZATION_HEIGHT_ADAPTER
  ELLIPTIC_HEIGHT_DISCRIMINANT_QUANTITATIVE_ADAPTER

EQUIVALENT_OR_DOMINATED:
  ANOTHER_BRIDGE_QUADRATIC_OR_QUARTIC_CHARACTER_REWRITE -> Goal4BU--BY
  DEEPER_RAMIFIED_GAUSSIAN_CHARACTER_TOWER -> Goal4BJ--BO
  SECOND_EUCLID_ALTERNATE_NORM_REENTRY -> 35EX-13 and descendants
```

Only one route is immediately actionable, so no split is triggered.

## 6. Selected next gate

Select

```text
35EX-35_GOAL4CA_SOURCE_BRIDGE_NORM_VALUATION_PARITY_PREFLIGHT
```

Required outputs:

1. prove the exact equivalence between the source bridge norm `B_e` and the E1 square target up to a source rational square scale;
2. prove `gcd(X,Y)=1` unconditionally for a Master-Hit;
3. prove the unbalanced valuation theorem `v_ell(c)!=v_ell(H) => v_ell(B_e)=0` for odd `ell|e`;
4. on the balanced stratum, express `v_ell(B_e)` as a one-sided Hensel/Gaussian valuation of a source-fixed factor;
5. certify the odd-valuation source-only kill predicate;
6. exhibit nonredundancy against Goal4BT/BU in both 2-adic branches if available;
7. do not promote bounded regression evidence to a universal theorem.

## 7. Cycle exit

```text
CYCLE_ROUTE_STATUS=PASS_NEW_GATE_FROM_STRONGER_VIEW
CYCLE_ACTIVE_RECEIVER=SOURCE_BRIDGE_NORM_B_e_AT_ODD_PRIMES_OF_e
CYCLE_LIVE_CANDIDATES=1
CYCLE_UNTESTED_CANDIDATES=1
CYCLE_EXHAUSTIVE_VIEW_AUDIT=true
CYCLE_BLIND_REDISCOVERY=true
CYCLE_SPLIT_TRIGGERED=false
CYCLE_PARKING_AUDIT_COMPLETE=false
CYCLE_NEW_VIEW=SOURCE_BRIDGE_NORM_ODD_VALUATION_PARITY
CYCLE_NEW_VIEW_SOURCE=BLIND_PLUS_INTERNAL_DERIVATION
```

No merge. No hostile-audit credit. No E1, Stage35, endpoint, or Perfect Cuboid credit. MAIN-STATE remains V74 / Goal4AK.