# Stage35-EX Goal4BP source lock — post-Gaussian ray-tower fresh-route audit

Scope: execute the Cycle Exploration Safety Protocol after Goal4BO fail-closes mechanical deeper Gaussian ray characters. This is a breadth audit, not theorem closure. Audited authority remains V74 / Goal4AK; Goal4AL–Goal4BO remain provisional stacked leaves.

## 1. Exact current information entering the blind pass

Goal4AT–Goal4BO have established, provisionally but exact-head checked through the current parent, the following route boundaries:

- the physical marked Kummer classes have exact cross-face relations but no branch pruning;
- the joint three-marked receiver is endpoint-equivalent;
- canonical-height contradiction is blocked by missing quantitative adapters;
- the six-norm torus is an endpoint reparameterization;
- the classical derived cuboid is a three-face involution, not a universal strict full-endpoint descent;
- finite Brauer subgroups are all nonobstructing, while the full infinite unit-character layer is endpoint-equivalent;
- Gaussian reservoir orientation can be globalized, but finite and deeper ramified character evaluation supplies no source-fixed value; increasing conductor gives an unbounded ray tower and is fail-closed as a standalone strategy.

Therefore the blind pass forbids as a new route any proposal whose only new datum is another character evaluation of the existing Gaussian carrier `Xi`.

## 2. Blind rediscovery before Arsenal

Working only from the exact E1 counterexample population and the above route boundaries, the following materially distinct lenses remain natural.

### BP-A — boundary-escape conductor depth

Goal4BB constructs every finite Brauer-compatible source-marked adelic population inside simultaneous local-constancy neighborhoods of the rational anchor

```text
P*=(272/225,0,353/225,1,272/225,353/225),
```

which lies in `U(Q)` and misses `U_PC` only on the boundary coordinate

```text
y=0.
```

At the real place the source-marked points use `y>0` sufficiently small, and at `2` they use `v2(y)>4`. Goal4AQ proves that imposing the **full** visible unit-character layer reconstructs a single rational tuple and is equivalent to the positive source-marked endpoint population.

Thus the exact finite/full gap is naturally a noncompactness question: finite character packets can remain in punctured neighborhoods of the fixed divisor `y=0`, whereas full character orthogonality removes that freedom by reconstructing rational unit values.

The missing object is an explicit function

```text
D(F)=minimum required boundary depth v2(y)
```

(or an equivalent local modulus) for a controlled increasing family of finite visible characters, together with a theorem relating `D(F)` to any global rational-height realization. This is not another evaluation of `Xi`; it is a source-fixed boundary-depth invariant attached to the fixed rational divisor `y=0`.

### BP-B — derived fourth-square defect

Goal4AY leaves the explicit derived space-square candidate

```text
Q_D=(x*a*b)^2+(y*a*c)^2+(z*b*c)^2
```

untested as an arithmetic defect. The completion identity

```text
(r_AB*r_AC*r_BC)^2+(x*y*z*a*b*c)^2=W^2*Q_D
```

shows that `[Q_D]` is a source-fixed sum-of-two-squares squareclass. This is genuinely distinct from the Gaussian orientation carrier and remains `UNTESTED`.

### BP-C — explicit height completion

Goal4AW remains blocked by the absence of a numerical upper-height constant, a lower discriminant-growth adapter, and a uniform Szpiro window. Goal4AT–Goal4BO do not supply those three missing adapters. Retain this as `BLOCKED`, not erased.

### BP-D — new counting/amplification

Goal4AZ proves the required exponent threshold `rho/kappa>1/2` and shows the currently available source maps do not meet it. No new amplifier is exposed by Goal4AT–Goal4BO. This remains `BLOCKED_NO_NEW_AMPLIFIER`.

## 3. Arsenal dedup after the blind pass

Repository asset discovery was run only after the missing objects above were identified.

- `S34-W01 SUCCESSIVE_EXACT_FACTOR_SQUARECLASS_DESCENT` is relevant to BP-B only **after** a bounded exact factorization/gcd-support theorem for `Q_D` is materialized. That trigger is not yet satisfied; the card grants no finite branch family now.
- No formal Arsenal card directly supplies the BP-A boundary-depth/finite-intersection-to-height adapter.
- Existing counting selectors may become relevant only after a global-height realization of boundary depth is proved; they do not construct it.

Hence BP-A survives Arsenal dedup as a genuinely new live route, while BP-B remains a distinct untested backup.

## 4. Exact ledger classification

```text
LIVE:
  Y_ZERO_BOUNDARY_ESCAPE_CONDUCTOR_DEPTH

UNTESTED:
  DERIVED_FOURTH_SQUARE_DEFECT_TWO_CYCLE

BLOCKED:
  EXPLICIT_HEIGHT_LOWER_VS_UPPER_CONSTANT_COMPLETION
  SUPER_SQRT_AMPLIFICATION_WITH_CURRENT_MAPS
  MECHANICAL_DEEPER_GAUSSIAN_RAY_CHARACTER_TOWER

EQUIVALENT_OR_DOMINATED:
  SIMULTANEOUS_THREE_MARKED_RECEIVER -> endpoint-equivalent by Goal4BD
  TORIC_SIX_NORM_COORDINATES -> endpoint reparameterization by Goal4AX/AQ
  FINITE_BRAUER_SHORTCUT -> nonobstructing by Goal4BB
  GAUSSIAN_FINITE/DEEP_CHARACTER_EVALUATION -> carrier evaluation without source-fixed value by Goal4BJ–Goal4BO
```

No split is triggered: BP-A is immediately actionable and BP-B does not require BP-A, but parallelization is not justified before the cheaper boundary-depth diagnostic is tested.

## 5. Selected next gate

Select

```text
35EX-35_GOAL4BQ_Y_ZERO_BOUNDARY_ESCAPE_CONDUCTOR_DEPTH_PREFLIGHT
```

Required outputs:

1. choose an explicit increasing finite family of visible global characters from Goal4AQ, preferably conductor-controlled and supported at `2` first;
2. compute the exact local congruence neighborhood of `P*` required to preserve all evaluations in that finite family;
3. express this neighborhood as a lower bound on `v2(y)` or an equivalent boundary parameter;
4. determine whether the required depth grows with character conductor or stabilizes;
5. if it grows, test whether any existing source/global reconstruction converts that depth into a rational height lower bound; if no such adapter exists, freeze that exact blocker rather than claiming compactness or endpoint existence.

Do not use Tychonoff/compactness heuristics as theorem credit without verifying that the relevant local population is closed/compact after removing the boundary divisor and that every evaluation set is closed on the chosen compactification.

## 6. Cycle exit

```text
CYCLE_ROUTE_STATUS=PASS_NEW_GATE_FROM_STRONGER_VIEW
CYCLE_ACTIVE_RECEIVER=SOURCE_MARKED_ADELIC_POPULATION_NEAR_FIXED_Y_ZERO_BOUNDARY_ANCHOR
CYCLE_LIVE_CANDIDATES=1
CYCLE_UNTESTED_CANDIDATES=1
CYCLE_EXHAUSTIVE_VIEW_AUDIT=true
CYCLE_BLIND_REDISCOVERY=true
CYCLE_SPLIT_TRIGGERED=false
CYCLE_PARKING_AUDIT_COMPLETE=false
CYCLE_NEW_VIEW=Y_ZERO_BOUNDARY_ESCAPE_CONDUCTOR_DEPTH
CYCLE_NEW_VIEW_SOURCE=BLIND
```

No merge. No hostile-audit credit. No E1, Stage35, endpoint, or Perfect Cuboid credit. MAIN-STATE remains V74 / Goal4AK.
