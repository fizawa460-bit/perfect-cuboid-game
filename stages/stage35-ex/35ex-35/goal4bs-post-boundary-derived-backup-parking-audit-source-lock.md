# Stage35-EX Goal4BS source lock — post-boundary/derived parking audit and intermediate hostile-audit checkpoint

Scope: execute the Cycle Exploration Safety Protocol after Goal4BQ and Goal4BR, and obey the long-lived shared-PR checkpoint rule in `docs/research-os/policies/hostile-audit-and-freshness.md`. Audited authority remains V74 / Goal4AK. Goal4BS is a consolidation/parking leaf, not E1 closure and not a hostile audit performed by this batch.

## 1. Exact parent state

Goal4BQ gives a genuine source-fixed boundary escape invariant:

```text
D(n)=n
```

for the selected 2-primary visible-character filtration near the fixed removed divisor `y=0`. A rational realization would satisfy `H(y)>=2^n`, but no retained theorem realizes the finite adelic packets by rational endpoints with controlled height.

Goal4BR gives the exact derived fourth-square defect class

```text
[Q_D]=[p^2+r^2],
gcd(p,r)=1,
p odd,
r even,
```

with squarefree support only at odd primes `1 mod 4`, and the exact reservoir swap

```text
(h_a,h_b,h_c) <-> (j_x,j_y,j_z)
```

under the derived involution. But the original perfect endpoint does not require `Q_D` to be a square, so this is not a legal exclusion receiver and does not trigger S34-W01.

Thus the Goal4BP selected route and its distinct backup both have exact named blockers.

## 2. Blind breadth pass after BQ/BR

Before consulting historical route names, the following structurally distinct attacks remain natural from the E1 target itself:

1. re-normalize the Pesch `g0` gcd and compare the Master and hypothetical E1 primitive Pythagorean triples;
2. use the shared raw leg to form the third right triangle between the Master hypotenuse, the hypothetical E1 hypotenuse, and `U1*U2`;
3. turn the BQ boundary depth into a global height/counting contradiction;
4. force arithmetic information from the BR derived fourth-square defect;
5. return to a Brauer/Gaussian character functional;
6. return to elliptic height/discriminant or amplification.

No proposal is counted as new merely because it uses different notation.

## 3. Targeted historical/Arsenal dedup

### 3.1 Pesch-native gcd/factor graph is already the initial Stage35-EX route

35EX-02 already defines

```text
c=gcd(U1,U2),
p=gcd(W1,V2),
q=gcd(V1,V2),
```

and proves exactly

```text
g0=c*p,
h=c*q,
gcd(c,p)=gcd(c,q)=gcd(p,q)=1,
```

plus the Master-Hit dichotomy `v2(V1)!=v2(V2)` and the primitive E1 normalization.

35EX-03 then puts a hypothetical E1 failure and the Master condition into two simultaneous primitive Pythagorean triples. 35EX-04 strips the cross-equations to a coprime product rectangle. 35EX-05 reaches a four-bilinear-factor product-square receiver. 35EX-06 derives its exact pairwise gcd/2-adic support and records that live parameter-dependent reservoirs remain.

Therefore the apparently fresh direct cross-gcd factor graph is

```text
HISTORICAL_EQUIVALENT_TO_35EX_02_THROUGH_06,
```

not a new post-Goal4BR route.

### 3.2 Shared-hypotenuse triangle is endpoint-equivalent

Let `S^2=M` be the Master/third-face square and hypothetically let `T^2=f1` be the E1 square. The Stage29 exact Pesch crosswalk identifies `M` with the third face radicand and `f1` with the space-diagonal norm. Hence

```text
T^2-S^2=(U1*U2)^2
```

is exactly the physical right triangle consisting of the odd edge `e=U1*U2`, the opposite face diagonal `S`, and the space diagonal `T`. Reparametrizing this triangle adds no condition beyond the E1 counterexample endpoint itself.

Classify:

```text
ENDPOINT_EQUIVALENT_NOT_NEW.
```

### 3.3 Boundary depth to height

Goal4BQ supplies the exact local depth and a conditional rational-height consequence. The missing implication is not another local calculation; it is a global rational realization/approximation theorem preserving the source-marked population and a usable height relation.

Classify:

```text
BLOCKED_MISSING_GLOBAL_RATIONAL_REALIZATION_HEIGHT_ADAPTER.
```

### 3.4 Derived defect

Goal4BR proves the strongest unconditional squareclass information currently available on `Q_D`, but `Q_D` square is not an original-endpoint condition. Further squareclass descent on the extra equation would narrow the population without an adapter.

Classify:

```text
BLOCKED_NONREQUIRED_DERIVED_SQUARE_CONDITION.
```

### 3.5 Brauer/Gaussian/ray re-entry

Goal4BB shows every finite Brauer subgroup is nonobstructing; Goal4AQ/AR identify the full infinite route with the endpoint problem; Goal4BJ--BO globalize the Gaussian orientation carrier and show that deeper character detection lacks a source-fixed functional value and becomes an unbounded ray tower.

Classify:

```text
BLOCKED_OR_ENDPOINT_EQUIVALENT_ALREADY_TESTED.
```

### 3.6 Height/discriminant and amplification

Goal4AW freezes the exact missing quantitative adapters for the elliptic-height route. Goal4AZ freezes the amplification threshold and shows no retained map reaches it. The Peschmann material source note independently records that the proposed general discriminant route stalls because the discriminant is not sufficiently factor-controllable.

Classify:

```text
BLOCKED_NO_NEW_ADAPTER.
```

### 3.7 Arsenal

`S34-W01` remains a router only when a required square receiver admits a finite exhaustive squareclass family with exact support control. Goal4BR does not satisfy the required-square hypothesis, while 35EX-06 already records live parameter-dependent reservoirs in the historical Pesch-native four-factor route. No formal Arsenal card supplies the missing BQ rational-realization/height adapter.

No Arsenal weapon closes the current E1 target from the retained data.

## 4. Parking ledger

```text
LIVE:
  none

UNTESTED_AND_ACTIONABLE_WITH_CURRENT_RETAINED_INPUTS:
  none

BLOCKED:
  BQ boundary-depth -> rational endpoint/height
  BR derived defect -> required-square adapter absent
  AW elliptic height/discriminant quantitative adapters
  AZ amplification with current maps
  BO deeper Gaussian ray tower without source-fixed value

EQUIVALENT/HISTORICAL:
  Pesch canonical gcd/factor graph -> 35EX-02..06
  shared Master/E1 hypotenuse triangle -> physical endpoint triangle
  finite/full Brauer and toric/norm reformulations -> previously exact-tested endpoint-equivalent routes
```

The cycle is therefore parkable under the repository protocol. This is not a proof that no future method exists. It records that the generated natural candidate set has no unprocessed live route from the currently retained inputs.

Exact missing objects retained for a future reopen are:

```text
A. GLOBAL_RATIONAL_REALIZATION_WITH_HEIGHT_CONTROL:
   a theorem taking the conductor-controlled BQ local packet to a source-marked rational endpoint (or an equivalent global object) while retaining a quantitative height relation;

B. NEW_SOURCE_FIXED_E1_INVARIANT:
   an invariant not equivalent to the endpoint equations, the historical 35EX-02..06 factor graph, the flexible Gaussian carrier, or the nonrequired derived defect squareclass;

C. QUANTITATIVE_HEIGHT_DISCRIMINANT_ADAPTER:
   the exact constants/uniformity frozen by Goal4AW, if a genuinely stronger theorem becomes available.
```

## 5. Long-lived PR checkpoint

PR #1723 has entered the repository warning zone for unaudited retained growth (roughly 90 commits since its retained starting boundary). By `docs/research-os/policies/hostile-audit-and-freshness.md`, the next retained consolidation checkpoint must be used as an intermediate hostile-audit boundary before further substantive retained research is appended toward/through the 100-commit threshold.

Goal4BS therefore freezes its exact-green head, once CI succeeds, as

```text
INTERMEDIATE_HOSTILE_AUDIT_CHECKPOINT=true.
```

This batch does **not** self-award hostile-audit PASS. After the checkpoint is frozen, #1723 must not receive further substantive retained mathematical leaves until an independent intermediate hostile audit result is known. Scratch-only exploration may continue off the shared PR if separately authorized by the Stage contract.

## 6. Cycle exit

```text
CYCLE_ROUTE_STATUS=BLOCKED_NO_NEW_INFORMATION
CYCLE_ACTIVE_RECEIVER=PESCH_E1_MASTER_HIT_GLOBAL_TARGET
CYCLE_LIVE_CANDIDATES=0
CYCLE_UNTESTED_CANDIDATES=0
CYCLE_EXHAUSTIVE_VIEW_AUDIT=true
CYCLE_BLIND_REDISCOVERY=true
CYCLE_SPLIT_TRIGGERED=false
CYCLE_PARKING_AUDIT_COMPLETE=true
INTERMEDIATE_HOSTILE_AUDIT_CHECKPOINT=true
```

No merge. No hostile-audit credit. No E1, Stage35, endpoint, or Perfect Cuboid credit. MAIN-STATE remains V74 / Goal4AK.
