# Stage35-EX MAIN batch handoff — Goal4BC post-ledger fresh-view audit

Audited authority remains **V74 / Goal4AK** (hostile review `5142248509`). Goal4AL–Goal4BC are provisional stacked research leaves on PR #1723. No E1, Stage35, endpoint, or Perfect Cuboid credit is promoted here.

## Exact-green parent

Goal4BB is exact-green:

- exact head: `72f9163b39041e2b5c13d8e511b534b018015ef8`
- aggregate: `34303063969`
- `verify-stage35-ex-current`: `102315193038`
- result: `SUCCESS`

Goal4BB proves `(A_src^loc)^F != empty` for every finite `F subset Br(U)`. Thus no finite Brauer subgroup, including arbitrary fixed transcendental classes, can by itself obstruct the source-local adelic population. The full infinite Brauer intersection remains endpoint-equivalent by Goal4AR.

## Goal4BC provisional exact route audit

Every lens generated by Goal4AS has now been exact-tested:

```text
marked Kummer/common-cover -> AT/AU/AV
canonical height -> AW
cross-face norm/torsor -> AX
nonlinear full-endpoint descent -> AY
super-sqrt amplification -> AZ
finite Brauer shortcut -> BA/BB
linked Selmer/Cassels common-cover boundary -> AU/AV
```

A new blind pass was executed before Arsenal consultation. New lenses generated:

```text
B1 simultaneous three-marked rank-jump fiber-product receiver
B2 gcd-reservoir quadratic-reciprocity cycle
B3 finite-conductor boundary escape versus height
B4 derived fourth-square defect two-cycle
B5 toric visible-unit coordinates plus source integrality
```

After the missing object was identified as a joint three-marked elliptic receiver plus an exact physical receiver condition, repository discovery followed the policy → Arsenal index → one matching card sequence. Formal `S34-W03` (`RECEIVER_RESTRICTED_INTERSECTION_EXCLUSION`) is the matching procedure, but it does not construct the missing receiver and grants no present closure.

Selected new view:

```text
SIMULTANEOUS_THREE_MARKED_RANKJUMP_FIBER_PRODUCT_RECEIVER
```

This is materially different from AU/AV: those leaves couple the three residual Kummer squareclasses/common coefficient torsor, whereas no existing leaf materializes the full joint algebraic image of the three cyclic Goal4L marked elliptic points together with their common physical-endpoint compatibility.

Priority after deduplication:

```text
1. simultaneous three-marked rank-jump fiber-product receiver
2. gcd-reservoir quadratic-reciprocity cycle
3. finite-conductor boundary escape versus height
4. derived fourth-square defect two-cycle
5. toric visible-unit coordinates without new integral invariant -> park equivalent
```

Artifacts:

- `stages/stage35-ex/35ex-35/goal4bc-post-goal4as-ledger-exhaustion-fresh-view-audit-source-lock.md`
- `stages/stage35-ex/35ex-35/goal4bc-post-goal4as-ledger-exhaustion-fresh-view-audit.json`
- `stages/stage35-ex/verify_stage35_ex_35_goal4bc_post_ledger_fresh_view_audit.py`

## Freshness

Current main observed during the parent batch: `9346a1b0cfdb6f2e8abe93b4aef9987698eeeb47`. Main-side drift from the audited merge base is Stage32 / Stage36 only; no Stage35-EX mathematical source drift was observed. PR #1723 may remain non-mergeable due branch divergence; no rebase/sync or freshness credit is taken for this provisional leaf.

## Next exact leaf

```text
35EX-35_GOAL4BD_SIMULTANEOUS_THREE_MARKED_RANKJUMP_FIBER_PRODUCT_PREFLIGHT
```

Required: put all three cyclic Goal4L maps in one convention, derive the joint image/fiber-product equations and exceptional loci, compute generic dimension relative to the physical endpoint surface, and determine whether the common physical compatibility gives a genuinely smaller receiver rather than another birational reparameterization. Apply `S34-W03` only if an actual extra receiver condition is materialized.

No merge. No hostile-audit credit. `MAIN-STATE.json` remains V74 / Goal4AK.
