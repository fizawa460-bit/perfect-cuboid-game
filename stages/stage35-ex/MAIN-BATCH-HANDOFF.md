# Stage35-EX MAIN batch handoff — Goal4BO deeper lambda8 ray tower boundary

Audited authority remains **V74 / Goal4AK** (hostile review `5142248509`). Goal4AL–Goal4BO are provisional stacked leaves on PR #1723. No E1, Stage35, endpoint, or Perfect Cuboid credit is promoted.

## Exact-green parent

Goal4BN is exact-green:

- exact head: `2ef61ba9d0919e9bdef33f1971c48650b5f8915a`
- aggregate: `34317632854`
- `verify-stage35-ex-current`: `102358457114`
- result: `SUCCESS`

Goal4BN proved the common-`W` norm coupling and supplied a full `41`-adic face-plus-space witness showing that the same selected primary prime `5+4*i` supports both secondary sigma signs. Thus the lambda7 order-two ray class is not removed by the common space diagonal.

## Goal4BO provisional exact result

Put

```text
lambda=1+i,
pi=5+4*i.
```

Then exactly

```text
pi=1-lambda^5,
v_lambda(pi^(2^k)-1)=5+2*k.
```

Hence for every `n>=6`,

```text
ord_{lambda^n}(pi)=2^ceil((n-5)/2).
```

In particular:

```text
lambda7: order 2,
lambda8: order 4,
lambda9: order 4,
lambda10: order 8,
lambda11: order 8.
```

The primary ray group modulo `lambda^8=16` has 32 classes and exact structure

```text
C8 x C4,
```

with generators

```text
g=3+2*i,
h=1+4*i.
```

The previously blind prime satisfies

```text
pi=g^2*h^2,
pi^(-1)=g^6*h^2.
```

Thus the ray character

```text
chi_8(g)=zeta_8,
chi_8(h)=1
```

detects orientation:

```text
chi_8(pi)=i,
chi_8(pi^(-1))=-i.
```

So Goal4BM's blindness was finite-conductor only. However Goal4BN already shows that the source permits both local sigma signs at the same selected prime, and no retained face/space identity fixes an independent value of `chi_8(Xi)`.

Moreover the ray order of `pi` is unbounded with conductor. Every two deeper powers of `lambda` expose another binary principal-unit layer. Therefore repeatedly raising the conductor is an unbounded character tower, not a closing argument, unless a new source theorem first fixes or bounds the deeper ray class of `Xi`.

Exact route status:

```text
lambda8 orientation detection = yes;
source-fixed lambda8 character value = no;
deep ray order unbounded = yes;
mechanical deeper-conductor strategy fail-closed = yes;
new reciprocity equation = no;
branch pruning = no.
```

Artifacts:

- `stages/stage35-ex/35ex-35/goal4bo-deeper-lambda8-order-lift-source-lock.md`
- `stages/stage35-ex/35ex-35/goal4bo-deeper-lambda8-order-lift.json`
- `stages/stage35-ex/verify_stage35_ex_35_goal4bo_deeper_lambda8_order_lift.py`

## Next exact leaf

```text
35EX-35_GOAL4BP_POST_GAUSSIAN_RAY_TOWER_FRESH_ROUTE_AUDIT
```

Stop increasing ramified conductor. Re-audit the post-Goal4AS candidate ledger using Goal4AT–Goal4BO, and admit only candidates that introduce a genuinely new source-fixed invariant rather than another evaluation of the already-flexible Gaussian orientation carrier.

No merge. No hostile-audit credit. `MAIN-STATE.json` remains V74 / Goal4AK.
