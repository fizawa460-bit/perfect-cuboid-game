# Stage35-EX MAIN batch handoff — Goal4AT provisional marked-Kummer gcd reservoir

Audited authority remains **V74 / Goal4AK** (hostile review `5142248509`). Goal4AL–Goal4AT remain a provisional stacked research surface on PR #1723 and grant no E1, Stage35, endpoint, or Perfect Cuboid credit.

## Current retained frontier

PR: `#1723`
Branch: `stage35-ex-goal4ak-audited-local-evaluation`

Parent exact-green Goal4AS:
- head: `8615800bdcf37bba1d6a16a0c08f14a6cf7a6448`
- aggregate run: `34284856812`
- `verify-stage35-ex-current`: `102259661960`
- result: physical marked Kummer image `delta(P)=(d,d,1)`

Goal4AT provisional exact result:

```text
N_minus = D_AB*D_AC-B*C
N_plus  = D_AB*D_AC+B*C
N_minus*N_plus = (A*W)^2

G = gcd(N_minus,N_plus)
G | 2*B*C

N_minus = G*r^2
N_plus  = G*s^2

d = [G/(2*B*C)] = [(2*B*C)/G].
```

Consequences:
- `d` is positive on the positive physical branch;
- every odd prime in `d` already divides `B*C`;
- the remaining parity is one dynamic gcd-reservoir allocation `G` inside `2BC`;
- using Goal4F after an edge permutation, every odd `ell != 3,5` can still occur in the **local** class `d` (odd local valuation), so current local/primitivity data do not yield a fixed finite global squareclass family;
- no claim is made that any such local witness is a global endpoint.

Artifacts:
- `stages/stage35-ex/35ex-35/goal4at-marked-residual-kummer-local-support-source-lock.md`
- `stages/stage35-ex/35ex-35/goal4at-marked-residual-kummer-local-support.json`
- `stages/stage35-ex/verify_stage35_ex_35_goal4at_marked_residual_kummer_local_support.py`

## Freshness

At the parent Goal4AS head, current `main` is `70265586b3f97be21c7621af73f443311f1f3fa3`; PR #1723 is ahead 35 / behind 1 from merge-base `42f20e47babdfdda068a605e3fec489eeace460c`. The one behind commit changes Stage32-EX5 / Stage32 claim-routing material only; no Stage35-EX mathematical source drift was observed. No freshness credit is claimed and no sync is performed in this provisional leaf.

## Next exact leaf

```text
35EX-35_GOAL4AU_MARKED_KUMMER_GCD_RESERVOIR_PRIME_ALLOCATION_PREFLIGHT
```

Use the primitive pair-gcd decomposition and three primitive face-Pythagorean structures to classify `G` prime-by-prime and test whether `(2BC)/G` is forced square on any branch. If not, freeze the exact remaining cross-face compatibility gap before changing route.

No merge. No hostile-audit credit is added by this handoff.
