# Stage35-EX — START HERE

## Purpose

Stage35-EX is an experimental research lane for the sibling Stage29 Class-3 kernel

```text
K16-C3-PESCH-EXPONENT-ONE
child: R29-PESCH-E1
```

Its immediate target is the weaker replacement theorem exposed by Stage35-35-10 / Peschmann Conjecture E1:

```text
For every Master-Hit, with
  g0 = gcd(W1*U2, U1*V2),
  xi = W1*U2/g0,
  eta = U1*V2/g0,
we have xi^2 + eta^2 != square.
```

If eventually proved and hostile-audited with the existing Stage29 global Master-Hit coverage adapter, this target is endpoint-decision capable. Until then it grants no theorem, receiver, Stage35, Stage29-parent, or perfect-cuboid credit.

## Authority firewall

Stage35 MAIN remains frozen at

```text
AUDITED_CLASS3_RETAINED_SHARPER_PARKED
R29-FIB2 = OPEN
J12-PARAMETRIC = OPEN
Stage35 = NOT CLOSED
```

Stage35-EX is **not** a reopen of `R29-FIB2` and does not modify `stages/stage35/MAIN-STATE.json`.

Any successful Stage35-EX theorem must be promoted to its true owner `R29-PESCH-E1` through a separate audited adapter/authority change.

## Ordinary startup: bounded by default

The commands `stage35exmain batch`, `stage35ex main batch`, and `stage35-ex main batch` all mean: execute this Stage35-EX ordinary-startup contract, then advance only the exact current leaf authorized by `MAIN-STATE.json`.

After confirming the live branch/PR head with the GitHub connector, read only:

1. root `AGENTS.md`
2. this file
3. `stages/stage35-ex/MAIN-STATE.json`
4. `stages/stage35-ex/MAIN-BATCH-HANDOFF.md`
5. only the immediate files named by the current leaf working set in `MAIN-STATE.json` (`current.working_set` in the current schema)

This list is the sole routine Stage35-EX startup enumeration authority.

Do not automatically read all Stage29/Stage35 history, prior Stage35-EX leaves, the full Arsenal, Research OS, broad literature, or unrelated Stage controllers. Expand only when the active leaf requires an exact source object, policy trigger, or hostile-audit input.

`ROADMAP.md`, where present, is planning provenance, not routine startup input.
`MAIN-BATCH-HANDOFF.md` is transient delta memory only. If it is `EMPTY`, read no prior narrative from it.

In particular, `AGENTS.md` is mandatory ordinary-startup input, not optional background context. Its repository traversal, context-safe inspection, Actions safety, research-credit, and routing triggers govern Stage35-EX exactly as they govern Stage35.

## Targeted expansion

Expand beyond the ordinary working set only for a named reason:

- the current leaf needs an exact Stage29/Stage35 source object not yet source-locked in Stage35-EX;
- a source path/SHA no longer matches;
- the active leaf has identified an existing-weapon type and must consult the canonical Arsenal router before fresh literature search or new route invention;
- hostile audit, claim promotion, heavy compute, closure, or release is being performed;
- a repo-wide `AGENTS.md` on-demand trigger applies;
- the user explicitly requests broader history or audit.

Open the smallest relevant source, export the needed fact into a compact Stage35-EX certificate, then return to the bounded working set.

## Work style

- Keep one active mathematical leaf.
- Advance exactly the current/next leaf authorized by `MAIN-STATE.json`; do not skip ahead because a later route looks promising.
- Split research into small proof obligations and small commits.
- Prefer exact identities/lemmas over broad computation.
- Bounded searches are regression/evidence only, never theorem credit.
- A failed proof route must freeze the exact obstruction before switching.
- Once an existing weapon/evidence type is identified, follow the `AGENTS.md` Arsenal-routing trigger before inventing a fresh route.
- Before heavy Actions compute, follow the repo-wide Actions-safety trigger in `AGENTS.md`.
- Before research-credit promotion or hostile audit, follow the repo-wide research-credit trigger in `AGENTS.md`.
- Hostile audit is required before any promotion out of Stage35-EX.
- At batch end, promote exact state only into `MAIN-STATE.json`; keep unpromoted transient delta only in `MAIN-BATCH-HANDOFF.md`.
