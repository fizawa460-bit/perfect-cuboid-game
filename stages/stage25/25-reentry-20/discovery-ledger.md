# Stage25-reentry-20 discovery / reuse ledger

```text
TASK_ID=Stage25-u24-r002a
REENTRY_PHASE=20
REPO_REUSE_PREFLIGHT=PASS
REUSE_SEARCH_SCOPE=STAGE24_FINAL,STAGE24_BACKFLOW,STAGE19_CURRENT,STAGE25_R501_R502,STAGE14_ARSENAL,PHASE10_REGISTRY
STRONGEST_KNOWN_CHECK=PASS
FINITE_DATA_USED_AS_PROOF=false
DISCOVERY_LEDGER_STATUS=COMPLETE_SUBMITTED_FOR_FRESH_AUDIT
```

## Sources inspected

- `stages/stage25/25-reentry-10/interface-registry.json` — normative current interface and phase20 weapon dependencies.
- `stages/stage24/final.md` and `stages/stage24/post-stage25-r01/result.md` — literal Stage18->Stage19 survival transition, directional Stage18 asymptotics, and current quarter-power global backflow.
- `stages/stage25/25-50/r501-parametric-positive-power.md` — audited R501 degree-eight family, original canonical shared-`b` cone, bounded similarity fibers, fixed genus-seven third-face exception.
- `stages/stage25/25-60/r502-primitive-height-no-upgrade.md` and `audit-recheck.md` — audited R502 `Theta(B^(1/4))` family and exact canonical order with shared edge `c`.
- `docs/stage14-arsenal.md` / `stage14-arsenal-index.md` — AR-003, AR-016, AR-023, AR-024, AR-028, AR-035 applicability and accounting firewalls.

Phase10 already scanned the complete 824-record Stage14/15 bound-attack ledger and Q01–Q11 terminal clusters. Phase20 does not repeat exhausted branches without a new receiver mutation.

## Candidate attacks

### D20-01 — transfer the existing R501 shared-`b` lower

Accepted as an audited upstream input. The original cone `7/2<t<4` gives canonical order `B<C<A`, so raw shared edge `C` is canonical `b` and `N2,b>>B^(1/4)`.

### D20-02 — expose the R502 shared-`c` lower

Accepted as a current theorem consequence not yet propagated directionally. R502 has canonical order `A<B<C`; its two guaranteed faces share raw `C`, so audited `Theta(B^(1/4))` family growth gives `N2,c>>B^(1/4)`. This upgrades the older C17 shared-`c` `sqrt(log B)` lower.

### D20-03 — recanonicalize R501 on a fresh cone

Accepted as the fresh receiver mutation `R20-M01`. On `9/2<t<5`, exact factor identities imply `C<A` and `C<B`; raw shared `C` is canonical `a`. The same R501 degree-eight height, genus-seven exception curve, primitive-reduction preservation, and bounded similarity fiber survive unchanged. A new coprime parameter rectangle gives `gg T^2` parameters, proving the candidate `N2,a>>B^(1/4)` after audit.

### D20-04 — strict sub-half whole-family upper from existing Stage14 machinery

Rejected. The phase20 arsenal contains no new same-measure theorem that turns the historical `B^(1/2+epsilon)` upper into a strict fixed power below one half. AR-016 is multiplicity only. AR-023/024 explicitly forbid a saving transfer from a shared kernel across different outer measures.

### D20-05 — growing-modulus power saving from AR-035

Rejected. AR-035 is a fixed-finite-prime ordered-limit overlap sieve. Its valid quantifier order is `fix primes -> B->infinity -> enlarge primes`; it cannot be promoted to `z=z(B)` without a new uniform theorem.

### D20-06 — multiply the positive interaction with another sieve saving

Rejected by AR-028 and the Stage24 double-charge discipline. `J2` and the Stage25 cross-ratio are comparison invariants, not independent probability factors.

### D20-07 — reopen R503/R504/R505

Rejected for this internal phase. Their residuals are audited external theorem gates. No new uniform base-change, Prym-isogeny, or common-core counting theorem entered phase20.

## Novelty and compatibility

The only new mathematical mutation is geometric/canonical rather than a new external formula: the already-audited R501 family is restricted to a different open rational cone, changing which physical ordered edge carries the shared-face incidence. This is not a permutation-copy argument: canonical order is recomputed on the new cone, the primitive object remains counted once, and the parameter set is disjointly defined by its rational interval.

```text
R20_M01_POPULATION_MATCH=EXACT_STAGE19
R20_M01_CUTOFF_MATCH=EXACT_R_LE_B
R20_M01_MULTIPLICITY_MATCH=BOUNDED_BY_AUDITED_SCALE_FREE_INVARIANT
R20_M01_MEASURE_MATCH=EXACT_TWO_FACE_PHYSICAL_FAMILY
R20_M01_QUANTIFIER_MATCH=ASYMPTOTIC_RATIONAL_PARAMETER_HEIGHT
R20_M01_THIRD_FACE_FILTER=SAME_FIXED_GENUS7_EXCEPTION_CURVE
```

For the Stage24 directional ratios, the denominator `M2,j` is already an audited directional Stage18 physical count with the same canonical shared-edge chamber, same `R<=B` cutoff, and one-object multiplicity. No new measure adapter is needed.

For Stage23 overlap backflow, each Stage19 exactly-two object lies in the corresponding raw Stage17 pair-overlap channel. The proposed lower is one-sided, so extra raw overlap records cannot invalidate it; no division by an unproved incidence multiplicity is used.

## Result of the search

```text
CANDIDATES_FOUND=D20-01,D20-02,D20-03,D20-04,D20-05,D20-06,D20-07
CANDIDATES_ACCEPTED=D20-01,D20-02,D20-03
CANDIDATES_REJECTED=D20-04,D20-05,D20-06,D20-07
FRESH_RECEIVER_MUTATION=R20-M01
STRONGER_RESULT_CANDIDATE=ALL_DIRECTIONAL_QUARTER_POWER
GLOBAL_EXPONENT_UPGRADE=false
DERIVED_ROUTE_REQUIRED_AFTER_AUDIT_PASS=true
DERIVED_ROUTE=Stage25-um-r008a
NEW_ENUMERATION_REQUIRED=false
FINITE_DATA_PROMOTED=false
```
