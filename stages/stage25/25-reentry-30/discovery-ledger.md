# Stage25-reentry phase30 discovery ledger

TASK_ID=Stage25-u23-r003a

## Sources revalidated

- Stage13 canonical raw-incidence definitions: `A_ab,ac`, `A_ab,bc`, `A_ac,bc`, `A3` are face-mask intersections in the same primitive canonical integral-space host.
- Stage17 final: `N1(B)~kappa/(24*pi) B(log B)^3` and the Stage13 overlap contract transfer exactly through `d=R`.
- Stage19 current post-Stage25 interface: `N2,j(B)>>B^(1/4)` for all canonical shared edges after audited phase20/r008a; whole-family `N2<<_epsilon B^(1/2+epsilon)` remains valid.
- Stage23 historical closeout: ratio is matched population-size ratio, not literal objectwise survival; space integrality is already paid in the source.

## Fresh derivation

D30-01. A two-face object in pair mask `(ab,ac)` is either exactly those two faces or all three faces. Hence `A_ab,ac=N2,a+A3`; cyclically for shared edges `b,c`.

D30-02. Summing gives `P2=N2+3A3`, so `N2=P2-3A3` exactly.

D30-03. Pairwise subtraction cancels `A3` identically. Therefore directional contrasts of raw pair overlaps equal directional contrasts of exactly-two targets without any perfect-cuboid hypothesis.

D30-04. Divide audited `N2,j>>B^(1/4)` by Stage17 `N1~cB(log B)^3` to obtain the directional Stage23 lower. Use `N2,j<=N2<<_epsilon B^(1/2+epsilon)` for the directional upper and zero-density limit.

## Negative certificates

- `A3=o(B(log B)^3)` is not enough to prove `A3=o(B^(1/4))`; no such promotion is made.
- Raw pair-overlap counts are not interpreted as independent second-face probabilities.
- No difference estimate between the three `N2,j` chambers follows merely from individual quarter-power lowers.
- No perfect-cuboid existence/nonexistence conclusion is extracted from `A3`.
- The global `N2` lower exponent stays `1/4`; no strict sub-half whole-family upper is proved.

## Reuse decision

The exact common-contamination decomposition and triple-free contrast identity are reusable enough to promote as a Stage25-reentry weapon after fresh audit. Because they change the strongest Stage17/23 receiver description, propagation route `Stage25-um-r009a` is queued but not executed before parent audit.

```text
DISCOVERY_COMPLETE=true
NEW_REUSABLE_WEAPON_CANDIDATE=true
THEOREM_CHANGING_BACKFLOW_REQUIRED=true
QUEUED_DERIVED_ROUTE=Stage25-um-r009a
FINITE_DATA_USED_AS_PROOF=false
PERFECT_CUBOID_CONCLUSION=NONE
```
