# Stage25-reentry-40 discovery ledger

TASK_ID=Stage25-u22-r004a
STATUS=SUBMITTED_PENDING_FRESH_AUDIT

## Routes executed

### D40-01 — directional Stage22 split

Reused the audited Stage18 directional source interface

`M2,j(B)~C_j B(log B)^5`, `C_j>0`, `j=a,b,c`.

Together with Stage22 `M1(B)~3/(4*pi^2)B^2 log B`, this yields the candidate theorem

`M2,j/M1~(4*pi^2*C_j/3)(log B)^4/B`.

Result: `LIVE_STRONGER_RECEIVER_CANDIDATE`.

### D40-02 — phase30 common-contaminant weapon transported to no-space host

Defined raw pair-incidence chambers `P_j`. The face-mask truth table gives

`P_j=M2,j+M3`.

Thus the common contaminant is exactly the Stage20 Euler population `M3`, and all pairwise raw-pair contrasts cancel it identically.

Result: `LIVE_EXACT_MASK_RECEIVER_CANDIDATE`.

### D40-03 — quantitative third-face contamination test

Stage20 proves for every fixed `eta<1/46`

`M3<<_eta B(log B)^(5-eta)`.

Against each directional Stage18 asymptotic this gives

`M3/M2,j<<_(j,eta)(log B)^(-eta)->0`.

Hence `P_j~M2,j` with the same leading constant and `M2,j/P_j->1` in every direction.

Result: `LIVE_STAGE20_READY_RECEIVER_CANDIDATE`.

### D40-04 — log^4 fine-mechanism reattack

Rechecked the Stage22 causal synthesis and phase10 weapon dependencies: shared-edge toric bulk, rank-6 anticanonical architecture, local/divisor diagnostics, and the newly proved directional robustness.

These inputs certify the net relative log power and rule out two coarse fake explanations:

- `log^4` is not produced only by one special shared-edge chamber;
- `log^4` is not produced by the third-face nonsquare exclusion.

They do not canonically identify four independent logarithmic factors or a unique allocation among local, valuation, squareclass, or boundary channels.

Result: `NO_CANONICAL_FOUR_FACTOR_DECOMPOSITION`; gate `G22_LOG4_FINE_MECHANISM` remains open.

## Accounting

No finite census is promoted. No independence model is used. No new Stage20 true exponent is claimed. The only proposed theorem changes are directional/receiver refinements of already-audited asymptotic interfaces.

```text
NEW_COMPUTATION_REQUIRED=false
FINITE_DATA_USED_AS_PROOF=false
NEW_EXTERNAL_THEOREM_REQUIRED=false
STRONGER_RESULT_CANDIDATE=true
NEW_REUSABLE_WEAPON_CANDIDATE=true
LOG4_FINE_MECHANISM_CLOSED=false
DERIVED_ROUTE_CANDIDATE=Stage25-um-r010a
FRESH_AUDIT_REQUIRED=true
```
