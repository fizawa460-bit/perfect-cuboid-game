# Stage25-reentry-40 weapon delta

STATUS=CANDIDATE_PENDING_FRESH_AUDIT

## S25R-W40-01 — common-triple mask receiver

On the no-space face-incidence host,

`P_a=M2,a+M3`, `P_b=M2,b+M3`, `P_c=M2,c+M3`.

The same Stage20 term contaminates all three raw pair chambers and cancels from every pairwise contrast.

Reuse class: exact face-mask bookkeeping on any contract with identical canonical/primitivity/cutoff semantics.

## S25R-W40-02 — directional postfilter stability

For every fixed `eta<1/46`,

`M3/M2,j<<_(j,eta)(log B)^(-eta)->0`.

Hence

`P_j~M2,j~C_j B(log B)^5`

and

`M2,j/P_j->1`.

This certifies that excluding the third square face is lower-order in every shared-edge chamber separately.

## S25R-W40-03 — directional Stage22 transition

For each `j=a,b,c`,

`M2,j/M1~(4*pi^2*C_j/3)(log B)^4/B->0`.

Also `C_M2=C_a+C_b+C_c` and `M2,j/M2->C_j/C_M2>0`.

This proves the Stage22 sharp scale is directionally robust rather than an averaging artifact.

## Not promoted

- no canonical four-independent-log-factor decomposition;
- no true `M3` exponent;
- no independence statement;
- no perfect-cuboid conclusion.

```text
NEW_REUSABLE_WEAPON_CANDIDATE=true
NEW_REUSABLE_WEAPON_PROVED=false
PROMOTION_REQUIRES_FRESH_AUDIT=true
```
