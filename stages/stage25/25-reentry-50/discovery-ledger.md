# Stage25-reentry-50 discovery ledger

TASK_ID=Stage25-u21-r005a
STATUS=AUDITED_PASS_AWAITING_MERGE_AND_DERIVED_ROUTE

## D50-01 — exact interaction normalization

Stage16S removes the polynomial ambiguity: the ambient space condition costs exactly one power `B^-1`, while Stage21 survival is `(log B)^2/B`. The interaction enhancement is therefore exactly logarithmic at net power `+2`.

## D50-02 — exact shared-P receiver

AR-038 identifies the target bulk with `2 sum H(P)L_B(P)` and an exact primitive multiplicity adapter. Lower-order multi-face corrections cannot create the leading `B(log B)^3` term.

## D50-03 — principal-sector localization

Stage13 R07 puts the target main term in the full principal multiplicative sector and forces every nonprincipal effective sector below the main term by at least one pole. Hence the net `+2` **log-power** enhancement is localized to the principal shared-P target architecture.

## D50-04 — pole-order subtraction is not yet certified

The source `M1` and target `N1` have not been materialized inside one common factor-by-factor pole ledger. Therefore the numerical log-power difference `3-1=2` is not promoted to a proved source-target principal pole-order difference. Separate `H(P)`/`L_B(P)` one-log assignments, named pole slots, and independent-factor products all remain open.

## D50-05 — bridge to Stage22

Phase40/r010a leaves `G22_LOG4_FINE_MECHANISM` inside the shared-edge rank-6 toric bulk. The safe derived attack compares Stage21's certified +2 log-power/principal-target localization with Stage22's +4 toric ledger, first asking whether a common pole ledger can be materialized. A `2+2` split is not assumed.

```text
NET_LOG2_LOCALIZATION=PRINCIPAL_SHARED_P_TARGET_BULK
SOURCE_TARGET_COMMON_POLE_LEDGER=OPEN
PRINCIPAL_POLE_ORDER_DIFFERENCE_TWO_PROVED=false
INDIVIDUAL_LOG_FACTOR_ASSIGNMENT=OPEN
G22_COMPARATIVE_POLE_ATTACK=QUEUED
FINITE_DATA_USED_AS_PROOF=false
```
