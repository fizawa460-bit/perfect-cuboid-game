# Stage25-reentry-50 discovery ledger

TASK_ID=Stage25-u21-r005a
STATUS=SUBMITTED_PENDING_FRESH_AUDIT

## D50-01 — exact interaction normalization

Stage16S removes the polynomial ambiguity: the ambient space condition costs exactly one power `B^-1`, while Stage21 survival is `(log B)^2/B`. The interaction enhancement is therefore exactly logarithmic at net power `+2`.

## D50-02 — exact shared-P receiver

AR-038 identifies the target bulk with `2 sum H(P)L_B(P)` and an exact primitive multiplicity adapter. Lower-order multi-face corrections cannot create the leading `B(log B)^3` term.

## D50-03 — principal-sector localization

Stage13 R07 puts the target main term in the full principal multiplicative sector and forces every nonprincipal effective sector below the main term by at least one pole. Hence the net `+2` log enhancement is localized to the principal shared-P architecture.

## D50-04 — factor assignment remains open

The repository does not currently prove separate asymptotics/moments assigning exactly one logarithm to `H(P)` and one to `L_B(P)`, nor two individually named simple pole slots. Any such split would be an overclaim.

## D50-05 — bridge to Stage22

Phase40/r010a leaves `G22_LOG4_FINE_MECHANISM` inside the shared-edge rank-6 toric bulk. Comparing the Stage21 principal pole surplus `+2` with Stage22 net surplus `+4` is now a precise derived attack. The tempting `2+2` decomposition is a hypothesis to test, not a theorem.

```text
NET_LOG2_LOCALIZATION=PRINCIPAL_SHARED_P_BULK
INDIVIDUAL_LOG_FACTOR_ASSIGNMENT=OPEN
G22_COMPARATIVE_POLE_ATTACK=QUEUED
FINITE_DATA_USED_AS_PROOF=false
```
