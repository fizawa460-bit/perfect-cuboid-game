# Stage15-6dh — negative-certificate closure and reconstructed-graph dispersion promotion

Base: Stage15-6df/6dg. The selected double-eliminant route has now been executed rather than merely ranked.

## 1. Certified output of the deterministic route

The exact sign-factor audit proves
\[
\gcd(R_-,R_+)=\varepsilon_R g,
\qquad
\gcd(S_-,S_+)=\varepsilon_S g,
\qquad
\varepsilon_R,\varepsilon_S\in\{1,2\},
\]
with
\[
g=\gcd(P,Q),\qquad kg^2\mid\Delta,
\qquad \Delta=(abM)^4-(cdN)^4.
\]
After dividing by `g`, the two sign pairs are coprime away from the explicit parity factor and their squareclass/divisor branching is `B^{o(1)}`.

Stage15-6dg then proves that the double eliminants are exactly Cramer's-rule coordinates for the same two survivor norm equations. Therefore they do not supply a third independent equation and do not deterministically remove a second graph degree of freedom.

This is the requested rigorous negative certificate for the selected deterministic mechanism.

## 2. Promotion of the next LIVE route

The reconstructed-graph exhaustive ledger from 6dc is preserved. With deterministic graph sparsity now tested and blocked as a standalone exponent source, promote
\[
\boxed{\text{ROOT-RATIO DISCREPANCY DISPERSION ON THE RECONSTRUCTED GRAPH}}
\]
from LIVE backup to the next selected route.

This promotion does **not** import a Stage14 fixed-packet exponent. The future route must prove a whole-family physical adapter on the exact three-variable/Pell graph.

Use `Q_mod` for a switched channel modulus to avoid confusion with the survivor square variable `Q`. For fixed legal cell/core/local data and a physical dyadic graph packet, the object to estimate is the main-density-subtracted count
\[
\mathcal D(Q_{\rm mod},\rho;\mathcal G)
=
N_{\rm graph}(Q_{\rm mod},\rho;\mathcal G)
-
\text{local main density},
\]
where graph nodes are formed **after** the Stage15-6da `B^{o(1)}` completion reduction. Absolute values must be taken only after the intended modulus/orientation averaging if a dispersion argument is used.

The exact local inputs now available to that route are stronger than before:

1. fourth residual completion multiplicity is `B^{o(1)}`;
2. `(Q_mod,H)=1` for switched channel moduli;
3. odd channel primes have bounded legal S/O root orientations;
4. every moving common sign-factor gcd is `g=gcd(P,Q)` and satisfies `kg^2|Delta`;
5. after removing `g`, sign-factor pairs are coprime up to `2`.

These are adapters/local structure only. None is charged as an independent power saving.

## 3. Delta/beta/sigma ledger resumed after route decision

The ledger is resumed only after the deterministic route verdict and dispersion promotion:

- conditional small-side modulus exponent: `beta=-1` PRESERVED;
- exact fourth-variable completion: `B^{o(1)}` CERTIFIED;
- exact sign-factor moving gcd: CLASSIFIED;
- certified `delta>0`: NO;
- certified `sigma>0`: NO;
- executable polynomial `theta` window: NONE;
- conditional window if a future dispersion estimate gives `delta>0`: `0<theta<delta`;
- split trigger: FALSE.

The next quantitative task is therefore not another deterministic reconstruction. It is to formulate and test a legal whole-family second-moment/dispersion inequality on reconstructed graph packets, including the physical product-height measure and the `kg^2|Delta` local support, before invoking any pre-existing large-sieve or Type-II estimate.

## 4. Controller output

```text
CURRENT_SUBSTAGE=Stage15-6dh
NEXT_GATE=FRESH_AUDIT_OF_SIGN_FACTOR_GCD_NEGATIVE_CERTIFICATE_AND_DISPERSION_PROMOTION
ALL_PRIOR_CHECKS_PASS=true
AUDIT_REQUIRED=true
CODEX_REQUIRED=false
MERGE_ALLOWED=false
SPLIT_TRIGGER=false
```

```text
STAGE15_6_SUBSTAGE=6dh
STAGE15_6DH_DOUBLE_FACTOR_ROUTE_EXECUTED=true
STAGE15_6DH_MOVING_GCD_CLASSIFICATION_PROVED=true
STAGE15_6DH_DETERMINISTIC_NEGATIVE_CERTIFICATE=true
STAGE15_6DH_NEXT_ROUTE=ROOT_RATIO_DISCREPANCY_DISPERSION_ON_RECONSTRUCTED_GRAPH
STAGE15_6DH_DISPERSION_PROMOTED=true
STAGE15_6DH_STAGE14_EXPONENT_PROMOTED=false
STAGE15_6DH_CONDITIONAL_BETA=-1
STAGE15_6DH_DELTA_PROVED=false
STAGE15_6DH_SIGMA_PROVED=false
STAGE15_6DH_EXECUTABLE_OVERLAP_WINDOW=false
STAGE15_6DH_SPLIT_TRIGGER=false
STAGE15_6DH_AUDIT_REQUIRED=true
STAGE15_6DH_CODEX_REQUIRED=false
STAGE15_6DH_MERGE_ALLOWED=false
STAGE15_6DH_EXIT=FRESH_AUDIT_OF_SIGN_FACTOR_GCD_NEGATIVE_CERTIFICATE_AND_DISPERSION_PROMOTION
```