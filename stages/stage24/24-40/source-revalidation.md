# Stage24-40 — source-level upper revalidation

CHECKPOINT=40
ROLE=SOURCE_LEVEL_ATTACK_REOPEN
STATUS=COMPLETE_WITH_REAUDIT_REPAIR_NOTE

## Sources opened

The checkpoint40 upper attack was executed from the actual source artifacts rather than theorem labels.

### R40-01 — Stage14 physical Kummer height / minimal curve degree

Source chain: Stage14-4ah -> 4ai -> 4ak.

Revalidated facts:

- physical polarization `M=pi^*(-K_Y)` with `M^2=8`;
- exact physical height `H_M=d=R`;
- every physical rational curve has `M.C>=4`;
- `M.C=4` is the unique fixed-curve square-root pattern;
- Stage14-4ai reduces all degree-four possibilities to one final split singular-anticanonical mechanism;
- Stage14-4ak independently enumerates the anti-invariant lattice and proves the required norm-16 parity coset empty;
- therefore `PHYSICAL_Q_RATIONAL_M4_BISECTION_EXISTS=false`.

Current Stage24 consequence: each fixed physical rational curve has `M.C>=5` and hence fixed-curve exponent at most `2/5`; every genuinely fixed finite union is strict sub-square-root. This source does **not** provide uniform implied constants or a uniform `o(1)` over a number of curves growing with `B`, so checkpoint40 makes no quantitative growing-family summation or `B^(1/10)` proliferation claim.

### R40-02 — Stage14 conditional principal-density localization

Source: `stages/stage14/archive/tasks/14-4dj/result.md`.

Revalidated facts:

- current whole-family scale entering that receiver is `B^(1/2+o(1))`;
- for `omega(c)=B^(-delta+o(1))`, the cell stratum is `B^(1/2-delta+o(1))`;
- square-root saturation requires `omega(c)=B^(-o(1))`;
- remaining obstruction is positive principal density plus pairwise/triple covariance or a fresh pre-absolute-value reduction.

No Stage24 theorem identifies the thin-cover zero-density measure with this cell occupancy ratio, so no unproved cross-measure saving is imported.

### R40-03 — Q06 physical Kummer/Jacobi receiver

Source: `stages/stage23/23-40/q06-source-execution.md`, itself opening Stage14-4ah / tH15 / t64.

Revalidated facts:

- Stage19 target height and Kummer height coincide exactly: `H_M=d`;
- object-to-oriented-receiver multiplicity is `O(1)`;
- same-row and same-column pieces are already partitioned;
- the residual transverse receiver is a moving Jacobi square-lift incidence;
- fixed rational curves alone cannot exceed half-power, and after 4ak the extremal fixed degree-four curve mechanism is absent;
- moving-family count/dispersion and uniform per-family bounded-height summation remain unproved.

Thus Q06 is genuinely executed but still blocked at a uniform moving-family theorem.

### R40-04 — Stage15 moving genus-one receiver

Source: `stages/stage15/15-6ai/result.md`.

Revalidated facts:

- the exact low-core one-point receiver is a smooth geometrically integral intersection of two quadrics in `P3`;
- geometric genus is one for every physical `m>n>0`;
- core/cross-gcd data are twists, but the outer ratio changes the pencil cross-ratio;
- the family is not one fixed elliptic curve;
- no uniform height/point-count theorem was applied and no causal fixed-power exponent was derived.

This independently confirms the moving-family nature of the unresolved upper gate.

### R40-05 — Stage15 character/growing-modulus route

Sources: `stages/stage15/15-6dr/result.md` and `stages/stage15/15-6-final.md`.

Revalidated facts:

- exact centered operator and dual exist;
- best certified occupancy exponent is `kappa=1`;
- no `kappa<1`, `delta>0`, `sigma>0`, or polynomial overlap window is proved;
- standard fixed-coefficient large sieve does not apply to the reconstructed completion-dependent graph sums;
- the internal Pell/factor-gap/norm-ideal/divisor-switching alternatives were subsequently exhausted or reduced to existing receivers;
- Stage15-6 closes with no internal fixed-power route remaining;
- growing-modulus local/adelic sieving is retained only as an external future gate.

### R40-06 — exact local parity tensor

Sources: Stage15 final / Stage19 final.

For good split primes,

`1-rho_p = 4/p + O(p^-2)`.

The fixed-prime theorem is correctly ordered: finite prime set first, then `B->infinity`, then enlarge the set. No growing-modulus error uniformity is present.

Even under a hypothetical uniform block through `p<=B^theta`, this same local product has only `(log B)^(-2+o(1))` scale. Hence it cannot itself yield a fixed power below `1/2`.

### R40-07 — Stage14 final route state

Source: `stages/stage14/final.md` and the final integrated route classification.

Revalidated facts:

- whole-family theorem is `N2(B)<<B^(1/2+o(1))`;
- strict fixed-power sub-square-root is explicitly unproved;
- MAIN, fixed-U/T, and conditioned-character/S routes end at distinct external gates;
- no legal cross-promotion/common adapter was found;
- restart requires materially new theorem input or a new exact measure-preserving reduction.

Checkpoint40 does not treat this final status as a reason to skip attacks; it is used only after U40-S1..S6 were re-executed against current Stage24 input.

## Revalidation verdict

No source-level contradiction to the inherited half-power theorem was found, and no certified input upgrades it to a strict sub-square-root theorem.

The strongest safe structural localization is:

1. fixed extremal rational curves are eliminated;
2. each fixed curve is at most `B^(2/5+o(1))`, and each fixed finite union is strict sub-square-root;
3. no uniform summation over a `B`-dependent moving curve family is certified;
4. fixed-power occupancy-deficit strata are eliminated from possible square-root saturation;
5. the unresolved half-power mass, if real, must be collective/moving and near-maximal in the surviving charged cells.

```text
SOURCE_LEVEL_ATTACK_OPENED=true
SOURCE_CLUSTERS_REVALIDATED=7
HEIGHT_ADAPTER_LOSS=0
FIXED_M4_MECHANISM_REJECTED=true
FIXED_CURVE_SINGLE_EXPONENT_UPPER=2/5+o(1)
FIXED_CURVE_FINITE_UNION_SUBSQRT=true
GROWING_CURVE_FAMILY_UNIFORM_SUMMATION_PROVED=false
B_ONE_TENTH_PROLIFERATION_CLAIM=false
MOVING_GENUS_ONE_FAMILY_CONFIRMED=true
GROWING_MODULUS_UNIFORMITY_PROVED=false
LOCAL_TENSOR_POWER_SAVING_CAPABLE=false
CHARACTER_KAPPA_LT_1_PROVED=false
STRICT_SUB_SQRT_WHOLE_FAMILY_PROVED=false
```
