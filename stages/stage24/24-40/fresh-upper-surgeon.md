# Stage24-40 — fresh Stage19 upper surgeon

CHECKPOINT=40
ROLE=FRESH_UPPER_SURGEON_FIRST
TARGET=STRICT_SUB_SQRT_FOR_N2
STATUS=REPAIRED_PENDING_FRESH_AUDIT

## 1. Frozen target and firewall

Stage24 must not stop at the inherited theorem

\[
N_2(B)\ll_\varepsilon B^{1/2+\varepsilon}.
\]

The surgeon therefore asks for a genuine whole-family theorem of the form

\[
N_2(B)\ll B^{1/2-\delta+o(1)}
\]

for some fixed \(\delta>0\), or an exact reduction that makes such a theorem materially more accessible.

The Stage24 population is the primitive canonical exactly-two-face, integral-space population under the exact physical height `R=d<=B`. No finite-data slope is promoted and no saving from different theorem species is multiplied without a proved same-measure adapter.

## 2. U40-S1 — eliminate fixed rational-curve sqrt saturation

Stage14-4ah proves that a fixed physical rational curve `C` on the Kummer cover with physical polarization `M` has fixed-curve height exponent `2/(M.C)`, and every physical rational curve satisfies `M.C>=4`. The only extremal square-root pattern is `M.C=4`.

Stage14-4ai reduces all such `M.C=4` possibilities to one final split singular-anticanonical case, and Stage14-4ak proves the required parity coset is empty. Therefore there is no physical Q-rational `M`-degree-four bisection.

Hence every fixed physical rational curve has integer degree

\[
M.C\ge5,
\]

so any one fixed rational curve contributes at most

\[
O(B^{2/5+o(1)}).
\]

Consequently any genuinely fixed finite collection of fixed physical rational curves is strict sub-square-root.

The first submission went one step too far by summing this estimate over a number of curves `K(B)` varying with `B`. That step is not certified: the fixed-curve theorem does not provide a uniform implied constant or uniform `o(1)` over a moving family. Therefore checkpoint40 makes **no** `K(B)B^(2/5+o(1))` claim and infers **no** `B^(1/10)` proliferation threshold.

The correct boundary is that any whole-family use of these fixed-curve exponents now requires a new uniform moving-family theorem. That missing uniformity is part of the live upper obstruction.

Verdict: `PARTIAL_ADVANCE_FIXED_CURVE_SQRT_MECHANISM_ELIMINATED_WITH_MOVING_FAMILY_UNIFORMITY_OPEN`.

## 3. U40-S2 — principal-density deficit localization

Stage14-4dj works inside the exact square-root proof receiver. For a full-conductor physical root cell `c`, define

`omega(c)=M(c)/M_max(c)`.

On any fixed-power deficit stratum

`omega(c)=B^(-delta+o(1))`, `delta>0`,

the positive principal contribution is

\[
\ll B^{1/2-\delta+o(1)}.
\]

Therefore every fixed-power occupancy-deficit layer is already strict sub-square-root. A square-root-saturating sequence must live in near-maximal conditional occupancy cells

`omega(c)=B^(-o(1))`.

Stage24-30's thin-cover theorem is a global zero-density statement on the Stage18 toric measure. It does not imply a fixed-power deficit for these Stage14 full-conductor cells. The cell measure and the Stage18 denominator are different accounting objects; no adapter proving `omega(c)<=B^-delta` is available.

Verdict: `PARTIAL_ADVANCE_SATURATION_LOCALIZED_TO_NEAR_MAXIMAL_CELLS`.

## 4. U40-S3 — growing-modulus local squareclass sieve

The exact Stage15/19 local acceptance at good split primes is

\[
\rho_p=1-4/p+O(p^{-2}).
\]

For fixed finite prime sets this gives zero density after the ordered limits already proved. Checkpoint40 rechecks the stronger hypothetical: suppose uniform congruence asymptotics were available for all good split primes up to `z=B^theta` for some fixed `theta>0`.

Then the same parity tensor would naturally give

\[
\prod_{p\le z,\ p\equiv1(4)}\rho_p
=(\log z)^{-2+o(1)}
=(\log B)^{-2+o(1)}.
\]

Thus even ideal polynomial-window uniformity for this *same local tensor* is logarithmic, not a fixed `B^-delta` saving. Moreover there is no proved legal multiplication of that factor onto the independent Stage14 half-power upper theorem.

Therefore growing-modulus uniformity remains useful for effective zero-density/logarithmic refinement, but it is not by itself a strict sub-square-root mechanism.

Verdict: `MECHANISM_INSUFFICIENT_FOR_FIXED_POWER_BY_ITSELF`.

## 5. U40-S4 — moving Jacobi / genus-one family

Stage14-t64 and the Stage23 Q06 source execution sharpen the generic `(4,4)` Kummer receiver to the Jacobi square-lift family

\[
y^2=(t^2+s)(1+s t^2),
\]

with exact physical height `H_M=d` and bounded physical orientation multiplicity. Stage15-6ai independently identifies the residual one-point receiver as a smooth geometrically integral genus-one `(2,2)` curve whose pencil cross-ratio varies nontrivially with the outer ratio `(m:n)`.

The fixed-curve `M=4` void does not kill this branch because the obstruction is a *moving* family. A theorem for one fixed elliptic curve, one fixed rank, or one fixed discriminant does not sum uniformly over the physical outer measure.

A strict sub-square-root result from this branch needs a new uniform/averaged theorem controlling at least one of:

1. frequency of active moving fibers;
2. first sufficiently small physical rational point in an active fiber;
3. transverse Jacobi/Kummer square-lift incidence under exact `d` height;
4. uniform bounded-height constants/exponents across a moving low-degree family with all physical masks retained.

No such theorem is certified in the repository.

Verdict: `LIVE_EXTERNAL_GATE_MOVING_FAMILY_CONTROL`.

## 6. U40-S5 — character / dispersion receiver

Stage15-6dr proves a current-input negative certificate for the centered character occupancy operator. The best certified occupancy exponent is `kappa=1`; no `kappa<1`, no polynomial overlap window, no `delta>0`, and no `sigma>0` is obtained. Stage15 subsequently exhausts the Pell/factor-gap/reconstruction variants and closes the internal fixed-power ledger.

Stage14's later MAIN/S/T analytic programs also finish at named external gates with the whole-family exponent still `1/2`; the final integration explicitly forbids automatic continuation without materially new theorem input or an exact measure-preserving adapter.

Stage24-30's thin-cover route is new geometry but does not supply the missing centered-kernel anti-concentration or first-moment theorem.

Verdict: `CURRENT_INPUT_NEGATIVE_CERTIFICATE_PERSISTS`.

## 7. U40-S6 — finite numerical pressure test

The exact matched Stage24 census gives

`B=100000: M2=796698, N2=89`

and

`B=1000000: M2=13817725, N2=255`.

The finite `N2` decade exponent is about `0.457`, while the matched survivor-ratio exponent is about `-0.782`. The larger Stage14 numerator census also fails its predeclared `N2/sqrt(B)` stability gate by `B=500m`.

These facts are compatible with strict sub-square-root growth, but do not prove it and do not select a true exponent.

Verdict: `DIAGNOSTIC_SUPPORT_ONLY`.

## 8. Surgeon conclusion

Checkpoint40 does not prove a whole-family strict sub-square-root theorem. It does sharpen the obstruction:

- fixed `M=4` rational-curve accumulation is impossible;
- each fixed physical rational curve is at most `B^(2/5+o(1))`, and a fixed finite collection is strict sub-square-root;
- summation over a `B`-dependent moving curve family is not justified without new uniformity;
- every fixed-power principal-density deficit layer is already strict sub-square-root;
- the local squareclass tensor is intrinsically logarithmic even under hypothetical polynomial-window uniformity;
- the remaining square-root obstruction is collective/moving and concentrated in near-maximal occupancy / first-small-point / transverse-incidence behavior.

The strongest certified whole-family upper bound remains

\[
\boxed{N_2(B)\ll_\varepsilon B^{1/2+\varepsilon}}.
\]

```text
FRESH_STAGE19_UPPER_SURGEON_EXECUTED=true
FRESH_UPPER_CANDIDATES=6
STRICT_SUB_SQRT_ATTACK_EXECUTED=true
FIXED_M4_SQRT_MECHANISM_ELIMINATED=true
FIXED_CURVE_SINGLE_EXPONENT_UPPER=2/5+o(1)
FIXED_CURVE_FINITE_UNION_SUBSQRT=true
GROWING_CURVE_FAMILY_UNIFORM_SUMMATION_PROVED=false
B_ONE_TENTH_PROLIFERATION_CLAIM=false
NEAR_MAXIMAL_OCCUPANCY_ONLY_SQRT_SATURATION=true
GROWING_MODULUS_SAME_TENSOR_FIXED_POWER=false
MOVING_FAMILY_CONTROL_PROVED=false
STRICT_SUB_SQRT_WHOLE_FAMILY_PROVED=false
HALF_POWER_INTRINSIC_PROVED=false
TRUE_TARGET_EXPONENT_IDENTIFIED=false
```
