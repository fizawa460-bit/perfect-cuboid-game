# Stage33 MAIN transient handoff

status: UNPROMOTED_DELTA
base_main_state_canonical_sha256: 7d52c93a517fc96050b2f78583ae05e5e4ff4f983c2533c673ca008060bd0226

## Exact delta retained this batch

New safe-geometric-route boundary certificate:
- `stages/stage33/33-12/j2-marked-glue-geometric-sign-route-gap.json`
- canonical SHA256 `23b6fc3e9cf666e81f0c11c4c57c7070a1cc4c459c35515a6d934db3a84f3ee9`
- verifier: `stages/stage33/33-12/verify_j2_marked_glue_geometric_sign_route_gap.py`

The current MAIN leaf remains open. No source coordinate, Kummer column, receiver, theorem, endpoint, closure, or downstream-release credit is added.

### Exact route boundary

1. The hostile-firewalled actual-geometry certifier still proves only existence of an actual isotropic glue `H=T(S)/L0` of order 512. It does not identify a labeled subgroup or labeled generators in the retained coordinate-K3 discriminant basis.
2. The safe elementary q256 aggregator uses the full finite-q data plus seven geometric coordinate-sign involutions and explicitly forbids arithmetic `cc/ct` promotion. Its own interpretation is only an elementary-branch census and explicitly does not identify `T(S)/L0`.
3. The committed non-elementary K1 census checks `1,311,205,952` weighted structural `H`, has zero survivors, and still records `actual_index512_glue_identified=false`.
4. The committed non-elementary K2 census checks `129,468,416` weighted `H`, has zero survivors, and still records `actual_index512_glue_identified=false`.
5. Therefore these seven-sign artifacts are exact rejection/filter evidence, not a positive marked-glue authority. They cannot by themselves define the missing marked discriminant/proper-Br2 adapter.

### Refined next exact leaf

`SOURCE_LOCK_POSITIVE_ACTUAL_LABELED_INDEX512_GLUE_FROM_SAFE_GEOMETRIC_ROUTE_OR_EQUIVALENT_MARKED_NS_T_DISCRIMINANT_ANTI_ISOMETRY_THEN_MATERIALIZE_2x14_J2_ADAPTER`

The positive input must supply either:
- the actual labeled order-512 subgroup `H=T(S)/L0` with generators in the retained coordinate-K3 discriminant basis; or
- an equivalent exact marked `NS(S) <-> T(S)` discriminant anti-isometry/basis adapter tied to the retained full-surface Smith basis.

Only after that may semantic `u1=[1,0]` / `u2` be transported and dualized into proper-Br2 and V4/75D compatibility replayed as a check.

Do not promote masks 742 or 736 from compatibility, and do not restore historical mask 6. Historical rep88 arithmetic `cc/ct` promotion remains provenance only, not authority.

## Work handoff addendum from interrupted MAIN batch

MAIN was stopped here because broad GitHub search repeatedly stalled. Work should **not** restart that search pattern. Start from the compact authority and exact paths below only.

Fresh PR state observed during this batch:
- PR `#1476` OPEN, unmerged.
- branch `stage33-post1475-j2-v4-generator-adapter`.
- latest observed head before this handoff write: `d2ed4ce9ab425831c4ecaead3bb2085f8eda5ca9`.
- do not create a new PR and do not merge.

### Additional exact observations

1. `stages/stage33/33-07/coordinate-k3-transcendental-glue-index.json` is the clean geometric predecessor. Canonical SHA256 `0cc5321d02b56cea801b8def71a4c3b0946bd8011d8c30767a9602faba2fa8d8`. It fixes rank-14 `L0=<8>^10 direct_sum <16>^4`, rank-14 endpoint `T`, and `[T:L0]=512`; it does not label `H`.

2. Current hostile-firewalled certifier `stages/stage33/33-07/certify_index512_actual_geometry_glue_adapter.py` has git blob SHA1 `d0917a48d15ea3ff4bd6dd4144d2acced0b27c2a`. It intentionally refuses rep88/labeled-glue promotion.

3. Historical commit `f4116371d7833c9e1d23ca26934823f661900a88` (`Stage33-07 certify actual geometry glue in rep88 orbit`) shows exactly what was later invalidated: it used finite `cc/ct` extension choices plus the endpoint-compatible candidate classification to place the actual geometric glue into the rep88 orbit. That membership inference is provenance only now. Do not restore it without a new current-valid source bridge.

4. The geometry-only/non-arithmetic rejection evidence is strong but negative:
- K1 certificate: `stages/stage33/33-07/nonelementary-k1-geometric-sign-fixed-p7-rescue128-census.json`, SHA256 `7ac64a76b8132e044b145d009e331476f55e04a78001a127bce6fe3034c206fa`, `arithmetic_generators_used=[]`, 1,311,205,952 weighted H covered, zero survivors. K1 uses endpoint finite-group type or seven-sign filtration; do not call it sign-only.
- K2 certificate: `stages/stage33/33-07/nonelementary-k2-geometric-sign-fixed-census.json`, SHA256 `44390c7bd74b8be73f74ccc305e1b4229a73433b20f1ce9f1d02a63e0526558b`, `arithmetic_generators_used=[]`, 129,468,416 weighted H covered, zero survivors; K2 is rejected by seven-sign fixed filtration.

5. Elementary safe machinery exists at `stages/stage33/33-07/aggregate_elementary_index512_q256_geometric_sign_split.py`. The script explicitly uses full finite q + seven geometric signs and forbids arithmetic `cc/ct`. Do not infer the aggregate result from the script alone; use the exact retained certificate / Actions evidence already source-locked by the new route-gap certificate.

6. Do not try the naive self-dualization shortcut `A_T[2] vector -> discriminant bilinear matrix -> proper-Br2 covector`. In the interrupted MAIN computation the order-2 subgroup paired isotropically and this produced zero covectors. This is negative guidance only, not a promoted repo theorem; it means an explicit marked duality/anti-isometry is still required.

### Work should do only this

Read in order:
1. `AGENTS.md`
2. `stages/stage33/MAIN-START-HERE.md`
3. `stages/stage33/MAIN-STATE.json`
4. this handoff
5. only the exact Stage33-07 / 33-12 sources directly needed by the leaf.

Then attempt one source-first result, not a survey:
- derive a positive actual **labeled** order-512 glue `H` from retained geometric markings; or
- derive an equivalent marked `NS(S)<->T(S)` discriminant anti-isometry / basis adapter; or
- prove with a verifier-backed gap artifact that the retained current sources determine only an orbit/filter and cannot determine the label, naming the minimal missing datum.

If a unique orbit but multiple labeled embeddings remain, use retained geometric coordinate-K3 markings/signs to resolve labeling. Do **not** use V4 compatibility, raw-75D target compatibility, mask 6, mask 742, or mask 736 to choose the label.

Only after the marked source bridge exists should Work transport semantic Kc `u1=[1,0]` / `u2`, materialize the accepted 14x14 or 2x14 adapter, and replay V4/raw-75D as checks.

Do not launch heavy Actions unless genuinely necessary. If needed, first obey `docs/research-os/policies/actions-storage-and-evidence-safety.md`, including <=18 effective heavy jobs and fresh commit-range run-key authorization.
