# Stage33-05 — K3 Br[2] Q(i)/Q descent production state

```text
STAGE33_UNIT=33-05
UNIT_STATUS=RUNNING
UNIT_CLOSED=false
DOWNSTREAM_RELEASED=false
K3_GEOMETRIC_BR2_DIM=2
Q_RELEVANT_SURVIVING_DIM=NOT_YET_CERTIFIED
QI_OVER_Q_ACTION_MATRIX_EXACT=false
THEOREM_CREDIT=false
ENDPOINT_CREDIT=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```

## 1. Exact ruled-branch preflight

The source-locked ruled model is independently regressed:

```text
F=(X+iY)(X-iY) over Q(i)
B+,B- have bidegree (2,2)
complex conjugation swaps B+ and B-
B+ intersect B- in exactly 8 frozen points
all 8 intersections are transverse
```

No action on `Br(K_c_Qbar)[2]` is inferred merely from the branch-component swap.

## 2. Exact Creutz--Viray dimension skeleton

The production continuation source-locks the Creutz--Viray presentation and applies its finite dimension count to this specific branch configuration.

Relative to the first ruling `W=P1_t`, both normalized branch components have the same quadratic function field

```text
K(sqrt(q)),
q(t)=t^4-6t^2+1
    =(t-(1+sqrt(2)))(t+(1+sqrt(2)))
     (t-(sqrt(2)-1))(t+(sqrt(2)-1)).
```

The four roots are distinct and are exactly the fibers where both normalized branch components ramify with even index. The generic quartic double-cover equation has square leading coefficient `t^2`, so after `y=w/t` the Creutz--Viray constant `c` may be chosen square.

For the normalization

```text
B = B+ disjoint_union B-
```

we have

```text
component genera       = 1,1
h0(B)                  = 2
Creutz--Viray g(B)     = 1
Jac(B)[2] dimension    = 4
```

The dual graph has two vertices joined by eight edges, hence

```text
b1(Gamma)=7.
```

The exact checker constructs the standard cycle basis `e_j+e_8`, `j=1..7`.

Because both branch components give the same quadratic extension, the kernel of

```text
K*/K*2 -> L*/L*2
```

has dimension one, generated by the squareclass of `q`. Combining the exact source formulas gives

```text
raw generator subspace mod L*2       = 12
kernel to K* L*2                      = 3
L_E dimension                         = 9
L_{c,E}=L_E because W=P1 is rational = true
L_{c,E} dimension                     = 9
```

The audited `rank NS(K_c)=20` and the Creutz--Viray `(4,4)` dimension formula give

```text
dim_F2 Br(K_c_Qbar)[2] = 2.
```

Therefore the exact presentation must have

```text
dim im(x-alpha) = 7
```

inside a 9-dimensional `L_{c,E}` space. The remaining explicit relation problem is thus pinned to a rank-7 relation subspace in dimension 9.

CI evidence:

```text
workflow_run                    = 32685844234
workflow_conclusion             = success
branch_certificate_sha256       = b8ecf7fcc710f736f23748f0f414e10a748d8006435e4b080294c820429a24e2
cv_dimension_certificate_sha256 = 0683014445edb29a9b5790ed77e3e7fe19055557b30dbb97cc96856910ab0484
artifact_id                     = 9505615578
artifact_zip_sha256             = 2a912e061bda1d6b0d86fd4050a668e99f963d8b3fb1501d695c1344a02fc754
```

## 3. Current exact Class-2 leaf

The source-space dimension is now exact, but the explicit arithmetic presentation is not yet materialized.

```text
LEAF_ID=L33-05-CV-EXPLICIT-9D-PRESENTATION
CLASS=2
NEW_THEOREM_REQUIRED=false
INPUT_SPACE_DIM=9
REQUIRED_XALPHA_IMAGE_RANK=7
REQUIRED_BRAUER_QUOTIENT_DIM=2
```

The remaining bounded work is:

```text
1. choose and certify an explicit 9-element basis of L_{c,E};
2. transport a certified NS(K_c) generating set to the ruled model / generic fiber;
3. compute the exact 7-dimensional x-alpha image as an explicit relation matrix;
4. choose two explicit quotient symbol representatives;
5. compute complex-conjugation action on that quotient basis;
6. compute the invariant/descended subspace and descent obstruction;
7. materialize every surviving Q-defined arithmetic representative, or exact zero survival.
```

A partial explicit Jacobian 2-torsion skeleton is already fixed by the four branch values: on each normalized component `z^2=q(t)`, ratios `(t-r_i)/(t-r_j)` have divisor twice a 2-torsion divisor class. This does not yet constitute a complete 9-element `L_{c,E}` basis because the seven dual-graph cycle generators and the quotient by `K*L*2` still need explicit compatible functions.

```text
EXPLICIT_LCE_BASIS_MATERIALIZED=false
EXPLICIT_XALPHA_MATRIX_MATERIALIZED=false
Q_RELEVANT_SURVIVING_DIM=NOT_YET_CERTIFIED
UNRESOLVED_UNKNOWN_IN_SCOPE>0
UNIT_STATUS=RUNNING
NEXT_EXPECTED_COMMAND=Stage33-main-batch
```
