# Stage33 33-12 micro-goal execution roadmap

```text
ROLE=PLANNING_AND_EXECUTION_CHECKLIST_ONLY
LIVE_AUTHORITY=stages/stage33/controller.json + stages/stage33/MAIN-STATE.json
CURRENT_LOCKED_FRONTIER=V41_THROUGH_V58
EFFECTIVE_DISCOVERY_ROUTING=V58_ARSENAL_FIRST_REPEATABLE_BOUNDED_SEARCH_NO_FIXED_CAP
CURRENT_LEAF=A2.4B_B1_BRANCH_H1_TO_PROPER14_BRAUER_IMAGE_MATRIX
ATOMICITY_RULE=ONE_SMALL_VERIFIABLE_GOAL_PER_COMMIT
MERGE_ALLOWED=false
```

This file changes execution granularity only. Mathematical credit comes from exact certificates and the live authority. A roadmap item is complete only when its acceptance condition is mechanically checkable.

## Operating rule

Every `stage33main batch` selects the first unfinished leaf, works only that leaf, adds one deterministic artifact/certificate and one dedicated replay, commits it, then re-evaluates the roadmap. If blocked, record the smallest exact missing datum/interface; do not reopen a broad goal.

## Research OS / Arsenal routing

For every leaf, identify the exact missing object first, then consult `docs/arsenal/index.json` and only the relevant generated card(s). Live Stage33 source locks override Arsenal cards. `S33-PW*` cards are PROVISIONAL routing aids and must be revalidated.

V58 supersedes only the inherited V41 fixed one-search count cap:

1. Arsenal first.
2. If needed, use a bounded repository search tied to a concrete load-bearing subquestion and narrow named scope.
3. Additional bounded searches have **no fixed per-object count cap**, but each repeat requires a materially new mathematical signal, alias, map/producer name, exact source path, or independently implicated candidate family.
4. Stop when the bounded question is answered, the named family is exhausted, an asset is source-locked, or the next move would become repository-wide enumeration, branch-history archaeology, unconstrained keyword expansion, or near-equivalent miss chasing.
5. A search miss proves neither repository absence nor mathematical nonexistence.
6. Construct as soon as the current leaf has enough exact information; search is not a goal by itself.

Unbounded/open-ended search remains forbidden. The StageA1/A2 -> Stage34 Class3 family (`S34-W01`--`S34-W03`) is not the default route here unless a later leaf independently satisfies its exact hypotheses.

## Locked starting point

- V41: independent `e3`, retained10 standard mask `4`, proper14 mask `20`; no `J2=e2+e3` split.
- V42: J2-specific V25 lift cannot be relabelled as e3.
- V45-V50: boundary-source presentations are not the proper geometric `Br(Sbar)[2]` module; the proposed 14x14 `P_W` route is retired as wrong object type.
- V51: active route rewired to source -> literal Cech `H2(mu2)` -> exact marked Brauer binding -> Kummer.
- V52: J2 `{f2,g22}` is literal geometry for J2 only; no e3 mask20 literal preimage is thereby supplied.
- V53-V54: mask20 has an exact marked Picard-adjoint candidate with semantic label `u1`; semantic `u1` is not an injective identifier.
- V55: the `u1` fiber has `4096` marked source points; mask20 is one and semantic data alone leave `4095` alternatives.
- V56: exact missing interface localized to `MASK20_MARKED_PICARD_ADJOINT_TO_LITERAL_FULL_SURFACE_CECH_GEOMETRY`.
- V57: the quotient-branch route is reduced exactly to
  `Phi_B1:H1(C21_tilde disjoint_union C22_tilde,F2)->Br(Kc_tilde_bar)[2]`,
  with domain dimension `4`, proper geometric Brauer dimension `14`, required matrix shape `14 x 4`, known J2 image `(0,kappa_D) -> mask25`, and e3 mask20 membership still `OPEN_NOT_COMPUTED`.
- V58: fixed one-search cap revoked operationally; repeatable bounded search is allowed under the Research OS stopping discipline above. V57 mathematics is unchanged.
- Stage33 remains `6/11`; adapted J2 columns `1/10`; original standard columns `0/10`; Stage33-12 not closed; Stage33-13 not released; merge forbidden.

## Phase A — finish one independent e3 column

### A1 — source and route lock — COMPLETE

#### A1.0 — independent e3 proper14 source — PASS V41
Arsenal: `○ S33-PW04 + S30-WF02`.

Acceptance: exact independent proper14 mask `20`, retained10 mask `4`, no XOR split.

#### A1.1 — retire invalid boundary14 -> proper14 bridge — PASS V45-V50
Arsenal: `◎ S33-PW05 + S33-PW04`, firewall `S30-WF03`.

Acceptance: wrong-type `P_W` route retired; no positional basis identification.

#### A1.2 — direct source-bound Cech route — PASS V51
Arsenal: `◎ S33-PW07 + S33-PW04`, firewall `S30-WF03`; `S33-PW08` only for a genuine Gersten construction.

Acceptance: J2-specific data are not reusable as e3 data; target remains mask20.

### A2 — materialize an explicit source-bound e3 Cech `H2(mu2)` preimage

#### A2.0 — literal/marked interface classification — PASS-BLOCKED V52
Arsenal: `◎ S33-PW07 + S33-PW04`.

Acceptance: J2 literal representative separated from e3; exact missing e3 literal geometry named without absence claim.

#### A2.1 — marked Picard-adjoint candidate — PASS V53
Arsenal: `◎ S33-PW04`, firewall `S30-WF03`.

Acceptance: exact 20D marked candidate for mask20; no literal Cech/full-surface lift credit.

#### A2.2 — semantic label binding — PASS V54
Arsenal: `◎ S33-PW04`, firewall `S30-WF03`.

Acceptance: semantic `u1/[1,0]` bound exactly but not treated as injective and not used to identify e3 with J2.

#### A2.3 — quantify fiber/localize geometry interface — PASS-BLOCKED V55-V56
Arsenal: `◎ S33-PW07 + S33-PW04`, firewall `S30-WF03`.

Acceptance: rank/fiber `4096` exact; mask20-specific literal geometry remains the missing interface.

#### A2.4A — reduce the B1 quotient geometry to a finite membership gate — PASS V57
Arsenal: `◎ S33-PW07 + S33-PW04`, firewall `S30-WF03`.

Acceptance already certified:
- `C21_tilde` and `C22_tilde` are genus-one branch components;
- `H1(C21 ⊔ C22,F2)` has dimension `4`;
- proper geometric `Br[2]` has dimension `14`;
- exact route is represented by a required `14 x 4` marked matrix;
- J2 supplies one exact nonzero route image, mask `25`;
- e3 target is mask `20`;
- one J2 image does not determine the matrix;
- no e3 membership, Cech lift, Kummer column, closure, or merge credit is granted.

#### A2.4B — materialize the B1 branch-H1 -> proper14 matrix and solve mask20 membership — CURRENT
Arsenal: `◎ S33-PW07 + S33-PW04`, firewall `S30-WF03`. Consult Arsenal first; then repeat bounded searches only under V58 when a genuinely new domain-basis/Gysin/marked-binding signal appears.

Exact object:
`B1_BRANCH_H1_TO_PROPER14_BRAUER_IMAGE_MATRIX`.

Acceptance:
- source-lock an ordered F2 basis of `H1(C21_tilde,F2) ⊕ H1(C22_tilde,F2)`, equivalently two independent `Pic0[2]` generators on each genus-one component;
- source-lock the exact Gysin/Brauer image producer into the V41/V50 proper14 marked basis;
- materialize all four image columns as an exact `14 x 4` F2 matrix `M`;
- replay rank/image data and solve `M*x = mask20`;
- record all preimage solutions if membership is true;
- if membership is false, freeze **only** this B1 quotient route for e3 and move to a different source-bound geometric realization;
- a B1 miss must not be promoted to nonexistence of an e3 `H2(mu2)` lift;
- semantic `u1`, the J2 input `(0,kappa_D)`, and `{f2,g22}` are not membership proofs.

Current next exact leaf:
`SOURCE_LOCK_ORDERED_B1_PIC0_2_BASIS_AND_EXACT_PROPER14_GYSIN_IMAGE_PRODUCER`.

A2.4B is not complete until the exact matrix and membership solve are committed.

#### A2.4C — construct literal e3 Cech geometry from the solved source — BLOCKED ON A2.4B
Arsenal: `◎ S33-PW07 + S33-PW04`; `S33-PW08` only if actual Gersten/residue data enter.

PASS branch: use an exact B1 preimage of mask20 to materialize literal function/divisor/transition/Cech geometry and bind its Brauer image to mask20.

FAIL branch: replace B1 with the next independently source-bound geometric route; do not claim lift nonexistence.

### A3 — residue/resolution audit of the concrete e3 representative
Arsenal: `◎ S33-PW07`; `S33-PW08` only for an actual Gersten route.

Acceptance: complete relevant codimension-one/resolution support and exact residues/square trivializations, including exceptional contributions.

### A4 — bind the residue-clean full-surface lift to exact e3
Arsenal: `◎ S33-PW04 + S33-PW07`, firewall `S30-WF03`.

Acceptance: Brauer image equals proper14 mask `20` / retained10 mask `4`; independence from XOR split; only here may `GENUINE_FULL_SURFACE_H2_MU2_LIFT_FOR_E3=true`.

### A5 — compute the e3 Kummer column
Arsenal: `◎ S33-PW05`, support `S33-PW02` where applicable.

Acceptance: exact target basis/order and full column replayed from A4; no guessed standard col2/col3 decomposition.

### A6 — integrate/audit the `2/10` adapted-column frontier
Arsenal: `△ S30-WF02 + S30-WF03`.

Acceptance: adapted columns `2/10` only after A1-A5; standard-column credit remains separately justified; controller/state/certificate and exact-head CI agree.

## Phase B — remaining adapted sources

Locked source order after e3:
`e1, e4, e5, e6, e7, e8, e9, e10`.

For each source `s`, use one commit per leaf:
- B(s).0 independent proper14/retained10 source — `○ S33-PW04 + S30-WF02`;
- B(s).1 marked Picard-adjoint candidate when applicable — `◎ S33-PW04 + S30-WF03`;
- B(s).2 semantic label binding without injectivity assumption — `◎ S33-PW04 + S30-WF03`;
- B(s).3 quantify/source-localize any semantic fiber — `◎ S33-PW04 + S30-WF03`;
- B(s).4 literal source-bound Cech `H2(mu2)` — `◎ S33-PW07 + S33-PW04`, conditional `S33-PW08`;
- B(s).5 residue/resolution audit — `◎ S33-PW07`, conditional `S33-PW08`;
- B(s).6 exact source/Brauer/full-surface binding — `◎ S33-PW04 + S33-PW07 + S30-WF03`;
- B(s).7 exact Kummer column — `◎ S33-PW05`, support `S33-PW02`;
- B(s).8 frontier integration/exact-head replay — `△ S30-WF02 + S30-WF03`.

A genuinely linear reusable adapter may be instantiated for later sources, but every source still requires separate binding and column credit.

## Phase C — close the finite V4 Kummer matrix repair

### C1 — assemble complete adapted-source matrix
Arsenal: `○ S33-PW05 + S33-PW02`.
Acceptance: all 10 adapted columns are materialized from genuine source-bound lifts with exact provenance.

### C2 — exact rank/kernel/image
Arsenal: `×`.
Acceptance: exact F2 rank, kernel basis, image basis, named survivor/vanishing data.

### C3 — translate to relevant Q-defined class inventory
Arsenal: `○ S33-PW01 + S33-PW06`.
Acceptance: every relevant class has order, provenance, source representative, and disposition; no dimension-only credit.

### C4 — satisfy Stage33-12 exit requirements
Treat each as a separate commit/gate:
- `ARITHMETIC_HS_D2_COMPUTED` — `◎ S33-PW01`;
- `GLOBAL_Q_RESIDUE_LIFT_COMPLETION` — no direct completion card; `S33-PW06`, `S33-PW08`, `S30-W02` are support only;
- `COMPLETE_RELEVANT_Q_DEFINED_CLASS_INVENTORY_FOR_FROZEN_STAGE33_BRAUER_SCOPE` — `○ S33-PW01 + S33-PW06`;
- `STAGE33_07_HOSTILE_RECERTIFICATION_PASS` — `△ S30-WF02 + S30-WF03`.

No Arsenal miss is a negative theorem.

### C5 — Stage33-07 hostile recertification/reclose
Arsenal: `△ S30-WF02 + S30-WF03`.
Acceptance: exact-head hostile replay passes and current authority explicitly recloses Stage33-07.

## Phase D — remaining five big Stage33 tasks

The denominator remains the original 11 big tasks; repair children do not increment it.

### D1 — Stage33-08 endpoint representatives
Arsenal: `○ S33-PW07`.
One surviving class per leaf; exact locally evaluable representative, field/domain/ramification, equivalence/independence.

### D2 — Stage33-40 relevant places
Arsenal: `×`.
Derive the finite set from actual D1 representatives and source-locked ramification/domain data.

### D3 — Stage33-41 exact local evaluations
Arsenal: `○ S33-PW06 + S33-PW08` as support only.
One `(place,class,physical-locus)` block per commit; exact image and constancy/nonconstancy.

### D4 — Stage33-42 adelic compatibility
Arsenal: `×`.
Assemble exact local images under reciprocity and compute the final Brauer disposition for the frozen scope.

### D5 — hostile audit / endpoint promotion
Arsenal: `△ S30-WF02 + S30-WF03`.
Only a passing exact-head authority transition may grant completion/endpoint credit. Perfect-cuboid existence/nonexistence remains forbidden absent a separate audited full-endpoint certificate.

## Batch selection rule

At each `stage33main batch`:
1. read live controller/state and this roadmap;
2. choose the first unfinished leaf with satisfied prerequisites;
3. identify its exact missing object and consult Arsenal first;
4. if needed, run only V58-compliant bounded searches; repeat only on materially new mathematical signal and stop before unbounded archaeology;
5. construct/replay only that leaf;
6. commit it;
7. if context remains, start the next leaf as a new commit.

Never skip forward because a later calculation looks easier. Never reopen a completed leaf without a new contradiction, failed verifier, hostile-audit finding, or changed authoritative input.
