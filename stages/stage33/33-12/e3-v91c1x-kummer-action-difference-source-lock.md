# V91C1X Kummer action-difference source lock

Scope: the literal `A2_02` full-surface Cech-Cartier seed assembled in
`e3-v91c1d-a2-02-purity-cech-cartier-assembly.json`, acted on by the exact
`swap12 * swap13 * swap12` word (the `swap23` coordinate automorphism) selected
by V91C1Q and transported on literal divisors by V91C1R/S.

## External theorem lock

The only external theorem used by V91C1X is the standard Kummer exact sequence
and functoriality of connecting maps.

1. Stacks Project, Tag 03PL, Lemma 59.28.1:
   https://stacks.math.columbia.edu/tag/03PL
   For `n` invertible on a scheme `S`,
   `0 -> mu_n -> G_m --(n-th power)--> G_m -> 0`
   is exact on the etale site. For the geometric characteristic-zero surface
   here, `n=2` is invertible. Its long exact sequence contains
   `Pic(Sbar)/2 -> H^2_et(Sbar,mu_2) -> Br(Sbar)[2] -> 0`.

2. Stacks Project, Tag 0117, Lemma 12.13.12:
   https://stacks.math.columbia.edu/tag/0117
   The long exact cohomology sequence attached to a short exact sequence is
   functorial in the short exact sequence. Hence the Kummer connecting map
   `delta` commutes with every automorphism `g` of the locked geometric surface.

Therefore, for a Cech-Cartier representative whose acted-minus-original
Cartier line-bundle class is `[L_g] in Pic(Sbar)/2`,
`g(seed) - seed = delta([L_g])`.
In particular, `[L_g]=0` in `Pic/2` implies `g(seed)=seed` in
`H^2_et(Sbar,mu_2)`.

## Stage33 source binding

V91C1D materializes the literal A2_02 full-surface Cech-Cartier seed, its
prime-level transition data, and Cartier binding. V91C1Q selects the shortest
residue-stabilizer word that moves proper14 mask20. V91C1R/S apply that exact
word to the literal Cech-Cartier data and materialize the complete
acted-minus-original codimension-one divisor difference. V91C1W source-binds
that same complete difference to retained Picard64 and computes its class in
`Pic/2` to be zero.

Thus the theorem above applies to the exact V91C1W class; no J2 value,
positional boundary/proper14 identification, target-side Picard adjoint, or
marked-coordinate guess is imported.

After seed fixedness is obtained, naturality of
`H^2_et(Sbar,mu_2) -> Br(Sbar)[2]` forces the unknown marked Brauer image of
this literal seed to be fixed by the same `swap23`. V91C1Q computes that the
proper14 vector mask20 is moved to mask22 (support `[2,3,5]`). Therefore the
unknown image is not mask20.

This does not compute the unknown marked Brauer image, does not materialize the
e3 genuine lift/Kummer column, and grants no Stage33-12, Stage33-13, theorem,
receiver, endpoint, or perfect-cuboid credit.
