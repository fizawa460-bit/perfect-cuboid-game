# Stage33-12 MAIN exact assembly checkpoint

Status: `MAIN_IN_PROGRESS_EXPLICIT_J2_d2_TORSOR_NEXT_4_OF_5`

Stage33-12 remains open. Stage33-07 remains open. Stage33-13 is not released.

## Fixed marked receiver

```text
T(Kc) ~= <4> direct_sum <8>
Br(Kc)[2] = Hom(T,Z/2)
[1,0] -> kernel minimum norm 8
[0,1] -> kernel minimum norm 4
[1,1] -> kernel minimum norm 12
```

The marked Brauer functional is still one of the three nonzero functionals. The independent branch-normalization datum `phi_*(E_J2)=(0,0) in E'[2]` remains retained.

## Exact elliptic fibration

For `K=Q(t)` and the `P1_t` ruling:

```text
H=t^4-4*t^2+1
q=t^4-6*t^2+1
D=(t^2-1)^2*q
E: Y^2=X*(X^2-2*H*X+D)
  =X*(X-(t^2-1)^2)*(X-q).
```

Hence this generic Jacobian has full rational 2-torsion, including `T0=(0,0)`.

## NEW exact CV -> Leray/Sha -> 2-isogeny coordinate

The named Creutz--Viray representative on the even quartic generic fiber is

```text
ell_J2 = 4*(t^2*beta+t^4-4*t^2+2)
         /((t^2-1)*(t^2-2*t-1)),
beta=alpha^2,
M=K[beta]/(beta^2+A*beta+1).
```

Thus `ell_J2` is invariant under the branch-root involution `alpha -> -alpha`. In the standard even-quartic descent realization

```text
L^x/(K^x L^{x2}) -> H^1(K,E[2]),
E[2] ~= ker(sum:F2^Omega->F2)/F2*D,
```

one may choose the square-root cochain constant on the two `alpha -> -alpha` orbits. Every cocycle value is therefore pair-constant, and modulo the diagonal lies in the one-dimensional subgroup generated by the corresponding partition class `T_iota`.

The quartic-to-Jacobian map

```text
x=(y+1)/s^2,
R=s*(x^2-1),
R^2=(x^2-1)*(2*x+A),
U=2*x+A,
V=2*R
```

identifies this partition class with `T_iota=(U,V)=(0,0)`, hence with `T0=(X,Y)=(0,0)` on the polynomial Kc model.

For the fixed quadratic subalgebra `M/K`, exact reduction gives

```text
Norm_M/K(t^2*beta+t^4-4*t^2+2)=2*(t^2-1)^2,
Norm_M/K(ell_J2)=32/(t^2-2*t-1)^2.
```

Therefore the induced `H^1(K,<T0>)` Kummer character has squareclass

```text
d = 2 in K^x/K^{x2}.
```

So the previous `d=Dplus` guess is rejected as the named J2 coordinate. It remains only a generic homogeneous-space candidate sharing the visible support polynomial.

Exact certificate: `j2-cv-leray-2isogeny-coordinate.json`; canonical SHA256 `52a3e741fbe6158a2a9434c9797207f6e126fc7d9df2ab09a80d3edd5e005771`; verifier `certify_j2_cv_leray_2isogeny_coordinate.py`.

## Explicit named J2 torsor

The J2 Weil-Chatelet/Sha class is now represented generically by the 2-isogeny homogeneous space

```text
Y_J2/K:
N^2 = 2*U^4 - 2*H*U^2*V^2 + (D/2)*V^4.
```

Thus

```text
J2_2ISOGENY_KERNEL_MEMBERSHIP_CERTIFIED=true
J2_2ISOGENY_SQUARECLASS_SELECTED=true
J2_2ISOGENY_SQUARECLASS=d=2
J2_TORSOR_EQUATION_MATERIALIZED=true
MARKED_BRAUER_FUNCTIONAL_SELECTED=false
CANDIDATES=3
```

The next exact leaf is now purely lattice/geometric:

`COMPUTE_NS_OR_T_OF_THE_EXPLICIT_d_EQUALS_2_GENUS_ONE_K3_TORSOR_AND_MATCH_MINIMUM_NORM_4_8_12`.

Once its transcendental minimum norm is `4`, `8`, or `12`, the fixed marked Brauer coordinate is respectively `[0,1]`, `[1,0]`, or `[1,1]`.

## Firewalls

```text
Stage33-12 visible progress = 4/5
J2 marked Brauer functional materialized = false
J2 twisted transcendental kernel identified = false
Stage33-12 exact closure = false
Stage33-13 released = false
heavy actions authorized = false
theorem credit = false
receiver credit = false
endpoint credit = false
perfect cuboid existence/nonexistence claim = false
```
