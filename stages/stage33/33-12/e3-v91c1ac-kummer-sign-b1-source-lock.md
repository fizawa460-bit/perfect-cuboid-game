# V91C1AC Kummer sign_b1 source lock

Scope: extend the already locked V91C1X Kummer action-difference theorem to the exact `sign_b1` coordinate automorphism selected by V91C1Z and materialized on the literal `A2_02` Cech-Cartier seed by V91C1AA/AB.

## Theorem lock

No new external theorem is added. V91C1X already locks the Kummer exact sequence and functoriality of connecting maps (Stacks Project Tags 03PL and 0117), and states for every automorphism `g` of the locked geometric surface:

`g(seed) - seed = delta([L_g])`, with `[L_g] in Pic(Sbar)/2` the acted-minus-original Cartier line-bundle class.

Hence `[L_g]=0` implies `g(seed)=seed` in `H^2_et(Sbar,mu_2)`.

## Exact Stage33 binding for sign_b1

`sign_b1` is the exact ambient coordinate involution `b1 -> -b1`, fixing the other six projective coordinates. V91C1Z selects it as one of the shortest residue-stabilizer target reducers. V91C1AA checks its literal boundary-function package transport without promoting package matching to cohomology. V91C1AB then acts the actual strict-transform prime ideals directly and evaluates all 48 frozen exceptional primes.

For `sign_b1`, V91C1AB computes:

- every one of the eight A2_02 components has zero strict-prime difference;
- every one of the eight components has zero exceptional-prime difference;
- the strict package difference is the zero divisor;
- the exceptional package difference is the zero divisor;
- therefore the complete acted-minus-original codimension-one Cartier divisor is literally zero, not merely zero after a retained-inventory match or a Picard-coordinate guess.

Thus `[L_sign_b1]=0` in `Pic(Sbar)/2` tautologically, and the V91C1X theorem lock gives exact literal H2(mu2) seed fixedness under `sign_b1`.

Naturality of `H^2_et(Sbar,mu_2) -> Br(Sbar)[2]` then forces the unknown proper14 marked Brauer image of the literal A2_02 seed to be fixed by the exact proper14 action of `sign_b1` as well as by the already source-bound `cc`, `ct`, and `swap23` constraints.

This source lock does not compute the marked Brauer image, does not assert `sign_a2` seed fixedness, does not materialize the e3 genuine full-surface lift/Kummer column, and grants no Stage33-12/33-13, theorem, receiver, endpoint, or perfect-cuboid credit.