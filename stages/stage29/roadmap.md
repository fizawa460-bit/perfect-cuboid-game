# Stage29 — audited R2 foundation synthesis, exact bridges, and multi-route endpoint attack

```text
STAGE=Stage29
ROADMAP_REVISION=R2_POST_29_02_FOUNDATION_SCREEN_AUDITED
ROADMAP_R2_AUDIT=PASS_AFTER_MATERIAL_REPAIR
ROLE=FOUNDATION_SYNTHESIS_PLUS_EXACT_BRIDGES_PLUS_MULTI_ROUTE_ENDPOINT_ATTACK
NUMBERING=INCREMENTAL_01_02_03...
LEGACY_10_70_SEQUENCE_USED=false
PERFECT_CUBOID_ENDPOINT_ITSELF_DEFERRED=true
STAGE28_FINAL_STATUS=CLOSED_AUDITED_PASS_MERGED
STAGE29_02_STATUS=AUDITED_SCREENING_COMPLETE_MERGED
```

Stage29 began as a search for materially different foundations beyond Stage16–28. Audited 29-02 work produced substantially more global structure than the original roadmap assumed: the full endpoint surface, the joint V4 completion model, the degree-64 seven-line sign/Kummer presentation, Campedelli quotients, Beauville/modular/L-function/Brauer routes, and the non-Fano/Hirzebruch recognition adapter over `Q(i)` with an explicit `Q` twist.

R2 therefore does not rediscover those objects. It must synthesize them without falsely collapsing distinct maps, prove or reject the exact bridges to the population program, attack several genuinely different endpoint routes, and compress only after evidence-based deduplication.

## Core operating policy

### 1. Living roadmap with a hard anti-loop rule

```text
ROADMAP_IS_LIVING=true
ROADMAP_REVIEW_DOES_NOT_IMPLY_REWRITE=true
ROADMAP_REWRITE_REQUIRES_MATERIALITY_CERTIFICATE=true
COMPLETED_AUDITED_ITEMS_IMMUTABLE=true
REOPEN_COMPLETED_ITEM_ONLY_VIA_TARGETED_ADDENDUM=true
REWRITE_APPLIES_TO_UNEXECUTED_FUTURE_ITEMS_BY_DEFAULT=true
COSMETIC_RESULT_DOES_NOT_TRIGGER_ROADMAP_REWRITE=true
OLD_GATE_REPLAY_WITHOUT_NEW_INPUT=false
```

A materiality certificate must name the exact new theorem/model/map/obstruction/coverage result, the affected future items, and why the current receiver graph is inadequate. A small receiver closure, a renamed coordinate system, or a merely suggestive analogy is not enough.

Every named gap scan is also a formal roadmap review, but `ROADMAP_REVIEW=STILL_VALID` is the default unless a materiality certificate exists.

### 2. Multi-route attack without duplicate work

```text
PREMATURE_SINGLE_ROUTE_SELECTION=false
MULTI_ROUTE_ATTACK_ALLOWED=true
PARALLEL_ROUTE_PORTFOLIO_DEFAULT=true
ROUTE_PRUNING_REQUIRES_EVIDENCE=true
EARLY_EXACT_DEDUP_REQUIRED=true
FINAL_PORTFOLIO_COMPRESSION_AT_29_16=true
```

A route may be retired or merged early when exact redundancy, insufficient coverage, a red theorem trigger, a field-of-definition obstruction, or strict domination is proved. Waiting until 29-16 is not required for exact deduplication; 29-16 is only the final mandatory portfolio compression.

By the end of 29-05 every live receiver must have one canonical `ROUTE_ID` and one primary portfolio owner. Other stages may cross-reference it, but may not count the same mathematical mechanism as independent progress.

### 3. Backflow rule

```text
SEQUENTIAL_STAGE16_TO_STAGE28_RERUN=false
TARGETED_BACKFLOW_REQUIRES_NEW_EXACT_RECEIVER=true
BACKFLOW_ONLY_TO_RELEVANT_STAGE=true
BACKFLOW_RESULT_REIMPORTED_ONCE=true
BACKFLOW_DOES_NOT_REOPEN_FROZEN_STAGE_GLOBALLY=true
```

### 4. Population / exact-stratum firewall

The full endpoint is an exact degree-64 sign/Kummer cover of `P2`, but the Stage16–20 counting strata are not automatically literal successive floors of that cover.

```text
FULL_ENDPOINT_IS_DEGREE64_SIGN_KUMMER_COVER=true
STAGE16_20_AS_LITERAL_SIGN_TOWER_LEVELS_PROVED=false
POPULATION_TRANSFER_TO_SIGN_TOWER_AUTOMATIC=false
HEIGHT_TRANSFER_AUTOMATIC=false
PRIMITIVITY_TRANSFER_AUTOMATIC=false
CANONICAL_ORDER_TRANSFER_AUTOMATIC=false
MULTIPLICITY_TRANSFER_AUTOMATIC=false
ASYMPTOTIC_TRANSFER_AUTOMATIC=false
EXACT_STRATUM_IS_NOT_AUTOMATIC_NESTED_TRANSITION=true
```

Every population comparison must first name a common host and exact predicate masks. In particular the audited numerical ledger already distinguishes:

```text
N2_over_M2_is_literal_finite_survival=true
M3_over_M2_is_objectwise_survival=false
M3_over_N2_is_survival_probability=false
FINITE_DATA_IS_NOT_ASYMPTOTIC_THEOREM=true
```

No roadmap item may silently convert a comparison of disjoint exact strata into a conditional survival probability.

### 5. Field-of-definition / rational-point direction firewall

Every node and arrow used for endpoint arithmetic must record:

```text
BASE_FIELD
Q_FORM_OR_TWIST
MAP_DIRECTION
MAP_TYPE
MAP_DEFINED_OVER_Q=true/false
RATIONAL_POINT_PUSHFORWARD_VALID=true/false
RATIONAL_POINT_LIFT_REQUIRES_TORSOR_OR_TWIST=true/false
```

A theorem over `Q(i)` is not a theorem for the physical `Q` endpoint without an audited descent/twist adapter.

## Audited infrastructure entering 29-03

The labels F1–F8 are an audited foundation/lens inventory, not a claim that all eight are pairwise independent foundations.

```text
INDEPENDENT_FOUNDATION_COUNT_ASSERTED=false
```

- `F1`: full perfect-cuboid surface / global endpoint geometry;
- `F2`: joint space-completion × third-face-completion V4 cover over the common two-face base;
- `F3`: parametrization/coverage lens;
- `F4`: joint local-arithmetic lens;
- `F5`: modular `M(4,8)` / 8-congruence / Weil-restriction route;
- `F6`: endpoint L-function, K3-character, and arithmetic-cohomology route;
- `F7`: full seven-line degree-64 `(Z/2)^6` sign/Kummer cover of `P2`;
- `F8`: classical Campedelli quotient compression.

Additional audited adapters/routes include the Beauville irregular degree-two cover of the endpoint and the non-Fano/Hirzebruch recognition of F7 over `Q(i)` with explicit constant-sign `Q` twist.

The broad 29-02hd pass found no certified ninth independent foundation in that pass. This is a routing stop only:

```text
NO_MORE_FOUNDATIONS_EXIST_CLAIM=false
LITERATURE_EXHAUSTIVENESS_CLAIM=false
H_NAMESPACE_REOPENABLE=true
PESCHMANN_INDEPENDENCE_RESOLVED=false
```

## Revised incremental sequence

```text
29-01 GLOBAL_CERTIFIED_MAP_LOCK                         COMPLETE
29-02 NEW_FOUNDATION_SCREENING                         COMPLETE
29-03 FOUNDATION_BACKFLOW_DECISION_AND_BRIDGE_QUEUE_LOCK
29-04 POPULATION_HOST_PREDICATE_AND_CONDITION_COST_MATRIX
29-05 DEPENDENCY_EQUIVALENCE_ROUTE_OWNERSHIP_AND_DOUBLE_CHARGE_LEDGER
GAP_SCAN_A / ROADMAP_REVIEW_A
29-06 GLOBAL_FOUNDATION_SYNTHESIS
29-07 SIGN_TOWER_JOINT_V4_AND_POPULATION_BRIDGE
29-08 PARAMETRIZATION_FIBRATION_AND_COVERAGE_ATLAS
29-09 FULL_ENDPOINT_LOCAL_ARITHMETIC
GAP_SCAN_B / ROADMAP_REVIEW_B
29-10 GLOBAL_AND_K3_ATTACK_PORTFOLIO
29-11 QUOTIENT_DESCENT_AND_MODULAR_ATTACK_PORTFOLIO
29-12 JOINT_LOCAL_PARAMETRIC_AND_INTERACTION_ATTACK_PORTFOLIO
GAP_SCAN_C / ROADMAP_REVIEW_C
29-13 A2_METHOD_TRANSFER_ACROSS_SURVIVING_ROUTES
29-14 NATURAL_SLICE_QUOTIENT_AND_COVERAGE_TEST
29-15 ENDPOINT_ARSENAL_REMATCH
29-16 RESIDUAL_RECEIVER_COMPRESSION_AND_ROUTE_PORTFOLIO
GAP_SCAN_FINAL / ROADMAP_REVIEW_FINAL
29-17 PERFECT_CUBOID_ATTACK_HANDOFF
29-close
```

### 29-01 — GLOBAL_CERTIFIED_MAP_LOCK — COMPLETE

Freeze the certified Stage16–28 population surface, Stage28 closure, physical conventions, and exact finite numerical baseline.

### 29-02 — NEW_FOUNDATION_SCREENING — COMPLETE

Import and audit the endpoint foundations/theorem ecosystems. The practical stop is only `NO_CERTIFIED_NINTH_FOUNDATION_IN_THIS_BROAD_PASS`.

### 29-03 — FOUNDATION_BACKFLOW_DECISION_AND_BRIDGE_QUEUE_LOCK

Do not re-ratify R2; #1307 is the roadmap audit. Instead decide the execution location of the exact bridge receivers and lock the immediate queue.

Priority receivers:

```text
R29-KUM3A = TwoFaceSignSubcoverToStage28ToricYBirationalAdapter
R29-KUM3B = JointV4AsResidualTwoSquareRootsOfFullSignTower
R29-KUM4  = Stage16To20PopulationMaskAsSignSubcoverLattice
R29-PESCH1 = EuclidPairToStage28TwoFaceHostAndJointV4ExactCrosswalk
```

For each choose exactly one:

```text
TARGETED_BACKFLOW_REQUIRED
STAGE29_INTERNAL_ADAPTER_ONLY
DEFER_WITH_REASON
REDUNDANT_WITH_AUDITED_ROUTE
```

A backflow must name the old stage, new receiver, new input, and exact reimport contract.

### 29-04 — POPULATION_HOST_PREDICATE_AND_CONDITION_COST_MATRIX

Rebuild the counting ledger from named ambient hosts and exact masks, not from informal arrows between exact strata.

For every comparison record:

```text
HOST_POPULATION
PREDICATE_OR_MASK
YES_STRATUM
NO_STRATUM
NESTED_CONDITIONING_VALID=true/false
FINITE_RATIO_INTERPRETATION
KNOWN_ASYMPTOTIC_OR_POWER_CORRIDOR
PHYSICAL_HEIGHT
PRIMITIVE_NORMALIZATION
CANONICAL_MULTIPLICITY
LOCAL_OBSTRUCTION_LAW
GEOMETRIC_COVER_OR_QUOTIENT_IF_PROVED
```

Required firewall examples:

- `N2/M2` may be treated as literal finite survival under the audited common definition;
- `M3/M2` is not objectwise survival because `M2` and `M3` are exact face-count strata;
- Stage28's `M3/N2` bridge is a population-size comparison, not a survival probability;
- when exact strata are disjoint, use a common host such as the appropriate `>=k` host and explicit masks.

Only after this host/mask ledger exists may 29-07 attempt a sign-tower population interpretation.

### 29-05 — DEPENDENCY_EQUIVALENCE_ROUTE_OWNERSHIP_AND_DOUBLE_CHARGE_LEDGER

Determine which mechanisms are genuinely independent and which are the same condition in different languages.

Audit at least squareclass, Gaussian norm, Pythagorean parametrization, local blocker laws, thin covers, K3 quotients, joint V4, sign/Kummer characters, Campedelli quotients, Beauville descent, modular 8-congruence, L-function pieces, and Brauer/cohomology receivers.

For every live receiver assign:

```text
ROUTE_ID
PRIMARY_PORTFOLIO=29-10|29-11|29-12|PRE_ATTACK_INFRASTRUCTURE
DEPENDENCIES
DUPLICATE_OF_OR_NONE
FIELD_OF_DEFINITION
ENDPOINT_MAP
COVERAGE_STATUS
```

The same receiver may be referenced elsewhere but may have only one primary owner. Exact `RED` or `MERGED` status is applied immediately rather than deferred to 29-16.

### GAP_SCAN_A / ROADMAP_REVIEW_A

This scan occurs after the population and dependency ledgers, not immediately after the roadmap audit. It asks whether those exact ledgers reveal a missing foundation, a necessary targeted backflow, or a material reorder before global synthesis.

### 29-06 — GLOBAL_FOUNDATION_SYNTHESIS

Build an auditable **endpoint-hub graph**, not a single F7 quotient chain.

The node set includes:

```text
S_endpoint = full canonical four-quadric cuboid surface
F7 degree-64 seven-line sign/Kummer presentation over P2
seven sign/K3 quotient directions
ten Campedelli quotient kernels
joint V4 and marginal K3 covers
Beauville irregular cover X_cub
modular M(4,8)/X(8) ecosystem
L-function/character decomposition
non-Fano/Hirzebruch recognition of F7 over Q(i)
```

Required edge semantics:

- `S_endpoint -> P2` is the degree-64 sign-cover presentation;
- the seven K3 and Campedelli objects are certified sign-subgroup quotients where already proved;
- `X_cub -> S_endpoint` is the Beauville degree-two cover on the audited Q-form; do **not** reverse it merely to fit an F7 hierarchy;
- the F2 joint V4 relation to F7 remains an open adapter until KUM3A/B are discharged;
- the modular relation must retain its `Q(i)` and generic-birational scope where appropriate;
- L-function/character decompositions are cohomological identifications, not automatically geometric morphisms;
- non-Fano/Hirzebruch recognition is geometric over `Q(i)` with an explicit Q-twist, not a Q-isomorphism to the standard cover.

Every edge must record direction, degree, field, exact/generic/birational/quotient/cover status, and rational-point functoriality.

```text
F7_UNIVERSAL_ORGANIZER_ASSUMED=false
ENDPOINT_HUB_GRAPH_REQUIRED=true
```

### 29-07 — SIGN_TOWER_JOINT_V4_AND_POPULATION_BRIDGE

Attempt to prove or refute KUM3A/B and then KUM4.

Population work proceeds in two layers:

1. identify predicates on a common ambient geometric host;
2. only then recover exact counting strata as intersections/differences of masks with physical normalization.

Do not require exact-count strata themselves to be nested cover floors.

Allowed conclusions:

```text
FULL_EXACT_TOWER_BRIDGE
PARTIAL_PREDICATE_BRIDGE
GEOMETRIC_ONLY_BRIDGE_NO_POPULATION_TRANSFER
NO_CLEAN_BRIDGE
```

A partial or negative result is valid progress and must not be repaired by analogy.

### 29-08 — PARAMETRIZATION_FIBRATION_AND_COVERAGE_ATLAS

Classify Saunderson, StageA2, Meskhishvili-style, Peschmann-style, low-degree curve, elliptic-fibration, and repository-native families against the endpoint-hub graph.

Record exact image dimension, generic degree/fiber, exceptional locus, field of definition, height distortion if proved, and coverage status. `R29-PESCH1` remains unresolved until an exact rational crosswalk is supplied.

This stage builds/adapts the atlas. It does not count as a 29-12 attack merely because a family is listed.

### 29-09 — FULL_ENDPOINT_LOCAL_ARITHMETIC

Build the exact local data and local-to-global receivers for the full endpoint architecture.

Priority receivers:

```text
R29-KUM-LOC1 = SevenLinearFormCommonSquareclassLocalDensity
R29-KUM-LOC2 = BranchValuationTransitionLedger
```

Compare with Stage14–20 blocker laws and joint-V4 finite-field correlations. Output exact local statements and candidate global receivers.

This is the **infrastructure/data stage**. 29-12 may later attack with these receivers but should not redo the same local computation as separate progress.

### GAP_SCAN_B / ROADMAP_REVIEW_B

Review foundation completeness, newly sharpened backflow receivers, and whether 29-10/11/12 ownership still matches the dependency ledger.

## Multi-route attack block

29-10/11/12 are separate by primary mechanism. A receiver has one owner under the 29-05 registry.

### 29-10 — GLOBAL_AND_K3_ATTACK_PORTFOLIO

Primary mechanism: rational-point/curve/fibration control on the full surface, F7 presentation, and certified K3/low-genus quotients.

Includes low-degree curve exclusions, finite Picard reductions, K3 fibrations, Terasoma specialization adapters where exact, and theorem species attached directly to these models.

Natural slices may be identified here, but systematic slice closure/coverage accounting belongs to 29-14.

### 29-11 — QUOTIENT_DESCENT_AND_MODULAR_ATTACK_PORTFOLIO

Primary mechanism: Q-defined quotient, torsor/descent, Albanese, modular, and Brauer arithmetic.

Campedelli bookkeeping must state exactly:

```text
GEOMETRIC_Qi_KERNEL_ORBITS=8+2
CERTIFIED_Q_DEFINED_S3_ACTION_ORBITS=6+2+2
EXACT_Q_ISOMORPHISM_CLASS_COUNT_PROVED=false
```

Thus `6+2+2` means three orbits under the certified Q-defined `S3` action, not a theorem that there are exactly three Q-isomorphism classes.

Also include Beauville Q-descent/twist/Albanese receivers, modular `M(4,8)` twisted descent with its generic-birational firewall, and open/proper Brauer receivers where exact.

A quotient obstruction is endpoint-decisive only after verifying that the quotient map is Q-defined and every endpoint Q-point pushes forward to a Q-point. Converse lifting requires its own torsor/twist analysis.

### 29-12 — JOINT_LOCAL_PARAMETRIC_AND_INTERACTION_ATTACK_PORTFOLIO

Primary mechanism: simultaneous completion, local/global interaction, exact parametrized/fibered crosswalks, and height-safe counting/sieve receivers.

Includes joint V4 arithmetic, Stage19/Stage20 marginal charts, surviving seven-linear-form local/global receivers from 29-09, Peschmann only if `R29-PESCH1` becomes exact, branch-sensitive interaction thresholds, and height/multiplicity-safe counting or sieve routes.

Finite computations remain regression/negative control, not global proof.

### GAP_SCAN_C / ROADMAP_REVIEW_C

Classify each canonical `ROUTE_ID`:

```text
GREEN = concrete theorem path with exact receiver
AMBER = meaningful but missing one identifiable theorem/adapter
RED = structurally blocked or dominated
MERGED = proved equivalent to another route
```

No winner is forced. Exact RED/MERGED routes stop immediately.

### 29-13 — A2_METHOD_TRANSFER_ACROSS_SURVIVING_ROUTES

Use StageA2 only as a method source: parameter reduction, quotient decomposition, low-genus models, Jacobian rank/torsion, descent, and rational-point closure.

```text
A2_FAMILY_SPECIFIC_EXCLUSION_NE_GLOBAL_EXCLUSION=true
COVERAGE_REQUIRED_FOR_GLOBAL_PROMOTION=true
```

### 29-14 — NATURAL_SLICE_QUOTIENT_AND_COVERAGE_TEST

Attempt exact closure on the most structurally natural surviving slices/quotients and state their global role.

Required output:

```text
SLICE_OR_QUOTIENT
Q_FORM_AND_FIELD
ENDPOINT_MAP_DIRECTION
SLICE_CLOSED=true/false
COVERAGE_STATEMENT_PROVED
GLOBAL_COVERAGE_PROVED=true/false
WHAT_REMAINS_OUTSIDE
```

Do not write `coverage fraction` unless a precise measure/counting model and theorem justify an actual fraction.

### 29-15 — ENDPOINT_ARSENAL_REMATCH

Rematch StructureRadar, Arsenal, Stage14–28 weapons, A2 methods, and 29-02 theorem ecosystems against the exact remaining GREEN/AMBER receivers. This is receiver-specific theorem matching, not generic literature drift.

### 29-16 — RESIDUAL_RECEIVER_COMPRESSION_AND_ROUTE_PORTFOLIO

Perform the final mandatory portfolio compression. Earlier exact deduplication remains required.

For each surviving receiver record:

```text
ROUTE_ID
exact population/variety
field of definition and Q-form
map/coverage relation to endpoint
measure/height
quantifiers
multiplicity
required theorem strength
near-misses already ruled out
perfect-cuboid consequence
status GREEN/AMBER
```

Retain the RED/MERGED registry to prevent replay.

### GAP_SCAN_FINAL / ROADMAP_REVIEW_FINAL

Check for an unused repo-native weapon, missing symmetry/height/Q-form adapter, overlooked endpoint model, or unjustified coverage assumption. A material discovery may alter only the necessary future work or create a targeted audited addendum; it does not erase completed audited history.

### 29-17 — PERFECT_CUBOID_ATTACK_HANDOFF

Freeze the final portfolio, not necessarily a single route. Include the endpoint-hub graph, the exact population/sign-tower bridge verdict, surviving GREEN/AMBER receivers, Q/Q(i) firewalls, coverage statements, finite numerical baseline, and the minimum next theorem/computation for each route.

### 29-close

Close Stage29 after fresh audit. Success means converting Stage16–29 knowledge into an exact global dependency map plus a sharply defined endpoint attack portfolio. Stage29 need not solve the perfect-cuboid problem.

## Gap-scan / roadmap-review contract

```text
GAP_SCAN_RESULT = NONE_FOUND | FOUND_NEW_FOUNDATION_INTERNAL_ANALYSIS_REQUIRED | FOUND_TARGETED_BACKFLOW_REQUIRED | FOUND_EXTERNAL_INPUT_REQUIRED
ROADMAP_REVIEW = STILL_VALID | LOCAL_REORDER_REQUIRED | MATERIAL_REVISION_REQUIRED
MATERIALITY_CERTIFICATE_REQUIRED_FOR_REVISION=true
```

A roadmap revision must preserve all prior audited metadata and completed-stage status unless a targeted audited addendum explicitly supersedes a statement.

## Stage28 synchronization

Stage28 is closed/audited/merged through checkpoint70 / PR #1284 and imported into Stage29. Future Stage28 work is only narrow targeted backflow through a Stage29 receiver.

```text
STAGE28_FINAL_IMPORTED=true
STAGE28_REFRESH_PENDING=false
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```