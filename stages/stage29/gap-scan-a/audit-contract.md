# Stage29 GAP_SCAN_A / ROADMAP_REVIEW_A — fresh audit contract

Fresh audit must attack the submission as a third-pass review of audited 29-03, 29-04 and 29-05.

Required checks:

1. Verify `GAP_SCAN_A_RESULT=NONE_FOUND` means no **new unowned material gap**, not that all endpoint mathematics is closed.
2. Recheck the 29-02hd broad-screen stop and preserve `H_NAMESPACE_REOPENABLE=true` and `PESCHMANN_INDEPENDENCE_RESOLVED=false`.
3. Recheck that KUM4A is genuinely pointwise discharged on the same F7 map and that KUM4B still carries all population/counting adapters.
4. Determine whether KUM4A or any 29-05 receiver recovery actually forces a Stage16--28 contract extension. Do not accept `backflow=0` by policy alone.
5. Recheck `route-registry.json` completeness against the current canonical controller and detect any newly omitted residual/retained IDs after #1310 merge.
6. Recheck that the non-Fano/Hirzebruch receivers routed to `S06-GLOBAL-SYNTHESIS` are not accidentally promoted to a twelfth attack route or collapsed into a false Q-isomorphism.
7. Recheck 29-06-before-29-07 ordering. A reorder is required if the hub graph cannot be defined safely with open edges; otherwise preserve sequence.
8. Recheck that 29-07 scope now says KUM3A/KUM3B/KUM4B rather than replaying discharged KUM4A.
9. Recheck Peschmann failure semantics: crosswalk failure requires independence reassessment and may reopen 29-02h*; it is not automatic RED.
10. Recheck 29-09 and 29-12 local-credit firewall so Stage19/20 marginal blocker laws cannot be counted again as independent endpoint restrictions.
11. Verify no perfect-cuboid existence/nonexistence conclusion or finite-to-global promotion appears.
12. Inspect controller preservation if audit writes the canonical controller; unrelated audited 29-02--05 metadata must remain unchanged.

Allowed final roadmap verdicts:

```text
STILL_VALID
LOCAL_REORDER_REQUIRED
MATERIAL_REVISION_REQUIRED
```

Allowed gap result classes:

```text
NONE_FOUND
FOUND_NEW_FOUNDATION_INTERNAL_ANALYSIS_REQUIRED
FOUND_TARGETED_BACKFLOW_REQUIRED
FOUND_EXTERNAL_INPUT_REQUIRED
```

A roadmap rewrite requires a materiality certificate naming the exact new theorem/model/map/obstruction/coverage result and the future items invalidated by the current sequence.
