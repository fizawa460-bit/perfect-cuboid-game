# Stage29-06 — fresh adversarial audit contract

Audit the submission as a global endpoint-hub synthesis, not as a theorem-proving attack.

Required checks:

1. Recheck every node/edge in `endpoint-hub-graph.json` against the audited Stage29 source artifacts; detect any direction reversal, field drift, degree drift, or generic-to-global promotion.
2. Verify `F7_PRESENTATION` is a presentation of the same canonical endpoint surface, not counted as a second endpoint surface or independent attack success.
3. Verify `Sbar -> P2` degree 64 and the seven-line branch divisor are exact global statements.
4. Verify the Campedelli arrows are Q-defined global quotient maps and that `6+2+2` is only the certified Q-defined S3 orbit decomposition, not an exact Q-isomorphism-class count.
5. Verify the Beauville arrow is `X_cub -> S_endpoint`, not reversed. Check Q-descent, degree two, physical-open etale scope, and the quadratic-twist lift firewall.
6. Attack the joint-V4 section carefully. Distinguish exact function-field/dense-open identification from any unresolved global boundary/canonical-model statement. Do not allow `R29-G1b` or `R29-X1` to be silently discharged.
7. Verify the three marginal V4 quotients and their degrees/directions over `Y`.
8. Verify the seven coordinate-sign K3 quotients are exact geometric quotients, but Stage19 `X_sp`/Stage20 `X_face` matching the same modular forms does not by itself prove geometric isomorphism to `K_b`/`K_c`.
9. Verify all L-function/character edges are typed as cohomological/module relations only and never used as rational-point maps or independent probability factors.
10. Verify the modular endpoint presentation is scoped over `Q(i)`, while the degree-24 forgetting map is generic/moduli-level and not an everywhere finite compactified morphism. Preserve the ordinary-8-congruence RED verdict.
11. Verify the abstract `S4` on the seven-line arrangement is not identified with the modular residual `S4` without `R29-KUM5`.
12. Verify the non-Fano branch arrangement is Q-equivalent but the standard non-Fano cover is only Q(i)-isomorphic to the cuboid cover; over Q retain the explicit constant-sign twist. No automatic Q-point transfer.
13. Enforce the Gap Scan A anti-loop repair: `R29-NF1QISO` stays ledger-only/dormant and is not reactivated. Active non-Fano synthesis is only `NF3,NF4,NF5,NF6,NF7`.
14. Recheck that physical-open/Brauer data are arithmetic tools attached to `U_phys`, not extra geometric foundation nodes and not certified obstructions.
15. Compare the open-receiver list against the audited 29-05 route registry. No receiver may become unowned or be counted twice.
16. Recheck the planned 29-07 queue: `KUM3A,KUM3B,KUM4B,G1b,X1`; KUM4A replay is forbidden.
17. Determine whether any exact synthesis result now forces Stage16--28 targeted backflow. Do not accept `backflow=0` by policy alone.
18. Verify no synthesis statement is credited as a rational-point attack, population saving, or perfect-cuboid existence/nonexistence theorem.
19. If the canonical controller is updated during audit, preserve all unrelated audited 29-02--GAP_SCAN_A metadata and synchronize merged PR #1311 exactly.

Allowed verdicts:

```text
PASS
PASS_AFTER_BOUNDED_REPAIR
FAIL_REPAIR_REQUIRED
```

A repair is material if it changes an edge's direction, degree, field, proof scope, rational-point functoriality, or receiver ownership. Cosmetic wording changes are not material progress.
