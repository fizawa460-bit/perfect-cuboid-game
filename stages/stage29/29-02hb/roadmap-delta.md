# Stage29 roadmap delta after 29-02hb

This file is proposal-only until fresh audit.

## Mining decision

`29-02ha` already invalidated the previous automatic stop after `29-02g`. `29-02hb` now finds a second materially different post-`g` foundation: a degree-8 classical Campedelli quotient layer of the full degree-64 sign cover.

Therefore, if audit passes:

```text
29-02ha  full seven-line sign/Kummer cover
29-02hb  Campedelli quotient compression
29-02hc  next independent foundation search
...
29-03     only after the mining stop condition is actually satisfied
```

The extension namespace remains

```text
ha, hb, hc, hd, ...
```

and must not be consumed by cosmetic subdivisions or by merely continuing an already-known receiver.

## Stop condition

Do not advance to `29-03` merely because a fixed suffix list has ended. Advance when a broad screening pass fails to produce another materially new HIGH_VALUE foundation and the remaining work consists of receivers internal to already-identified foundations.

## Current high-value post-g foundations

```text
29-02ha: full sign/Kummer cover of P^2 by seven linear forms
29-02hb: classical Campedelli quotient compression, 10 kernels / 2 S4 types
```

Both remain Stage29-native. No automatic Stage16--28 rerun is authorized.
