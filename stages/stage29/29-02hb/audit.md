# Stage29-02hb — adversarial audit

```text
AUDITED_PR=1304
AUDITED_SUBMISSION_HEAD=c3966a07156a0f820acda25fcd77c4f59d2cf57a
AUDIT_MODE=EXTRA_ADVERSARIAL
AUDIT_VERDICT=PASS_AFTER_BOUNDED_REPAIR
```

## Executive verdict

The Campedelli quotient foundation survives an audit deliberately aimed at breaking the unusually clean correspondence. It is not merely a similarity between two seven-line covers: for each admissible kernel `H`, the Campedelli canonical model is literally

```text
Cbar_H = Sbar/H
```

and its degree-8 map to `P2` is the factor of the same audited endpoint morphism `Sbar -> P2`.

Two substantive scope repairs were necessary:

1. `8+2` is geometric / `Q(i)` symmetry, **not** a two-type Q-arithmetic classification. Under the certified Q-liftable endpoint symmetry the ten kernels split `6+2+2`, so arithmetic routing needs three certified Q-symmetry representatives.
2. the finite degree-64 cover is `Sbar -> P2`, not the minimal resolution `S -> P2`; the resolution-level degree-8 quotient is established separately by etale base change.

A third firewall was added: the literature supplies geometric Campedelli technology, while each cuboid quotient carries a particular Q-form. Arithmetic claims about another displayed Q-model do not transfer without a Q-form/twist adapter.

No fatal defect was found in the underlying quotient construction.

## Attack 1 — is this really the same map?

Yes, globally.

Let

```text
pi:Sbar -> P2
Gamma=(Z/2)^6
q:Gamma -> G=(Z/2)^3
H=ker(q).
```

The normal finite quotient exists globally and factors the original map:

```text
Sbar --/H--> Cbar_H=Sbar/H --beta_H--> P2.
```

The inertia of `beta_H` on each of the same seven branch lines is exactly `q(e_L)`. If these are the seven nonzero elements of `G`, with nonzero triple sums at the six triple points, this is precisely a Campedelli line arrangement after base change to an algebraic closure.

No separately constructed Campedelli cover is substituted for the cuboid quotient and no function-field-only identification is promoted without normalization.

```text
SAME_GLOBAL_MAP_AUDIT=PASS
NORMAL_QUOTIENT_ADAPTER=PASS
```

## Attack 2 — was a generic statement extended globally?

The submission did blur the canonical model and its resolution. The repaired scope is:

```text
Sbar -> P2  finite degree 64
S -> P2     generically finite, not finite because exceptional curves contract.
```

For the quotient itself, the stabilizer calculation is global. Every stabilizer of the sign action is generated by inertia of the branch lines through the base point. For every admissible kernel, the relevant labels are independent at one-, two-, and three-line strata, so

```text
H ∩ I_x = 0
```

for every point of `Sbar`. Therefore

```text
Sbar -> Cbar_H
```

is a finite etale `H`-torsor on the whole canonical model, including its singular points.

```text
GENERIC_TO_GLOBAL_AUDIT=PASS_AFTER_CANONICAL_MODEL_SCOPE_REPAIR
```

## Attack 3 — Q versus Q(i)

This is where the submission actually over-compressed.

The exact enumeration is correct:

```text
admissible labelings = 1680
GL(3,F2)             = 168
distinct kernels     = 10.
```

The full arrangement `S4` gives geometric / `Q(i)` kernel orbits

```text
8 + 2.
```

But Stage29-02ha already audited that only the coordinate-permutation `S3` lifts to the endpoint cover over `Q`. Re-running the kernel action gives

```text
certified Q-symmetry orbits = 6 + 2 + 2.
```

Thus

```text
TWO_Q_ARITHMETIC_TYPES=false
THREE_CERTIFIED_Q_SYMMETRY_REPRESENTATIVES=true
EXACT_Q_ISOMORPHISM_CLASS_COUNT_PROVED=false
```

The last statement is deliberately conservative: quotient surfaces in different endpoint-symmetry orbits might conceivably be Q-isomorphic by maps not lifting to the endpoint. No such collapse is assumed.

Each individual kernel `H` is nevertheless Q-defined because the coordinate-sign deck group is constant over `Q`.

```text
Q_QI_FIELD_SCOPE_AUDIT=PASS_AFTER_MATERIAL_REPAIR
```

## Attack 4 — does resolution change the quotient property?

No.

At a triple branch point the quotient canonical model has the local independent-root equation

```text
u^2=r,
v^2=s,
w^2=r+s,
```

hence one `A1` singularity. There are six such points. The full endpoint has eight A1 nodes above each base triple point, and `H` permutes those eight points freely and transitively.

Because `Sbar -> Cbar_H` is finite etale, pulling back the minimal resolution of the six A1 points on `Cbar_H` gives the minimal resolution of the 48 A1 points on `Sbar`. Therefore

```text
S -> C_H
```

is genuinely finite etale of degree eight.

This is stronger than an informal simultaneous-resolution assertion.

The Campedelli invariants also cross-check independently from the abelian cover:

```text
(beta_H)_* O = O + O(-2)^7
pg=q=0
chi=1
K^2=2.
```

Mendes Lopes--Pardini--Reid may then legitimately be applied: after base change to `C`, `S` is the universal cover and `pi1(C_H)=(Z/2)^3`.

```text
R29-CAMP1=DISCHARGED
RESOLUTION_ETALE_AUDIT=PASS
UNIVERSAL_COVER_ADAPTER=PASS_GEOMETRIC
```

## Attack 5 — was population-stage meaning smuggled in?

No population theorem survives automatically through this quotient, and the audited route now says so explicitly.

The only immediate arithmetic transfer is pointwise:

```text
perfect-cuboid Q-point -> C_H(Q).
```

No transfer is certified for

```text
M1,N1,M2,N2,M3,
R<=B,
primitivity,
canonical ordering,
exact-face multiplicities,
or asymptotic densities.
```

```text
STAGE16_20_POPULATION_TRANSFER=false
HEIGHT_TRANSFER=false
PRIMITIVITY_TRANSFER=false
ASYMPTOTIC_TRANSFER=false
BACKFLOW_TO_STAGE16_28=false
```

## Source audit

Kharlamov--Kulikov Section 1.5 / Theorems 1.9--1.10 give the seven-line Campedelli arrangement criterion and its canonical-model cover. Calabri--Mendes Lopes--Pardini Section 5, Example 1 gives the exact three-line condition, A1 resolution, building bundles and bicanonical cover. Mendes Lopes--Pardini--Reid give the degree-8 etale universal-cover theorem.

The source literature is geometric. The specific Q-form `C_H=S/H` is inherited from the cuboid quotient; external arithmetic about another Q-model requires an explicit Q-form adapter.

```text
SOURCE_LOCK_AUDIT=PASS_AFTER_QFORM_SCOPE_REPAIR
```

## Exact finite classification after audit

```text
R29-CAMP0=DISCHARGED
R29-CAMP0A=DISCHARGED_EXACT_10_KERNELS
R29-CAMP0B=DISCHARGED_GEOMETRIC_QI_8_PLUS_2
R29-CAMP0C=DISCHARGED_CERTIFIED_Q_6_PLUS_2_PLUS_2
R29-CAMP1=DISCHARGED
```

Remaining receivers:

```text
R29-CAMP2=ArithmeticHTorsorDescentForThreeCertifiedQSymmetryRepresentatives
R29-CAMP3=SevenCampedelliInvolutionQuotientsWithQFormAndRationalVsEnriquesLedger
R29-CAMP4=CampedelliBrauerAndTwoPrimaryDescentCompatibilityWith29_02f
```

## Final audit state

```text
AUDIT_REQUIRED=false
AUDIT_VERDICT=PASS
CHECKPOINT29_02HB_AUDIT=PASS
BOUNDED_REPAIR=Q_QI_ORBIT_SPLIT_PLUS_CANONICAL_RESOLUTION_SCOPE_PLUS_QFORM_FIREWALL
SAME_GLOBAL_MAP_AUDIT=PASS
GENERIC_TO_GLOBAL_AUDIT=PASS_AFTER_SCOPE_REPAIR
Q_QI_FIELD_SCOPE_AUDIT=PASS_AFTER_MATERIAL_REPAIR
RESOLUTION_ETALE_AUDIT=PASS
POPULATION_FIREWALL_AUDIT=PASS
R29_CAMP0=DISCHARGED
R29_CAMP1=DISCHARGED
GEOMETRIC_QI_KERNEL_ORBITS=8+2
CERTIFIED_Q_KERNEL_ORBITS=6+2+2
EXACT_Q_ISOMORPHISM_CLASS_COUNT_PROVED=false
REPAIR_REQUIRED=false
MERGE_ALLOWED=true
ADVANCE_ALLOWED=true
AUTO_ADVANCE_TO_29_03=false
STAGE29_02_MINING_STOP_CONDITION_SATISFIED=false
BACKFLOW_TO_STAGE16_28=false
NEXT_ITEM=29-02hc
NEXT_EXPECTED_COMMAND=Stage29-main-batch
PERFECT_CUBOID_EXISTENCE_CLAIM=false
PERFECT_CUBOID_NONEXISTENCE_CLAIM=false
```
