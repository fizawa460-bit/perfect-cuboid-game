# Stage29-05 fresh-audit contract

Audit the submission adversarially. Do not merely check formatting.

## Required attacks

1. **Receiver completeness.** Verify that every active named receiver imported from audited 29-02/03/04 has exactly one primary owner or an explicit already-discharged/umbrella status. Missing live receivers are a material defect.
2. **False equivalence.** Attack every `MERGED`/same-predicate statement. In particular, verify that the Stage19 squareclass normal form and F7 coordinate squareclass are being merged only at the physical-predicate level, not as identical global varieties or theorem packages.
3. **False independence.** Verify that Campedelli, Beauville, modular, Brauer, K3, joint V4, local arithmetic, and population interaction are not split into multiple routes merely to inflate the portfolio.
4. **F7 universal-organizer firewall.** The registry must not imply that Beauville, modular, or every arithmetic route is an F7 subgroup quotient.
5. **Q versus Q(i).** Verify the non-Fano recognition, modular route, node fields, Campedelli symmetry statements, and every descent-sensitive route retain the exact field/twist scope.
6. **KUM4A/KUM4B.** Verify KUM4A is only the pointwise physical-to-F7 squareclass crosswalk and that KUM4B still carries height, primitivity, canonical ordering, multiplicity, lift/sign, and subcover/complement counting issues.
7. **V4 double charge.** Verify the three characters of the joint V4 cover are not treated as independent probabilistic restrictions.
8. **Population semantics.** Verify no disjoint exact-stratum ratio is promoted to objectwise survival.
9. **Parametric coverage.** Verify Peschmann failure would trigger independence reassessment, not automatic RED; verify A2/Saunderson family closure is not globalized.
10. **Portfolio ownership.** Check that pre-attack infrastructure `I07/I08/I09` is not counted as attack-route success and that 29-10/11/12 are primary mechanism owners rather than duplicate execution stages.
11. **Roadmap scope.** Determine whether 29-05 creates a real materiality certificate requiring R2 rewrite. Default should be no unless exact mathematics changes the future sequence.
12. **Controller preservation.** If audit passes, synchronize canonical controller without deleting unrelated 29-02/03/04 metadata; record merged #1309.

## Audit outcomes

```text
PASS
PASS_AFTER_BOUNDED_REPAIR
PASS_AFTER_MATERIAL_REPAIR
FAIL_REPAIR_REQUIRED
```

If the route registry is incomplete or materially misclassifies equivalence/independence, require repair before advance.

## Next routing after PASS

```text
NEXT_ITEM=GAP_SCAN_A_ROADMAP_REVIEW_A
NEXT_EXPECTED_COMMAND=Stage29-main-batch
```
