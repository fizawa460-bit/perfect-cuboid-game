# Stage29-02ha roadmap delta

This file is proposal-only until fresh audit.

The previous controller routed directly from audited `29-02g` to `29-03`. Stage29-02ha found a materially new HIGH_VALUE foundation after that nominal stopping point, so the foundation-mining stop condition must be reopened if the route passes audit.

Proposed suffix queue:

```text
29-02ha FULL_SEVEN_LINE_SIGN_KUMMER_COVER
29-02hb NEXT_INDEPENDENT_FOUNDATION_MINING
29-02hc ... only if another materially new foundation appears
...
29-03 FOUNDATION_BACKFLOW_DECISION only after HIGH_VALUE additions dry up
```

This uses the already-approved `ha/hb/...` namespace and does not rename or disturb audited `29-02a..g`.

Stop rule remains novelty-based, not count-based:

1. discover candidate foundation;
2. deduplicate against endpoint surface / joint V4 / Picard-low-genus / Beauville / L-function / Brauer / modular foundations;
3. keep only if it changes coordinates, theorem access, or exact receivers;
4. when successive mining passes produce no new HIGH_VALUE route, advance to `29-03`;
5. later genuinely new discoveries may still be appended under the next unused `h*` suffix.

```text
A_TO_G_RENAMED=false
H_NAMESPACE_ACTIVE_IF_AUDIT_PASS=true
29_03_CANCELLED=false
29_03_DEFERRED_UNTIL_MINING_STOP=true
```
