# Stage29-02 — bounded reuse / anti-loop preflight

```text
ROLE=REPOSITORY_WIDE_REUSE_PREFLIGHT
STATUS=SUBMITTED_PENDING_FRESH_AUDIT
BROAD_EXTERNAL_SEARCH_REPEATED=false
```

## Direct Stage28 weapons reused

The audited Stage28 promotion cards remain active with their original firewalls:

- `S28-W01` common-host bridge adapter;
- `S28-W02` normalized interaction-curvature adapter;
- `S28-W03` branch-profile geometric differential;
- `S28-W04` common-polarization fixed-curve differential.

Stage29-02 uses W03 to distinguish the two branch supports and uses the Stage28 squareclass-separation certificate to prove that the two quadratic extensions over the fixed common base are distinct.  It does **not** import any global counting saving from W03/W04.

## StructureRadar rematch against the new receiver

The exact new receiver is the simultaneous completion / cross-quotient object, not the old Stage27 physical-selector wall.

Closest already-classified cards are:

### SR-STR-223 — moving compatible small-point / fiber-product control

Current classification: `EXTERNAL_GATE`, frozen at

```text
MovingCompatibleSmallPointFiberProductFamilyControl
```

This is the closest prior geometric/fiber-product theorem species.  It does not close Stage29 F2: fixed-fiber geometry and diagnostics were explicitly not promoted to moving-family control.  Conversely, the new Stage29 joint `(Z/2)^2` endpoint cover is a changed exact receiver, so a future rematch is legitimate if 29-07 produces a compatible family/height adapter.  No immediate reopening is performed at 29-02.

### SR-STR-163 — branch-sensitive Prym / 2-descent interface

Current classification: `ACTIVE` as a structural interface.  It is potentially relevant after 29-07 identifies quotients/fibrations of the joint cover.  It is not by itself a global endpoint theorem.

### SR-STR-164 — square-lift collision-cover / sieve interface

Current classification: `ACTIVE` after its diagonal/singularity/height checks.  It is a plausible structural adapter for simultaneous square lifts, but Stage29-02 does not claim its old receiver matches the new joint-cover measure automatically.

### SR-STR-173 — same-measure support/moment firewall

Current classification: `ACTIVE` structurally.  It may normalize a future same-measure joint-local/global moment receiver, but it supplies no fixed-power saving by itself.

### SR-STR-169 — same-measure signed correlation theorem gate

Current classification: `EXTERNAL_GATE` / anti-loop frozen for the Stage27 `H_phys^MAIN` receiver.  The Stage29 cross-character covariance is mathematically a correlation object, but it lives on a different exact common-base endpoint receiver.  No cross-measure promotion is allowed.  A rematch is legal only after 29-09 supplies an explicit adapter.

## Anti-loop verdict

```text
DIRECT_EXISTING_WEAPON_CLOSES_29_02=false
NEW_RECEIVER_MAKES_FUTURE_REMATCH_LEGAL=true
REOPEN_SR_STR_223_NOW=false
REOPEN_SR_STR_169_NOW=false
ACTIVE_STRUCTURAL_CANDIDATES=SR-STR-163,SR-STR-164,SR-STR-173
EXTERNAL_GATE_CANDIDATES_FOR_LATER_EXACT_REMATCH=SR-STR-223,SR-STR-169
OLD_GATE_REPLAY=false
```

This preflight is why the 29-02 submission keeps the new work Stage29-native rather than reopening Stage27/28 or restarting a broad StructureRadar search.
