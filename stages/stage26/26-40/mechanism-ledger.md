# Stage26-40 mechanism ledger

## Quantitative source

The strongest current same-population Stage20 upper theorem is

\[
M_3(B)\ll_\eta B(\log B)^{5-\eta}
\]

for every fixed `eta<1/46`, with the endpoint excluded. Against the audited Stage18 denominator

\[
M_2(B)\sim C_{M_2}B(\log B)^5,
\]

this gives the Stage26 upper completion family.

## Quantifier extraction

For any fixed `0<delta<1/46`, choose a fixed `eta` with `delta<eta<1/46`. Then

\[
(\log B)^\delta\frac{M_3(B)}{M_2(B)}
\ll_\eta (\log B)^{-(\eta-\delta)}\to0.
\]

No uniformity in `eta` as `eta->1/46` is needed or claimed. The proof is pointwise in each fixed `delta`.

## Geometric/arithmetic boundary

The two-face pre-completion host is the split `4A1` quartic del Pezzo surface with smooth resolution `Bl_4(P1xP1)` and Picard rank six. Requiring the third face square defines the audited degree-two K3 cover.

The local blocker masses on the exact host are

\[
\delta_2=2/9,
\qquad
\delta_p=\frac{2(p-\chi_4(p))}{p^2+6p+1}=2/p+O(p^{-2}).
\]

This local law is causal evidence for thinning. The growing-prime Selberg sieve and the explicit thin-cover/Huang upper theorem are separate proof routes. Their savings are not multiplied.

## Directional receiver

For `j=a,b,c`, the audited phase60 identities are

\[
P_j=M_{2,j}+M_3,
\qquad
\Theta_j=M_3/P_j,
\qquad
M_{2,j}\sim C_jB(\log B)^5.
\]

Therefore the same endpoint-free little-o statement holds in every fixed shared-edge chamber.

## Open gates

- endpoint `1/46`;
- any exact logarithmic decay exponent for completion;
- any fixed polynomial saving in `B` for `M3` relative to `B`;
- an asymptotic for `M3`;
- the true `M3` exponent;
- any independence factorization of local obstructions;
- any transfer of the toric Manin `(a,b)` ledger through the K3 cover.

```text
MECHANISM_LEDGER_STATUS=CANDIDATE_PENDING_FRESH_AUDIT
ENDPOINT_EXCLUDED=true
THIN_COVER_AND_LOCAL_SIEVE_MULTIPLIED=false
TRUE_M3_EXPONENT_IDENTIFIED=false
PERFECT_CUBOID_CONCLUSION=NONE
```
